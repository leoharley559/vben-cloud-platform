# 前端「返水」字段使用汇总（发给后端）

> 范围：`vben-admin-demo/apps/web-antd`  
> 整理日期：2026-08-01  
> 说明：仅统计**列表列 / 汇总指标**中展示的「返水」金额类数据；不含「返水方案配置」「返点比例配置」「提现流水」等非金额配置项。  
> 金额约定：未特别说明时，接口返回值为**最小金额单位（分）**，前端按 `/100` 展示为元。

---

## 1. 总览

| 字段名 | 含义（前端用法） | 出现模块 |
|--------|------------------|----------|
| `SumBetWaterMoney` | 日报类汇总「返水」 | 公司日报、上架包日报、设备/VIP日报、代理数据(渠道)、分组日报、运营日分析 |
| `BackWaterMoney` | 玩家统计列表「返水」 | 玩家统计报表 |
| `SumBackWaterMoney` | 玩家统计合计 / 代理数据列表「返水」 | 玩家统计合计、代理账号详情·代理数据 |
| `BackWater` | 返水业务明细金额 | 返水记录/审核/手动、玩家详情·返水信息、代理记录查询 |
| `BackWaterTotal` / `SumBackWater` | 返水合计 | 记录查询合计、返水记录汇总、玩家返水合计 |
| `ApplyBackWater` | 审核申请金额 | 返水审核 |
| `BackWaterGold` / `TotalBackWaterGold` | 佣金场景「返水」 | 佣金钱包、佣金管理 |
| `BetWater` / `SumBetWater` | 玩家金币周期统计「返水」 | 玩家详情·金币统计 |
| `rebateAmount` | **前端计算**的场馆返点金额（非接口直出） | 代理账号详情·场馆明细 |

---

## 2. 分页面明细

### 2.1 数据汇总 · 公司日报

| 子页 | 页面组件 | 列标题 | 接口 | 字段 |
|------|----------|--------|------|------|
| 日报 | `everydayData` → `daily-report-table` | 返水 | `GET /backend/operation/dailyreport`（`SearchType=old`→`Items`；`today`→`TodayItems`） | `SumBetWaterMoney` |
| 日报汇总横幅 | `daily-report-panel` | （计算收入用到返水，非单独列） | `GET /backend/operation/dailyreporttotal` | 计算侧用 `SumBetWaterMoney` |
| 上架包日报 | `package-daily-panel` | 返水 | `GET /backend/iosappstoredata/getiosappstoredata` | `SumBetWaterMoney` |
| 设备日报 | `device-daily-panel` | 返水 | `GET /backend/operation/devicedayreport` | `SumBetWaterMoney` |
| VIP日报 | `vip-daily-panel` | 返水 | `GET /backend/operation/vipdayreport` | `SumBetWaterMoney` |

### 2.2 运营数据 · 代理数据（渠道日报）

| 页面组件 | 列标题 | 接口 | 字段 |
|----------|--------|------|------|
| `channel-report-table` | 返水 | `GET /backend/operation/channelreport` | `SumBetWaterMoney` |
| 同上（按渠道） | 返水 | `GET /backend/operation/channelreportbychannel` | `SumBetWaterMoney` |

### 2.3 运营数据 · 代理分组日报

| 页面组件 | 列标题 | 接口 | 字段 |
|----------|--------|------|------|
| `group-daily-table` | 返水 | `GET /backend/agentnetcashgroup/dailyreport` | `SumBetWaterMoney` |

### 2.4 报表 · 玩家统计报表

| 页面组件 | 展示位置 | 接口 | 字段 |
|----------|----------|------|------|
| `dataClose/playerStatistics` | 列表列「返水」 | `GET /backend/operation/playerstatistics` | `BackWaterMoney` |
| 同上 | 合计行「返水」 | 同上响应 `Total` | `SumBackWaterMoney` |

### 2.5 报表 · 数据分析 · 数据比较

| 页面组件 | 展示位置 | 接口 | 字段 |
|----------|----------|------|------|
| `operationDaily/data-compare-panel` | KPI / 对比「返水金额」 | `GET /backend/operationdayreport/dataanalyze` | `SumBetWaterMoney` |

### 2.6 代理管理 · 代理账号详情

| Tab / 区块 | 页面组件 | 列标题 | 接口 | 字段 |
|------------|----------|--------|------|------|
| 代理数据 · 自身/直属会员/直属代理表 | `agency-data-panel` | 返水 | `GET /backend/agentnetcash/personalstats`<br>`GET /backend/agentnetcash/directlowerplayerstats`<br>`GET /backend/agentnetcash/directloweradminstats` | `SumBackWaterMoney` |
| 代理数据 · 场馆明细 | `agency-data-panel` | 返点金额 | 同上 `personalstats` 的 `GameTypeStats` + `AgentFanDianConfig` | **前端计算**：`(SumValidBetMoney/100) * rebate`（结果为元） |
| 佣金钱包 · 佣金信息 | `agency-wallet-panel` | 返水 | `GET /backend/sendcommission/commissioninfolist` | 列 `BackWaterGold`；合计 `TotalBackWaterGold` |

### 2.7 代理管理 · 佣金管理

| 页面组件 | 列标题 | 接口 | 字段 |
|----------|--------|------|------|
| `commission-ledger-panel` | 返水 | `GET /backend/sendcommission/list`（发放）<br>`GET /backend/sendcommission/commissionlist`（记录）<br>`GET /backend/sendcommission/listadmin`（团队） | `BackWaterGold` |
| 同上 · 明细弹窗 | 返水 | `GET /backend/sendcommission/personaldetail`<br>`GET /backend/sendcommission/teamdetail` | `BackWaterGold` |

### 2.8 代理管理 · 记录查询 · Tab「返水」

| 页面组件 | 列标题 | 接口 | 字段 |
|----------|--------|------|------|
| `netcash/recordInquire` | 返水金额 | `GET /backend/recordquery/backwaterlist` | `BackWater` |
| 同上 | 返水金额总计 | 同上 | `BackWaterTotal` |

### 2.9 产品管理 · 返水设置

| 子页 | 页面组件 | 列标题 | 接口 | 字段 |
|------|----------|--------|------|------|
| 返水记录 | `back-water-record-panel` | 返水金额 | `GET /backend/playerbackwaterrecord/list`<br>`GET /backend/playerbackwaterrecord/detaillist` | `BackWater` |
| 返水记录 · 汇总 | 同上 | 已发/待发/返水总计等 | 同上响应合计 | `SumBackWater` / `Sum` / `UnSum` / `Total`（前端兼容多字段） |
| 返水记录 · 订单明细 | 同上 | 应发返水 | `GET /backend/playerbackwaterrecord/detail` | `BackWater` |
| 手动返水 | `back-water-hand-panel` | 返水 / 应发返水 | `GET /backend/playerbackwaterrecord/waterlist` | `BackWater`；上限 `BackWaterMax` |
| 返水审核 | `back-water-review-panel` | 返水金额 / 申请金额 | `GET /backend/playerbackwaterrecord/list`<br>`GET /backend/playerbackwaterrecord/reviewlist` | `BackWater`；`ApplyBackWater` |

### 2.10 日常运营 · 玩家详情

| Tab | 页面组件 | 列标题 | 接口 | 字段 |
|-----|----------|--------|------|------|
| 返水信息 | `player-rebate` | 返水金额 | `GET /backend/playerbackwaterrecord/list` | `BackWater`；合计 `SumBackWater` |
| 金币统计 · 周期 | `player-coin-period` | 返水 | `GET /backend/playerinfo/goldtotal` | 列 `BetWater`；合计 `SumBetWater` |

---

## 3. 字段 → 接口速查（按字段）

| 字段 | 主要接口 |
|------|----------|
| `SumBetWaterMoney` | `/backend/operation/dailyreport`<br>`/backend/operation/devicedayreport`<br>`/backend/operation/vipdayreport`<br>`/backend/iosappstoredata/getiosappstoredata`<br>`/backend/operation/channelreport`<br>`/backend/operation/channelreportbychannel`<br>`/backend/agentnetcashgroup/dailyreport`<br>`/backend/operationdayreport/dataanalyze` |
| `BackWaterMoney` | `/backend/operation/playerstatistics`（列表行） |
| `SumBackWaterMoney` | `/backend/operation/playerstatistics`（Total）<br>`/backend/agentnetcash/personalstats`<br>`/backend/agentnetcash/directlowerplayerstats`<br>`/backend/agentnetcash/directloweradminstats` |
| `BackWater` | `/backend/playerbackwaterrecord/list`<br>`/backend/playerbackwaterrecord/detaillist`<br>`/backend/playerbackwaterrecord/detail`<br>`/backend/playerbackwaterrecord/waterlist`<br>`/backend/playerbackwaterrecord/reviewlist`<br>`/backend/recordquery/backwaterlist` |
| `BackWaterTotal` | `/backend/recordquery/backwaterlist` |
| `SumBackWater` | `/backend/playerbackwaterrecord/list`（合计） |
| `ApplyBackWater` | `/backend/playerbackwaterrecord/reviewlist` |
| `BackWaterMax` | `/backend/playerbackwaterrecord/waterlist` |
| `BackWaterGold` | `/backend/sendcommission/commissioninfolist`<br>`/backend/sendcommission/list`<br>`/backend/sendcommission/commissionlist`<br>`/backend/sendcommission/listadmin`<br>`/backend/sendcommission/personaldetail`<br>`/backend/sendcommission/teamdetail` |
| `TotalBackWaterGold` | `/backend/sendcommission/commissioninfolist`（合计） |
| `BetWater` / `SumBetWater` | `/backend/playerinfo/goldtotal` |

---

## 4. 排除项（不是金额「返水列」）

以下页面有「返水」字样，但属于**方案名 / 比例配置 / 流水倍数**，未列入上表：

- 返水设置 · 返水方案配置（比例区间、VIP 配置）
- 代理列表「返水配置」`AgentFanDianConfig`、返点弹窗比例
- 代理概览「返水占成」`WaterRate`（占比%）
- 玩家层级 / 基础信息「返水方案」名称
- 提现流水相关（`WithdrawWater*`，业务是流水而非返水）
- 邀请好友活动「流水金额」`WaterAmount`（稽核流水，非返水）

---

## 5. 前端关注点（供后端核对）

1. **日报系统一字段**：公司日报 / 渠道 / 分组 / 数据分析对比，列表「返水」都读 **`SumBetWaterMoney`**。  
2. **玩家统计不同字段**：列表是 **`BackWaterMoney`**，合计是 **`SumBackWaterMoney`**。  
3. **代理数据列表**用 **`SumBackWaterMoney`**，与日报的 `SumBetWaterMoney` 命名不一致。  
4. **佣金场景**用 **`BackWaterGold`**（金流/佣金域命名）。  
5. **返水业务中心**（记录/审核/手动/玩家返水 Tab）统一主字段 **`BackWater`**。  
6. **玩家金币周期**用 **`BetWater` / `SumBetWater`**，又是另一套命名。  

如需统一口径，建议后端确认：以上字段在各自接口中是否均为「玩家/代理返水金额（分）」，以及命名差异是否有意为之。

---

**文档结束**
