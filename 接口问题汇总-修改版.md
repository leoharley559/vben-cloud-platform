# 接口问题汇总

---

## 清单

| 编号 | 日期 | 协作状态 | 后端解决 | 前端验证 | 优先级 | 页面 | 接口 | 问题 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| API-001 | 2026-07-17 | 待验证 | ✅（现网） | **通过**（2026-08-03） | P1 | 数据总览 `/dashboard/index` | `GET /backend/dashboard/reportinfo` | `TotalHours` 分时异常；自选日期 `Date1/2/3` 对应分时也为空 |
| API-002 | 2026-07-17 | 待反馈 | 未处理 | — | P1 | 数据总览 `/dashboard/index` | `GET /backend/dashboard/reportchannelinfo` | `RealtimeItems` 为空，渠道今日数据无展示 |
| API-003 | 2026-07-17 | 待验证 | ✅（本地，待发布） | **通过**（现网已可见，2026-08-03） | P1 | 数据总览 `/dashboard/index` | `GET /backend/dashboard/rankbygameid` | 仅返回 `ItemsLose`，`ItemsWin` 为 `null`，亏损游戏无数据 |
| API-004 | 2026-07-17 | 待反馈 | 未处理 | — | P2 | 数据总览 `/dashboard/index` | `GET /backend/dashboard/reporttoday` | 多个数组字段返回 `null`；充值通道 `TodayItems` 缺 `PayTypeName` |
| API-005 | 2026-07-17 | 待反馈 | 未处理 | — | P2 | 在线总览 `/dashboard/onlineSummary` | `GET /backend/dashboard/onlinesummary` | `GameResult.LastGameId=0` 致场馆名为 `-`；`MapResult.IpDetailName=""` 致地区表格为空 |
| API-006 | 2026-07-17 | 待反馈 | 未处理 | — | P1 | 公司日报·上架包日报 `/operationalData/everydayData`<br>包体管理·iOS 上架包 `/gameManage/packageManage` | `GET /backend/iosappstoredata/getiosappstoredata`<br>`GET /backend/iosappstoredata/list`<br>`GET /backend/iosappstoredata/todayappstorecsv` | 历史 `Items` / iOS 上架包列表 / 今日导出均为 `null`；`BannerItems` 有汇总但明细缺失 |
| API-007 | 2026-07-17 | 待反馈 | 未处理 | — | P1 | 代理分组日报 `/operationalData/groupDaily` | `GET /backend/agentnetcashgroup/dailyreport`<br>`GET /backend/agentnetcashgroup/grouplist` | 日报 `Items` 恒为 `null`；分组树仅返回 `Id=0`「推广中心」且 `List=null` |
| API-008 | 2026-07-17 | 待反馈 | 未处理 | — | P2 | 代理数据 `/operationalData/channelData` | `GET /backend/operation/channelreportbychannel` | `SearchType=old` 时返回 `RealTimeItems` / `TotalRealTimeItems`，与代理侧 `Items` / `TotalItems` 字段名不一致 |
| API-009 | 2026-07-17 | 待反馈 | 未处理 | — | P2 | 排行数据·充兑 `/operationalData/ranking` | `GET /backend/operation/gamepaymentrank` | 单日（昨日）无充值时 `Items`/`Users` 返回 `null`；有数据时字段为 `Items`+`SumGold`（非 `ItemsWin`） |
| API-010 | 2026-07-17 | 待验证 | ✅（本地，待发布） | **通过**（现网已可见，2026-08-03） | P1 | 排行数据·游戏 `/operationalData/ranking` | `GET /backend/operation/realgamewinrank` | 仅返回 `ItemsWin`，`ItemsLose` 恒为 `null`，亏损游戏排行无数据 |
| API-011 | 2026-07-17 | 待反馈 | 未处理 | — | P1 | 排行数据·无限代理 `/operationalData/ranking` | `GET /backend/wnlimitedproxydata/ranking` | `Items` / `Pagination` 恒为 `null`，无限代理排行无数据 |
| API-012 | 2026-07-17 | 待验证 | ✅（现网） | **通过**（2026-08-03） | P1 | 充值列表·自助查单·访问记录 `/operationalManage/rechargeList` | `GET /backend/visitstatistic/selfreviewlist`<br>`GET /backend/visitstatistic/selfreviewstatisticslist` | 均返回 `status=20001`「The remote server is not connected and the operation failed.」，明细/统计无法加载 |
| API-013 | 2026-07-17 | 待验证 | ✅（本地，待发布） | **通过**（现网已可见，2026-08-03） | P1 | 充值列表·自助查单 `/operationalManage/rechargeList` | `GET /backend/playerpaymentselfreview/checkconfig` | 返回 `status=10021`「Data formatting failed」，游戏端入口开关无法读取 |
| API-014 | 2026-07-17 | 待反馈 | 未处理 | — | P2 | 充值列表 `/operationalManage/rechargeList` | `GET /backend/rechargelimitconfig/list`<br>`GET /backend/playereasyrecharge/list`<br>`GET /backend/playerusdteasyrecharge/list`<br>`GET /backend/rechargeblackdevice/list`<br>`GET /backend/fixpayment/list` 等 | 无数据时 `Items` 返回 `null`（非 `[]`）；Pagination 有时仍带 MaxCount=0 |
| API-015 | 2026-07-17 | 待反馈 | 未处理 | — | P2 | 提现列表 `/operationalManage/withdrawList` | `GET /backend/withdrawwhitelist/list`<br>`GET /backend/withdrawsendorderusermanage/list`<br>`GET /backend/withdrawsendorderlist/list`<br>`GET /backend/playerwithdraw/revertedlist`<br>`GET /backend/playerwithdrawflow/list`<br>`GET /backend/playerwithdrawflow/flowList` | 无数据时 `Items` 恒为 `null`；冲正记录 Pagination 缺 `MaxCount` |
| API-016 | 2026-07-17 | 待反馈 | 未处理 | — | P2 | 提现列表·派单列表 `/operationalManage/withdrawList` | `GET /backend/withdrawsendorderlist/list` | 近 7 日仍无明细（`Items=null`，MaxCount=0），但额外返回 `Account` 接单态结构；需确认本环境是否本应有派单数据 |
| API-017 | 2026-07-17 | 待反馈 | 未处理 | — | P1 | 客服工单 `/operationalManage/applyService` | `GET /backend/customsupport/list` | 近 365 日 / 不传时间 / 各 Status 均为 `Items=null` 且 `MaxCount=0`；导出 `IsExp=true` 时 `Pagination` 也为 `null` |
| API-018 | 2026-07-17 | 待验证 | 不存在（现网复测） | **通过**（现返回 10002，2026-08-03） | P1 | 客服工单 `/operationalManage/applyService` | `GET /backend/customsupport/{id}` | 传合法路径 id（如 `1` / `0`）均返回 `status=10001`「数据库异常，请联系管理员」 |
| API-019 | 2026-07-17 | 待反馈 | 未处理 | — | P1 | 公告管理·邮件通知 `/operationalManage/gameNotice` | `GET /backend/gameemail/list` | 近 7/30/365 日及不传时间均为 `Items=null`、`MaxCount=0`，邮件列表无数据 |
| API-020 | 2026-07-17 | 待验证 | ✅（现网） | **通过**（2026-08-03） | P1 | 公告管理·公告明细/统计 `/operationalManage/gameNotice`<br>提现通道·提现访问记录 `/gameManage/withdrawConfig` | `GET /backend/visitstatistic/list`<br>`GET /backend/visitstatistic/statisticslist` | 均返回 `status=20001`「The remote server is not connected and the operation failed.」；提现访问明细/统计复用同一服务与接口（`Group=Withdraw`），与 API-012 同属 visitstatistic |
| API-021 | 2026-07-17 | 待反馈 | 未处理 | — | P2 | 公告管理·新建/编辑推送跳转 `/operationalManage/gameNotice` | `GET /backend/gameadconfig/getactivitylist` | `respond=null`，活动跳转参数下拉无数据（旧站 OpenType=活动 选项虽已注释，接口仍被依赖） |
| API-022 | 2026-07-17 | 待反馈 | 未处理 | — | P2 | 红利发放·发放记录 `/operationalManage/playerGoldHandle` | `GET /backend/playergoldhandle/list` | 无明细时 `Items=null`（非 `[]`）；昨日默认区间常 MaxCount=0，但 `Total` 仍返回；不传时间有历史数据 |
| API-023 | 2026-07-17 | 待反馈 | 未处理 | — | P2 | 账户调整·调整审核/记录 `/operationalManage/accountAdjust` | `GET /backend/playergoldaccountchange/approvelist` | 无明细时 `Items=null`（非 `[]`）；审核态 `Approve=1,4` 近 90 日仍空（MaxCount=0），记录态昨日有数据 |
| API-024 | 2026-07-17 | 待反馈 | 未处理 | — | P2 | 红利管理 `/operationalManage/audit` | `GET /backend/playergoldhandle/approvelist`<br>`GET /backend/playergoldhandle/redlist`<br>`GET /backend/activitymatchbonusapplyrecord/list` | 无明细时 `Items=null`；待审红利/活动红利/申请记录近 90 日常空；`redlist` 缺默认筛参（如 `IsWater=-1`）时即便有时间也 MaxCount=0；`IsExp=true` 单独导出易空 |
| API-025 | 2026-07-17 | 待反馈 | 未处理 | — | P2 | 礼品管理 `/operationalManage/giftManage` | `GET /backend/playergift/list`<br>`GET /backend/playergift/deliverlist` | 发货列表无数据时 `Items=null`（审核列表为空时是 `[]`，不一致）；本环境近 365 日晋级豪礼/主题抽奖审核与发货均为 MaxCount=0，需确认是否本应有数据 |
| API-026 | 2026-07-17 | 待反馈 | 未处理 | — | P2 | 游戏风控 `/operationalManage/gameRiskControl` | `GET /backend/gameipriskcontrol/list`<br>`GET /backend/gameriskcontrol/list`<br>`GET /backend/playerbankcard/blacklist`<br>`GET /backend/appstorewhitelistrisk/list` | 无数据时 `Items`/`WhitelistList` 为 `null`；IP/设备列表无 `Pagination` 仅返回 `Total`；本环境各列表 Total/MaxCount 均为 0，需确认是否本应有数据 |
| API-027 | 2026-07-17 | 待反馈 | 未处理 | — | P2 | 导出管理 `/operationalManage/downloadCsvManage` | `GET /backend/downloadexcel/list` | 无数据时 `Items=null`；近 30/365 日及不传时间均为 MaxCount=0，需确认本环境是否本应有导出任务 |
| API-028 | 2026-07-17 | 待反馈 | 未处理 | — | P1 | 区域屏蔽 `/operationalManage/countrySet` | `GET /backend/countriesconfig/list` | 国家配置列表恒返回 `Items=null`（无 Pagination）；页面无法展示可选国家；`GET /backend/gamecountries/` 状态可读但列表为空时无法配置 |
| API-029 | 2026-07-17 | 待反馈 | 未处理 | — | P1 | 运营活动 `/operationalManage/activity` | 见详情（活动列表/历史/签到/邀请/优惠码/预约明细等） | 多列表无数据时 `Items=null`；存款方案 `list` 返回 10000；代理佣金缺 `Mode` 返回 10037；确认到账误用 `/list` 无权限（正确为根路径 GET） |
| API-030 | 2026-07-17 | 待反馈 | 未处理 | — | P1 | 排行榜 `/operationalManage/leaderboard` | `GET /backend/leaderboard/list`<br>`GET /backend/leaderboard/record`<br>`GET /backend/leaderboard/getleaderboardmainconfig` | 无数据 `Items=null`；传 `StartTime/EndTime=YYYY-MM-DD`（旧站格式）时 list 返回 10000；unix 秒可通但空；主配置 `Items=null` |
| API-031 | 2026-07-17 | 待反馈 | 未处理 | — | P2 | 票券中心 `/operationalManage/voucher` | `GET /backend/voucher/list`<br>`GET /backend/voucher/getvouchermainconfig`<br>`GET /backend/voucher/voucherdetailrecord`<br>`GET /backend/voucher/issuevoucherrecord` | 列表/记录无数据时 `Items=null`；主配置 `Items=null`；发放记录缺参返回 10037；本环境多为空需确认是否本应有票券 |
| API-032 | 2026-07-18 | 待验证 | 部分✅（积分记录，本地待发布） | **通过**（point/recordlist，2026-08-03） | P1 | 积分商城 `/operationalManage/rewardMall` | 见详情（商品/兑换/积分记录/主配置/积分返水等） | 多列表无数据 `Items=null`；主配置/页签/任务 `Items=null`；**积分记录 `point/recordlist` 返回 10001 Database Exception**；本环境商品/兑换多为空 |
| API-033 | 2026-07-18 | 待反馈 | 未处理 | — | P1 | 杯赛专题 `/operationalManage/customLeague` | `GET /backend/gamecustomleague/list`<br>`GET /backend/gamecustomleague/leaguelist` | 列表无数据 `Items=null`；**联赛下拉 `leaguelist` 返回 10163 Related service request error**（本环境 GameType=7/8 均失败） |
| API-043 | 2026-07-18 | 待反馈 | 未处理 | — | P2 | 身份验证 `/memberManage/playerAuthentication` | `GET /backend/playerauthentication/list`<br>`GET /backend/playerauthentication/record`<br>`GET /backend/playerauthentication/setting`<br>`GET /backend/playerauthentication/recordexport` | 审核列表/记录无数据时 `Items=null`；`setting` 缺 `SubType=1005`（验证信息开关）；导出需密码（pageId=90） |
| API-044 | 2026-07-18 | 待反馈 | 未处理 | — | P2 | 钱包管理 `/memberManage/walletManage` | `GET /backend/playerbankcard/list`<br>`GET /backend/playerwalletaccount/listall`<br>`GET /backend/playerwithdrawdigitaladdress/list`<br>`GET /backend/playerbindcardconfig/listcardmultiple`<br>`POST /backend/playerinfo/queryplayerexcel` | 电子钱包/虚拟币/多绑定无数据 `Items=null`；`queryplayerexcel` 返回 10053（前端已改用 `queryplayer`） |
| API-045 | 2026-07-18 | 待反馈 | 未处理 | — | P2 | 验证码查询 `/memberManage/verifyCode` | `GET /backend/phoneverifycode/list`<br>`GET /backend/emailverifycode/list`<br>`GET /backend/emailverifycode/listemailconfig`<br>`GET /backend/phonecountrycode/list`<br>`GET /backend/gameotpconfig/getconfig` | 手机验证码缺参返回 10037；邮箱验证码/通道本环境 `respond=null`；区码与 OTP 正常 |
| API-046 | 2026-07-18 | 待反馈 | 未处理 | — | P2 | 称号管理 `/operationalManage/gameTitle` | `GET /backend/playerbadge/list`<br>`GET /backend/badgecategory/list`<br>`GET /backend/playerbadge/playerlist`<br>`GET /backend/playerbadge/playerlistcsv` | 称号/类别/拥有者本环境多为空 `Items=null`；导出需密码（pageId=75） |
| API-047 | 2026-07-18 | 待反馈 | 未处理 | — | P2 | 玩家详情·注单 `/operationalManage/playerDetails` | `GET /backend/operation/gametransactiondetail`<br>`GET /backend/operation/gametransactiondetaillog`<br>`GET /backend/operation/getbyorderid` | 账号 `18857985770` 有注单；`SumAll=1` 时 Items 为空但 MoreItems 有汇总；`getbyorderid` 常失败但行内 `Detail` 可展示三方详情；结算流水常 `respond=null` |
| API-049 | 2026-07-19 | 待反馈 | 未处理 | — | P1 | 短信管理·通道配置/首存次日召回 `/gameManage/messageManage` | `GET /backend/smschannelconfig/list`<br>`GET /backend/operation/depositrecall` | 测试账号具备 `12906/12908/13414` 权限，但通道列表无参/带分页均返回 `10000`；首存召回按旧版日期参数测试单日及区间均返回 `10000`，同模块详情接口正常 |
| API-050 | 2026-07-19 | 待反馈 | 未处理 | — | P2 | 短信管理 `/gameManage/messageManage` | `GET /backend/shortmessageservice/list`<br>`GET /backend/shortmessageservice/monthlist`<br>`GET /backend/operation/phoneregisterotpday`<br>`GET /backend/operation/phoneregisterotpdetail`<br>`GET /backend/operation/registerrecall`<br>两类召回详情 | 本环境无数据时多数列表返回 `Items=null`，仅短信模板返回 `Items=[]`，空列表结构不一致；页面已统一兼容 |
| API-051 | 2026-07-19 | 待反馈 | 未处理 | — | P2 | 返水配置 `/gameManage/backWater` | `GET /backend/playerbackwaterrecord/package` | 测试账号具备返水页面及全部子操作权限（`11073~11100`、`12662~12674` 对应权限均存在），接口仍返回 `10010`「权限不足([权限配置不正确])」；当前页面改用全局产品配置，不阻塞查询 |
| API-052 | 2026-07-19 | 待反馈 | 未处理 | — | P2 | 返水记录/审核 `/gameManage/backWater` | `GET /backend/playerbackwaterrecord/list`<br>`GET /backend/playerbackwaterrecord/detaillist`<br>`GET /backend/playerbackwaterrecord/reviewlist` | 昨日合法查询参数均返回 `status=200`，但空数据统一为 `Items=null`；三个接口分别使用 `Total` / `Count`，且均不返回 `Pagination`，列表总数字段结构不一致；页面已兼容 |
| API-071 | 2026-07-20 | 待反馈 | 未处理 | — | P2 | 玩家统计/玩家盈亏/玩家分析 `/dataClose/playerStatistics`<br>`/dataClose/userWinLoss`<br>`/dataClose/playerAnalyze` | `GET /backend/operation/playerstatistics`<br>`GET /backend/operation/userstatistics`<br>`GET /backend/playeranalysis/playerlist`<br>`GET /backend/operation/playerstatisticscsv` | 无数据时 `Items`/`MoreItems` 常为 `null`（非 `[]`）；玩家统计需 `SearchType=list/total` 双请求；CSV 导出走 PageId=46；前端已用 `toListResult` 归一化 |
| API-073 | 2026-07-20 | 待验证 | 不存在（现网复测） | **通过**（JSON 已合法，2026-08-03） | P1 | 溢出管理 `/netcash/spillManage` | `GET /backend/agentplayermanage/list` | 响应非法 JSON：`respond: success`（未加引号），列表无法解析 |
| API-074 | 2026-07-20 | 待验证 | ✅（现网） | **通过**（2026-08-03） | P1 | 记录查询 `/netcash/recordInquire` | `GET /backend/recordquery/loginlist` | 传 `PageSize=5` 仍返回 144 条，且 `Pagination=null` |
| API-075 | 2026-07-20 | 待反馈 | 未处理 | — | P1 | 提款管理 `/netcash/drawmoneyManage` | `GET .../agentwithdrawaccountdetail`<br>`GET .../check/{id}`<br>`GET .../statuslist` | 合法 Id 详情/校验返回 `10001`；`statuslist` 成功但 `respond=null` |
| API-076 | 2026-07-20 | 待反馈 | 未处理 | — | P2 | 提款管理 `/netcash/drawmoneyManage` | `GET .../list`<br>`GET .../agentwithdrawaccountlist`<br>`GET .../withdrawlist`<br>`GET /api/loginuser/getstatus` | 空列表 `Items/Total=null`；自动刷新状态 `10010` 权限不足 |
| API-077 | 2026-07-20 | 待反馈 | 未处理 | — | P1 | 佣金管理 `/netcash/commissionManage` | `GET /backend/commissionconfig/list`<br>`GET /backend/commissionconfigmulti/list`<br>`GET /backend/sendcommission/teamdetail` 等 | 配置 `respond=null`；多层配置 `10304`；`teamdetail` 对同 Id 返回 `10000` |
| API-078 | 2026-07-20 | 待反馈 | 未处理 | — | P1 | 代客额度 `/netcash/dkCreditManage` | `GET .../getagentdkaccountlimit`<br>`GET /backend/sharedComponent/list` | 额度概览 `10505`；共享组件 `20001` 远程未连接 |
| API-079 | 2026-07-20 | 待反馈 | 未处理 | — | P2 | 创建金钱渠道 `/netcash/createMoneyChannel` | `GET /backend/gameadtemplate/getcustomlogo` | 业务 ChannelId 字符串 `10000`；数字主键可通但 `Items=null` |
| API-080 | 2026-07-20 | 待反馈 | 未处理 | — | P2 | 代理账号详情 `/netcash/agencyAccountDetails` | `GET .../accountloginip/list` 等 | 登录 IP / 直属代理统计 / 提款账户空结果时 `Items` 或 `Pagination` 为 `null` |
| API-081 | 2026-07-20 | 待反馈 | 未处理 | — | P2 | 额度/素材/佣金等多页 | 多列表 GET | 无数据时 `Items=null`，与同批已返回 `[]` 的接口不一致 |
| API-082 | 2026-07-20 | 待反馈 | 未处理 | — | P1 | 投注行为报表 `/operationalData/gameDetails` | `GET /backend/operation/gametransactionanalysisreport` | 多组合法日期/`DataSearchType` 均 `status=200` 但 `Items=null`，`message` 为空 |
| API-083 | 2026-07-20 | 待验证 | ✅（现网） | **通过**（2026-08-03） | P2 | 运营日报 `/dataClose/operationDaily` | `GET /backend/operationdayreport/dayreport` | `TodayItems`/`BeforeItems` 有数据时，`TodayHourItems`/`BeforeHourItems` 仍为 `null`，分时图无数据 |
| API-084 | 2026-07-20 | 待反馈 | 未处理 | — | P2 | 粘度分析 `/dataClose/keepData` | `GET /backend/operation/realtimedau`<br>`GET /backend/operation/realtimeltv`<br>`GET /backend/operation/everydaylogindau` | 登录留存/LTV 仅返回 `ItemsOld`（无 `Items`）；区间留存 `Items` 为对象而非数组 |
| API-085 | 2026-07-20 | 待反馈 | 未处理 | — | P2 | 游戏报表 `/dataClose/gameStatement` | `GET /backend/operation/gamestatisticsbygametype` | 游戏 Tab 成功时只返回 `ItemsMoney`（无 `Items`）；分类/子游戏同域接口返回 `Items`，字段名不一致 |
| API-086 | 2026-07-20 | 待验证 | ✅（现网） | **通过**（2026-08-03） | P2 | 运营日报·游戏盈亏排行 `/dataClose/operationDaily` | `GET /backend/operationdayreport/dayreportwinrank` | 有 `TodayGameTypeWinItems`/`TodayPlayerItemsLose` 时，`TodayGameTypeLoseItems` 仍为 `null` |
| API-087 | 2026-07-20 | 待反馈 | 未处理 | — | P2 | 运营日报·数据报表 / 日/月报子包网 | `GET /backend/operationdayreportscheduler/list`<br>`GET /backend/agentbetwinmanageday/sonlist`<br>`GET /backend/agentbetwinmanagemonth/sonlist` | 空结果 `Items=null`（非 `[]`）；子包网另返回空对象 `ItemsAgents={}` |

---

---

## 前端复测记录（2026-08-03）

> 账号：`leying_niu` · 环境：`ccccc-admin.dk888.link`  
> 范围：仅核验修改版中「后端解决」非「未处理」的条目（共 12 条）。

| 编号 | 后端标记 | 前端结论 | 实测摘要 |
| --- | --- | --- | --- |
| API-001 | ✅现网 | **通过** | `TotalHours`：今日 19 段、昨日 22 段、自选 `Date1=2026-07-31` 24 段，均有非零金额 |
| API-003 | ✅本地待发布 | **通过（现网已可见）** | `ItemsWin`/`ItemsLose` 均为数组，不再为 `null`（亏损侧可为空数组） |
| API-010 | ✅本地待发布 | **通过（现网已可见）** | `ItemsWin`/`ItemsLose` 均为数组；无亏损时 `ItemsLose=[]` |
| API-012 | ✅现网 | **通过** | 不再 `20001`；`selfreviewlist`/`statisticslist` 均 `status=200` |
| API-013 | ✅本地待发布 | **通过（现网已可见）** | `checkconfig` → `200`，返回 `SelfReviewConfig.DialogSwitch=0` |
| API-018 | 不存在 | **通过** | `customsupport/1` 现返回 `10002 无法找到该记录`（不再 `10001` 库异常） |
| API-020 | ✅现网 | **通过** | `visitstatistic/list`、`statisticslist` 均 `status=200`（不再 `20001`） |
| API-032 | 部分✅本地待发布 | **通过（积分记录）** | `point/recordlist` → `200`，`Items=[]`（不再 `10001`） |
| API-073 | 不存在 | **通过** | 响应为合法 JSON；`respond` 为对象且含 `Pagination`（不再裸 `success`） |
| API-074 | ✅现网 | **通过** | `PageSize=5` 有数据时返回 5 条并带 `Pagination`（例近 31 日 `MaxCount=47`） |
| API-083 | ✅现网 | **通过** | `TodayHourItems`/`BeforeHourItems` 均为长度 22 的数组（不再 `null`） |
| API-086 | ✅现网 | **通过** | `TodayGameTypeLoseItems` 为数组（可为 `[]`，不再 `null`） |

**结论：** 后端本批标注修复/不存在的 12 项，现网接口复测 **全部通过**。其中标注「本地待发布」的 003/010/013/032 在现网已可测通。
