# 后台模版使用说明

> 基于 [vue-vben-admin](https://github.com/vbenjs/vue-vben-admin) **5.7.0** 裁剪的 **web-antd 专用模版**。  
> 本文档面向团队开发与维护人员，说明：**如何拷贝模版、如何使用、文件放哪、如何接 API、如何用 OpenAPI/AI 开发、如何升级模版**。

---

## 目录

1. [模版是什么](#1-模版是什么)
2. [环境要求](#2-环境要求)
3. [拷贝模版（创建新项目）](#3-拷贝模版创建新项目)
4. [使用模版（新项目开工流程）](#4-使用模版新项目开工流程)
5. [文件类型与目录对照表](#5-文件类型与目录对照表)
6. [环境变量与 API 配置详解](#6-环境变量与-api-配置详解)
7. [OpenAPI / Postman 与 AI 编码](#7-openapi--postman-与-ai-编码)
8. [新增业务模块标准流程](#8-新增业务模块标准流程)
9. [保留的基础页面](#9-保留的基础页面)
10. [升级模版（同步 vben 官方）](#10-升级模版同步-vben-官方)
11. [团队分工与协作建议](#11-团队分工与协作建议)
12. [常用命令](#12-常用命令)
13. [常见问题](#13-常见问题)
14. [附录：新项目 Checklist](#14-附录新项目-checklist)

---

## 1. 模版是什么

### 1.1 定位

| 项 | 说明 |
|---|---|
| 类型 | 前端后台管理 **脚手架模版**（非某个具体业务系统） |
| UI | Ant Design Vue，主应用 `apps/web-antd` |
| 目标 | 新项目改好 API 配置 → 导入接口文档 → 开发业务页面 |
| 升级策略 | 业务只写在 `apps/web-antd`，框架层尽量与官方 vben 保持一致 |

### 1.2 已包含

- 登录 / 个人中心 / 仪表盘（分析页、工作台）/ 异常页
- 布局、权限、Tab、主题、国际化、请求封装、表单与表格适配
- 本地 Mock 服务 `apps/backend-mock`（可选）
- `api-docs/` 接口文档目录 + **虚构示例** `swagger/example.openapi.json`
- Cursor AI 规则 `.cursor/rules/web-antd-template.mdc`

### 1.3 已裁剪（相对官方 vben）

| 已移除 | 原因 |
|---|---|
| `web-ele` / `web-naive` / `web-tdesign` / `web-antdv-next` | 团队只用 Ant Design Vue |
| `docs/`、`playground/` | 演示能力由 `web-antd` 自带页面承担 |
| 左侧菜单「演示」「项目」「关于」 | 减少干扰，保留通用基础页 |

### 1.4 仓库结构总览

```text
vben-admin/                          # 模版仓库根目录
├── 模版使用说明.md                   # ★ 本文档
├── api-docs/                        # 接口文档（Swagger / Postman）
│   ├── README.md
│   └── swagger/
│       └── example.openapi.json     # 虚构示例，仅供学习
├── apps/
│   ├── web-antd/                    # ★★ 业务开发主战场
│   └── backend-mock/                # 本地 Mock API
├── packages/                        # vben 框架共享包（尽量少改）
├── internal/                        # 构建、lint、vite 配置
├── scripts/                         # 工具脚本
├── .cursor/rules/                   # AI 编码约定
├── package.json
└── pnpm-workspace.yaml
```

---

## 2. 环境要求

| 工具 | 版本 | 备注 |
|---|---|---|
| Node.js | `^20.19.0` / `^22.18.0` / `^24.0.0` | 见 `.node-version` |
| pnpm | `>= 10.0.0` | 仓库锁定 `10.33.0`，必须用 pnpm |
| Git | 建议安装 | 用于版本管理与 lefthook |

```bash
corepack enable
corepack prepare pnpm@10.33.0 --activate
node -v
pnpm -v
```

---

## 3. 拷贝模版（创建新项目）

> **模版仓库**建议由团队单独维护一份「黄金模版」；每个新业务项目从模版**复制一份**，不要多个产品共改同一个仓库。

### 3.1 方式 A：从 Git 模版仓库克隆（推荐）

适合：团队已把本仓库 push 到 GitLab / GitHub。

```bash
# 1. 克隆模版
git clone <模版仓库地址> my-new-admin
cd my-new-admin

# 2. 断开与模版仓库的历史（可选，推荐新项目这样做）
rm -rf .git
git init
git add .
git commit -m "chore: init from vben-admin template 5.7.0"

# 3. 关联新业务远程仓库
git remote add origin <新业务仓库地址>
git push -u origin main
```

### 3.2 方式 B：直接复制文件夹

适合：模版在本地、无需 Git 历史。

```bash
cp -R vben-admin/ ../客户A-后台/
cd ../客户A-后台/

# 必须删除依赖与构建产物后重装
rm -rf node_modules apps/*/node_modules apps/web-antd/dist
pnpm install
```

### 3.3 方式 C：GitHub Template / 组织模版

在 Git 平台将本仓库标记为 **Template repository**，成员点「Use this template」即可生成独立仓库。  
优点：自带独立 Git 历史，不会和模版仓库混在一起。

### 3.4 拷贝后必须修改的「身份项」

| 项 | 文件 | 说明 |
|---|---|---|
| 系统名称 | `apps/web-antd/.env` → `VITE_APP_TITLE` | 浏览器标题、系统名 |
| 存储命名空间 | `VITE_APP_NAMESPACE` | **每个项目必须不同**，避免 localStorage 冲突 |
| 加密密钥 | `VITE_APP_STORE_SECURE_KEY` | 随机字符串，勿用模版默认值 |
| Logo | `apps/web-antd/public/` | 替换 favicon、logo 图片 |
| API 地址 | `.env.development` / `.env.production` | 见第 6 节 |
| 仓库名 | 根 `package.json` 的 `name`（可选） | 如 `customer-a-admin` |

**不要修改：** `packages/`、`internal/` 除非框架升级或确有定制需求。

---

## 4. 使用模版（新项目开工流程）

### 4.1 第一天：跑起来（约 15 分钟）

```bash
# 1. 进入项目
cd my-new-admin

# 2. 安装依赖
pnpm install
# 若无 .git：CI=true pnpm install

# 3. 复制环境变量
cp apps/web-antd/.env.example apps/web-antd/.env

# 4. 编辑配置（至少改标题、namespace、API）
#    apps/web-antd/.env
#    apps/web-antd/.env.development
#    apps/web-antd/.env.production

# 5. 启动
pnpm dev
```

浏览器打开 http://localhost:5666/ ，使用 mock 账号 **vben / 123456** 登录（`VITE_NITRO_MOCK=true` 时）。

### 4.2 第二天：接入真实后端

1. 向后端索要 Swagger 或 Postman，放入 `api-docs/`  
2. 修改 `.env.development`：

```env
VITE_GLOB_API_URL=/api
VITE_API_PROXY_TARGET=https://你的后端域名/api
VITE_API_SUCCESS_CODE=200
VITE_NITRO_MOCK=false
```

3. 若登录接口不是 `/auth/login` 或返回字段不是 `accessToken`，改：
   - `apps/web-antd/src/api/core/auth.ts`
   - `apps/web-antd/src/store/auth.ts`
   - `apps/web-antd/src/views/_core/authentication/login.vue`

4. `pnpm dev` 验证登录与首个业务接口。

### 4.3 第三天起：按模块迭代

每个业务模块按 [第 8 节](#8-新增业务模块标准流程) 增加 `api` → `types` → `router` → `views` → `i18n`。

### 4.4 开发态数据流示意

```text
浏览器
  → 请求 /api/xxx
  → Vite 开发服务器代理（vite.config.ts + VITE_API_PROXY_TARGET）
  → 后端真实 API  或  backend-mock:5320
  → 响应 { code, data, message }
  → request.ts 按 VITE_API_SUCCESS_CODE 判断是否成功
  → 页面展示
```

---

## 5. 文件类型与目录对照表

> **黄金法则：业务代码只写在 `apps/web-antd/src/`。**  
> 下表是「我要做 X，文件放哪、叫什么」的快速查阅。

### 5.1 业务开发（团队日常）

| 你要做的事 | 文件类型 | 放置目录 | 命名示例 |
|---|---|---|---|
| 新页面 | `.vue` | `apps/web-antd/src/views/<模块>/` | `views/order/index.vue` |
| 子页面 / 详情 | `.vue` | `apps/web-antd/src/views/<模块>/` | `views/order/detail.vue` |
| 可复用业务组件 | `.vue` | `apps/web-antd/src/components/` | `components/order-status-tag.vue` |
| 路由 | `.ts` | `apps/web-antd/src/router/routes/modules/` | `modules/order.ts` |
| 接口请求 | `.ts` | `apps/web-antd/src/api/<模块>/` | `api/order/index.ts` |
| 全局请求配置 | `.ts` | `apps/web-antd/src/api/` | `request.ts`（一般只改配置） |
| 登录等核心 API | `.ts` | `apps/web-antd/src/api/core/` | `auth.ts`、`user.ts` |
| TS 业务类型 | `.ts` | `apps/web-antd/src/types/` | `types/orderTypes.ts` |
| 应用级状态 | `.ts` | `apps/web-antd/src/store/` | `store/order.ts` |
| 组合式函数 | `.ts` | `apps/web-antd/src/composables/` | `useOrderFilters.ts` |
| 工具函数 | `.ts` | `apps/web-antd/src/` 下自建 `utils/` | `utils/format.ts` |
| 中文文案 | `.json` | `apps/web-antd/src/locales/langs/zh-CN/` | `page.json` |
| 英文文案 | `.json` | `apps/web-antd/src/locales/langs/en-US/` | `page.json` |
| 应用偏好 / 首页 | `.ts` | `apps/web-antd/src/` | `preferences.ts` |
| 静态图片 / Logo | 图片 | `apps/web-antd/public/` | `logo.png` |
| 环境变量 | 无扩展名 | `apps/web-antd/` | `.env`、`.env.development` |
| 构建代理 | `.ts` | `apps/web-antd/` | `vite.config.ts` |

### 5.2 接口文档

| 你要做的事 | 文件类型 | 放置目录 | 命名示例 |
|---|---|---|---|
| Swagger / OpenAPI | `.json` / `.yaml` | `api-docs/swagger/` | `order.openapi.json` |
| 学习用虚构示例 | `.json` | `api-docs/swagger/` | `example.openapi.json`（已内置） |
| Postman 集合 | `.json` | `api-docs/postman/` | `order.collection.json` |

### 5.3 框架层（升级时再动，日常少改）

| 目录 | 作用 | 团队是否常改 |
|---|---|---|
| `packages/@core/*` | 基础 UI、偏好、工具 | 否 |
| `packages/effects/*` | 布局、权限、请求、插件 | 否 |
| `packages/*` | 常量、图标、stores 等 | 否 |
| `internal/vite-config` | Vite 通用配置 | 极少 |
| `internal/lint-configs` | ESLint / oxfmt 等 | 否 |
| `apps/backend-mock` | 本地假数据 API | 可按需加 mock 路由 |

### 5.4 路由模块自动加载机制

`apps/web-antd/src/router/routes/index.ts` 会通过 `import.meta.glob('./modules/**/*.ts')` **自动加载** `modules/` 下所有路由文件。  
因此新增业务路由时：**只需新建 `modules/xxx.ts`，无需改 index.ts**。

### 5.5 路径别名

| 别名 | 指向 |
|---|---|
| `#/*` | `apps/web-antd/src/*` |

代码中引用示例：`import { xxx } from '#/api/order'`。

---

## 6. 环境变量与 API 配置详解

所有环境变量模板见：**`apps/web-antd/.env.example`**。

### 6.1 文件分工

| 文件 | 何时生效 | 典型内容 |
|---|---|---|
| `.env` | 所有环境 | 应用名、namespace、密钥、可选上传地址 |
| `.env.development` | `pnpm dev` | 端口、代理目标、是否开 Mock、成功 code |
| `.env.production` | `pnpm build` | 生产 API 完整 URL、压缩、路由模式 |
| `.env.analyze` | 构建分析 | 包体积可视化 |

### 6.2 核心变量说明

| 变量 | 必填 | 说明 |
|---|---|---|
| `VITE_APP_TITLE` | 是 | 系统名称 |
| `VITE_APP_NAMESPACE` | 是 | 本地缓存前缀，**每项目唯一** |
| `VITE_APP_STORE_SECURE_KEY` | 是 | Pinia 持久化加密密钥 |
| `VITE_GLOB_API_URL` | 是 | 前端 axios baseURL；开发多为 `/api` |
| `VITE_API_PROXY_TARGET` | 开发建议 | Vite 把 `/api` 转发到的后端地址 |
| `VITE_API_SUCCESS_CODE` | 是 | 后端 JSON 里 `code` 的成功值；mock=`0`，很多业务=`200` |
| `VITE_NITRO_MOCK` | 否 | `true` 时 dev 自动起 mock；接真实后端改 `false` |
| `VITE_FILE_UPLOAD_URL` | 否 | 上传接口根地址，缺省走 `VITE_GLOB_API_URL` |
| `VITE_STATIC_URL` | 否 | 静态资源根地址，用于拼接头像等 |

### 6.3 三种常见场景

**场景 1：纯本地 Mock 开发**

```env
# .env.development
VITE_GLOB_API_URL=/api
VITE_API_PROXY_TARGET=http://localhost:5320/api
VITE_API_SUCCESS_CODE=0
VITE_NITRO_MOCK=true
```

**场景 2：开发连测试服**

```env
VITE_GLOB_API_URL=/api
VITE_API_PROXY_TARGET=https://test-api.example.com/api
VITE_API_SUCCESS_CODE=200
VITE_NITRO_MOCK=false
```

**场景 3：生产构建**

```env
# .env.production
VITE_GLOB_API_URL=https://api.example.com/api
VITE_API_SUCCESS_CODE=200
```

### 6.4 `request.ts` 与后端约定

`apps/web-antd/src/api/request.ts` 默认认为响应格式为：

```json
{
  "code": 0,
  "data": {},
  "message": "ok"
}
```

若后端字段名不同（如 `msg`、`status`），在 `defaultResponseInterceptor` 与 `errorMessageResponseInterceptor` 中调整。

---

## 7. OpenAPI / Postman 与 AI 编码

### 7.1 内置虚构示例

路径：**`api-docs/swagger/example.openapi.json`**

| 说明 | 内容 |
|---|---|
| 是否真实接口 | **否**，全部为虚构示例 |
| 用途 | 培训新人、演示 AI 如何读文档写代码 |
| 包含模块 | 认证、用户、虚构「订单」CRUD |
| 响应约定 | `code: 0` 表示成功 |

**不要把它当生产接口使用。** 真实项目请让后端导出 Swagger，保存为 `api-docs/swagger/<项目>.openapi.json`。

### 7.2 OpenAPI 是什么、要解决什么问题

| 问题 | OpenAPI 的作用 |
|---|---|
| 接口有哪些 | `paths` 列出路径与方法 |
| 入参是什么 | `parameters`、`requestBody` |
| 返回什么结构 | `responses` → `schemas` |
| 前端类型怎么写 | 按 `schemas` 生成 `types/*.ts` |
| AI 怎么写页面 | 把文档 `@` 进对话，减少猜字段 |

### 7.3 团队标准工作流

```text
1. 后端导出 Swagger
      ↓
2. 放入 api-docs/swagger/xxx.openapi.json
      ↓
3. 核对 code 成功值、分页字段 → 改 .env / request.ts（如需要）
      ↓
4. Cursor 引用文档生成 api + types + router + views
      ↓
5. 人工或 AI 自检：路径、字段、i18n、权限
      ↓
6. pnpm dev 联调
      ↓
7. 后端变更 → 更新 swagger → 同步改前端
```

### 7.4 Cursor 提示词模板

**生成完整模块：**

```text
请阅读 @api-docs/swagger/example.openapi.json 中 tags 为「订单」的接口，
在 apps/web-antd 生成订单管理模块：

1. src/api/order/index.ts
2. src/types/orderTypes.ts
3. src/router/routes/modules/order.ts
4. src/views/order/index.vue（搜索 + 表格 + 分页）

约束：
- 使用 #/api/request 的 requestClient
- 成功 code 与 .env 中 VITE_API_SUCCESS_CODE 一致
- meta.title 使用 page.json 中的 i18n key
- 参考 views/dashboard 的布局与 adapter/form、adapter/vxe-table
- 不要修改 packages/
```

**只生成 API 层：**

```text
根据 @api-docs/swagger/真实文档.openapi.json 的 paths，
仅生成 apps/web-antd/src/api 和 src/types，不要写页面。
```

### 7.5 Postman 用法

1. Export Collection v2.1 → `api-docs/postman/`  
2. 对话中 `@api-docs/postman/xxx.collection.json`  
3. 密码、Token 放 Postman Environment，**勿提交到 Git**

### 7.6 AI 生成后自检清单

- [ ] 请求路径、Method 与文档一致  
- [ ] `VITE_API_SUCCESS_CODE` 与后端一致  
- [ ] 分页参数名一致（如 `page` / `pageSize`）  
- [ ] 路由已出现在左侧菜单（`meta.title`、`icon`）  
- [ ] zh-CN / en-US 文案已补充  
- [ ] 未误改 `packages/`  
- [ ] `pnpm dev` 无报错，接口无 CORS（开发应用代理）

### 7.7 相关文件

- 更细的 API 目录说明：`api-docs/README.md`  
- AI 规则：`.cursor/rules/web-antd-template.mdc`

---

## 8. 新增业务模块标准流程

以「订单管理」为例（可与 `example.openapi.json` 对照）。

### 步骤 1：文档就位

```text
api-docs/swagger/order.openapi.json
```

### 步骤 2：类型定义

```text
apps/web-antd/src/types/orderTypes.ts
```

### 步骤 3：API 封装

```text
apps/web-antd/src/api/order/index.ts
```

```ts
// 示例结构
import { requestClient } from '#/api/request';
import type { Order, OrderQuery } from '#/types/orderTypes';

export function getOrderList(params: OrderQuery) {
  return requestClient.get<{ items: Order[]; total: number }>('/order/list', { params });
}
```

### 步骤 4：路由

```text
apps/web-antd/src/router/routes/modules/order.ts
```

### 步骤 5：页面

```text
apps/web-antd/src/views/order/index.vue
```

### 步骤 6：国际化

在 `locales/langs/zh-CN/page.json` 与 `en-US/page.json` 增加：

```json
"order": {
  "title": "订单管理",
  "list": "订单列表"
}
```

路由 `meta.title` 使用 `'page.order.title'`。

### 步骤 7：验证

```bash
pnpm dev
# 登录 → 打开订单菜单 → 看网络请求与表格数据
```

---

## 9. 保留的基础页面

| 模块 | 路由 | 用途 | 是否可删 |
|---|---|---|---|
| 分析页 | `/analytics` | 图表示例，默认首页 | 可删，需改 `preferences.ts` 的 `defaultHomePath` |
| 工作台 | `/workspace` | 卡片布局示例 | 可选 |
| 登录 | `/auth/login` | 账号密码登录 | 建议保留 |
| 验证码登录 | `/auth/code-login` | 示例 | 可删 |
| 扫码登录 | `/auth/qrcode-login` | 示例 | 可删 |
| 注册 | `/auth/register` | 示例 | 可删 |
| 忘记密码 | `/auth/forget-password` | 示例 | 可删 |
| 个人中心 | `/profile` | 资料 / 安全 / 密码 | 建议保留 |
| 异常页 | 403 / 404 / 500 等 | 兜底 | 建议保留 |

删除认证子页时，同步修改 `router/routes/core.ts` 中对应 children。

---

## 10. 升级模版（同步 vben 官方）

### 10.1 为什么要升级

- 安全修复、依赖更新  
- Vue / Vite / vben 新特性  
- 官方 bug 修复  

### 10.2 升级原则

| 尽量少改（跟随官方合并） | 可以改（本模版定制） |
|---|---|
| `packages/**` | `apps/web-antd/**` |
| `internal/**`（谨慎） | `api-docs/**` |
| `pnpm-workspace.yaml` 的 catalog 主体 | 根 `package.json` 的 scripts 裁剪 |
| `apps/backend-mock/**`（按需） | `.cursor/rules/**` |

**业务项目升级时：** 优先在「模版仓库」完成升级与验证，再让各业务项目拉取新版模版或 merge。

### 10.3 模版维护者升级流程（推荐）

```bash
# 0. 在模版仓库执行，确保工作区干净
git status

# 1. 添加上游远程（仅首次）
git remote add vben https://github.com/vbenjs/vue-vben-admin.git
git remote -v

# 2. 拉取官方标签（版本号按官方最新调整）
git fetch vben --tags

# 3. 基于当前 main 建升级分支
git checkout -b upgrade/vben-5.8.0

# 4. 合并官方 tag（示例）
git merge v5.8.0
# 或使用：git cherry-pick / 手动对比，视冲突情况而定

# 5. 解决冲突 —— 重点文件见 10.5

# 6. 安装依赖并验证
pnpm install
pnpm dev
pnpm build:antd
pnpm check:type
pnpm lint

# 7. 合并回 main，打模版版本 tag
git checkout main
git merge upgrade/vben-5.8.0
git tag template-v5.8.0
git push origin main --tags
```

### 10.4 业务项目如何跟随模版升级

| 方式 | 做法 | 适用 |
|---|---|---|
| 重新拷贝 | 用新版模版建新目录，把 `apps/web-antd/src` 业务复制过去 | 业务较少 |
| Git merge | 业务仓库添加模版为 remote，`git merge template/vX.X` | 长期项目 |
| 手工对比 | 仅把框架层变更 cherry-pick 到业务仓库 | 重度定制 |

推荐业务仓库**只重度定制 `apps/web-antd/src`**，这样每次升级只需：

1. 合并模版仓库更新  
2. 解决 `apps/web-antd` 内冲突  
3. 回归登录 + 核心业务页  

### 10.5 合并冲突时优先核对清单

**本模版相对官方的定制（合并后需恢复）：**

- [ ] `apps/web-ele` 等额外 UI 应用仍删除状态  
- [ ] `docs/`、`playground/` 仍删除状态  
- [ ] 无 `demos.ts`、`vben.ts` 路由  
- [ ] 存在 `profile.ts` 路由模块  
- [ ] `apps/web-antd/.env.example` 含 API 相关变量  
- [ ] `vite.config.ts` 使用 `VITE_API_PROXY_TARGET`  
- [ ] `request.ts` 使用 `VITE_API_SUCCESS_CODE`  
- [ ] 根 `package.json` 的 `dev` → `dev:antd`  
- [ ] `api-docs/` 与 `模版使用说明.md` 仍在  

**官方可能覆盖、需手工合并的文件：**

- [ ] `apps/web-antd/vite.config.ts`  
- [ ] `apps/web-antd/src/api/request.ts`  
- [ ] `apps/web-antd/src/bootstrap.ts`  
- [ ] `apps/web-antd/src/preferences.ts`  
- [ ] `apps/web-antd/package.json`  
- [ ] `pnpm-workspace.yaml` / `pnpm-lock.yaml`  

### 10.6 升级失败回滚

```bash
git merge --abort
# 或
git reset --hard HEAD~1
```

建议在升级分支完成验证后再合并 `main`。

---

## 11. 团队分工与协作建议

| 角色 | 职责 |
|---|---|
| 模版维护者（1 人） | 跟进 vben 官方版本、合并升级、维护 `模版使用说明.md` |
| 后端 | 提供 Swagger / 测试环境；约定 `code`、分页字段 |
| 前端 | 在 `apps/web-antd/src` 开发业务；更新 `api-docs` |
| 全员 | 新业务从模版拷贝，不共用一个业务仓库 |

### Git 分支建议（业务项目）

```text
main          # 生产
develop       # 开发
feature/xxx   # 功能分支
```

### 不要做的事

- 不要在 `packages/` 里写某个客户独有业务  
- 不要把客户真实 API 密钥写进 `.env` 并提交（用 `.env.local` 或 CI 注入）  
- 不要多个产品共用一个 `VITE_APP_NAMESPACE`  

---

## 12. 常用命令

| 命令 | 说明 |
|---|---|
| `pnpm install` | 安装依赖 |
| `pnpm dev` | 启动 web-antd（5666） |
| `pnpm build:antd` | 生产构建 |
| `pnpm preview` | 预览构建结果 |
| `pnpm lint` | 格式化 + lint |
| `pnpm check:type` | TypeScript 检查 |
| `pnpm clean` | 清理构建缓存 |
| `CI=true pnpm install` | 跳过 lefthook 的安装方式 |

---

## 13. 常见问题

### Q1：`pnpm install` 报 `lefthook` / `git` 错误？

```bash
git init && pnpm install
# 或
CI=true pnpm install
```

### Q2：接口 HTTP 200 但前端提示失败？

检查 `VITE_API_SUCCESS_CODE` 是否与后端 `code` 一致。mock 一般为 `0`，很多业务 API 为 `200`。

### Q3：开发环境 CORS 跨域？

保持 `VITE_GLOB_API_URL=/api`，用 `VITE_API_PROXY_TARGET` 做服务端代理，不要让浏览器直连不同域后端。

### Q4：如何换 Logo 和系统名？

- Logo、图标 → `apps/web-antd/public/`  
- 名称 → `.env` 的 `VITE_APP_TITLE`  
- 侧栏 Logo → `preferences.ts` 的 `logo.source`

### Q5：如何关闭内置 Mock？

`.env.development` 设置 `VITE_NITRO_MOCK=false`，并配置真实 `VITE_API_PROXY_TARGET`。

### Q6：权限模式怎么选？

在 `preferences.ts` 设置 `app.accessMode`：

| 值 | 含义 |
|---|---|
| `frontend` | 路由写在前端，按角色过滤 |
| `backend` | 菜单由后端 `menu/all` 下发 |
| `mixed` | 混合模式 |

### Q7：`example.openapi.json` 和真实 Swagger 同时存在？

可以。示例文件保留作培训；真实文档用不同文件名，如 `crm.openapi.json`。

### Q8：多个业务项目如何共用模版改进？

在**模版仓库**改一次 → 打 tag → 各业务项目 merge 该 tag 或重新拷贝。

---

## 14. 附录：新项目 Checklist

```text
□ 从模版克隆或复制，并 git init
□ 修改 VITE_APP_TITLE、VITE_APP_NAMESPACE、VITE_APP_STORE_SECURE_KEY
□ 配置 VITE_GLOB_API_URL、VITE_API_PROXY_TARGET、VITE_API_SUCCESS_CODE
□ 替换 public/ 下 Logo、favicon
□ 导入 api-docs/swagger/真实文档（保留 example 作参考）
□ 非标准登录时改 api/core/auth.ts、store/auth.ts、login.vue
□ pnpm install && pnpm dev 验证登录
□ 删除不需要的认证子页面（可选）
□ 按模块添加 api / types / router / views / i18n
□ pnpm build:antd 验证生产构建
□ 将本项目登记到团队项目列表（负责人、仓库地址、API 环境）
```

---

## 参考链接

- [vben 官方文档](https://doc.vben.pro)  
- [vben GitHub](https://github.com/vbenjs/vue-vben-admin)  
- [Ant Design Vue](https://antdv.com)  
- [OpenAPI 规范](https://swagger.io/specification/)  
- 内置示例：`api-docs/swagger/example.openapi.json`  
- API 目录说明：`api-docs/README.md`  

---

**文档维护：** 模版结构变更、升级流程变化时，由模版维护者同步更新本文档版本号与日期。
