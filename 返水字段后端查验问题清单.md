# 返水字段后端查验问题清单

> 查验环境：`https://ccccc-admin.dk888.link`  
> 查验账号：`leying_niu`  
> 查验时间范围：**2026-07-01 ~ 2026-07-31**  
> 整理日期：2026-08-02  
> 说明：前端按现有字段映射读数；以下为接口实测结果，请后端协助确认数据补充是否完整、字段是否正确落库/汇总。

---

## 1. 前端字段约定（对照用）

| 场景 | 前端读取字段 | 主要接口 |
| --- | --- | --- |
| 日报类（公司/设备/VIP/上架包/渠道/分组/数据分析） | `SumBetWaterMoney` | 见下文各接口 |
| 玩家统计列表 | `BackWaterMoney` | `GET /backend/operation/playerstatistics` |
| 玩家统计合计 | `SumBackWaterMoney`（期望在 `Total`） | 同上 |
| 代理账号详情·代理数据 | `SumBackWaterMoney` | `/backend/agentnetcash/personalstats` 等 |
| 返水业务（记录/明细/审核/记录查询） | `BackWater` 等 | `/backend/playerbackwaterrecord/*`、`/backend/recordquery/backwaterlist` |
| 佣金场景 | `BackWaterGold` / `TotalBackWaterGold` | `/backend/sendcommission/*` |
| 玩家金币周期 | `BetWater` / `SumBetWater` | `GET /backend/playerinfo/goldtotal` |

金额单位：接口返回按**分**，前端 `/100` 展示为元。

---

## 2. 查验结论总览

| 状态 | 含义 | 涉及页面 |
| --- | --- | --- |
| ✅ 有非零数据 | 字段有值，补充看起来生效 | 公司日报·日报、渠道日报、数据分析、记录查询·返水、返水记录/明细、玩家详情·返水信息、部分代理·自身统计 |
| ⚠️ 字段存在但全 0 | 接口有字段，七月样本为 0，需确认是否应有业务数据 | 公司日报·设备、公司日报·VIP、佣金钱包/发放、部分代理 |
| ❌ 无历史行 / 空列表 | 七月查不到 `Items` 或 Count=0 | 上架包日报、代理分组日报、返水审核、佣金记录 |
| ❓ 参数/结构问题 | 需后端确认参数约定或合计字段名 | 玩家统计合计字段、手动返水、金币周期、佣金团队 |

---

## 3. 已通过（有非零返水）——供后端对照「正常样例」

| # | 页面 | 接口 | 字段 | 实测摘要 |
| --- | --- | --- | --- | --- |
| 1 | 公司日报 · 日报（历史） | `GET /backend/operation/dailyreport`（`SearchType=old`） | `SumBetWaterMoney` | 31 天均有非零；单日最大约 `2401978` 分（24019.78 元） |
| 2 | 运营数据 · 代理数据（渠道日报） | `GET /backend/operation/channelreport` | `SumBetWaterMoney` | 15 行中 14 行非零；最大约 `364069` 分 |
| 3 | 报表 · 数据分析 · 数据比较 | `GET /backend/operationdayreport/dataanalyze` | `SumBetWaterMoney` | 多周期对象中均有非零 |
| 4 | 代理管理 · 记录查询 · Tab「返水」 | `GET /backend/recordquery/backwaterlist` | `BackWater` / `BackWaterTotal` | 列表非零；总计约 `13716688` 分（137166.88 元） |
| 5 | 产品管理 · 返水设置 · 返水记录 | `GET /backend/playerbackwaterrecord/list` | `BackWater` / `SumBackWater` | 列表与合计均有非零 |
| 6 | 产品管理 · 返水设置 · 返水明细 | `GET /backend/playerbackwaterrecord/detaillist` | `BackWater` | 有非零 |
| 7 | 日常运营 · 玩家详情 · 返水信息 | `GET /backend/playerbackwaterrecord/list` | `BackWater` | 有非零 |
| 8 | 代理账号详情 · 代理数据 · 自身 | `GET /backend/agentnetcash/personalstats` | `SumBackWaterMoney` | **部分代理有值**，例如：`AdminId=707` → `473503`；`709` → `143285`；`708` → `674` |

---

## 4. 请后端重点排查

### 4.1 字段在，但七月全为 0

请确认：这些维度七月是否本应有返水；若应有，是否漏补统计表/汇总任务。

| # | 页面 | 接口 | 字段 | 实测 |
| --- | --- | --- | --- | --- |
| A1 | 公司日报 · 设备日报 | `GET /backend/operation/devicedayreport` | `SumBetWaterMoney` | 有字段，样本全 `0` |
| A2 | 公司日报 · VIP日报 | `GET /backend/operation/vipdayreport` | `SumBetWaterMoney` | 有字段，7 行全 `0` |
| A3 | 代理账号详情 · 佣金钱包 · 佣金信息 | `GET /backend/sendcommission/commissioninfolist` | `BackWaterGold` / `TotalBackWaterGold` | 有行，返水均为 `0` |
| A4 | 代理管理 · 佣金管理 · 发放 | `GET /backend/sendcommission/list` | `BackWaterGold` | 有行（约 11 条），均为 `0` |
| A5 | 代理账号详情 · 自身（部分代理） | `GET /backend/agentnetcash/personalstats` | `SumBackWaterMoney` | 如 `AdminId=717/715/714/713/711` 为 `0`（同接口其他代理有非零，见 §3） |

**请后端确认：**

1. 设备/VIP 日报的返水是否与「公司日报」同源？为何公司日报有值，设备/VIP 为 0？
2. 佣金侧 `BackWaterGold` 是否独立汇总？七月是否漏跑任务？
3. 代理 `personalstats` 的 `SumBackWaterMoney` 统计口径与公司日报 `SumBetWaterMoney` 是否一致？

---

### 4.2 七月无历史列表数据

| # | 页面 | 接口 | 实测 |
| --- | --- | --- | --- |
| B1 | 公司日报 · 上架包日报 | `GET /backend/iosappstoredata/getiosappstoredata`（`SearchType=old`，`BeginTime/EndTime=2026-07-01~31`） | 返回有 `TodayItems` / `YesterdayItems`，但 **`Items` 为空/不存在**，无法核对历史「返水」列 |
| B2 | 运营数据 · 代理分组日报 | `GET /backend/agentnetcashgroup/dailyreport` | `Items` 为空 |
| B3 | 产品管理 · 返水设置 · 返水审核 | `GET /backend/playerbackwaterrecord/reviewlist` | `Count=0`，无审核单 |
| B4 | 代理管理 · 佣金管理 · 记录 | `GET /backend/sendcommission/commissionlist` | `Items` 为空 |

**请后端确认：**

1. 上架包/分组日报七月是否应有历史行？若应有，是查询参数问题还是统计未写入？
2. 返水审核、佣金记录空列表是「确实无业务」还是查询/落库问题？

---

### 4.3 字段名 / 参数约定需确认

| # | 问题 | 接口 | 说明 | 期望后端答复 |
| --- | --- | --- | --- | --- |
| C1 | 玩家统计合计字段缺失 | `GET /backend/operation/playerstatistics` | 使用 **Unix 秒** `BeginTime/EndTime` 可正常出列表；列表有 `BackWaterMoney`（补参后可见非零样本，如 `308` 分）。但响应 `Total` 中**未见**前端约定的 `SumBackWaterMoney` | 合计返水字段实际叫什么？是否未返回？ |
| C2 | 玩家统计日期参数 | 同上 | 传字符串日期 `2026-07-01` 会 `status=10000`；Unix 时间戳可 `200` | 请固定文档：必须 Unix 还是支持日期字符串？ |
| C3 | 佣金管理 · 团队 | `GET /backend/sendcommission/listadmin` | 返回 `status=10037` | 必填参数是什么？是否权限问题？ |
| C4 | 手动返水 | `GET /backend/playerbackwaterrecord/waterlist` | 空参/简单参返回 `10043 请求参数格式错误` | 正式必填参数列表（账号/玩家 ID 等） |
| C5 | 玩家详情 · 金币周期 | `GET /backend/playerinfo/goldtotal` | 带 `PlayerId` 后响应结构与前端预期（`Items[].BetWater` / `SumBetWater`）不一致或为空 | 请确认七月该玩家是否应有周期返水，以及实际返回字段名 |

---

## 5. 建议后端优先处理顺序

1. **P0**：玩家统计 `Total.SumBackWaterMoney` 是否漏返回（前端合计行依赖该字段）。
2. **P0**：公司日报有返水，但 **设备/VIP 日报全 0** —— 核对同源汇总是否漏补。
3. **P1**：上架包日报、分组日报七月 **无 `Items`** —— 确认是否应有历史数据。
4. **P1**：佣金 `BackWaterGold` 全 0、佣金记录空 —— 确认佣金域返水是否纳入本次补充。
5. **P2**：统一文档：各列表接口日期参数（Unix / 字符串）、`listadmin` / `waterlist` / `goldtotal` 必填参数。

---

## 6. 便于复现的请求示例

### 6.1 正常有数据（公司日报历史）

```http
GET /backend/operation/dailyreport?SearchType=old&BeginTime=2026-07-01&EndTime=2026-07-31&ReportType=1&AdminSearchType=0&ChannelSearchType=0
```

关注：`respond.Items[].SumBetWaterMoney`

### 6.2 设备日报（字段为 0）

```http
GET /backend/operation/devicedayreport?SearchType=old&BeginTime=2026-07-01&EndTime=2026-07-31&AdminSearchType=0&ChannelSearchType=0&DataSearchType=0
```

关注：`respond.Items[].SumBetWaterMoney`（当前为 0）

### 6.3 代理自身统计（有值样例）

```http
GET /backend/agentnetcash/personalstats?AdminId=707&BeginTime=1782835200&EndTime=1785513599&CountBeginTime=1782835200&CountEndTime=1785513599
```

关注：`respond.SumBackWaterMoney`（样例约 `473503`）

### 6.4 玩家统计（列表 OK，合计字段待确认）

```http
GET /backend/operation/playerstatistics?BeginTime=1782835200&EndTime=1785513599&Page=1&PageSize=50
```

关注：

- `respond.Items[].BackWaterMoney`
- `respond.Total` 内是否有 `SumBackWaterMoney`（或实际合计字段名）

> Unix：`1782835200` = 2026-07-01 00:00:00 +08；`1785513599` = 2026-07-31 23:59:59 +08。

---

## 7. 不在本次金额查验范围

以下页面有「返水」字样，但属于方案名/比例/流水，本次不要求核金额：

- 返水方案配置、代理返点比例 `AgentFanDianConfig`
- 代理概览 `WaterRate`
- 提现流水 `WithdrawWater*`
- 邀请好友活动流水 `WaterAmount`

---

**请后端按 §4、§5 逐项回复：是「无业务故为 0」、还是「漏补数」、还是「字段名/参数约定与前端不一致」。前端可按回复再改映射或联调。**

---

## 8. 复测结果（2026-08-02 下午，后端称已修复后）

> 同一环境 / 账号 / 七月区间再次核验。

### 8.1 对比上次：已改善

| 项 | 上次 | 复测 | 说明 |
| --- | --- | --- | --- |
| 玩家统计 · 列表 `BackWaterMoney` | 参数不对易失败 / 难见非零 | ✅ 50 行中 11 行非零，最大约 19.99 元 | Unix 时间戳可正常出数 |
| 玩家详情 · 金币周期 `BetWater` | 结构/数据不清 | ✅ 有 `Items[].BetWater` 非零（样例最大约 25.74 元） | 列表字段可用 |
| 佣金钱包（带 `AdminId=707`） | 未指定代理时多为空/0 | ✅ `BackWaterGold` 有非零；`Total.TotalBackWaterGold=484023` | **需带代理 ID**；说明佣金域有数，不是全局没补 |

### 8.2 仍未解决（请继续查）

| # | 问题 | 复测结果 |
| --- | --- | --- |
| A1 | 设备日报 `SumBetWaterMoney` | 仍全 **0** |
| A2 | VIP日报 `SumBetWaterMoney` | 仍全 **0** |
| B1 | 上架包日报历史 `Items` | 仍为空（`Today/Yesterday` 的返水也是 0） |
| B2 | 代理分组日报 `Items` | 仍为空 |
| B3 | 返水审核列表 | 仍 `Count=0` |
| B4 | 佣金管理 · 记录 `commissionlist` | 仍无 `Items` |
| A4 | 佣金管理 · 发放 `list` | 仍有行但 `BackWaterGold` 全 **0**（与带 AdminId 的佣金信息有数不一致，请核对发放列表是否漏字段/漏汇总） |
| C1 | 玩家统计合计 `Total.SumBackWaterMoney` | **仍缺失**：响应仅有 `Items`、`Pagination`，**无 `Total` 对象** |
| C3 | 佣金团队 `listadmin` | 仍 `10037 参数错误` |
| C5 | 金币周期合计 `SumBetWater` | `Total.SumBetWater` 存在，但样例玩家为 **0**（列表行有 `BetWater` 时合计是否应按区间汇总？） |

### 8.3 复测结论（给后端）

1. **主链路（公司日报 / 渠道 / 数据分析 / 返水记录 / 记录查询）仍正常有数。**
2. **玩家统计列表、金币周期列表、指定代理佣金信息** 比上次更好，说明部分修复已生效。
3. **仍卡在：**
   - 设备 / VIP 日报返水仍为 0；
   - 上架包 / 分组日报仍无七月历史行；
   - **玩家统计不返回 `Total`（前端合计行无法展示 `SumBackWaterMoney`）**；
   - 佣金「发放」列表返水仍为 0，与「佣金信息」带 AdminId 有数矛盾；
   - `listadmin` 参数错误未修。

请后端优先回复 **C1（玩家统计 Total）**、**A1/A2（设备/VIP）**、**发放列表 vs 佣金信息字段一致性**。
