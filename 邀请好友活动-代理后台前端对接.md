# 邀请好友活动——代理后台前端对接文档

## 1. 接口约定

- 前端请求基础路径：`/api/backend/invitefriendactivity`
- `cloud` 服务内部路由为 `/backend/invitefriendactivity`，网关会处理前端的
  `/api` 前缀；前端不要自行移除 `/api`。
- 鉴权：沿用代理后台现有登录态和请求头，不需要额外 Token。
- 请求中的代理 ID 以后端登录态为准，前端不要传 `AgentId`。
- 金额字段均使用项目最小金额单位，前端按现有金额规则格式化。
- 时间字段均为 Unix 秒。
- 保存接口请求格式为 `application/json`。
- 列表分页默认 `Page=1`、`PageSize=20`，`PageSize` 最大为 200。

通用成功响应：

```json
{
  "status": 200,
  "message": "",
  "respond": {}
}
```

只有 `status=200` 才表示成功。其他状态统一走代理后台现有错误提示逻辑，
展示 `message`；不要根据 HTTP 200 判断业务成功。

分页结构：

```json
{
  "CurrPage": 1,
  "PageSize": 20,
  "MaxCount": 100,
  "MaxPageCount": 5
}
```

## 2. 配置接口

### 2.1 获取配置列表

```http
GET /api/backend/invitefriendactivity/config
```

返回当前代理全部语言组的配置：

```json
{
  "status": 200,
  "message": "",
  "respond": [
    {
      "Id": 1,
      "AgentId": 659,
      "LangGroupId": 0,
      "Title": "邀请好友送彩金",
      "Open": true,
      "BeginTime": 1785081600,
      "EndTime": 1788191999,
      "DepositThreshold": 10000,
      "IncludeDepositTypes": ["success"],
      "InviteeReward": 1000,
      "InviterTiers": [
        {"MinCount": 1, "MaxCount": 3, "Reward": 2000},
        {"MinCount": 4, "MaxCount": 0, "Reward": 3000}
      ],
      "WaterMultiple": 1,
      "RiskDimensions": ["ip", "device"],
      "RiskAction": 1,
      "RuleContent": ["活动规则"],
      "Banner": "",
      "ShareImage": "",
      "ShareUrlTemplate": "https://example.com/register?inviteCode={inviteCode}",
      "IsActive": 1,
      "CreateAdminId": 1001,
      "CreateTime": 1785000000,
      "UpdateTime": 1785000000
    }
  ]
}
```

### 2.2 创建或更新配置

```http
PUT /api/backend/invitefriendactivity/config
Content-Type: application/json
```

前端不传 `Id`、`AgentId`、创建人和创建时间。后端按照
`当前代理 + LangGroupId` 创建或更新。

请求示例：

```json
{
  "LangGroupId": 0,
  "Title": "邀请好友送彩金",
  "Open": true,
  "BeginTime": 1785081600,
  "EndTime": 1788191999,
  "DepositThreshold": 10000,
  "IncludeDepositTypes": ["success"],
  "InviteeReward": 1000,
  "InviterTiers": [
    {"MinCount": 1, "MaxCount": 3, "Reward": 2000},
    {"MinCount": 4, "MaxCount": 0, "Reward": 3000}
  ],
  "WaterMultiple": 1,
  "RiskDimensions": ["ip", "device"],
  "RiskAction": 1,
  "RuleContent": ["活动规则第一条", "活动规则第二条"],
  "Banner": "",
  "ShareImage": "",
  "ShareUrlTemplate": "https://example.com/register?inviteCode={inviteCode}",
  "IsActive": 1
}
```

保存成功后，`respond` 返回保存后的完整配置，结构与配置列表单项一致。

配置校验：

- `Title` 必填。
- `BeginTime > 0`，且 `EndTime > BeginTime`。
- `DepositThreshold > 0`。
- `InviteeReward`、`WaterMultiple` 和每档 `Reward` 不能小于 0。
- `InviterTiers` 至少一档，第一档必须从 `MinCount=1` 开始。
- 阶梯必须连续；只有最后一档允许 `MaxCount=0`，表示无上限。
- `ShareUrlTemplate` 必须是完整的 HTTP/HTTPS 地址。
- 分享链接只支持 `{inviteCode}`、`{playerId}` 两种占位符。
- 同一个代理只能有一个 `Open=true` 的配置；开启一个语言组时，其他语言组会被关闭。
- `IsActive=2` 表示停用，后端会强制设置 `Open=false`。
- `RiskAction=0` 按默认值 1 保存；`IsActive=0` 按默认值 1 保存。

`RiskDimensions` 可选值：

| 值 | 含义 |
| --- | --- |
| `ip` | IP |
| `device` | 设备 |
| `phone` | 手机号 |
| `name` | 姓名 |
| `email` | 邮箱 |
| `bankcard` | 银行卡 |

保存是同步操作。只有游戏侧配置成功并且后台数据库提交成功，接口才返回
`status=200`；保存过程中不要提前显示成功。

## 3. 邀请关系列表

```http
GET /api/backend/invitefriendactivity/relations
```

查询参数：

| 参数 | 类型 | 必填 | 说明 |
| --- | --- | --- | --- |
| `Page` | int64 | 否 | 页码 |
| `PageSize` | int64 | 否 | 每页数量，最大 200 |
| `InviterId` | int64 | 否 | 邀请人玩家 ID |
| `InviteeId` | int64 | 否 | 被邀请人玩家 ID |
| `InviteeAccount` | string | 否 | 被邀请人账号，精确匹配 |
| `Source` | string | 否 | 来源，例如 `link`、`qrcode`、`code` |
| `RewardStatus` | int32 | 否 | 关系/发奖状态 |
| `BusinessOrderId` | string | 否 | 业务单号，精确匹配 |
| `BeginTime` | int64 | 否 | 绑定开始时间 |
| `EndTime` | int64 | 否 | 绑定结束时间 |

响应示例：

```json
{
  "status": 200,
  "message": "",
  "respond": {
    "Items": [
      {
        "Id": 10,
        "EventType": "invite_reward_success",
        "AgentId": 659,
        "InviterId": 10001,
        "InviteeId": 10002,
        "InviteeAccount": "player10002",
        "Source": "link",
        "BindTime": 1785081600,
        "RegisterIp": "203.0.113.10",
        "RegisterDeviceId": "device-id",
        "QualifiedTime": 1785082600,
        "TotalDepositAmount": 10000,
        "InviterTierSeq": 1,
        "InviterReward": 2000,
        "InviteeReward": 1000,
        "RewardStatus": 5,
        "RewardTime": 1785082700,
        "BusinessOrderId": "INVITE-ORDER-001",
        "RiskReason": "",
        "UpdatedAt": 1785082700
      }
    ],
    "Pagination": {
      "CurrPage": 1,
      "PageSize": 20,
      "MaxCount": 1,
      "MaxPageCount": 1
    }
  }
}
```

关系状态：

| 值 | 含义 |
| --- | --- |
| 1 | 已绑定，尚未充值 |
| 2 | 已充值，累计未达标 |
| 3 | 已达标，等待风控或发奖 |
| 4 | 风控拒绝 |
| 5 | 已发奖 |
| 6 | 发奖失败 |
| 7 | 活动结束未达标 |

## 4. 发奖记录

### 4.1 发奖列表

```http
GET /api/backend/invitefriendactivity/rewards
```

查询参数：

| 参数 | 类型 | 必填 | 说明 |
| --- | --- | --- | --- |
| `Page` | int64 | 否 | 页码 |
| `PageSize` | int64 | 否 | 每页数量，最大 200 |
| `PlayerId` | int64 | 否 | 获奖玩家 ID |
| `Account` | string | 否 | 获奖账号，精确匹配 |
| `InviterId` | int64 | 否 | 邀请人玩家 ID |
| `InviteeId` | int64 | 否 | 被邀请人玩家 ID |
| `Role` | string | 否 | `inviter` 或 `invitee` |
| `RewardStatus` | int32 | 否 | 发奖状态 |
| `BusinessOrderId` | string | 否 | 活动发奖业务单号 |
| `BeginTime` | int64 | 否 | 发奖记录创建开始时间 |
| `EndTime` | int64 | 否 | 发奖记录创建结束时间 |

响应示例：

```json
{
  "status": 200,
  "message": "",
  "respond": {
    "Items": [
      {
        "Id": 20,
        "EventType": "invite_reward_success",
        "AgentId": 659,
        "InviterId": 10001,
        "InviteeId": 10002,
        "PlayerId": 10001,
        "Account": "player10001",
        "VipLevel": 3,
        "Role": "inviter",
        "Amount": 2000,
        "WaterMultiple": 1,
        "WaterAmount": 2000,
        "BusinessOrderId": "INVITE-REWARD-001",
        "WalletOrderId": "WALLET-001",
        "RewardStatus": 2,
        "RewardTime": 1785082700,
        "FailReason": "",
        "CreatedAt": 1785082650,
        "UpdatedAt": 1785082700
      }
    ],
    "Pagination": {
      "CurrPage": 1,
      "PageSize": 20,
      "MaxCount": 1,
      "MaxPageCount": 1
    }
  }
}
```

发奖状态：

| 值 | 含义 |
| --- | --- |
| 1 | 待发放 |
| 2 | 已发放 |
| 3 | 发放失败 |

角色：

| 值 | 含义 |
| --- | --- |
| `inviter` | 邀请人奖励 |
| `invitee` | 被邀请人奖励 |

### 4.2 发奖详情

```http
GET /api/backend/invitefriendactivity/rewards/{id}
```

- `id` 为发奖记录数据库 ID。
- 返回结构与发奖列表的单项一致。
- 后端会自动附加当前代理条件，不能跨代理读取。
- 记录不存在时返回非 200 业务状态。

## 5. 前端页面建议

第一期建议拆成三个页签：

1. 活动配置：语言组配置列表、创建/编辑。
2. 邀请关系：筛选、分页列表。
3. 发奖记录：筛选、分页列表、详情抽屉或详情页。

第一期没有统计、Excel 导出、手工补发和旧活动数据兼容接口。不要调用旧的
`/api/backend/playerinvitefunc` 接口，也不要把新旧活动数据混合展示。
