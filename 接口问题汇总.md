# 接口问题汇总

按天汇总发给后端。状态：`待反馈` / `处理中` / `已关闭`。

---

## 清单

| 编号 | 日期 | 状态 | 优先级 | 页面 | 接口 | 问题 |
| --- | --- | --- | --- | --- | --- | --- |
| API-001 | 2026-07-17 | 待反馈 | P1 | 数据总览 `/dashboard/index` | `GET /backend/dashboard/reportinfo` | `TotalHours` 分时异常；自选日期 `Date1/2/3` 对应分时也为空 |
| API-002 | 2026-07-17 | 待反馈 | P1 | 数据总览 `/dashboard/index` | `GET /backend/dashboard/reportchannelinfo` | `RealtimeItems` 为空，渠道今日数据无展示 |
| API-003 | 2026-07-17 | 待反馈 | P1 | 数据总览 `/dashboard/index` | `GET /backend/dashboard/rankbygameid` | 仅返回 `ItemsLose`，`ItemsWin` 为 `null`，亏损游戏无数据 |
| API-004 | 2026-07-17 | 待反馈 | P2 | 数据总览 `/dashboard/index` | `GET /backend/dashboard/reporttoday` | 多个数组字段返回 `null`；充值通道 `TodayItems` 缺 `PayTypeName` |
| API-005 | 2026-07-17 | 待反馈 | P2 | 在线总览 `/dashboard/onlineSummary` | `GET /backend/dashboard/onlinesummary` | `GameResult.LastGameId=0` 致场馆名为 `-`；`MapResult.IpDetailName=""` 致地区表格为空 |
| API-006 | 2026-07-17 | 待反馈 | P1 | 公司日报·上架包日报 `/operationalData/everydayData`<br>包体管理·iOS 上架包 `/gameManage/packageManage` | `GET /backend/iosappstoredata/getiosappstoredata`<br>`GET /backend/iosappstoredata/list`<br>`GET /backend/iosappstoredata/todayappstorecsv` | 历史 `Items` / iOS 上架包列表 / 今日导出均为 `null`；`BannerItems` 有汇总但明细缺失 |
| API-007 | 2026-07-17 | 待反馈 | P1 | 代理分组日报 `/operationalData/groupDaily` | `GET /backend/agentnetcashgroup/dailyreport`<br>`GET /backend/agentnetcashgroup/grouplist` | 日报 `Items` 恒为 `null`；分组树仅返回 `Id=0`「推广中心」且 `List=null` |
| API-008 | 2026-07-17 | 待反馈 | P2 | 代理数据 `/operationalData/channelData` | `GET /backend/operation/channelreportbychannel` | `SearchType=old` 时返回 `RealTimeItems` / `TotalRealTimeItems`，与代理侧 `Items` / `TotalItems` 字段名不一致 |
| API-009 | 2026-07-17 | 待反馈 | P2 | 排行数据·充兑 `/operationalData/ranking` | `GET /backend/operation/gamepaymentrank` | 单日（昨日）无充值时 `Items`/`Users` 返回 `null`；有数据时字段为 `Items`+`SumGold`（非 `ItemsWin`） |
| API-010 | 2026-07-17 | 待反馈 | P1 | 排行数据·游戏 `/operationalData/ranking` | `GET /backend/operation/realgamewinrank` | 仅返回 `ItemsWin`，`ItemsLose` 恒为 `null`，亏损游戏排行无数据 |
| API-011 | 2026-07-17 | 待反馈 | P1 | 排行数据·无限代理 `/operationalData/ranking` | `GET /backend/wnlimitedproxydata/ranking` | `Items` / `Pagination` 恒为 `null`，无限代理排行无数据 |
| API-012 | 2026-07-17 | 待反馈 | P1 | 充值列表·自助查单·访问记录 `/operationalManage/rechargeList` | `GET /backend/visitstatistic/selfreviewlist`<br>`GET /backend/visitstatistic/selfreviewstatisticslist` | 均返回 `status=20001`「The remote server is not connected and the operation failed.」，明细/统计无法加载 |
| API-013 | 2026-07-17 | 待反馈 | P1 | 充值列表·自助查单 `/operationalManage/rechargeList` | `GET /backend/playerpaymentselfreview/checkconfig` | 返回 `status=10021`「Data formatting failed」，游戏端入口开关无法读取 |
| API-014 | 2026-07-17 | 待反馈 | P2 | 充值列表 `/operationalManage/rechargeList` | `GET /backend/rechargelimitconfig/list`<br>`GET /backend/playereasyrecharge/list`<br>`GET /backend/playerusdteasyrecharge/list`<br>`GET /backend/rechargeblackdevice/list`<br>`GET /backend/fixpayment/list` 等 | 无数据时 `Items` 返回 `null`（非 `[]`）；Pagination 有时仍带 MaxCount=0 |
| API-015 | 2026-07-17 | 待反馈 | P2 | 提现列表 `/operationalManage/withdrawList` | `GET /backend/withdrawwhitelist/list`<br>`GET /backend/withdrawsendorderusermanage/list`<br>`GET /backend/withdrawsendorderlist/list`<br>`GET /backend/playerwithdraw/revertedlist`<br>`GET /backend/playerwithdrawflow/list`<br>`GET /backend/playerwithdrawflow/flowList` | 无数据时 `Items` 恒为 `null`；冲正记录 Pagination 缺 `MaxCount` |
| API-016 | 2026-07-17 | 待反馈 | P2 | 提现列表·派单列表 `/operationalManage/withdrawList` | `GET /backend/withdrawsendorderlist/list` | 近 7 日仍无明细（`Items=null`，MaxCount=0），但额外返回 `Account` 接单态结构；需确认本环境是否本应有派单数据 |
| API-017 | 2026-07-17 | 待反馈 | P1 | 客服工单 `/operationalManage/applyService` | `GET /backend/customsupport/list` | 近 365 日 / 不传时间 / 各 Status 均为 `Items=null` 且 `MaxCount=0`；导出 `IsExp=true` 时 `Pagination` 也为 `null` |
| API-018 | 2026-07-17 | 待反馈 | P1 | 客服工单 `/operationalManage/applyService` | `GET /backend/customsupport/{id}` | 传合法路径 id（如 `1` / `0`）均返回 `status=10001`「数据库异常，请联系管理员」 |
| API-019 | 2026-07-17 | 待反馈 | P1 | 公告管理·邮件通知 `/operationalManage/gameNotice` | `GET /backend/gameemail/list` | 近 7/30/365 日及不传时间均为 `Items=null`、`MaxCount=0`，邮件列表无数据 |
| API-020 | 2026-07-17 | 待反馈 | P1 | 公告管理·公告明细/统计 `/operationalManage/gameNotice`<br>提现通道·提现访问记录 `/gameManage/withdrawConfig` | `GET /backend/visitstatistic/list`<br>`GET /backend/visitstatistic/statisticslist` | 均返回 `status=20001`「The remote server is not connected and the operation failed.」；提现访问明细/统计复用同一服务与接口（`Group=Withdraw`），与 API-012 同属 visitstatistic |
| API-021 | 2026-07-17 | 待反馈 | P2 | 公告管理·新建/编辑推送跳转 `/operationalManage/gameNotice` | `GET /backend/gameadconfig/getactivitylist` | `respond=null`，活动跳转参数下拉无数据（旧站 OpenType=活动 选项虽已注释，接口仍被依赖） |
| API-022 | 2026-07-17 | 待反馈 | P2 | 红利发放·发放记录 `/operationalManage/playerGoldHandle` | `GET /backend/playergoldhandle/list` | 无明细时 `Items=null`（非 `[]`）；昨日默认区间常 MaxCount=0，但 `Total` 仍返回；不传时间有历史数据 |
| API-023 | 2026-07-17 | 待反馈 | P2 | 账户调整·调整审核/记录 `/operationalManage/accountAdjust` | `GET /backend/playergoldaccountchange/approvelist` | 无明细时 `Items=null`（非 `[]`）；审核态 `Approve=1,4` 近 90 日仍空（MaxCount=0），记录态昨日有数据 |
| API-024 | 2026-07-17 | 待反馈 | P2 | 红利管理 `/operationalManage/audit` | `GET /backend/playergoldhandle/approvelist`<br>`GET /backend/playergoldhandle/redlist`<br>`GET /backend/activitymatchbonusapplyrecord/list` | 无明细时 `Items=null`；待审红利/活动红利/申请记录近 90 日常空；`redlist` 缺默认筛参（如 `IsWater=-1`）时即便有时间也 MaxCount=0；`IsExp=true` 单独导出易空 |
| API-025 | 2026-07-17 | 待反馈 | P2 | 礼品管理 `/operationalManage/giftManage` | `GET /backend/playergift/list`<br>`GET /backend/playergift/deliverlist` | 发货列表无数据时 `Items=null`（审核列表为空时是 `[]`，不一致）；本环境近 365 日晋级豪礼/主题抽奖审核与发货均为 MaxCount=0，需确认是否本应有数据 |
| API-026 | 2026-07-17 | 待反馈 | P2 | 游戏风控 `/operationalManage/gameRiskControl` | `GET /backend/gameipriskcontrol/list`<br>`GET /backend/gameriskcontrol/list`<br>`GET /backend/playerbankcard/blacklist`<br>`GET /backend/appstorewhitelistrisk/list` | 无数据时 `Items`/`WhitelistList` 为 `null`；IP/设备列表无 `Pagination` 仅返回 `Total`；本环境各列表 Total/MaxCount 均为 0，需确认是否本应有数据 |
| API-027 | 2026-07-17 | 待反馈 | P2 | 导出管理 `/operationalManage/downloadCsvManage` | `GET /backend/downloadexcel/list` | 无数据时 `Items=null`；近 30/365 日及不传时间均为 MaxCount=0，需确认本环境是否本应有导出任务 |
| API-028 | 2026-07-17 | 待反馈 | P1 | 区域屏蔽 `/operationalManage/countrySet` | `GET /backend/countriesconfig/list` | 国家配置列表恒返回 `Items=null`（无 Pagination）；页面无法展示可选国家；`GET /backend/gamecountries/` 状态可读但列表为空时无法配置 |
| API-029 | 2026-07-17 | 待反馈 | P1 | 运营活动 `/operationalManage/activity` | 见详情（活动列表/历史/签到/邀请/优惠码/预约明细等） | 多列表无数据时 `Items=null`；存款方案 `list` 返回 10000；代理佣金缺 `Mode` 返回 10037；确认到账误用 `/list` 无权限（正确为根路径 GET） |
| API-030 | 2026-07-17 | 待反馈 | P1 | 排行榜 `/operationalManage/leaderboard` | `GET /backend/leaderboard/list`<br>`GET /backend/leaderboard/record`<br>`GET /backend/leaderboard/getleaderboardmainconfig` | 无数据 `Items=null`；传 `StartTime/EndTime=YYYY-MM-DD`（旧站格式）时 list 返回 10000；unix 秒可通但空；主配置 `Items=null` |
| API-031 | 2026-07-17 | 待反馈 | P2 | 票券中心 `/operationalManage/voucher` | `GET /backend/voucher/list`<br>`GET /backend/voucher/getvouchermainconfig`<br>`GET /backend/voucher/voucherdetailrecord`<br>`GET /backend/voucher/issuevoucherrecord` | 列表/记录无数据时 `Items=null`；主配置 `Items=null`；发放记录缺参返回 10037；本环境多为空需确认是否本应有票券 |
| API-032 | 2026-07-18 | 待反馈 | P1 | 积分商城 `/operationalManage/rewardMall` | 见详情（商品/兑换/积分记录/主配置/积分返水等） | 多列表无数据 `Items=null`；主配置/页签/任务 `Items=null`；**积分记录 `point/recordlist` 返回 10001 Database Exception**；本环境商品/兑换多为空 |
| API-033 | 2026-07-18 | 待反馈 | P1 | 杯赛专题 `/operationalManage/customLeague` | `GET /backend/gamecustomleague/list`<br>`GET /backend/gamecustomleague/leaguelist` | 列表无数据 `Items=null`；**联赛下拉 `leaguelist` 返回 10163 Related service request error**（本环境 GameType=7/8 均失败） |
| API-043 | 2026-07-18 | 待反馈 | P2 | 身份验证 `/memberManage/playerAuthentication` | `GET /backend/playerauthentication/list`<br>`GET /backend/playerauthentication/record`<br>`GET /backend/playerauthentication/setting`<br>`GET /backend/playerauthentication/recordexport` | 审核列表/记录无数据时 `Items=null`；`setting` 缺 `SubType=1005`（验证信息开关）；导出需密码（pageId=90） |
| API-044 | 2026-07-18 | 待反馈 | P2 | 钱包管理 `/memberManage/walletManage` | `GET /backend/playerbankcard/list`<br>`GET /backend/playerwalletaccount/listall`<br>`GET /backend/playerwithdrawdigitaladdress/list`<br>`GET /backend/playerbindcardconfig/listcardmultiple`<br>`POST /backend/playerinfo/queryplayerexcel` | 电子钱包/虚拟币/多绑定无数据 `Items=null`；`queryplayerexcel` 返回 10053（前端已改用 `queryplayer`） |
| API-045 | 2026-07-18 | 待反馈 | P2 | 验证码查询 `/memberManage/verifyCode` | `GET /backend/phoneverifycode/list`<br>`GET /backend/emailverifycode/list`<br>`GET /backend/emailverifycode/listemailconfig`<br>`GET /backend/phonecountrycode/list`<br>`GET /backend/gameotpconfig/getconfig` | 手机验证码缺参返回 10037；邮箱验证码/通道本环境 `respond=null`；区码与 OTP 正常 |
| API-046 | 2026-07-18 | 待反馈 | P2 | 称号管理 `/operationalManage/gameTitle` | `GET /backend/playerbadge/list`<br>`GET /backend/badgecategory/list`<br>`GET /backend/playerbadge/playerlist`<br>`GET /backend/playerbadge/playerlistcsv` | 称号/类别/拥有者本环境多为空 `Items=null`；导出需密码（pageId=75） |
| API-047 | 2026-07-18 | 待反馈 | P2 | 玩家详情·注单 `/operationalManage/playerDetails` | `GET /backend/operation/gametransactiondetail`<br>`GET /backend/operation/gametransactiondetaillog`<br>`GET /backend/operation/getbyorderid` | 账号 `18857985770` 有注单；`SumAll=1` 时 Items 为空但 MoreItems 有汇总；`getbyorderid` 常失败但行内 `Detail` 可展示三方详情；结算流水常 `respond=null` |
| API-049 | 2026-07-19 | 待反馈 | P1 | 短信管理·通道配置/首存次日召回 `/gameManage/messageManage` | `GET /backend/smschannelconfig/list`<br>`GET /backend/operation/depositrecall` | 测试账号具备 `12906/12908/13414` 权限，但通道列表无参/带分页均返回 `10000`；首存召回按旧版日期参数测试单日及区间均返回 `10000`，同模块详情接口正常 |
| API-050 | 2026-07-19 | 待反馈 | P2 | 短信管理 `/gameManage/messageManage` | `GET /backend/shortmessageservice/list`<br>`GET /backend/shortmessageservice/monthlist`<br>`GET /backend/operation/phoneregisterotpday`<br>`GET /backend/operation/phoneregisterotpdetail`<br>`GET /backend/operation/registerrecall`<br>两类召回详情 | 本环境无数据时多数列表返回 `Items=null`，仅短信模板返回 `Items=[]`，空列表结构不一致；页面已统一兼容 |
| API-051 | 2026-07-19 | 待反馈 | P2 | 返水配置 `/gameManage/backWater` | `GET /backend/playerbackwaterrecord/package` | 测试账号具备返水页面及全部子操作权限（`11073~11100`、`12662~12674` 对应权限均存在），接口仍返回 `10010`「权限不足([权限配置不正确])」；当前页面改用全局产品配置，不阻塞查询 |
| API-052 | 2026-07-19 | 待反馈 | P2 | 返水记录/审核 `/gameManage/backWater` | `GET /backend/playerbackwaterrecord/list`<br>`GET /backend/playerbackwaterrecord/detaillist`<br>`GET /backend/playerbackwaterrecord/reviewlist` | 昨日合法查询参数均返回 `status=200`，但空数据统一为 `Items=null`；三个接口分别使用 `Total` / `Count`，且均不返回 `Pagination`，列表总数字段结构不一致；页面已兼容 |
| API-071 | 2026-07-20 | 待反馈 | P2 | 玩家统计/玩家盈亏/玩家分析 `/dataClose/playerStatistics`<br>`/dataClose/userWinLoss`<br>`/dataClose/playerAnalyze` | `GET /backend/operation/playerstatistics`<br>`GET /backend/operation/userstatistics`<br>`GET /backend/playeranalysis/playerlist`<br>`GET /backend/operation/playerstatisticscsv` | 无数据时 `Items`/`MoreItems` 常为 `null`（非 `[]`）；玩家统计需 `SearchType=list/total` 双请求；CSV 导出走 PageId=46；前端已用 `toListResult` 归一化 |
| API-073 | 2026-07-20 | 待反馈 | P1 | 溢出管理 `/netcash/spillManage` | `GET /backend/agentplayermanage/list` | 响应非法 JSON：`respond: success`（未加引号），列表无法解析 |
| API-074 | 2026-07-20 | 待反馈 | P1 | 记录查询 `/netcash/recordInquire` | `GET /backend/recordquery/loginlist` | 传 `PageSize=5` 仍返回 144 条，且 `Pagination=null` |
| API-075 | 2026-07-20 | 待反馈 | P1 | 提款管理 `/netcash/drawmoneyManage` | `GET .../agentwithdrawaccountdetail`<br>`GET .../check/{id}`<br>`GET .../statuslist` | 合法 Id 详情/校验返回 `10001`；`statuslist` 成功但 `respond=null` |
| API-076 | 2026-07-20 | 待反馈 | P2 | 提款管理 `/netcash/drawmoneyManage` | `GET .../list`<br>`GET .../agentwithdrawaccountlist`<br>`GET .../withdrawlist`<br>`GET /api/loginuser/getstatus` | 空列表 `Items/Total=null`；自动刷新状态 `10010` 权限不足 |
| API-077 | 2026-07-20 | 待反馈 | P1 | 佣金管理 `/netcash/commissionManage` | `GET /backend/commissionconfig/list`<br>`GET /backend/commissionconfigmulti/list`<br>`GET /backend/sendcommission/teamdetail` 等 | 配置 `respond=null`；多层配置 `10304`；`teamdetail` 对同 Id 返回 `10000` |
| API-078 | 2026-07-20 | 待反馈 | P1 | 代客额度 `/netcash/dkCreditManage` | `GET .../getagentdkaccountlimit`<br>`GET /backend/sharedComponent/list` | 额度概览 `10505`；共享组件 `20001` 远程未连接 |
| API-079 | 2026-07-20 | 待反馈 | P2 | 创建金钱渠道 `/netcash/createMoneyChannel` | `GET /backend/gameadtemplate/getcustomlogo` | 业务 ChannelId 字符串 `10000`；数字主键可通但 `Items=null` |
| API-080 | 2026-07-20 | 待反馈 | P2 | 代理账号详情 `/netcash/agencyAccountDetails` | `GET .../accountloginip/list` 等 | 登录 IP / 直属代理统计 / 提款账户空结果时 `Items` 或 `Pagination` 为 `null` |
| API-081 | 2026-07-20 | 待反馈 | P2 | 额度/素材/佣金等多页 | 多列表 GET | 无数据时 `Items=null`，与同批已返回 `[]` 的接口不一致 |
| API-082 | 2026-07-20 | 待反馈 | P1 | 投注行为报表 `/operationalData/gameDetails` | `GET /backend/operation/gametransactionanalysisreport` | 多组合法日期/`DataSearchType` 均 `status=200` 但 `Items=null`，`message` 为空 |
| API-083 | 2026-07-20 | 待反馈 | P2 | 运营日报 `/dataClose/operationDaily` | `GET /backend/operationdayreport/dayreport` | `TodayItems`/`BeforeItems` 有数据时，`TodayHourItems`/`BeforeHourItems` 仍为 `null`，分时图无数据 |
| API-084 | 2026-07-20 | 待反馈 | P2 | 粘度分析 `/dataClose/keepData` | `GET /backend/operation/realtimedau`<br>`GET /backend/operation/realtimeltv`<br>`GET /backend/operation/everydaylogindau` | 登录留存/LTV 仅返回 `ItemsOld`（无 `Items`）；区间留存 `Items` 为对象而非数组 |
| API-085 | 2026-07-20 | 待反馈 | P2 | 游戏报表 `/dataClose/gameStatement` | `GET /backend/operation/gamestatisticsbygametype` | 游戏 Tab 成功时只返回 `ItemsMoney`（无 `Items`）；分类/子游戏同域接口返回 `Items`，字段名不一致 |
| API-086 | 2026-07-20 | 待反馈 | P2 | 运营日报·游戏盈亏排行 `/dataClose/operationDaily` | `GET /backend/operationdayreport/dayreportwinrank` | 有 `TodayGameTypeWinItems`/`TodayPlayerItemsLose` 时，`TodayGameTypeLoseItems` 仍为 `null` |
| API-087 | 2026-07-20 | 待反馈 | P2 | 运营日报·数据报表 / 日/月报子包网 | `GET /backend/operationdayreportscheduler/list`<br>`GET /backend/agentbetwinmanageday/sonlist`<br>`GET /backend/agentbetwinmanagemonth/sonlist` | 空结果 `Items=null`（非 `[]`）；子包网另返回空对象 `ItemsAgents={}` |

---

## 详情

### API-001

**现象：** 上方 KPI（`Items`）有投注金额，折线（`TotalHours`）几乎没数据；自选日期后对应分时仍为空。

**请求参数（自选日期）：**

- 前端传 `Date1` / `Date2` / `Date3`（`YYYY-MM-DD`，须小于今日且不等于昨日）
- 例：`GET /backend/dashboard/reportinfo?Date1=2026-07-15`

**字段用途：**

- `Items` → 按天，KPI 卡片
- `TotalHours` → 按小时，业务折线（含今日/昨日/自选日期）
- `TotalCount` → 按 5 分钟，仅在线人数折线

**实测（2026-07-17）：**

- `Items`：今日/昨日投注正常（`55015729` / `621889885`）
- `TotalHours["2026-07-16"]`：`null`
- `TotalHours["2026-07-17"]`：仅 1 条（`Hours: 14`），金额全 `0`
- 自选日期（如 `Date1=2026-07-15`）：`TotalHours["2026-07-15"]` 也为空 / `null` / 无有效小时数据
- `TotalCount`：结构有，`Count` 全 `0`

**请后端确认：**

1. 为何 `Items` 有金额，`TotalHours` 同日为 0 / `null`
2. 传 `Date1/2/3` 后，`TotalHours` 是否应返回对应日期的按小时数据
3. 昨日 / 自选日期不要返回 `null`（应 `[]` 或完整小时数组）
4. 今日应按小时返回（约 0–当前小时），金额单位与 `Items` 一致（分）

**摘录：**

```json
{
  "Items": [
    { "ReportDay": "2026-07-17", "SumTransBetMoney1": 55015729 },
    { "ReportDay": "2026-07-16", "SumTransBetMoney1": 621889885 },
    { "ReportDay": "2026-07-15", "SumTransBetMoney1": 12345678 }
  ],
  "TotalHours": {
    "2026-07-16": null,
    "2026-07-17": [{ "Hours": 14, "SumTransBetMoney1": 0 }],
    "2026-07-15": null
  }
}
```

### API-002

**现象：** 「渠道今日数据」模块无数据展示。

**实测（2026-07-17）：**

- 接口：`GET /backend/dashboard/reportchannelinfo`
- 返回：`RealtimeItems` 为空（空数组或空值）

**请后端确认：**

1. 当日渠道统计是否已产出（`RealtimeItems` 是否应有数据）
2. 无数据场景请统一返回 `[]`，避免 `null`/缺字段
3. 字段名保持旧站一致（`ChannelName`、`ChannelId`、`NewCountNum` 等）

**摘录：**

```json
{
  "RealtimeItems": []
}
```

### API-003

**现象：** 「盈利游戏」有数据，「亏损游戏」无数据。

**数据依据（前端实际请求，对齐旧站）：**

| 模块 | 接口 | 前端传参 | 时间范围 |
| --- | --- | --- | --- |
| 盈利游戏 / 亏损游戏 | `GET /backend/dashboard/rankbygameid` | **无参数** | 由后端默认（旧站同为无参；非上方自选日期） |
| 盈利玩家 / 亏损玩家 | `GET /backend/dashboard/goldbyplayergame` | `GameId=''`、`StartTime=''`、`Sort=''` | `StartTime` 为空，由后端默认（通常为当日实时；非上方自选日期） |

说明：

- 上方折线「自选日期」（`Date1/2/3`）只作用于 `reportinfo`，**不作用于**这四个盈亏模块。
- 「更多」跳转排行榜页后，才有独立时间筛选；总览页本身不传业务日期。

**字段用途（旧站约定）：**

- 游戏：`ItemsLose` → 盈利游戏；`ItemsWin` → 亏损游戏；金额展示 `SumAddGold * -1`
- 玩家：`ItemsWin` → 盈利玩家；`ItemsLose` → 亏损玩家；金额直接用 `SumAddGold`

**实测（2026-07-17）：**

- 接口：`GET /backend/dashboard/rankbygameid`
- `ItemsLose`：有数据
- `ItemsWin`：`null`

**请后端确认：**

1. 亏损游戏是否应写入 `ItemsWin`
2. 无亏损数据时请返回 `[]`，不要 `null`
3. 字段与旧站保持一致（`GameId`、`SumAddGold`）
4. 总览页无参 / `StartTime=''` 时，默认统计口径是「今日实时」还是其他区间？（请明确，便于前端展示文案）

**摘录：**

```json
{
  "ItemsLose": [{ "GameId": 1001, "SumAddGold": -12300 }],
  "ItemsWin": null
}
```

### API-004

**现象：** `reporttoday` 返回中多个数组字段为 `null`，充值通道名只有数字。

**实测（2026-07-17）：**

| 字段路径 | 期望 | 实际 |
| --- | --- | --- |
| `OnlineUser.TodayOnlineUserItems` | `[]` | `null` |
| `OnlineUser.YestDayOnlineUserItems` | `[]` | `null` |
| `Recharged15.Curr15Items` | `[]` | `null` |
| `Recharged15.Last15Items` | `[]` | `null` |
| `Recharged.TodayItems[].PayType` | 有 `PayTypeName` 或后端翻译 | 仅数字 `1`，无名称字段 |
| `Recharged.YestDayTotal` | 有 | 有（正常） |
| `WithDraw` | 有 | 有（正常） |

**问题 1：数组字段 `null`**

前端做了兼容（`|| []`），功能不阻断，但建议后端统一：**无数据时返回 `[]` 而非 `null`**，减少前端防御代码。

**问题 2：充值通道只有 `PayType` 数字**

旧站用 `GLOBAL.paymentTypeFilter(PayType)` 把数字翻译成中文名（如 `1` → 支付宝），数据来源是登录时 `projectConfig.RechargeTypeList`。

- `TodayItems` 和 `Children` 都只返回 `PayType: 1`，没有 `PayTypeName` 字段。
- 前端已用 `RechargeTypeList` 翻译，**如果 `RechargeTypeList` 也没有对应项则仍显示数字**。
- 建议后端确认：`RechargeTypeList` 中 `Key=1` 是否有配置？或后端直接在 `TodayItems` 里补上 `PayTypeName`。

**请后端确认：**

1. 所有列表字段无数据时统一返回 `[]`
2. `RechargeTypeList` 是否完整覆盖所有 `PayType`（或后端直接返回 `PayTypeName`）

**摘录：**

```json
{
  "OnlineUser": {
    "OnlineCount": 0,
    "TodayOnlineUserItems": null,
    "YestDayOnlineUserItems": null
  },
  "Recharged": {
    "TodayItems": [
      {
        "PayType": 1,
        "SuccessCount": 0,
        "TotalCount": 1,
        "Children": [{ "PayType": 1, "RechargeId": 9, "ShowName": "乐赢-v2" }]
      }
    ],
    "TodayTotal": { "SumSuccessCount": 0, "SumTotalCount": 1 }
  },
  "Recharged15": {
    "Curr15Items": null,
    "Last15Items": null
  }
}
```

### API-005

**现象：** 在线总览「场馆名称」显示 `-`；**在线用户地区分布表格「地区」列为空**。

**字段用途（旧站约定）：**

- `GameResult` → 在线场馆分布（饼图 + 表格）；场馆名由 `LastGameId` + 游戏配置 `games` 翻译
- `DeviceResult` → 在线设备分布
- `MapResult` → 在线用户地区分布（表格 + 世界地图）；地区名用 `IpDetailName`

**前端排查（2026-07-17）：**

- 新站 `userList` 直接取自 `onlinesummary.respond.MapResult`，字段名与旧站一致
- 表格列 `dataIndex: 'IpDetailName'`，与旧站 `scope.row.IpDetailName` 一致，**无字段映射错误**
- 接口返回 `IpDetailName: ""`（空字符串，非 `null`、非缺字段），表格单元格因此渲染为空白
- `MapResult` 当前仅含 `Count`、`IpDetailName` 两字段，前端无其它字段可兜底展示地区名
- 旧站在相同返回下地区列同样会空白（旧站也未对空值做占位）

**实测（2026-07-17）：**

| 字段 | 现象 | 前端表现 |
| --- | --- | --- |
| `GameResult[0]` | `Count: 1`，`LastGameId: 0` | 场馆名显示 `-`（`0` 表示无有效场馆 ID，旧站同样处理） |
| `MapResult[0]` | `Count: 1`，`IpDetailName: ""` | 地区表格列为空；地图侧会显示 `-` |
| `DeviceResult[0]` | `DevicePlatform: "uniapp2-web"`，`Count: 1` | 正常 |

**请后端确认：**

1. 有在线人数时，`LastGameId` 是否应返回真实场馆/游戏 ID（不应为 `0`）？或是否应额外返回 `GameName` / `GameId`
2. `MapResult` 有 `Count` 时，`IpDetailName` 为何为空？在线用户 IP 归属地解析是否未写入或未聚合
3. 无地区信息时是否应返回占位文案（如「未知」）而非空字符串 `""`
4. 若地区名在其它字段（如 `AreaName`、`Country`），请明确字段名并在 `MapResult` 中返回

**摘录：**

```json
{
  "message": "操作成功",
  "status": 200,
  "respond": {
    "DeviceResult": [{ "Count": 1, "DevicePlatform": "uniapp2-web" }],
    "GameResult": [{ "Count": 1, "LastGameId": 0 }],
    "MapResult": [{ "Count": 1, "IpDetailName": "" }]
  }
}
```

### API-006

**现象：** 公司日报 →「上架包日报」历史数据为空（或仅有总计行）；上架包下拉无选项；导出今日数据无内容。同时影响包体管理 →「iOS 上架包」Tab，列表为空，已有配置无法查看、编辑或删除。设备日报 / VIP 日报同账号同日期区间正常。

**账号 / 环境（2026-07-17）：**

- 账号：`leying_niu`
- 环境：`https://ccccc-admin.dk888.link`
- 查询：`BeginTime=2026-07-01`，`EndTime=2026-07-17`，`ReportType=1`

**前端排查：**

- 新站字段映射对齐旧站 `PutawayDaily.vue`：`TodayItems` → 实时，`Items` → 历史，`BannerItems` → 总计
- 前端已对 `Items || []` 兜底，**不会因 `null` 崩溃**，但历史表无明细行
- 同账号同区间：
  - `GET /backend/operation/dailyreport`（日报）`Items` 有 16 条
  - `GET /backend/operation/devicedayreport`（设备）实时 12 条 / 历史 15 条，正常
  - `GET /backend/operation/vipdayreport`（VIP）实时 11 条 / 历史 15 条，正常
  - 上架包相关接口异常如下

**实测：**

| 接口 | 字段 | 实际 | 影响 |
| --- | --- | --- | --- |
| `getiosappstoredata` | `Items` | `null` | 历史数据表格无按日明细 |
| `getiosappstoredata` | `TodayItems` / `YesterdayItems` | 有对象，金额多为 0 | 实时行可展示但业务数据空 |
| `getiosappstoredata` | `BannerItems` | 有对象，`SumLogin=85`，但 `SumReg/SumDevice/SumPayMergerMoney` 为 0 | 有汇总无明细，数据不一致 |
| `iosappstoredata/list` | `Items` | `null` | 日报上架包筛选项及包体管理 iOS 上架包列表均为空 |
| `todayappstorecsv` | `Items` | `null` | 「导出今日数据」无内容 |

**请后端确认：**

1. `getiosappstoredata` 在有 `BannerItems` 汇总时，为何历史 `Items` 为 `null`？应按日返回数组（无数据请返回 `[]`）
2. `iosappstoredata/list` 为何 `Items=null`？若未配置上架包，是否应返回 `[]`，并说明如何配置后才有日报明细
3. `todayappstorecsv` 的 `Items` 同为 `null`，导出接口是否依赖 list/配置数据
4. `BannerItems.SumLogin=85` 与明细全空是否合理（是否误用了非上架包维度的汇总）

**摘录：**

```json
{
  "status": 200,
  "message": "操作成功",
  "respond": {
    "Items": null,
    "TodayItems": {
      "ReportDay": "2026-07-17",
      "SumLogin": 0,
      "SumTransBetMoney1": 0
    },
    "YesterdayItems": { "ReportDay": "2026-07-16", "SumLogin": 0 },
    "BannerItems": {
      "SumLogin": 85,
      "SumReg": 0,
      "SumDevice": 0,
      "SumPayMergerMoney": 0
    }
  }
}
```

```json
// GET /backend/iosappstoredata/list
{ "status": 200, "respond": { "Items": null } }

// GET /backend/iosappstoredata/todayappstorecsv
{ "status": 200, "respond": { "Items": null } }
```

### API-007

**现象：** 代理分组日报列表无数据；分组级联几乎不可用（仅有一个无效根节点）。

**账号 / 环境（2026-07-17）：**

- 账号：`leying_niu`
- 环境：`https://ccccc-admin.dk888.link`
- 默认日期：前天（对齐旧站 `getBeforeDateStr(2)`）；另测 `2026-07-01`～`2026-07-17`、月报、`DataSearchType=0/2`、`ParentTreeStr=,0,`

**前端排查：**

- 新站已对齐旧站 `groupDaily.vue`：分组级联、`ParentTreeStr` / `Level` 下钻、日报/月报、数据类型、分页、总计（`SearchType=total`）、导出（10687）
- 列表请求 `Items || []` 兜底，**不会因 `null` 崩溃**
- Cascader 对 `Id=0` 做了字符串化，避免被当成空值

**实测：**

| 接口 | 字段 | 实际 | 影响 |
| --- | --- | --- | --- |
| `grouplist?IsTop=1` | `Items` | 仅 1 条：`Id=0`，`GroupName=推广中心`，`List=null` | 无法选择真实多级分组 |
| `dailyreport`（列表） | `Items` | 恒为 `null`，`Pagination.MaxCount=0` | 表格无明细 |
| `dailyreport&SearchType=total` | `BannerItems` | 有对象，金额字段全 `0` | 仅能看到空总计 |

**请后端确认：**

1. `dailyreport` 无数据时为何返回 `Items: null` 而非 `[]`？
2. 该账号/环境是否确实无代理分组日报数据？若业务应有数据，请排查统计任务 / 权限 / `ParentTree` 过滤
3. `grouplist` 返回的 `Id=0`「推广中心」且 `List=null` 是否为占位脏数据？正常应返回可下钻的分组树
4. `Id=0` 是否合法主键？前端 Cascader / 过滤字符串 `,0,` 可能受影响，建议使用真实正整数 Id

**摘录：**

```json
// GET /backend/agentnetcashgroup/grouplist?IsTop=1
{
  "status": 200,
  "respond": {
    "Items": [
      {
        "Id": 0,
        "GroupName": "推广中心",
        "ParentId": -1,
        "Level": 1,
        "List": null,
        "ParentTree": ",0,"
      }
    ]
  }
}

// GET /backend/agentnetcashgroup/dailyreport?...
{
  "status": 200,
  "respond": {
    "Items": null,
    "Pagination": { "CurrPage": 1, "MaxCount": 0, "MaxPageCount": 1, "PageSize": 20 }
  }
}
```

### API-008

**现象：** 代理数据 → 历史「渠道数据」维度，若前端只读 `Items`/`TotalItems` 会空表；需兼容另一套字段名。

**账号 / 环境（2026-07-17）：**

- 账号：`leying_niu`
- 对比：`channelreport`（代理）vs `channelreportbychannel`（渠道），同 `SearchType=old`、昨日日期

**前端排查：**

- 旧站 `HistoryAgentData` 取 `Items` + `TotalItems`
- 旧站 `HistoryChannelData` 取 `RealTimeItems` + `TotalRealTimeItems`（即使 `SearchType=old`）
- 新站已按旧站双字段兜底：`Items || RealTimeItems`，`TotalItems || TotalRealTimeItems`

**实测：**

| 接口 | SearchType | 列表字段 | 合计字段 | 有数据 |
| --- | --- | --- | --- | --- |
| `channelreport` | today | `RealTimeItems`（20 条，MaxCount=37） | 无 | 是 |
| `channelreportbychannel` | today | `RealTimeItems`（20 条，MaxCount=84） | 无 | 是 |
| `channelreport` | old | `Items`（19 条） | `TotalItems` | 是 |
| `channelreportbychannel` | old | **`RealTimeItems`**（20 条），`Items=null` | **`TotalRealTimeItems`**，无 `TotalItems` | 是 |

**请后端确认：**

1. `channelreportbychannel` 在 `SearchType=old` 时，是否应与代理接口统一返回 `Items` / `TotalItems`？
2. 若历史原因必须保留 `RealTimeItems` 命名，请在接口文档明确，避免新客户端按字段名误判无数据
3. 无数据时请返回 `[]` 而非 `null`（今日代理侧 `Items: null` 同理）

**摘录：**

```json
// GET /backend/operation/channelreportbychannel?SearchType=old&...
{
  "status": 200,
  "respond": {
    "RealTimeItems": [
      {
        "ChannelId": "140670_1",
        "ReportDay": "2026-07-16",
        "SumTransBetMoney1": 150000
      }
    ],
    "Items": null,
    "Pagination": { "MaxCount": 24 }
  }
}
```

---

### API-009

**现象：** 充兑排行里，充值接口在「昨日」单日查询时常返回 `Items: null`、`Users: null`；拉宽区间后才有 `Items`。字段名为 `Items` + `SumGold`（不是输赢排行的 `ItemsWin` / `SumAddGold`）。

**页面：** `/operationalData/ranking` → 充兑排行

**接口：** `GET /backend/operation/gamepaymentrank`

**请求参数：**

- `BeginTime` / `EndTime`：unix 秒
- `Limit`：如 `100`
- 可选：`AdminSearch` / `ChannelSearch` 等

**实测（2026-07-17）：**

| 区间     | `Items`                        | `Users` |
| -------- | ------------------------------ | ------- |
| 昨日单日 | `null`                         | `null`  |
| 近 7 日  | 6 条（`PlayerId` + `SumGold`） | 6 条    |

同页兑换接口 `gamewithdrawrank` 昨日单日即可返回 `Items`（有数据时）。

**请后端确认：**

1. 无数据时请返回 `Items: []`、`Users: []`，不要 `null`
2. 文档明确列表字段为 `Items`、金额为 `SumGold`

**摘录：**

```json
// GET /backend/operation/gamepaymentrank?Limit=100&BeginTime=<昨日0点>&EndTime=<昨日末>
{ "status": 200, "respond": { "Items": null, "Users": null } }

// 近 7 日有数据时
{ "respond": { "Items": [{ "PlayerId": 123, "SumGold": 10000 }], "Users": [{ "PlayerId": 123, "LoginAccount": "..." }] } }
```

---

### API-010

**现象：** 游戏排行仅返回盈利侧 `ItemsWin`，亏损侧 `ItemsLose` 恒为 `null`，前端亏损排行无数据。

**页面：** `/operationalData/ranking` → 游戏排行

**接口：** `GET /backend/operation/realgamewinrank`

**实测（2026-07-17）：**

| 区间     | `ItemsWin`                                        | `ItemsLose` |
| -------- | ------------------------------------------------- | ----------- |
| 昨日单日 | 7 条（含 `GameId`/`SumBet`/`SumWin`/`SumProfit`） | `null`      |
| 近 7 日  | 8 条                                              | `null`      |

**请后端确认：**

1. `ItemsLose` 是否应返回亏损游戏列表（对齐旧站 `gameRanking` 左右双表）
2. 无亏损数据时返回 `[]` 而非 `null`
3. 字段含义与旧站一致：盈利/亏损金额 = `SumBet - (SumWin - SumProfit)`

**摘录：**

```json
{
  "status": 200,
  "respond": {
    "ItemsWin": [
      {
        "GameId": 1030,
        "SumBet": 648088500,
        "SumWin": -22491640,
        "SumProfit": 0
      }
    ],
    "ItemsLose": null
  }
}
```

---

### API-011

**现象：** 无限代理排行接口成功（status 200）但 `Items` / `Pagination` 恒为 `null`，页面无数据。

**页面：** `/operationalData/ranking` → 无限代理排行

**接口：** `GET /backend/wnlimitedproxydata/ranking`

**请求参数：** `BeginTime` / `EndTime`（unix 秒）、`Limit`、`DataSearchType`（0 正式 / 1 测试 / 2 全部）等

**实测（2026-07-17）：** 昨日单日、近 7 日均为：

```json
{
  "status": 200,
  "message": "操作成功",
  "respond": { "Items": null, "Pagination": null }
}
```

**请后端确认：**

1. 该环境是否本应有无限代理排行数据
2. 无数据时返回 `Items: []`、`Pagination: { MaxCount: 0 }`
3. 列表行是否包含 `LoginAccount` / `PackageName` / `ChannelName` / `Award`（对齐旧站 `endlessRanking`）

---

### API-012

**现象：** 自助查单 → 访问记录（明细 / 统计）接口均失败，前端无法展示访问明细与统计。

**页面：** `/operationalManage/rechargeList` → 自助查单 → 访问记录

**接口：**

- `GET /backend/visitstatistic/selfreviewlist`（明细）
- `GET /backend/visitstatistic/selfreviewstatisticslist`（统计）

**请求参数（示例）：**

- `BeginTime` / `EndTime`：unix 秒
- `Group=Cash`、`SubGroup=Recharge_Appeal_Click`
- 明细另有 `Page` / `PageSize`、`LoginAccount`、`PlayerId` 等

**实测（2026-07-17，昨日 / 近 7 日）：**

```json
{
  "status": 20001,
  "message": "The remote server is not connected and the operation failed.",
  "respond": null
}
```

**请后端确认：**

1. 访问统计服务是否宕机 / 未接入该环境
2. 错误码 `20001` 含义与恢复方式
3. 正常时应返回：明细 `Items`+`Pagination`；统计 `DeviceList` / `UserTypeList` / `VipList` / `Total`（对齐旧站）

---

### API-013

**现象：** 自助查单「游戏端入口开关」读取失败，开关状态无法初始化。

**页面：** `/operationalManage/rechargeList` → 自助查单

**接口：** `GET /backend/playerpaymentselfreview/checkconfig`

**实测（2026-07-17）：**

```json
{
  "status": 10021,
  "message": "Data formatting failed",
  "respond": null
}
```

无参与带 `AgentId=0` 均失败。同模块 `GET .../icon` 可正常返回 `Items: []`。

**请后端确认：**

1. `checkconfig` 是否缺必填参数或响应结构异常导致反序列化失败
2. 正常响应是否应含 `SelfReviewConfig.DialogSwitch`（0/1）
3. 修复前前端已兜底吞错，不影响查单列表主流程

---

### API-014

**现象：** 充值列表多个列表接口在「无数据」时返回 `Items: null`，而不是空数组；前端需统一 `Items \|\| []` 兜底。

**页面：** `/operationalManage/rechargeList`（快捷充值 / 充值次数 / 黑名单设备 / CP补单等）

**实测（2026-07-17）示例：**

| 接口                          | status | Items  | 备注                      |
| ----------------------------- | ------ | ------ | ------------------------- |
| `rechargelimitconfig/list`    | 200    | `null` | 无次数配置                |
| `playereasyrecharge/list`     | 200    | `null` | MaxCount=0，有 Total 汇总 |
| `playerusdteasyrecharge/list` | 200    | `null` | 同上                      |
| `rechargeblackdevice/list`    | 200    | `null` | MaxCount=0                |
| `fixpayment/list`             | 200    | `null` | 有权限账号近 7 日无 CP 单 |
| `playerpayment/list`          | 200    | 有数据 | 通道充值正常              |
| `playerpayment/faillist`      | 200    | 有数据 | 失败记录正常              |
| `rechargeblackplayer/list`    | 200    | 有数据 | 黑名单账号正常            |
| `golobalgametipconfig/list`   | 200    | 有数据 | 充值统计正常              |

**请后端确认：** 无数据时统一返回 `Items: []`，避免各端重复兜底。

**前端修复备注（非接口问题）：** CP 补单路径曾误写为 `formpayment`，已更正为旧站一致的 `fixpayment`。

---

### API-015

**现象：** 提现列表多个 Tab 在无数据时返回 `Items: null`，前端需 `Items \|\| []` 兜底；三方冲正记录空结果时 `Pagination` 还不带 `MaxCount`。

**页面：** `/operationalManage/withdrawList`

**实测（2026-07-17）：**

| Tab | 接口 | status | Items | Pagination |
| --- | --- | --- | --- | --- |
| 兑换列表 | `playerwithdraw/list` | 200 | 有数据（昨日 1 条） | 有 MaxCount |
| 出款列表 | `withdrawfinancelist/list` | 200 | 有数据（昨日 1 条） | 有 MaxCount |
| 订单统计 | `playerwithdrawreport/list` | 200 | 有数据 | 有 MaxCount |
| 提现白名单 | `withdrawwhitelist/list` | 200 | `null` | MaxCount=0 |
| 派单管理 | `withdrawsendorderusermanage/list` | 200 | `null` | MaxCount=0 |
| 派单列表 | `withdrawsendorderlist/list` | 200 | `null` | MaxCount=0（含 Total/Account） |
| 三方冲正 | `playerwithdraw/revertedlist` | 200 | `null` | **无 MaxCount**（仅 CurrPage/PageSize） |
| 提款流水 | `playerwithdrawflow/list` | 200 | `null` | MaxCount=0 |
| 流水设置 | `playerwithdrawflow/flowList` | 200 | `null` | MaxCount=0，有 Switch |

**请后端确认：**

1. 无数据时统一返回 `Items: []`
2. `Pagination` 始终包含 `MaxCount`（冲正记录空结果也不要省略）

**前端备注：** 订单统计列字段曾误用 `Date/SumCount/...`，已按旧站改为 `ReportDay/Count/HighCount/...`（接口本身正常）。

---

### API-016

**现象：** 派单列表近 7 日查询仍无订单明细（`Items=null`，`MaxCount=0`），但响应里带有 `Account`（接单账号状态）与全 0 的 `Total`。

**页面：** `/operationalManage/withdrawList` → 派单列表

**接口：** `GET /backend/withdrawsendorderlist/list`

**实测摘录：**

```json
{
  "status": 200,
  "respond": {
    "Items": null,
    "Pagination": { "CurrPage": 1, "PageSize": 20, "MaxCount": 0 },
    "Total": { "Amount": 0, "WithdrawCount": 0, "ProcessingCount": 0 },
    "Account": { "AdminId": 716, "Status": 0, "IsOnline": 0, "MaxOrderNum": 0 }
  }
}
```

**请后端确认：** 该测试环境是否应有派单列表历史数据；若确实无单，`Items` 请返回 `[]`。

---

### API-017

**现象：** 客服工单列表在本测试环境查不到任何工单；无数据时 `Items` 为 `null`（非 `[]`）。导出参数下 `Pagination` 也会变成 `null`。

**页面说明：** 旧站 `/operationalManage/applyService` 实际是**单列表页**（复用 `playerOrderPage`，`inServiceManage=false`），**没有**充值/提现那种多 Tab；状态筛选为多选下拉，不是 Tab。

**接口：** `GET /backend/customsupport/list`

**实测（2026-07-17，账号 leying_niu）：**

| 条件                         | status | Items  | Pagination            |
| ---------------------------- | ------ | ------ | --------------------- |
| 近 7 日 / Status=2（处理中） | 200    | `null` | MaxCount=0            |
| 近 7 日 / Status 空或不传    | 200    | `null` | MaxCount=0            |
| 昨日（旧站默认区间）         | 200    | `null` | MaxCount=0            |
| 近 30 / 90 / **365** 日      | 200    | `null` | MaxCount=0            |
| 不传 BeginTime/EndTime       | 200    | `null` | MaxCount=0            |
| `IsExp=true`（导出）         | 200    | `null` | **`Pagination=null`** |

**同页相关接口（正常，供对照）：**

| 接口 | 结果 |
| --- | --- |
| `GET /backend/customersupporterquestiontype/list?Type=0` | 200，`Items` 8 条（充值/提现/补单等） |
| `GET /backend/customersupporterquestiontype/simplelist?Type=0` | 200，同样 8 条 |
| `GET /backend/customersupporterquestiontype/getspecialqustiontypesetting` | 200，返回扩展栏位数组 |
| `GET /backend/customsupport/alert` | 200，`Count=0` / `LastId=0` |
| `PUT /backend/customsupport/handle`（不存在 id） | `10002` 无法找到该记录（接口可达） |
| `PUT /backend/customsupport/reject`（不存在 id） | `10002` Record not found（接口可达） |
| `POST /backend/customsupport/`（PlayerId=1） | `10140` 玩家不存在（接口可达） |

**请后端确认：**

1. 该测试环境是否本应有客服工单历史；若有，为何近一年 `MaxCount` 仍为 0
2. 无数据时 `Items` 返回 `[]`，导出时 `Pagination` 也不要为 `null`
3. 是否与 API-018（详情库异常）同源

**摘录：**

```json
{
  "status": 200,
  "respond": {
    "Items": null,
    "Pagination": {
      "CurrPage": 1,
      "MaxCount": 0,
      "MaxPageCount": 1,
      "PageSize": 20
    }
  }
}
```

---

### API-018

**现象：** 按 id 拉工单详情直接库异常，无法支撑详情弹窗 / 历史核对。

**页面：** `/operationalManage/applyService`（旧站详情/处理记录依赖列表字段拼装；详情接口仍被 API 层暴露）

**接口：** `GET /backend/customsupport/{id}`

**实测（2026-07-17）：**

- `GET /backend/customsupport/1` → `status=10001`，`message=数据库异常，请联系管理员`，`respond=null`
- `GET /backend/customsupport/0` → 同上 `10001`

**对比：** 同模块的 `handle` / `reject` / `cancel` 对不存在记录返回 `10002`（正常业务码），唯独按 id 查询走库异常。

**请后端确认：** 修复 `customsupport/{id}` 查询；不存在记录应返回 `10002`，而不是 `10001`。

**摘录：**

```json
{ "message": "数据库异常，请联系管理员", "respond": null, "status": 10001 }
```

---

### API-019

**现象：** 公告管理「邮件通知」Tab 列表始终无数据。

**页面：** `/operationalManage/gameNotice` → 邮件通知

**接口：** `GET /backend/gameemail/list`

**实测（2026-07-17，账号 leying_niu）：**

| 条件                   | status | Items  | Pagination |
| ---------------------- | ------ | ------ | ---------- |
| 近 7 日                | 200    | `null` | MaxCount=0 |
| 近 30 日               | 200    | `null` | MaxCount=0 |
| 近 **365** 日          | 200    | `null` | MaxCount=0 |
| 不传 BeginTime/EndTime | 200    | `null` | MaxCount=0 |

**对照：** 同页「游戏公告」`GET /backend/gamenotice/list` 正常（不传时间 MaxCount=8，有 Items）。

**请后端确认：**

1. 该测试环境是否本应有邮件通知历史；若有，为何全年 MaxCount 仍为 0
2. 无数据时 `Items` 返回 `[]`

**摘录：**

```json
{
  "status": 200,
  "respond": {
    "Items": null,
    "Pagination": {
      "CurrPage": 1,
      "MaxCount": 0,
      "MaxPageCount": 1,
      "PageSize": 5
    }
  }
}
```

---

### API-020

**现象：** 旧站点击公告/邮件标题进入「明细 / 统计」依赖的 visitstatistic 接口不可用。

**页面：**

- `/operationalManage/gameNotice`（旧站 `noticeTitle` / `emailTitle` 子页）
- `/gameManage/withdrawConfig` → 提现访问记录（明细 / 统计，固定 `Group=Withdraw`、`SubGroup=Enter`）

**接口：**

- `GET /backend/visitstatistic/list`
- `GET /backend/visitstatistic/statisticslist`

**实测（2026-07-17）：** 均返回 `status=20001`，`message=The remote server is not connected and the operation failed.`，`respond=null`

**说明：** 与 API-012（自助查单访问记录同属 `visitstatistic` 服务）现象一致，疑为 visitstatistic 远程服务未连通。提现访问记录也直接依赖这两个接口，因此服务恢复前该 Tab 无法取得明细与统计数据。

**请后端确认：** 修复 visitstatistic 远程依赖；公告明细/统计与自助查单访问记录共用同一服务。

**摘录：**

```json
{
  "id": "3e60a25b-aa12-4ac8-a2c4-6ac7bb281d5f",
  "message": "The remote server is not connected and the operation failed.",
  "respond": null,
  "status": 20001
}
```

---

### API-021

**现象：** 公告新建/编辑中「推送跳转 → 活动」依赖的活动列表接口无数据。

**页面：** `/operationalManage/gameNotice` → 游戏公告 → 新增/编辑

**接口：** `GET /backend/gameadconfig/getactivitylist`

**实测（2026-07-17）：** `status=200`，`respond=null`

**对照：** 同模块 `GET /backend/gameadconfig/getnoticelist` 正常返回公告数组，可用于「跳转类型=公告」。

**请后端确认：** 无活动时应返回 `[]`；若本环境本应有活动数据，请排查为何为 `null`。

---

### API-022

**现象：** 红利发放「发放记录」在无明细时 `Items` 为 `null`；默认昨日区间经常为空，但响应仍带 `Total` 汇总字段。

**页面：** `/operationalManage/playerGoldHandle` → 发放记录

**接口：** `GET /backend/playergoldhandle/list`（`HandleType=1`）

**实测（2026-07-17）：**

| 条件 | status | Items | MaxCount | Total |
| --- | --- | --- | --- | --- |
| 昨日 + DataSearchType=0 | 200 | `null` | 0 | `{Total:0, TotalReal:0, TotalRealApplyAmount:0}` |
| 近 7 日 | 200 | `null` | 0 | 全 0 |
| **不传 BeginTime/EndTime** | 200 | **5 条** | **27** | `{Total:458900, ...}` |
| `IsExp=true`（90 日） | 200 | 有数据 | 27 | 有汇总 |

**请后端确认：**

1. 无数据时 `Items` 返回 `[]`
2. 前端默认昨日对齐旧站；若业务上希望默认有数据，请确认是否应改默认区间

**摘录（昨日空结果）：**

```json
{
  "status": 200,
  "respond": {
    "Items": null,
    "Pagination": { "CurrPage": 1, "MaxCount": 0, "PageSize": 5 },
    "Total": { "Total": 0, "TotalReal": 0, "TotalRealApplyAmount": 0 }
  }
}
```

---

### API-023

- 日期：2026-07-17
- 状态：待反馈
- 优先级：P2
- 页面：账户调整 `/operationalManage/accountAdjust`（调整审核 / 调整记录）
- 接口：`GET /backend/playergoldaccountchange/approvelist`

**现象：**

1. 无明细时 `Items` 返回 `null`（非 `[]`），前端需兜底
2. 审核列表固定 `Approve=1,4`（待审核/进行中）：昨日与近 90 日均为 `Items=null`、`MaxCount=0`
3. 记录列表 `Approve=2,3,4` + `IsApprove=1`：昨日有数据（MaxCount=73），不传时间 MaxCount=3084，导出 `IsExp=true` 正常

**期望：** 无数据时 `Items` 返回 `[]`；确认测试环境是否本应存在待审核单

**摘录（审核空结果）：**

```json
{
  "status": 200,
  "respond": {
    "Items": null,
    "Pagination": { "CurrPage": 1, "MaxCount": 0, "PageSize": 5 },
    "Total": { "Total": 0, "TotalReal": 0, "TotalRealApplyAmount": 0 }
  }
}
```

**摘录（记录昨日有数据）：**

```json
{
  "status": 200,
  "respond": {
    "Items": [
      {
        "Id": 3087,
        "LoginAccount": "pjj456",
        "Amount": 30000,
        "Approve": 2,
        "Done": 2
      }
    ],
    "Pagination": { "CurrPage": 1, "MaxCount": 73, "PageSize": 5 },
    "Total": { "Total": 16794200 }
  }
}
```

---

### API-024

- 日期：2026-07-17
- 状态：待反馈
- 优先级：P2
- 页面：红利管理 `/operationalManage/audit`
- 接口：
  - `GET /backend/playergoldhandle/approvelist`（红利发放审核 / 活动红利）
  - `GET /backend/playergoldhandle/redlist`（红利记录）
  - `GET /backend/activitymatchbonusapplyrecord/list`（申请记录）

**现象：**

1. 无明细时 `Items` 均为 `null`（非 `[]`）
2. 红利发放审核 `Approve=1,4`、活动红利 `ActivityType=10009&ApproveStatus=1`：昨日/近 90 日常 `MaxCount=0`（有历史已审数据 `Approve=2,3,4` MaxCount=27）
3. 申请记录近 90 日亦空（`MaxCount=0`）
4. **`redlist` 参数敏感：** 仅传申请时间 + `Status=-1` 时 MaxCount=0；补齐旧站默认筛参（`IsWater=-1`、`WaterType=0`、`VipLevel=-1`、`PlayerStatus=-1`、`ActivityType=-1`、`PageType=-1`、`SendType=-1` 等）后近 90 日有数据（MaxCount=178，`SumBonus=763700`）
5. `redlist` 仅 `IsExp=true`（缺上述筛参）时导出结果为空

**期望：**

1. 无数据返回 `Items: []`
2. 确认 `redlist` 缺省筛参语义，避免前端必须硬编码一堆 `-1` 才能出数
3. 确认测试环境待审单 / 申请记录是否本应为空

**摘录（redlist 缺参空结果）：**

```json
{
  "status": 200,
  "respond": {
    "Items": null,
    "Pagination": { "MaxCount": 0 },
    "Total": { "SumBonus": 0 }
  }
}
```

**摘录（redlist 完整默认筛参有数据）：**

```json
{
  "status": 200,
  "respond": {
    "Items": [
      /* 5 */
    ],
    "Pagination": { "MaxCount": 178 },
    "Total": { "SumBonus": 763700 }
  }
}
```

---

### API-025

- 日期：2026-07-17
- 状态：待反馈
- 优先级：P2
- 页面：礼品管理 `/operationalManage/giftManage`
- 接口：
  - `GET /backend/playergift/list`（审核列表：晋级豪礼 `ApplyType=4` / 主题抽奖 `ApplyType=5,6,7,8`）
  - `GET /backend/playergift/deliverlist`（发货列表：晋级豪礼 `ApplyType=4` / 主题抽奖 `ApplyType=5,6,7`）

**现象：**

1. **发货列表无数据时 `Items` 为 `null`（非 `[]`）**；同场景审核列表返回 `Items: []`，前后端约定不一致。前端已用 `Items || []` 兜底。
2. 按旧站默认筛参探测：近 7/30/365 日、不传申请时间，晋级豪礼与主题抽奖审核/发货均为 `MaxCount=0`（审核 `Items=[]`，发货 `Items=null`）。需确认测试环境是否本应有礼品单。
3. 审核导出 `IsExp=true` 在空数据时返回 `Items: []`（正常），但 `Total` 为 `null`。

**请求参数要点（与旧站对齐）：**

- 时间字段：`ApplyBeginTime` / `ApplyEndTime`、`ApproveBeginTime` / `ApproveEndTime`、发货另加 `DeliverBeginTime` / `DeliverEndTime`（秒级时间戳）
- 晋级豪礼：`ApplyType=4`；产品筛选用 `PackageName`（文本）
- 主题抽奖审核：`ApplyType=5,6,7,8`，状态筛参为 `AuditDeliverStatus`（非 `Status`）
- 主题抽奖发货：`ApplyType=5,6,7`，待发货默认 `AuditDeliverStatus=1`

**期望：**

1. 无数据统一返回 `Items: []`
2. 确认本环境礼品单是否本应为空；若有历史数据，说明正确筛参或数据范围

**摘录（发货列表空）：**

```json
{
  "status": 200,
  "respond": {
    "Items": null,
    "Pagination": { "MaxCount": 0 },
    "Total": null
  }
}
```

**摘录（审核列表空）：**

```json
{
  "status": 200,
  "respond": {
    "Items": [],
    "Pagination": { "MaxCount": 0 }
  }
}
```

---

### API-026

- 日期：2026-07-17
- 状态：待反馈
- 优先级：P2
- 页面：游戏风控 `/operationalManage/gameRiskControl`
- 接口：
  - `GET /backend/gameipriskcontrol/list`（IP 黑/白名单）
  - `GET /backend/gameriskcontrol/list`（设备黑/白名单）
  - `GET /backend/playerbankcard/blacklist`（银行卡黑名单）
  - `GET /backend/appstorewhitelistrisk/list`（App Store 白名单）
  - `GET /backend/gameipriskcontrol/registerconfigs`（注册上限，正常）

**现象：**

1. IP/设备列表无数据时 `Items=null`，且 **无 `Pagination`**，仅有 `Total: 0`。旧站前端用 `respond.Total` 做分页总数；若前端误用 `Pagination.MaxCount` 会恒为 0。
2. 银行卡黑名单无数据时 `Items=null`（有 `Pagination.MaxCount=0`）。
3. App Store 白名单无数据时 `WhitelistList=null`（`TotalItems=0`），非 `[]`。
4. 本环境：近 30/365 日、不传时间，IP/设备/银行卡均为空；`Key=h4_ios` 的 App Store 白名单亦空。需确认测试环境是否本应有风控数据。
5. 注册上限 `registerconfigs?Type=1` 正常返回 2 条（IP/设备 SubType）。

**期望：**

1. 无数据统一返回 `Items: []` / `WhitelistList: []`
2. IP/设备列表补齐 `Pagination`（或文档明确以 `Total` 为准）
3. 确认本环境黑白名单是否本应为空

**摘录（IP 黑名单空）：**

```json
{ "status": 200, "respond": { "Items": null, "Total": 0 } }
```

**摘录（银行卡空）：**

```json
{ "status": 200, "respond": { "Items": null, "Pagination": { "MaxCount": 0 } } }
```

**摘录（App Store 空）：**

```json
{
  "status": 200,
  "respond": { "WhitelistList": null, "TotalItems": 0, "Status": 0 }
}
```

**摘录（注册上限正常）：**

```json
{
  "status": 200,
  "respond": [
    { "Type": 1, "SubType": 1, "IsOn": false, "LimitAmt": 0 },
    { "Type": 1, "SubType": 2, "IsOn": false, "LimitAmt": 0 }
  ]
}
```

---

### API-027

- 日期：2026-07-17
- 状态：待反馈
- 优先级：P2
- 页面：导出管理 `/operationalManage/downloadCsvManage`
- 接口：`GET /backend/downloadexcel/list`

**现象：**

1. 无数据时 `Items=null`（非 `[]`），前端已用 `Items || []` 兜底。
2. 按旧站默认筛参探测：`Status=-1` + 近 30/365 日发起时间，以及不传时间，均为 `Pagination.MaxCount=0`。需确认测试环境是否本应有历史导出任务。

**请求参数要点（与旧站对齐）：**

- `Id`（任务编号）、`Path`（文件名关键字）、`Status`（`-1` 全部 / `0` 导出中 / `1` 成功 / `2` 失败）
- `BeginTime` / `EndTime`（发起时间，秒）
- `Auto`（自动刷新开关，布尔）
- 下载校验：`GET /backend/downloadexcel/download?Id=&FileName=&ValidCode=`，返回 `Path` 后再走 `/api/download`

**期望：**

1. 无数据返回 `Items: []`
2. 确认本环境导出任务是否本应为空

**摘录：**

```json
{
  "status": 200,
  "message": "操作成功",
  "respond": {
    "Items": null,
    "Pagination": { "MaxCount": 0 }
  }
}
```

---

### API-028

- 日期：2026-07-17
- 状态：待反馈
- 优先级：P1
- 页面：区域屏蔽 `/operationalManage/countrySet`
- 接口：
  - `GET /backend/countriesconfig/list`（国家/地区配置列表）
  - `GET /backend/gamecountries/`（当前屏蔽状态，可读）
  - `PUT /backend/gamecountries/`（保存 Countries / CountriesAllow / Option）

**现象：**

1. **`countriesconfig/list` 恒返回 `Items=null`**（仅有 `Items` 字段，无 `Pagination`）。已试：无参、`Page=1&PageSize=9999`、`PageType=0/1`、带 `Name` 过滤，结果相同。
2. 页面依赖该列表渲染国家开关；列表为空则无法配置区域屏蔽。
3. `gamecountries/` 可正常返回状态结构（本环境 `Countries`/`CountriesAllow` 为空串，`Option=0`）。

**字段语义（与旧站对齐）：**

- 模式 Option=`1`「按钮关闭为屏蔽」→ 开关绑定 `Status1`，保存字段 `CountriesAllow`
- 模式 Option=`2`「按钮关闭允许访问」→ 开关绑定 `Status2`，保存字段 `Countries`
- 保存时应同时提交 `Countries`、`CountriesAllow`、`Option`

**期望：**

1. `countriesconfig/list` 返回完整国家配置 `Items: [...]`（无数据用 `[]`）
2. 确认测试环境国家配置数据是否缺失或接口异常

**摘录（列表异常）：**

```json
{ "status": 200, "respond": { "Items": null } }
```

**摘录（状态可读）：**

```json
{
  "status": 200,
  "respond": {
    "AdminId": 659,
    "Countries": "",
    "CountriesAllow": "",
    "CountriesForBackstage": "",
    "Option": 0
  }
}
```

---

### API-029

- 日期：2026-07-17
- 状态：待反馈
- 优先级：P1
- 页面：运营活动 `/operationalManage/activity`
- 接口（抽样探测）：
  - `GET /backend/operationactivitiesconfig/list`（当前活动）
  - `GET /backend/operationactivitiesconfig/history`（历史活动）
  - `GET /backend/operationactivitiesdefaultconfig/list`（活动模版，本环境有数据）
  - `GET /backend/dailycheckin/list`
  - `GET /backend/playerinvitefunc/listdetail`
  - `GET /backend/operationactivitiesconfig/giftcoderecord`
  - `GET /backend/activitychangelog/list`
  - `GET /backend/newplayerdrawconfig/list`
  - `GET /backend/reservewithdrawactivity/list`
  - `GET /backend/playerrechargeofferscheme/list`（存款优惠方案）
  - `GET /backend/playeragent/listplayeragentreport`（会员代理佣金）
  - `GET /backend/bonuswithdrawconfig` vs `.../list`（确认到账）
  - `GET /backend/jackpotconfig/list`（滚动大奖，正常，`Items` 为配置对象）
  - `GET /backend/vipfunction/list`（VIP 介绍，返回数组）

**现象：**

1. 当前活动 / 历史 / 签到 / 邀请明细 / 优惠码兑换 / 编辑记录 / 转盘明细 / 预约明细：无数据时多为 `Items=null`（`MaxCount=0`）。本环境上述列表多为空，需确认是否本应有数据。
2. **存款优惠** `GET /backend/playerrechargeofferscheme/list` 返回 `status=10000`「Request Operation Failed(No permission or incorrect parameter)」、`respond=null`。
3. **会员代理佣金** `GET /backend/playeragent/listplayeragentreport` 缺必填 `Mode` 时返回 `status=10037`「Parameter error([[Mode must have value]])」。
4. **确认到账** `GET /backend/bonuswithdrawconfig/list` 返回 `status=10010` Insufficient permissions；正确路径为 **`GET /backend/bonuswithdrawconfig`**（单对象配置，含 `UserCountdown`/`AutoCountdown`/`Param`）。前端已改为根路径。
5. **VIP** `vipfunction/list` 直接返回数组（非 `{Items}`）；前端已做 normalize。
6. **滚动大奖** `jackpotconfig/list` 的 `Items` 是**配置对象**而非数组（结构特殊，前端已按对象解析）。
7. 活动模版 `operationactivitiesdefaultconfig/list?IsTemplate=false` 本环境有数据（MaxCount=1）。

**期望：**

1. 无数据统一 `Items: []`
2. 明确存款方案 list 所需权限/参数
3. 明确代理报表 `Mode` 可选值与缺省语义（避免前端硬编码）
4. 废弃或文档说明 `bonuswithdrawconfig/list`，避免与根路径混淆

**摘录（当前活动空）：**

```json
{ "status": 200, "respond": { "Items": null, "Pagination": { "MaxCount": 0 } } }
```

**摘录（存款方案失败）：**

```json
{
  "status": 10000,
  "message": "Request Operation Failed(No permission or incorrect parameter)",
  "respond": null
}
```

**摘录（代理缺 Mode）：**

```json
{ "status": 10037, "message": "Parameter error([[Mode must have value]])" }
```

**摘录（确认到账正确）：**

```json
{
  "status": 200,
  "respond": {
    "Id": ...,
    "UserCountdown": ...,
    "AutoCountdown": ...,
    "Param": ...
  }
}
```

---

### API-030

- 日期：2026-07-17
- 状态：待反馈
- 优先级：P1
- 页面：排行榜 `/operationalManage/leaderboard`
- 接口：
  - `GET /backend/leaderboard/list`（当前/历史排行榜）
  - `GET /backend/leaderboard/record`（排行结算记录）
  - `GET /backend/leaderboard/getleaderboardmainconfig`（全局配置）

**现象：**

1. 列表/结算记录无数据时 `Items=null`（非 `[]`）。本环境多为空，需确认是否本应有排行榜活动。
2. **时间参数格式异常：** 旧站前端传 `StartTime`/`EndTime` 为 **`YYYY-MM-DD`**；本环境传该格式时 list 返回 `status=10000`「Request Operation Failed(No permission or incorrect parameters)」；传 **unix 秒** 则 `status=200`（仍可能空列表）。前端已改为 unix 以可查询。
3. 全局配置 `getleaderboardmainconfig` 返回 `{ Items: null }`，无法读取 `IsActive` 等字段。

**期望：**

1. 无数据返回 `Items: []`
2. 明确 list/record 的 `StartTime`/`EndTime` 应为日期字符串还是 unix；两种格式行为一致
3. 主配置无数据时 `Items` 应为空对象 `{}` 或明确缺省结构，勿 `null`

**摘录（日期字符串失败）：**

```json
{
  "status": 10000,
  "message": "Request Operation Failed(No permission or incorrect parameters)",
  "respond": null
}
```

请求示例：`StartTime=2024-01-01&EndTime=2026-07-17`

**摘录（unix 可通但空）：**

```json
{ "status": 200, "respond": { "Items": null, "Pagination": { "MaxCount": 0 } } }
```

**摘录（主配置空）：**

```json
{ "status": 200, "respond": { "Items": null } }
```

---

### API-031

- 日期：2026-07-17
- 状态：待反馈
- 优先级：P2
- 页面：票券中心 `/operationalManage/voucher`
- 接口：
  - `GET /backend/voucher/list`（票券列表 / 历史 `IsHistory=true`）
  - `GET /backend/voucher/getvouchermainconfig`（全局配置）
  - `GET /backend/voucher/voucherdetailrecord`（票券记录）
  - `GET /backend/voucher/voucherprizeredeemrecord`（领奖记录，旧 redemption 已隐藏）
  - `GET /backend/voucher/issuevoucherrecord`（发放记录）

**现象：**

1. 票券列表（含 `StartTime`/`EndTime` unix）、历史列表（`IsHistory=true` + `ClaimBeginTime`/`ClaimEndTime`）、票券记录、领奖记录：无数据时均为 `Items=null`（`MaxCount=0`）。本环境多为空，需确认是否本应有票券数据。
2. 全局配置 `getvouchermainconfig` 返回 `{ Items: null }`，无法读取 `IsActive` 等字段（结构与排行榜主配置类似）。
3. **发放记录** `issuevoucherrecord` 无必填筛参时返回 `status=10037`「Parameter error」；需传 `VoucherId`（票券详情发放记录）。
4. 错误路径探测：`/backend/voucher/getvoucherconfig` 亦返回 10037（正确路径为 `getvouchermainconfig`）。
5. 导出接口：`GET /backend/voucher/voucherdetailrecordcsv`（pageId=86）、`GET /backend/voucher/issuevoucherrecordexport`（pageId=88）已按旧站对接，本环境空数据时导出行为待验证。

**请求参数要点（与旧站对齐）：**

- 进行中列表：`Id`、`Name`、`Type`、`StartTime`/`EndTime`（unix，来自 ActiveTimeSpan）
- 历史列表：同上 + `IsHistory=true`、`IsExp=false`、`ClaimBeginTime`/`ClaimEndTime`
- 票券记录：`LoginAccount`、`VipLevel`、`PackageId`、`StartRegisterTime`/`EndRegisterTime`、`StartReceiveVoucherTime`/`EndReceiveVoucherTime`、`ActivityType`、`ActivityName`、`Status`

**期望：**

1. 无数据返回 `Items: []`
2. 主配置无数据返回空对象而非 `Items: null`
3. 文档化 `issuevoucherrecord` 必填参数

**摘录（列表空）：**

```json
{ "status": 200, "respond": { "Items": null, "Pagination": { "MaxCount": 0 } } }
```

**摘录（主配置空）：**

```json
{ "status": 200, "respond": { "Items": null } }
```

**摘录（发放记录缺参）：**

```json
{ "status": 10037, "message": "Parameter error", "respond": null }
```

---

### API-032

- 日期：2026-07-18
- 状态：待反馈
- 优先级：P1
- 页面：积分商城 `/operationalManage/rewardMall`
- 接口：
  - `GET /backend/rewardsmall/product/list`（商品列表 / `IsHistory=true` 历史）
  - `GET /backend/rewardsmall/getrewardsmallmainconfig`（全局配置）
  - `GET /backend/rewardsmall/producttag/list`（商品页签）
  - `GET /backend/rewardsmall/rewardtask/list`（积分任务）
  - `GET /backend/rewardsmall/exchange/recordlist`（兑换记录）
  - `GET /backend/rewardsmall/point/recordlist`（积分记录）
  - `GET /backend/rewardsmall/water/payment`、`GET /backend/rewardsmall/water/bet`（积分设置）
  - `GET /backend/playerpointhandle/approvelist`（积分调整审核/记录）

**现象：**

1. 商品列表、兑换记录、积分调整列表：无数据时 `Items=null`（`MaxCount=0`）。本环境多为空，需确认是否本应有积分商城数据。
2. 全局配置 `getrewardsmallmainconfig`、页签 `producttag/list`、任务 `rewardtask/list` 返回 `{ Items: null }`（页签/任务亦无 Pagination）。
3. **P1：积分记录** `GET /backend/rewardsmall/point/recordlist?Page=1&PageSize=5` 返回 `status=10001`「Database Exception，please contact admin」，页面无法查询。
4. 存款/投注返水配置 `water/payment`、`water/bet` 可通，返回 `Config` JSON 字符串（VIP 档位多为 0），前端已对接。

**期望：**

1. 无数据返回 `Items: []`，列表类带 Pagination
2. 主配置无数据返回空对象而非 `Items: null`
3. 修复 `point/recordlist` 数据库异常，恢复可查询

**摘录（商品/兑换空列表）：**

```json
{
  "status": 200,
  "respond": {
    "Items": null,
    "Pagination": { "CurrPage": 1, "MaxCount": 0, "PageSize": 5 }
  }
}
```

**摘录（主配置空）：**

```json
{ "status": 200, "respond": { "Items": null } }
```

**摘录（积分记录异常）：**

```json
{
  "status": 10001,
  "message": "Database Exception，please contact admin",
  "respond": null
}
```

---

### API-033

- 日期：2026-07-18
- 状态：待反馈
- 优先级：P1
- 页面：杯赛专题 `/operationalManage/customLeague`
- 接口：
  - `GET /backend/gamecustomleague/list`
  - `GET /backend/gamecustomleague/leaguelist`

**现象：**

1. 列表无数据时 `Items=null`（`MaxCount=0`）。本环境多为空，需确认是否本应有杯赛配置。
2. 传 `StartTime`/`EndTime` 为旧站格式 `yyyy-MM-dd HH:mm:ss` 或 unix 秒均可 `status=200`（空列表），前端已按旧站字符串格式传参。
3. **P1：联赛下拉** `GET /backend/gamecustomleague/leaguelist?GameType=7&SportId=1` 返回 `status=10163`「Related service request error」；新建/编辑时无法拉取联赛赛事选项（依赖下游赛事服务）。

**期望：**

1. 无数据返回 `Items: []`
2. 修复 `leaguelist` 下游服务异常，或明确 `GameType`/`SportId` 合法取值与错误码含义

**摘录（列表空）：**

```json
{
  "status": 200,
  "respond": {
    "Items": null,
    "Pagination": { "CurrPage": 1, "MaxCount": 0, "PageSize": 5 }
  }
}
```

**摘录（联赛下拉失败）：**

```json
{
  "status": 10163,
  "message": "Related service request error",
  "respond": null
}
```

---

### API-034

- 日期：2026-07-18
- 状态：待反馈
- 优先级：P1
- 页面：
  - 协助管理 `/operationalManage/helpManage`（代理侧，权限 10230–10233）
  - 协助工单 `/operationalManage/helpOrder`（协助者侧，权限 10234–10235）
- 接口：
  - `GET /backend/helprecords/list`
  - `GET /backend/helprecords/listhelp`
  - `GET /backend/helprecords/help`
  - （动作，未对空库强测写）`PUT /backend/helprecords/confirm|denied|cancel`

**现象：**

1. 列表无数据时 `Items=null`（`MaxCount=0`）。带时间范围 / Keyword 仍为空，无法验证字段与分页。
2. **P1：协助登录链路** `GET /backend/helprecords/help?Id=1`（无真实工单时）返回 `status=10001`「Database Exception，please contact admin」。协助工单「协助」依赖该接口返回 `Link`，再配合 `HelpLink` cookie + `POST /public/user/login` 换 Token。空/无效 Id 即 DB 异常，不利于前端兜底。

**期望：**

1. 无数据返回 `Items: []`
2. 无效/不存在的 `Id` 返回明确业务错误码（如记录不存在），而非 Database Exception；有权限的有效工单应返回可用 `Link`

**摘录（列表空）：**

```json
{
  "status": 200,
  "respond": {
    "Items": null,
    "Pagination": { "CurrPage": 1, "MaxCount": 0, "PageSize": 5 }
  }
}
```

**摘录（help 异常）：**

```json
{
  "status": 10001,
  "message": "Database Exception，please contact admin",
  "respond": null
}
```

---

### API-035

- 日期：2026-07-18
- 状态：待反馈
- 优先级：P2
- 页面：关联号查询 `/operationalManage/relationQuery`
- 接口：
  - `GET /backend/relationsearch/list`
  - `GET /backend/relationsearch/recordexport`（pageId=91，后端导出）

**现象：**

1. 列表无数据时 `Items=null`（`MaxCount=0`），`Total` 为全 0；宽时间窗 + 空 `PackageId` 仍为空，无法验证字段映射与导出。

**期望：**

1. 无数据返回 `Items: []`
2. 有关联登录数据时 `Items`/`Total`/`Pagination` 字段齐全，导出接口可创建下载任务

**摘录：**

```json
{
  "status": 200,
  "message": "操作成功",
  "respond": {
    "Items": null,
    "Pagination": { "CurrPage": 1, "MaxCount": 0, "PageSize": 5 },
    "Total": { "DeviceIdCount": 0, "IpCount": 0, "PlayerCount": 0 }
  }
}
```

---

### API-036

- 日期：2026-07-18
- 状态：待反馈
- 优先级：P1
- 页面：
  - 公告访问统计 `/operationalManage/noticeTitle`（`Group=Dialog`）
  - 邮件访问统计 `/operationalManage/emailTitle`（`Group=Mail`）
- 接口：
  - `GET /backend/visitstatistic/list`
  - `GET /backend/visitstatistic/statisticslist`
  - `GET /backend/visitstatistic/visitstatisticexcel`（pageId=33，明细导出）

**现象：**

1. **P1：下游不可用** — `list` / `statisticslist` 在 `Group=Dialog|Mail`、`Key=1`、宽时间窗下均返回 `status=20001`「The remote server is not connected and the operation failed.」`respond=null`。明细与三表统计（设备/用户类型/VIP）均无法加载。
2. 导出接口未在本环境用真实条件强测（依赖同一下游）。

**期望：**

1. 修复访问统计下游连通性；正常空数据应 `status=200` 且 `Items: []` / 各 List 为空数组
2. 错误码与文案可区分「下游断开」与「业务无数据」

**摘录：**

```json
{
  "status": 20001,
  "message": "The remote server is not connected and the operation failed.",
  "respond": null
}
```

---

### API-037

- 日期：2026-07-18
- 状态：待反馈
- 优先级：P2
- 页面：会员列表 `/operationalManage/playerList`
- 接口：
  - `GET /backend/playerinfo/list`
  - `GET /backend/playertag/list`
  - `GET /backend/playerlevel/list`
  - `GET /backend/playerinfo/listcsv`（pageId=32，导出）

**现象：**

1. 列表接口正常：`status=200`，本环境有数据（`MaxCount≈548`）。字段含 `LastTime`（非 `LastLoginTime`）、`Gold`/`WalletBalance`（分）、`Total.SumGold`/`SumWalletBalance`。
2. 标签列表无数据时 `Items=null`（`MaxCount=0`），打标签/批量上标签依赖该列表。
3. 会员层级列表可用（至少有「未分層」等配置）。

**期望：**

1. 无标签数据返回 `Items: []`
2. 文档确认最后登录字段名为 `LastTime`（前端已按此兼容）

**摘录（列表成功字段）：**

```json
{
  "status": 200,
  "respond": {
    "Items": [{ "LastTime": 1784304758, "Gold": 0, "WalletBalance": 0 }],
    "Pagination": { "MaxCount": 548 },
    "Total": { "SumGold": "...", "SumWalletBalance": "..." }
  }
}
```

**摘录（标签空）：**

```json
{
  "status": 200,
  "respond": { "Items": null, "Pagination": { "MaxCount": 0, "PageSize": 5 } }
}
```

---

### API-038

- 日期：2026-07-18
- 状态：待反馈
- 优先级：P2
- 页面：会员层级 `/operationalManage/playerLevel`
- 接口：
  - `GET /backend/playerlevel/list`
  - `GET /backend/playerlevel/listPlayerDetail`
  - `POST /backend/playerlevel/deletePlayerDetail`
  - `GET /backend/playerbackwaterscheme/allscheme`
  - `GET /backend/withdrawautoconfig/listSchemeName`

**现象：**

1. 层级列表正常：`status=200`，本环境仅 1 条「未分層」（`DefaultLevel=0`）。字段含 `WithdrawAutoConfigSchemeName` / `WithdrawAutoConfigSchemeNameId`，无 `CreateTime`/`Operator`。
2. 所属会员列表无数据时 `Items=null`（`MaxCount=0`），前端已兼容为 `[]`。
3. 返水方案 `allscheme`、风控方案 `listSchemeName` 均可用。

**期望：**

1. 无会员数据时返回 `Items: []` 而非 `null`
2. 若业务需要展示创建时间/操作人，列表接口补充对应字段

**摘录（所属会员空）：**

```json
{
  "status": 200,
  "message": "操作成功",
  "respond": {
    "Items": null,
    "Pagination": {
      "CurrPage": 1,
      "MaxCount": 0,
      "MaxPageCount": 1,
      "PageSize": 5
    }
  }
}
```

---

### API-039

- 日期：2026-07-18
- 状态：待反馈
- 优先级：P3
- 页面：会员日志 `/memberManage/memberLog`
- 接口：
  - `GET /backend/playeranalysis/log`（明细 / `Summary=1` 统计）
  - `GET /backend/playeranalysis/listcsv`（pageId=34，导出）

**现象：**

1. 明细列表正常：`status=200`，昨日样例 `MaxCount=3`，字段含 `LoginAccount`/`LoginPlatform`/`CreateTime` 等。
2. 统计正常：返回 `TotalPlatform`/`TotalVip`/`Ios`/`Android`/`Web`/`H5`/`AppStore`/`Other`/`VipList`。
3. 导出未带文件密码时返回 `status=10103`（Wrong password），属安全校验预期；前端已走 PassPopup（pageId=34）。
4. 部分明细行 `Ip`/`FromDomain`/`IpDetailName` 为空，同时存在 `IpEncrypt` 字段（前端仍按旧站展示 `Ip`）。

**期望：**

1. 若业务需展示明文 IP，确认是否应由后端解密填充 `Ip`，或前端改读其他字段
2. 无数据时 `Items` 建议返回 `[]`（前端已兼容 `null`）

**摘录（导出无密码）：**

```json
{ "status": 10103, "message": "Wrong password", "respond": null }
```

---

### API-040

- 日期：2026-07-18
- 状态：待反馈
- 优先级：P2
- 页面：游戏记录 `/memberManage/gameRecord`
- 接口：
  - `GET /backend/operation/gametransactiondetail`（列表 / `SumAll=1` 汇总）
  - `GET /backend/operation/gametransactiondetaillog`（已结算状态流水）
  - `GET /backend/operation/getbyorderid`（三方详情兜底）
  - `GET /backend/operation/gametransactiondetailexcel`（pageId=35，导出）

**现象：**

1. 列表正常：`status=200`，样例区间 `MaxCount≈12907`；含 `Detail` JSON、`BetGold`/`TotalBetGold`/`WinGold`（分）、`MoreItems` 汇总字段。
2. `SumAll=1` 汇总正常：`SumBetGold`/`SumTotalBetGold`/`SumValidWater`/`SumWinGold` 有值。
3. 结算流水 `gametransactiondetaillog` 对有效 `TransactionId` 返回 `respond: null`（`status=200`），已结算状态弹层无法展示流水。
4. 导出未带文件密码时返回 `status=10103`（Wrong password），属安全校验预期；前端已走 PassPopup（pageId=35）。
5. 部分行 `VendorCode` 为空，前端回退展示场馆名映射。

**期望：**

1. `gametransactiondetaillog` 有数据返回数组，无数据返回 `[]` 而非 `null`
2. 若业务以 `VendorCode` 为场馆展示主字段，请保证该字段有值

**摘录（结算流水空）：**

```json
{ "status": 200, "message": "操作成功", "respond": null }
```

---

### API-041

- 日期：2026-07-18
- 状态：待反馈
- 优先级：P2
- 页面：资金流管理 `/memberManage/fundFlowManage`
- 接口：
  - `GET /backend/operation/gamedetail`

**现象：**

1. 必须带 `LoginAccount` 或 `LogId`，否则 `status=10000`（No permission or incorrect parameters）。前端已拦截提示。
2. 有数据时列表正常：样例账号 `MaxCount=44`，字段含 `AddGold`/`OldGold`/`NewGold`（分）、`ExInfo`、`PlayerId`、`Reason` 等。
3. 无数据时 `Items=null`（`MaxCount=0`），前端已兼容为 `[]`。
4. `MoreItems.SumAddGold` 在有账变明细时仍可能为 `0`（本环境样例），账变总金额卡片可能长期显示 0。
5. 非法短账号等场景可能返回 `status=10140`。

**期望：**

1. 无数据返回 `Items: []`
2. 确认 `SumAddGold` 是否应按区间账变汇总；若应为合计，请返回正确汇总

**摘录（无账号参数）：**

```json
{
  "status": 10000,
  "message": "Request Operation Failed(No permission or incorrect parameters)"
}
```

**摘录（有数据但 SumAddGold=0）：**

```json
{
  "status": 200,
  "respond": {
    "Items": [{ "AddGold": -652133, "Reason": 97 }],
    "MoreItems": { "SumAddGold": 0 },
    "Pagination": { "MaxCount": 44 }
  }
}
```

---

### API-042

- 日期：2026-07-18
- 状态：待反馈
- 优先级：P3
- 页面：场馆账变 `/memberManage/venueChange`
- 接口：
  - `GET /backend/playerwallettransferorder/list`
  - `POST /backend/playerwallettransferorder/manual`
  - `POST /backend/playerwallettransferorder/editstate`

**现象：**

1. 列表正常：`status=200`，昨日样例 `MaxCount≈329`，字段含 `Type`/`GameId`/`Amount`(分)/`OrderId`/`State`/`ApproveTime` 等。
2. 无数据时可能 `Items=null`（前端已兼容为 `[]`）。
3. 导出为前端拉取大页（`IsExp=true`，`PageSize=99999`）再生成文件，与旧站一致；非 PassPopup 异步导出。

**期望：**

1. 无数据返回 `Items: []`
2. 若后续改为服务端导出，请补充导出接口与 pageId

**摘录（列表成功）：**

```json
{
  "status": 200,
  "respond": {
    "Items": [{ "Type": 1, "GameId": 1024, "Amount": 10000, "State": 0 }],
    "Pagination": { "MaxCount": 329 }
  }
}
```

---

### API-043

- 日期：2026-07-18
- 状态：待反馈
- 优先级：P2
- 页面：身份验证 `/memberManage/playerAuthentication`
- 接口：
  - `GET /backend/playerauthentication/list`（审核列表）
  - `PUT /backend/playerauthentication/approve`（审核通过/拒绝）
  - `GET /backend/playerauthentication/record`（审核记录）
  - `GET /backend/playerauthentication/recordexport`（审核记录导出，pageId=90）
  - `GET /backend/playerauthentication/setting`（验证设置）
  - `PUT /backend/playerauthentication/edit` / `PUT /backend/playerauthentication/switch`

**现象：**

1. 审核列表：`status=200`，本环境无待审时 `respond.Items=null` 且无 `Pagination`（前端已兼容为 `[]`）。传/不传 `AuthScenario`、传 `AuthScenario=0` 均为空。
2. 审核记录：`status=200`，无数据时 `Items=null`，`Pagination.MaxCount=0`（有分页壳）。带 unix 秒 `BeginTime/EndTime`、带/不带 `AuthScenario=-1` 均可通，本环境仍为空。
3. 验证设置：`status=200`，返回 8 条：`SubType` 为 `1,2,3,4,1001,1002,1003,1004`。**缺少 `SubType=1005`（验证信息开关设置）**，导致「验证信息开关」区块恒为空。
4. 审核记录导出：`GET /backend/playerauthentication/recordexport`（与旧站 pageId=90 一致）；未带二级密码时返回 `status=10103`「Wrong password」（符合预期）。
5. 设置项样例：场景 1–4 的 `Config` 含 `IsOpen`/`ExInfo`；`SubType=4` 另有 `Events`；1001/1003/1004 为开关；1002 为 `Days`（未验证宽限期）。

**期望：**

1. 无数据返回 `Items: []`（列表可补 `Pagination`）
2. 确认本环境是否本应有审核/记录数据；若有请排查写入链路
3. `setting` 是否应返回 `SubType=1005`（验证信息字段开关）；若已下线请告知前端可隐藏该区块
4. 导出在密码正确且有数据时应写入下载中心（pageId=90）

**摘录（审核列表空）：**

```json
{ "status": 200, "message": "操作成功", "respond": { "Items": null } }
```

**摘录（审核记录空）：**

```json
{
  "status": 200,
  "respond": {
    "Items": null,
    "Pagination": {
      "CurrPage": 1,
      "MaxCount": 0,
      "MaxPageCount": 1,
      "PageSize": 5
    }
  }
}
```

**摘录（setting 缺 1005）：**

```json
{
  "status": 200,
  "respond": {
    "Items": [
      { "SubType": 1 },
      { "SubType": 2 },
      { "SubType": 3 },
      { "SubType": 4 },
      { "SubType": 1001 },
      { "SubType": 1002 },
      { "SubType": 1003 },
      { "SubType": 1004 }
    ]
  }
}
```

**摘录（导出无密码）：**

```json
{ "status": 10103, "message": "Wrong password", "respond": null }
```

---

### API-044

- 日期：2026-07-18
- 状态：待反馈
- 优先级：P2
- 页面：钱包管理 `/memberManage/walletManage`
- 接口：
  - `GET /backend/playerbankcard/list`（银行卡）
  - `POST|PUT|DELETE /backend/playerbankcard`（增改删，删可带 `IsBlack`，安全 pageId=8）
  - `GET /backend/playerwalletaccount/listall`（电子钱包）
  - `POST /backend/playerwalletaccount/add` / `PUT .../edit` / `DELETE .../del/{id}`（pageId=9）
  - `GET /backend/playerwithdrawdigitaladdress/list`（虚拟币地址）
  - `POST|PUT|DELETE /backend/playerwithdrawdigitaladdress`（pageId=18）
  - `GET /backend/playerbindcardconfig/listcardmultiple`（多账号绑定）
  - `POST .../addcardmultiple` / `DELETE .../delcardmultiple/{id}`
  - `POST /backend/playerinfo/queryplayerexcel`（旧站新增查玩家，本环境异常）
  - `GET /backend/playerinfo/queryplayer`（可用，前端已改用）

**现象：**

1. 银行卡列表正常：近 30 日样例 `MaxCount≈5`，近 365 日 `MaxCount≈11`；字段含 `RealName`/`BankRealName`/`BankCardNum`/`BankCode`/`BankCardTime` 等。无数据时 `Items=null`。
2. 电子钱包 `listall`、虚拟币地址 `list`、多账号绑定 `listcardmultiple`：近 30/365 日均为 `Items=null`、`MaxCount=0`（需确认本环境是否本应有数据）。
3. 旧站新增银行卡用的 `POST /backend/playerinfo/queryplayerexcel`（body: `LoginAccount`+`PackageName`）本环境返回 `status=10053`「The uploaded file does not contain any game account」。同账号用 `GET /backend/playerinfo/queryplayer` 可正常返回 `PlayerId`。
4. 无数据时各列表 `Items=null`（前端已兼容 `[]`）。本页无导出。

**期望：**

1. 无数据返回 `Items: []`
2. 确认电子钱包 / 虚拟币 / 多绑定本环境是否应有历史数据
3. 确认 `queryplayerexcel` 是否仍支持 JSON 账号查询；若仅支持文件上传，请文档说明，前端将继续用 `queryplayer`

**摘录（银行卡有数据）：**

```json
{
  "status": 200,
  "respond": {
    "Items": [
      { "LoginAccount": "q7950029", "RealName": "覃晴", "BankCode": "BOC" }
    ],
    "Pagination": { "MaxCount": 5 }
  }
}
```

**摘录（电子钱包空）：**

```json
{
  "status": 200,
  "respond": {
    "Items": null,
    "Pagination": { "CurrPage": 1, "MaxCount": 0, "PageSize": 5 }
  }
}
```

**摘录（queryplayerexcel 失败）：**

```json
{
  "status": 10053,
  "message": "The uploaded file does not contain any game account",
  "respond": null
}
```

---

### API-045

- 日期：2026-07-18
- 状态：待反馈
- 优先级：P2
- 页面：验证码查询 `/memberManage/verifyCode`
- 接口：
  - `GET /backend/phoneverifycode/list`（手机验证码；须带 LoginAccount 或 PhoneNum）
  - `GET /backend/phoneverifycode/whitelist` / `POST .../addwhitelist` / `PUT .../delwhitelist`
  - `GET /backend/phoneverifycode/generatecode`
  - `GET /backend/phonecountrycode/list`（区码；含 FrequentCount）
  - `POST /backend/phonecountrycode/switch|frequentlyused|resetdefault` / `PUT .../switchsort`
  - `GET /backend/emailverifycode/list`（邮箱验证码）
  - `GET /backend/emailverifycode/listemailconfig` 等（邮箱通道）
  - `GET /backend/gameotpconfig/getconfig` / `PUT .../editconfig`

**现象：**

1. 手机验证码列表：不传 `LoginAccount`/`PhoneNum` 返回 `status=10037`「Parameter error」；传账号后 `status=200`，本环境样例 `Items=null`、`MaxCount=0`。
2. 白名单列表：`status=200`，本环境 `Items=null`、`MaxCount=0`。
3. 区码列表正常：`status=200`，样例 `MaxCount=244`，`FrequentCount=7`，字段含 `Key`/`DialingCode`/`FrequentlyUsed`/`Status` 等。
4. 邮箱验证码 `list`、邮箱通道 `listemailconfig`：本环境 `status=200` 但 `respond=null`（前端按空列表处理）。
5. OTP 配置正常：`respond=[{ IpLimit: 1, DeviceLimit: 10 }]`。
6. 无数据时列表常见 `Items=null`（前端已兼容）。

**期望：**

1. 无数据返回 `Items: []`（邮箱相关勿返回裸 `null` respond）
2. 确认本环境手机验证码/白名单/邮箱通道是否本应有数据
3. 手机验证码缺参 10037 可保留，但建议 message 明确「需游戏账号或手机号」

**摘录（手机验证码缺参）：**

```json
{ "status": 10037, "message": "Parameter error", "respond": null }
```

**摘录（区码成功）：**

```json
{
  "status": 200,
  "respond": {
    "FrequentCount": 7,
    "Items": [{ "Key": 101, "DialingCode": "855", "FrequentlyUsed": 1 }],
    "Pagination": { "MaxCount": 244 }
  }
}
```

**摘录（邮箱通道空）：**

```json
{ "status": 200, "message": "操作成功", "respond": null }
```

---

### API-046

- 日期：2026-07-18
- 状态：待反馈
- 优先级：P2
- 页面：称号管理 `/operationalManage/gameTitle`
- 接口：
  - `GET /backend/playerbadge/list`（称号列表）
  - `POST|PUT|DELETE /backend/playerbadge` / `PUT .../switch` / `PUT .../batchedit`
  - `GET /backend/playerbadge/playerlist`（拥有者）
  - `POST /backend/playerbadge/addplayer` / `addplayercheck` / `multiaddplayer`
  - `DELETE /backend/playerbadge/deleteplayer`
  - `GET /backend/playerbadge/playerlistcsv`（导出，pageId=75）
  - `GET /backend/badgecategory/list`（称号类别）
  - `POST|PUT|DELETE /backend/badgecategory` / `PUT .../switch`

**现象：**

1. 称号列表、类别列表、拥有者列表本环境均为 `status=200`，`Items=null`，`MaxCount=0`（前端已兼容 `[]`）。
2. 拥有者导出 `playerlistcsv` 未带二级密码时返回 `status=10103`「Wrong password」（符合预期）。
3. 需确认本环境是否本应配置称号/类别数据。

**期望：**

1. 无数据返回 `Items: []`
2. 确认本环境是否应有称号配置；若有请排查写入/环境差异
3. 导出在密码正确且有数据时应写入下载中心（pageId=75）

**摘录（称号列表空）：**

```json
{
  "status": 200,
  "respond": {
    "Items": null,
    "Pagination": { "CurrPage": 1, "MaxCount": 0, "PageSize": 5 }
  }
}
```

**摘录（导出无密码）：**

```json
{ "status": 10103, "message": "Wrong password", "respond": null }
```

---

### API-047

- 日期：2026-07-18
- 状态：待反馈
- 优先级：P2
- 页面：玩家详情 · 注单详情 `/operationalManage/playerDetails`（账号样例 `18857985770` / `PlayerId=6499867`）
- 接口：
  - `GET /backend/operation/gametransactiondetail`（与游戏记录共用；玩家场景带 `PlayerId` + `DataSearchType=2`）
  - `GET /backend/operation/gametransactiondetaillog`（结算流水）
  - `GET /backend/operation/getbyorderid`（三方详情兜底）
  - `GET /backend/operation/gametransactiongametype` / `gametransactiongameday`（场馆/日期统计）

**现象：**

1. 注单列表正常：前天→昨天样例 `MaxCount≈2779`，`Items` 有明细；字段含 `TransactionId`/`Detail`(JSON)/`GameId`/`TotalBetGold` 等；`VendorCode` 常为空字符串（前端回退 `GameId` 场馆名）。
2. `SumAll=1` 时：`MoreItems` 汇总正常（如 `SumTotalBetGold=4502200`），但 **`Items` 为空数组**（前端已分两次请求：汇总与列表）。
3. 三方详情 `getbyorderid`：样例返回 `status=10000`「Request Operation Failed…([[三方注单查询失败]])」。行内 `Detail` JSON 仍有内容，前端优先解析 `Detail` 展示。
4. 结算流水 `gametransactiondetaillog`：`status=200` 但 `respond=null`（弹层常无流水）。

**期望：**

1. `SumAll=1` 时可不返回明细，但建议文档明确；无数据时 `Items: []`
2. `getbyorderid` 修复三方注单查询失败；或确认可永久依赖行内 `Detail`
3. `gametransactiondetaillog` 有结算时应返回数组，勿 `null`

**摘录（列表有数据）：**

```json
{
  "status": 200,
  "respond": {
    "Items": [
      {
        "TransactionId": "7663527308965561499",
        "LoginAccount": "18857985770",
        "GameId": 1038
      }
    ],
    "Pagination": { "MaxCount": 2779 }
  }
}
```

**摘录（SumAll 汇总）：**

```json
{
  "MoreItems": {
    "SumTotalBetGold": 4502200,
    "SumValidWater": 4358200,
    "SumWinGold": -41501
  }
}
```

**摘录（三方失败）：**

```json
{
  "status": 10000,
  "message": "Request Operation Failed(No permission or incorrect parameters)([[三方注单查询失败]])"
}
```

---

### API-048

- 日期：2026-07-18
- 状态：待反馈
- 优先级：P2
- 页面：新增 APP / 产品配置 `/gameManage/addPackage`、`/gameManage/inclusionDeploy`
- 接口：
  - `GET /backend/gameadtemplate/listlogogroup`
  - `GET /api/resource/packagecolorthemelist`
  - `GET /api/resource/packagecolorthemedetaillist`

**现象：**

1. Logo 方案没有数据时返回 `status=200`、`respond=null`，旧页和新页均按数组消费。
2. 产品配色主题与配色明细没有数据时均返回 `status=200`，但 `respond.Items=null`。
3. 同页其他已验证接口（产品列表/详情、图标与 APP/PC 皮肤、广告方案、活动、返水、未成年、数据统计、登录注册、第三方登录、充提绑定、SEO、邀请站点、欢迎邮件及可用渠道）均能正常返回。

**期望：**

1. Logo 方案无数据时返回 `respond: []`。
2. 配色主题及明细无数据时返回 `Items: []`，保持列表接口结构稳定。

**摘录：**

```json
{ "status": 200, "respond": null }
```

```json
{
  "status": 200,
  "respond": {
    "Items": null,
    "Pagination": {}
  }
}
```

---

### API-049

- 日期：2026-07-19
- 状态：待反馈
- 优先级：P1
- 页面：短信管理·通道配置 / 首存次日召回 `/gameManage/messageManage`
- 接口：
  - `GET /backend/smschannelconfig/list`
  - `GET /backend/operation/depositrecall`

**现象：**

1. 测试账号已确认具备通道 Tab、通道配置及首存召回权限：`12906=true`、`12908=true`、`13414=true`。
2. `smschannelconfig/list` 不传参数以及传 `Page=1&PageSize=20` 均返回 `status=10000`，无法读取 OTP、公告、次日召回当前通道及密钥配置。
3. `depositrecall` 按旧版契约传 `BeginTime/EndTime=YYYY-MM-DD`，分别测试昨日、前日单日及 3 日区间，均返回 `status=10000`。
4. 同账号 `depositrecalldetail` 返回 `status=200`，说明首存召回并非整个服务或权限均不可用。
5. 注册召回 `registerrecall`、短信模板及 OTP 查询等同页接口均能正常返回 `status=200`。

**期望：**

1. 通道配置列表在账号具备 `12908` 权限时正常返回通道及三个当前通道 ID。
2. 首存召回列表按旧版日期参数正常返回 `Items/Pagination/Switch/Message`；无数据时返回空列表而非通用 `10000`。
3. 若接口新增了必传参数或权限点，请同步接口契约及权限配置。

**实测摘录：**

```json
{
  "status": 10000,
  "message": "请求操作失败(无权限或输入参数有误)([{\"Code\":10000,\"Data\":null,\"Msg\":\"10000\"}])",
  "respond": null
}
```

```json
{
  "status": 10000,
  "message": "请求操作失败(无权限或输入参数有误)",
  "respond": null
}
```

---

### API-050

- 日期：2026-07-19
- 状态：待反馈
- 优先级：P2
- 页面：短信管理 `/gameManage/messageManage`
- 接口：
  - `GET /backend/shortmessageservice/list`
  - `GET /backend/shortmessageservice/monthlist`
  - `GET /backend/operation/phoneregisterotpday`
  - `GET /backend/operation/phoneregisterotpdetail`
  - `GET /backend/operation/registerrecall`
  - `GET /backend/operation/registerrecalldetail`
  - `GET /backend/operation/depositrecalldetail`

**现象：**

1. 上述接口本次均返回 `status=200`，但无数据时 `Items=null`。
2. 同页 `GET /backend/smsannouncementconfig/list` 无数据时返回 `Items=[]`，空列表结构不一致。
3. 每日统计、月度统计仍返回 `Total`；注册召回仍返回 `Switch/Message`，只是列表字段为 `null`。
4. 新页面已将 `null` 统一转换为空数组，不影响渲染。

**期望：**

所有列表接口无数据时统一返回 `Items: []`，并保持 `Pagination` 结构稳定。

**实测摘录：**

```json
{
  "status": 200,
  "respond": {
    "Items": null,
    "Pagination": {
      "MaxCount": 0
    }
  }
}
```

---

### API-051

- 日期：2026-07-19
- 状态：待反馈
- 优先级：P2
- 页面：返水配置 `/gameManage/backWater`
- 接口：`GET /backend/playerbackwaterrecord/package`

**现象：**

1. 测试账号已登录成功，返水主 Tab、记录、手动返水、审核及配置/审核全部细分权限均存在。
2. 该接口仍返回 `status=10010`、`message=权限不足([权限配置不正确])`。
3. 旧页面 API 文件声明了该接口，但当前实际页面使用全局产品配置取得包体选项；新页面沿用全局配置，因此暂不阻塞页面。

**期望：**

确认该接口是否已废弃；若仍需使用，应修正接口权限映射，使具备返水查看权限的账号可访问。

---

### API-052

- 日期：2026-07-19
- 状态：待反馈
- 优先级：P2
- 页面：返水记录/审核 `/gameManage/backWater`
- 接口：
  - `GET /backend/playerbackwaterrecord/list`
  - `GET /backend/playerbackwaterrecord/detaillist`
  - `GET /backend/playerbackwaterrecord/reviewlist`

**现象：**

1. 统计记录、明细记录、系统申请和手动申请按旧版昨日时间及合法筛选参数实测均为 `status=200`。
2. 本环境无数据时均返回 `Items=null`。
3. `list` / `detaillist` 总数使用 `Total`，`reviewlist` 使用 `Count`；三个接口都没有 `Pagination`，与多数后台列表接口结构不一致。
4. 新页面已统一兼容 `Items=null` 及 `Total/Count/Pagination.MaxCount` 三种总数字段。

**期望：**

无数据时统一返回 `Items: []`，并统一为 `Pagination.MaxCount`（或至少统一总数字段）。

**实测摘录：**

```json
{
  "list": {
    "status": 200,
    "respondKeys": ["Items", "Sum", "SumBackWater", "SumTotal", "Total", "UnSum"],
    "Items": null
  },
  "detaillist": {
    "status": 200,
    "respondKeys": ["Items", "Sum", "SumValidWater", "Total"],
    "Items": null
  },
  "reviewlist": {
    "status": 200,
    "respondKeys": ["Count", "Items"],
    "Items": null
  }
}
```

---

### API-053

- 日期：2026-07-19
- 状态：待反馈
- 优先级：P2
- 页面：广告管理 `/gameManage/advertisementManage`
- 接口：
  - `GET /backend/gameadconfig/list`
  - `GET /backend/gameadconfig/getactivitylist`
  - `GET /backend/gameadimage/list`

**现象：**

1. 使用测试账号重新登录后进行了只读实测，四类方案列表（`Type=1/3/4/6`）均返回 `status=200`，且 `respond.Items` 为数组。
2. `gameadconfig/list` 在首页轮播广告有数据时直接返回数组；首页弹窗、支付广告及其他广告无数据时返回 `respond=null`，未返回空数组，列表结构不稳定。
3. 活动下拉接口和系统广告图库无数据时同样返回 `respond=null`；公告下拉接口则返回数组。
4. 新页面已统一将 `null` 兼容为空数组，不影响空状态渲染。

**期望：**

所有列表/下拉接口无数据时统一返回 `respond: []`；同一列表接口应保持固定结构，避免在数组、对象和 `null` 之间切换。

**实测摘录：**

```json
{
  "programmeList": {
    "Type=1": { "status": 200, "ItemsType": "array", "count": 1 },
    "Type=3": { "status": 200, "ItemsType": "array", "count": 1 },
    "Type=4": { "status": 200, "ItemsType": "array", "count": 1 },
    "Type=6": { "status": 200, "ItemsType": "array", "count": 1 }
  },
  "advertisementList": {
    "Type=1": { "status": 200, "respondType": "array", "count": 2 },
    "Type=3": { "status": 200, "respondType": "null" },
    "Type=4, SubType=1~6": { "status": 200, "respondType": "null" },
    "Type=6": { "status": 200, "respondType": "null" }
  },
  "activityList": { "status": 200, "respondType": "null" },
  "noticeList": { "status": 200, "respondType": "array", "count": 2 },
  "systemImageList": { "status": 200, "respondType": "null" }
}
```

---

### API-054

- 日期：2026-07-20
- 状态：待反馈
- 优先级：P2
- 页面：推广列表 `/generalizeManage/generalizeManageact`
- 接口：
  - `GET /backend/promoter/list`
  - `GET /backend/domain/list?Type=10`

**现象：**

1. 使用测试账号登录后进行只读实测，两个接口均返回 `status=200`。
2. 推广列表无数据时返回 `respond.Items=null`，同时 `Pagination.MaxCount=0`；已购推广域名无数据时也返回 `Items=null`。
3. 新页面已统一将 `Items=null` 兼容为空数组，不影响空状态及分页渲染。
4. 当前账号没有推广记录，因此详情接口无法使用真实 ID 只读验证；新增、编辑、启停、成本及域名设置接口均未执行，避免产生实质性数据变更。

**期望：**

列表接口无数据时统一返回 `Items: []`，保持响应字段类型稳定。

**实测摘录：**

```json
{
  "promoterList": {
    "status": 200,
    "respondKeys": ["Config", "Items", "ItemsTotal", "Pagination"],
    "Items": null,
    "Pagination": {
      "CurrPage": 1,
      "MaxCount": 0,
      "MaxPageCount": 1,
      "PageSize": 20
    }
  },
  "promotionDomainList": {
    "status": 200,
    "respondKeys": ["Items", "Pagination"],
    "Items": null
  }
}
```

---

### API-055

- 日期：2026-07-20
- 状态：待反馈
- 优先级：P1
- 页面：创建/编辑推广账号 `/generalizeManage/addGeneralize`
- 接口：
  - `GET /backend/sportsteamerteam/list`

**现象：**

1. 使用测试账号登录后连续两次进行只读实测，接口均返回业务状态 `20001`。
2. 返回文案为“远程服务器未连接,操作失败”，`respond=null`。
3. 该接口是创建推广账号第二步“直播分组”的基础数据源；接口异常时无法选择必填的直播分组，因此无法正常完成创建流程。
4. 为避免产生实质性数据，推广账号新增、编辑接口未执行。

**期望：**

直播分组服务保持可用；成功时稳定返回 `status=200` 及 `respond.Items` 数组，无数据时返回空数组。

**实测摘录：**

```json
{
  "status": 20001,
  "message": "远程服务器未连接,操作失败",
  "respond": null
}
```

---

### API-056

- 日期：2026-07-20
- 状态：待反馈
- 优先级：P2
- 页面：推广报表 `/generalizeManage/promoteData`
- 接口：
  - `GET /backend/handrecord/list`
  - `GET /backend/landingpage/listall`
  - `GET /backend/operation/exchangeratesetting`

**现象：**

1. 使用测试账号进行只读实测，三个接口均返回 `status=200`。
2. 数据填写列表无数据时返回对象，但 `respond.Items=null`。
3. 落地页下拉和汇率下拉无数据时直接返回 `respond=null`，响应结构不稳定。
4. 新页面已统一将这些空值兼容为空数组。

**期望：**

所有列表及下拉接口无数据时统一返回数组字段或空数组，避免在对象、数组与 `null` 之间切换。

**实测摘录：**

```json
{
  "handRecord": {
    "status": 200,
    "respondKeys": ["Items", "Pagination"],
    "Items": null
  },
  "landingPages": { "status": 200, "respond": null },
  "exchangeRates": { "status": 200, "respond": null }
}
```

---

### API-057

- 日期：2026-07-20
- 状态：待反馈
- 优先级：P1
- 页面：推广报表-渠道回本数据 `/generalizeManage/promoteData`
- 接口：
  - `GET /backend/operation/channelbreakevenreport`

**现象：**

1. 使用已成功登录的测试账号连续以完整周报参数、精简参数进行只读请求。
2. 两次均返回业务状态 `10103`、文案“密码错误”、`respond=null`。
3. 同一登录凭证访问推广报表其他只读接口均正常返回 `status=200`，因此不是登录密码错误。
4. 该异常导致“渠道回本数据”整个 Tab 无法获取数据。

**期望：**

合法登录会话及查询参数应返回 `status=200`；若接口需要额外鉴权字段，应返回准确的缺失字段提示并补充接口约定。

**实测摘录：**

```json
{
  "status": 10103,
  "message": "密码错误",
  "respond": null
}
```

---

### API-062

- 日期：2026-07-20
- 状态：待反馈
- 优先级：P2
- 页面：代理设定 `/generalizeManage/brokerageSet`
- 接口：
  - `GET /backend/accountteamgamerate/list`

**现象：**

接口成功返回 `TeamGameDefaultRate=25`，但无自定义配置时 `Items=null`。新页面已将其兼容为空数组，并使用默认佣金系数补全完整游戏树。

**期望：**

无自定义配置时返回 `Items: []`。

---

### API-061

- 日期：2026-07-20
- 状态：待反馈
- 优先级：P2
- 页面：收益结算 `/generalizeManage/closeManage`
- 接口：
  - `GET /backend/accountteamwithdraw/list`
  - `GET /backend/accountteambank/list`

**现象：**

1. 资金明细空列表返回 `Items=null`，但 `MoreItems` 和 `Pagination` 为对象。
2. 没有提现账号时账号接口直接返回 `respond=null`，不是空数组。
3. 新页面已分别兼容为空数组，避免列表与提现账号初始化异常。

**期望：**

- 资金明细无数据时返回 `Items: []`。
- 无提现账号时返回 `respond: []`。

---

### API-060

- 日期：2026-07-20
- 状态：待反馈
- 优先级：P2
- 页面：分销结算列表 `/generalizeManage/closeOrder`
- 接口：
  - `GET /backend/accountteamwithdrawapply/list`

**现象：**

1. 使用测试账号进行只读实测，接口返回 `status=200`，分页结构正常。
2. 空数据时 `Items=null`、`MoreItems=null`，列表和汇总字段类型不稳定。
3. 新页面已分别兼容为 `[]`，避免列表渲染和状态汇总计算异常。

**期望：**

空数据时统一返回 `Items: []`、`MoreItems: []`。

**实测摘录：**

```json
{
  "status": 200,
  "respond": {
    "Items": null,
    "MoreItems": null,
    "Pagination": {
      "CurrPage": 1,
      "MaxCount": 0,
      "MaxPageCount": 1,
      "PageSize": 20
    }
  }
}
```

---

### API-059

- 日期：2026-07-20
- 状态：待反馈
- 优先级：P2
- 页面：分销列表 `/generalizeManage/teamQuery`
- 接口：
  - `GET /backend/accountteamsearch/list`

**现象：**

1. 使用测试账号按默认 31 个自然日范围进行只读实测，接口返回 `status=200`。
2. 空列表返回 `Items=null`、`Items2=null`，而不是空数组。
3. 响应中没有 `Pagination`，与页面提交的 `Page/PageSize` 分页参数及旧版分页设计不一致，空数据场景下无法获得稳定的分页结构。
4. 新页面已将 `Items=null` 兼容为空数组，并在存在 `Pagination.MaxCount` 时启用服务端分页。

**期望：**

- 空列表统一返回 `Items: []`。
- 明确 `Items2` 的业务含义。
- 统一返回分页信息，例如 `Pagination.MaxCount/Page/PageSize`。

**实测摘录：**

```json
{
  "message": "",
  "respond": {
    "Items": null,
    "Items2": null
  },
  "status": 200
}
```

---

### API-058

- 日期：2026-07-20
- 状态：待反馈
- 优先级：P2
- 页面：代理日报表 `/generalizeManage/teamDaily`
- 接口：
  - `GET /backend/accountteamdaily/list`

**现象：**

1. 使用测试账号分别以 `TeamType=1`、`TeamType=2` 进行只读实测，接口均返回 `status=200`。
2. 无历史日报时 `TodayItems`、`BannerItems` 仍为对象，但 `HistoryItems=null`。
3. 同一响应内对象字段与列表字段的空值约定不一致；新页面已将 `HistoryItems=null` 兼容为空数组。

**期望：**

无历史数据时统一返回 `HistoryItems: []`，保持列表字段类型稳定。

**实测摘录：**

```json
{
  "status": 200,
  "respond": {
    "BannerItems": {},
    "HistoryItems": null,
    "TodayItems": {}
  }
}
```

---

### API-066

- 日期：2026-07-20
- 状态：待反馈
- 优先级：P2
- 页面：团队管理 `/netcash/teamManage`
- 接口：
  - `GET /backend/agentnetcashteam/list`
  - `GET /backend/agentnetcashteamdeputy/list`
  - `GET /backend/agentnetcashteam/record`

**现象：**

只读实测均返回 `status=200`。空数据时三个接口均返回 `Items=null`；其中副线列表还返回 `Pagination=null`，其余两个接口返回分页对象。

**期望：**

空列表统一返回 `Items: []`，分页接口统一返回稳定的 `Pagination` 对象。

---

### API-065

- 日期：2026-07-20
- 状态：待反馈
- 优先级：P2
- 页面：代理列表 `/netcash/agency`
- 接口：
  - `GET /backend/agentnetcashregister/list`
  - `GET /backend/countriesconfig/list`
  - `GET /backend/agentnetcashdevelopername/list`

**现象：**

只读实测均返回 `status=200`，但注册列表、国家地区配置和发展人列表在空数据时均返回 `Items=null`。

**期望：**

列表为空时统一返回 `Items: []`。

---

### API-064

- 日期：2026-07-20
- 状态：待反馈
- 优先级：P2
- 页面：红利管理 `/netcash/bonusManage`
- 接口：
  - `GET /backend/agentnetcashbonus/approvelist`
  - `GET /backend/agentnetcashbonus/list`

**现象：**

只读实测均返回 `status=200`，并包含分页及合计对象，但空列表返回 `Items=null`。

**期望：**

空列表统一返回 `Items: []`，避免前端对同一列表字段做额外类型兼容。

---

### API-063

- 日期：2026-07-20
- 状态：待反馈
- 优先级：P2
- 页面：代理分组 `/netcash/proxyGrouping`
- 接口：
  - `GET /backend/agentnetcashgroup/grouplist`

**现象：**

只读实测返回 `status=200`，无自定义分组时返回 `Items=null`；同页成员列表接口能够正常返回数组和分页对象。

**期望：**

无分组时返回 `Items: []`。

---

### API-067

- 日期：2026-07-20
- 状态：待反馈
- 优先级：P2
- 页面：代理域名管理 `/netcash/agentDomainManage`
- 接口：待后端提供

**现象：**

旧项目及现有接口定义仅包含域名列表、整行编辑和可分配域名查询，未发现域名新增、删除或独立启停接口，无法实现这些扩展操作。

**期望：**

如需支持域名新增、删除和独立启停，请提供明确的接口路径、请求参数、权限编号及返回契约；当前页面仅保留旧版已有能力。

---

### API-068

- 日期：2026-07-20
- 状态：待确认
- 优先级：P1
- 页面：记录查询 `/netcash/recordInquire`
- 接口：
  - `GET /backend/recordquery/loginlist`
  - `GET /backend/recordquery/gamelist`
  - 安全 CSV 导出接口：待后端确认

**现象：**

旧版登录记录、游戏记录使用安全 CSV 页面编号 `44/45` 执行导出；当前接口定义中没有对应安全导出契约，只能沿用列表接口的 `IsExp` 参数。若后端强制安全导出流程，登录和游戏记录导出可能不可用。

**期望：**

确认列表接口 `IsExp` 是否可直接导出；如不可，请提供页面编号 `44/45` 对应的安全 CSV 接口、鉴权方式、验证码参数及文件返回格式。

---

### API-069

- 日期：2026-07-20
- 状态：待确认
- 优先级：P2
- 页面：代理账号详情 `/netcash/agencyAccountDetails`
- 接口：
  - `GET /backend/agentnetcash/{id}`
  - `GET /backend/netcashwithdrawaccount/getlogs`

**现象：**

代理详情的 `CommissionRate` 可能返回数字、JSON 字符串或数组；提款账户日志使用 `Data` 包装，而同页其他列表通常使用 `respond`/标准列表结构。前端已增加兼容，但接口契约不稳定。

**期望：**

统一 `CommissionRate` 的数据类型与单位，并统一列表接口的响应包装、`Items`、`Pagination` 和 `Total` 结构。

---

### API-070

- 日期：2026-07-20
- 状态：待确认
- 优先级：P2
- 页面：
  - 推广素材 `/netcash/extensionMaterial`
  - 帮助中心 `/netcash/helpCenter`
- 接口：
  - `PUT /backend/promotionmaterials/`
  - `POST/PUT /backend/helpcenter`

**现象：**

旧接口没有素材独立启停或排序端点，当前启停需复用素材编辑接口；帮助内容与素材编辑涉及新增的 `Status`、`LangText` 字段，尚未连接真实后台确认各环境是否均接受。主题尺寸也没有独立启停、排序接口。

**期望：**

确认编辑接口对 `Status`、`LangText` 的字段格式和兼容范围；如需独立排序或启停能力，请补充接口契约与权限编号。

---

### API-071

- 日期：2026-07-20
- 状态：待反馈
- 优先级：P2
- 页面：
  - 玩家统计报表 `/dataClose/playerStatistics`
  - 玩家盈亏报表 `/dataClose/userWinLoss`
  - 玩家分析 `/dataClose/playerAnalyze`
- 接口：
  - `GET /backend/operation/playerstatistics`
  - `GET /backend/operation/playerstatisticscsv`（PageId=46）
  - `GET /backend/operation/userstatistics`
  - `GET /backend/playeranalysis/playerlist`
  - `PUT /backend/playerext/`

**现象：**

无数据时 `Items` / `MoreItems` 常返回 `null` 而非空数组；玩家统计需 `SearchType=list` 与 `SearchType=total` 两次请求分别取列表与汇总；CSV 导出为异步任务（返回 `Id/Status/Remark`）。

**期望：**

空列表统一返回 `Items: []`、`MoreItems: {}`；确认 `playerstatisticscsv` 与 PageId=46 安全校验在各环境可用。

**前端处理：**

已通过 `toListResult` 归一化 `null` Items；页面功能按旧站已恢复。

---

### API-072

- 日期：2026-07-20
- 状态：待确认
- 优先级：P2
- 页面：游戏月报表 `/dataClose/monthStatement`（自营 Tab）
- 接口 / 权限：导出权限编号待确认

**现象：**

旧站自营月报模板误用日报导出权限 `10500`，`created` 仅校验列表权限 `10510`。新项目按月报权限序号使用导出权限 `10511`。若后台权限表未配置 `10511`，导出按钮会隐藏。

**期望：**

确认自营月报导出的正式权限 ID；若不是 `10511`，请提供正确编号以便前端修正。

---

### API-073

- 日期：2026-07-20
- 状态：待反馈
- 优先级：P1
- 页面：溢出管理 `/netcash/spillManage`
- 接口：`GET /backend/agentplayermanage/list`

**现象：**

1. 使用测试账号 `leying_niu` 登录后只读实测，无论无参、带 `Page/PageSize`、带 `Status`、或带近 31 日 `BeginTime/EndTime`，HTTP 均为 200。
2. 响应体不是合法 JSON：`{"status": 200, "message": "", "respond": success}`，其中 `respond` 为未加引号的裸词 `success`，前端 `JSON.parse` 直接失败，页面无法渲染列表。

**期望：**

返回标准业务包，例如 `status=200` 且 `respond` 为对象；列表无数据时 `Items: []` 并带稳定 `Pagination`。

**实测摘录：**

```json
{"status": 200, "message": "", "respond": success}
```

---

### API-074

- 日期：2026-07-20
- 状态：待反馈
- 优先级：P1
- 页面：记录查询 `/netcash/recordInquire` → 登录记录
- 接口：`GET /backend/recordquery/loginlist`

**现象：**

1. 只读实测传 `Page=1&PageSize=5` 及近 31 日时间区间，业务 `status=200`。
2. 返回 `Items` 实际长度 144，明显未按 `PageSize` 分页。
3. 同响应 `Pagination=null`，前端无法做服务端分页。
4. 同页 `adjustmentlist/paymentlist/withdrawlist/bonuslist/gamelist` 在相同分页参数下可正常返回 5 条并带 `Pagination` 对象，唯登录记录异常。

**期望：**

1. 遵守 `Page/PageSize`，返回当前页数据。
2. 始终返回 `Pagination`（含 `MaxCount/CurrPage/PageSize`）；无数据时 `Items: []`。

**实测摘录：**

```json
{
  "status": 200,
  "respond": {
    "Items": ["/* 实际 144 条 */"],
    "Pagination": null
  }
}
```

---

### API-075

- 日期：2026-07-20
- 状态：待反馈
- 优先级：P1
- 页面：提款管理 `/netcash/drawmoneyManage`
- 接口：
  - `GET /backend/netcashwithdraw/agentwithdrawaccountdetail`
  - `GET /backend/netcashwithdraw/check/{id}`
  - `GET /backend/netcashwithdraw/statuslist`

**现象：**

1. `managelist` 正常返回 6 条兑换类型（如 `Id=13~18`）。
2. 用上述真实 `Id=13` 请求 `agentwithdrawaccountdetail`、`check/13`，均返回 `status=10001`「数据库异常，请联系管理员」，`respond=null`。
3. `statuslist?Ids=13,14,15,16,17,18` 返回 `status=200`，但 `respond=null`，通道余额/状态刷新无数据。

**期望：**

1. 合法 `Id` 应返回详情对象；不存在记录返回明确业务码（如 `10002`），不要 `10001`。
2. `statuslist` 成功时应返回数组；无状态数据返回 `[]`，不要裸 `null`。

**实测摘录：**

```json
{ "status": 10001, "message": "数据库异常，请联系管理员", "respond": null }
```

```json
{ "status": 200, "message": "操作成功", "respond": null }
```

---

### API-076

- 日期：2026-07-20
- 状态：待反馈
- 优先级：P2
- 页面：提款管理 `/netcash/drawmoneyManage`
- 接口：
  - `GET /backend/netcashwithdraw/list`
  - `GET /backend/netcashwithdraw/agentwithdrawaccountlist`
  - `GET /backend/netcashwithdraw/withdrawlist`
  - `GET /api/loginuser/getstatus`

**现象：**

1. 提款列表无数据时 `Items=null`（同页黑名单 `blacklist` 无数据返回 `Items=[]`，结构不一致）。
2. `agentwithdrawaccountlist` 成功返回 `Cl` 汇总，但 `Items=null` 且 `Total=null`。
3. 按旧站传 `Ids` 后 `withdrawlist` 可通，但 `Items=null` 且无 `Pagination`。
4. 自动刷新状态 `GET /api/loginuser/getstatus?Key=NetCashWithdraw` 返回 `status=10010`「权限不足」。

**期望：**

1. 空列表统一 `Items: []`，`Total` 保持对象或明确可空约定。
2. 具备提款页查看权限的账号应可读自动刷新状态；若需额外权限点请同步权限配置。

---

### API-077

- 日期：2026-07-20
- 状态：待反馈
- 优先级：P1
- 页面：佣金管理 `/netcash/commissionManage`
- 接口：
  - `GET /backend/commissionconfig/list`
  - `GET /backend/commissionconfigmulti/list`
  - `GET /backend/commissiontemplatemulti/list`
  - `GET /backend/sendcommission/teamdetail`

**现象：**

1. 存在佣金模板 `TemplateId=3`，但 `commissionconfig/list?TemplateId=3` 返回 `status=200` 且 `respond=null`（不是数组，也不是 `{Items:[]}`）。
2. `commissionconfigmulti/list`（无参或带 `TemplateId=3`）均返回 `status=10304`「查询出错，请稍后再试」。
3. `commissiontemplatemulti/list` 成功但 `Items=null`。
4. 同笔发放单 `Id=30070`：`personaldetail` 正常返回 3 条；按旧站参数 `teamdetail?Id=30070&IsMulti=0` 返回 `status=10000`。
5. 对照：`listadmin` 在补齐旧站必填 `ReportMonth`+`TeamLeaderId` 后可通（空时 `Items=null`）。

**期望：**

1. 配置列表无数据返回 `[]` 或 `{Items:[]}`，保持类型稳定。
2. 修复 `commissionconfigmulti` 查询异常。
3. 明确 `teamdetail` 适用单据类型；不适用时返回明确业务码，而非通用 `10000`。

**实测摘录：**

```json
{ "status": 200, "message": "操作成功", "respond": null }
```

```json
{ "status": 10304, "message": "查询出错，请稍后再试", "respond": null }
```

---

### API-078

- 日期：2026-07-20
- 状态：待反馈
- 优先级：P1
- 页面：代客额度管理 `/netcash/dkCreditManage`
- 接口：
  - `GET /backend/agentdkaccountlimit/getagentdkaccountlimit`
  - `GET /backend/sharedComponent/list`

**现象：**

1. `getagentdkaccountlimit` 无参及带 `AdminId=715` 均返回 `status=10505`，`message=10505`，`respond=null`，额度概览无法初始化。
2. 新增代客账号依赖的 `sharedComponent/list` 返回 `status=20001`「远程服务器未连接,操作失败」。
3. 同页其他只读列表（申请记录/代客账号列表/额度流水/`playerinfo/list`）可返回 `status=200`。

**期望：**

1. 明确 `10505` 含义与所需参数/权限；正常时应返回额度对象。
2. 恢复 `sharedComponent` 下游连通性，或提供替代账号下拉接口。

**实测摘录：**

```json
{ "status": 10505, "message": "10505", "respond": null }
```

```json
{ "status": 20001, "message": "远程服务器未连接,操作失败", "respond": null }
```

---

### API-079

- 日期：2026-07-20
- 状态：待反馈
- 优先级：P2
- 页面：创建金钱渠道 `/netcash/createMoneyChannel`
- 接口：`GET /backend/gameadtemplate/getcustomlogo`

**现象：**

1. `channel/list`、`clonechannelplan/list`、`channel/availablechannels`、`channel/{numericId}` 只读正常。
2. `getcustomlogo` 传业务 `ChannelId` 字符串（如 `7775754_6`）返回 `status=10000`。
3. 传列表主键数字 `ChannelId=23` 可通，但 `Items=null`；仅有占位 `PackageLogo`（`LogoGroupId=0`，`TemplateName=""`）。

**期望：**

1. 文档明确 `ChannelId` 应传数字主键还是业务渠道号。
2. 无自定义 Logo 时 `Items` 返回 `[]`，避免 `null`。

**实测摘录：**

```json
{
  "status": 200,
  "respond": {
    "Items": null,
    "PackageLogo": {
      "LogoGroupId": 0,
      "LogoGroupTemplateType": 0,
      "SourceType": 0,
      "TemplateName": ""
    }
  }
}
```

---

### API-080

- 日期：2026-07-20
- 状态：待反馈
- 优先级：P2
- 页面：代理账号详情 `/netcash/agencyAccountDetails`
- 接口：
  - `GET /backend/accountloginip/list`
  - `GET /backend/agentnetcash/directloweradminstats`
  - `GET /backend/netcashwithdrawaccount/{adminId}`
  - `GET /backend/netcashwithdrawaccount/getlogs`

**现象：**

1. 使用真实 `AdminId=715` 后，详情、备注、设备汇总、直属会员统计、钱包余额等可读。
2. `accountloginip/list?AdminId=715` 返回 `Items=null` 且 `Pagination=null`（同页 `accountlogininfo/list` 有数据且分页正常）。
3. `directloweradminstats` 空结果时 `Items=[]`，但 `Pagination=null`。
4. 提款账户列表与 `getlogs` 成功时仅有 `Items=null`，无 `Pagination`；与 API-069 所述契约不稳定一致，本条补充空列表实测。

**期望：**

空列表统一 `Items: []`，并始终返回稳定 `Pagination` 对象。

---

### API-081

- 日期：2026-07-20
- 状态：待反馈
- 优先级：P2
- 页面：
  - 额度管理 `/netcash/creditLimitManage`
  - 平台额度管理 `/netcash/creditLimitPlatformManage`
  - 推广素材 `/netcash/extensionMaterial`
  - 佣金管理 `/netcash/commissionManage`
- 接口：
  - `GET /backend/agentcreditlimitapplyrecord/list`
  - `GET /backend/agentcreditlimitpermission/list`
  - `GET /backend/agentcreditlimitrestrict/list`
  - `GET /backend/agentcreditlimitapplyrecord/duecreditlist`
  - `GET /backend/netcashlog/list`
  - `GET /backend/promotionconf/list`
  - `GET /backend/promotionconf/listall`
  - `GET /backend/sendcommission/commissionlist`
  - `GET /backend/sendcommission/listadmin`（已补齐 `ReportMonth`+`TeamLeaderId`）

**现象：**

上述接口只读实测均为 `status=200`，但无数据时 `Items=null`（同批 `promotionmaterials/list`、`helpcenter/list`、`netcashwithdraw/blacklist` 已能返回 `Items=[]`），空列表类型不统一。

**期望：**

所有列表接口无数据时统一返回 `Items: []`。

---

### API-082

- 日期：2026-07-20
- 状态：待反馈
- 优先级：P1
- 页面：投注行为报表 `/operationalData/gameDetails`
- 接口：`GET /backend/operation/gametransactionanalysisreport`

**账号 / 环境：**

- 账号：`leying_niu`（复用已登录 Token，经本地 `5666` 代理只读 GET）
- 环境：`https://ccccc-admin.dk888.link`

**现象：**

按旧站 `betDetails.vue` / 新站页面参数（`BeginTime`/`EndTime` 为 `YYYY-MM-DD`，`AdminIds`/`ChannelIds`/`DataSearchType` 等）实测多组区间（含 `2026-07`、`2026-06`、`2026-01`、`2025-01`、`2024` 全年，以及 `DataSearchType=0/2`），HTTP 均为 200、业务 `status=200`，但 `respond.Items` 恒为 `null`，且 `message` 为空字符串。同账号同环境其他报表（游戏报表、日报、粘度）有数据，故更像本接口无明细或聚合未产出，而非整站无业务数据。

**期望：**

有投注行为数据时返回 `Items` 数组；无数据时返回 `Items: []`；`message` 与其它成功接口一致（如「操作成功」）。

**实测摘录：**

```json
{
  "message": "",
  "status": 200,
  "respond": { "Items": null }
}
```

---

### API-083

- 日期：2026-07-20
- 状态：待反馈
- 优先级：P2
- 页面：运营日报 `/dataClose/operationDaily`（运营日报 Tab）
- 接口：`GET /backend/operationdayreport/dayreport`

**现象：**

合法参数（例：`BeginTime=2026-07-19`、`BeforeTime=2026-07-18`、`ReportType=1`、`ContrastType=1`、`DataSearchType=0`）返回 `status=200`，`TodayItems`/`BeforeItems` 均为含约 51 个字段的对象，但 `TodayHourItems`、`BeforeHourItems` 为 `null`。前端分时折线依赖上述小时数组，导致有日汇总却无分时曲线。

**期望：**

有日汇总时同步返回小时数组；无分时数据时返回 `[]` 而非 `null`。

**实测摘录：**

```json
{
  "status": 200,
  "message": "操作成功",
  "respond": {
    "TodayItems": { "...": "51 keys" },
    "BeforeItems": { "...": "51 keys" },
    "TodayHourItems": null,
    "BeforeHourItems": null
  }
}
```

---

### API-084

- 日期：2026-07-20
- 状态：待反馈
- 优先级：P2
- 页面：粘度分析 `/dataClose/keepData`
- 接口：
  - `GET /backend/operation/realtimedau`（登录留存）
  - `GET /backend/operation/realtimeltv`（LTV）
  - `GET /backend/operation/everydaylogindau`（区间留存）

**现象：**

1. `realtimedau` / `realtimeltv` 在合法 `YYYY-MM-DD` 区间下成功返回列表，但字段名为 `ItemsOld`，响应中**没有** `Items`（例：近 7 日 `ItemsOld` 长度 63）。
2. `everydaylogindau` 成功时 `Items` 是单对象（如 `CountLogin1`…`CountLogin60`），不是行数组。
3. 同页 `firstpayrealtimedau` / `onetimeuser` 使用常规 `Items` 数组，契约不统一。

**说明：** 日期须传 `YYYY-MM-DD`；误传 unix 秒会得到 `20002`/`10000`。前端已用 `ItemsOld` 与 `wrapMatrixAsList` 兼容，但契约仍不稳定。

**期望：**

列表统一 `Items: []`；矩阵类接口明确文档化为对象或统一为行数组，避免同一模块混用 `Items` / `ItemsOld` / 单对象。

---

### API-085

- 日期：2026-07-20
- 状态：待反馈
- 优先级：P2
- 页面：游戏报表 `/dataClose/gameStatement`（游戏报表 Tab）
- 接口：`GET /backend/operation/gamestatisticsbygametype`

**现象：**

只读实测 `status=200` 且有数据（例：`ItemsMoney` 长度 8~9），但响应**不含** `Items`，仅有 `ItemsMoney` + `Total` + `IsNegativeWinCount`。同页分类报表 `gamestatisticsbygameplatformtype`、子游戏 `gamestatisticsbysubgame` 则返回 `Items`。前端已优先读 `ItemsMoney`，但后端字段名不一致易导致其它调用方漏数。

**期望：**

同域报表统一列表字段名；或同时返回 `Items` 与 `ItemsMoney` 并文档说明优先级。

---

### API-086

- 日期：2026-07-20
- 状态：待反馈
- 优先级：P2
- 页面：运营日报 `/dataClose/operationDaily`（游戏盈亏排行）
- 接口：`GET /backend/operationdayreport/dayreportwinrank`

**现象：**

完整查询参数（含 `BeginTime`/`BeforeTime`/`ReportType`/`ContrastType` 等）可成功；`TodayGameTypeWinItems`、`TodayPlayerItemsWin`、`TodayPlayerItemsLose` 有数据时，`TodayGameTypeLoseItems` 仍为 `null`。缺关键参数时接口返回 `10000`（前端需带齐对比日期等字段）。

**期望：**

亏损场馆排行有数据时返回数组；无数据返回 `[]`。确认必填参数集合并在错误信息中区分「缺参」与「无权限」。

---

### API-087

- 日期：2026-07-20
- 状态：待反馈
- 优先级：P2
- 页面：
  - 运营日报·数据报表 `/dataClose/operationDaily`
  - 游戏日报·子包网 `/dataClose/dayStatement`
  - 游戏月报·子包网 `/dataClose/monthStatement`
- 接口：
  - `GET /backend/operationdayreportscheduler/list`
  - `GET /backend/agentbetwinmanageday/sonlist`
  - `GET /backend/agentbetwinmanagemonth/sonlist`

**现象：**

上述接口只读实测均为 `status=200`，空结果时 `Items=null`（非 `[]`）。子包网另返回 `ItemsAgents: {}`。同账号下汇总/自营日、月报在正确月份区间有 `Items` 数据，子包网为空可能是本环境无子包网业务，但空列表类型仍不统一。数据报表另有 `ItemsLimit` 对象可返回。

**期望：**

无数据统一 `Items: []`；`ItemsAgents` 无成员时返回 `[]` 或明确空对象语义。

---

## 新增模板

```
### API-00X
- 日期 / 状态 / 优先级：
- 页面：
  - 接口：
- 现象：
- 期望：
- 实际 / 摘录：
```
