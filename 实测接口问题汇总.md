# 实测接口问题汇总

按天汇总实测结果，供前端与后端互相核对修复进度。  
（与《接口问题汇总》结构一致，本文件仅收录实测确认的问题。）

**核查范围约定：** 页面若存在多层 Tab / 子 Tab（含 Radio 内层切换），**每一层都须分项对照旧站参数并登录态实打**，不得只测父 Tab 首屏。

**实测账号 / 环境：** `leying_niu` @ `https://ccccc-admin.dk888.link`（2026-07-21 登录态实打）  
**对照旧站：** `cloudPlatform/src/views/dashboard/admin/`、`cloudPlatform/src/views/dashboard/onlineSummary/`、`cloudPlatform/src/views/operationalData/everydayData/` + `channelData/{Putaway,Device,Vip}*`、`cloudPlatform/src/views/operationalData/groupDaily.vue`、`cloudPlatform/src/views/operationalData/channelData.vue` + `channelData/{today,history}Records`、`cloudPlatform/src/views/operationalData/{ranking,payranking,gameRanking,endlessRanking}.vue`、`cloudPlatform/src/views/operationalManage/rechargeList.vue` + `rechargeList/*`、`cloudPlatform/src/views/operationalManage/withdrawList.vue` + `withdrawList/*`、`cloudPlatform/src/views/operationalManage/applyService.vue` + `components/playerOrder/playerOrderPage.vue`、`cloudPlatform/src/views/operationalManage/gameNotice.vue` + `gameNotice/{notice,email,noticeDetail,noticeStatistics,emailDetail,emailStatistics}.vue` + `noticeTitle.vue`/`emailTitle.vue`、`cloudPlatform/src/views/operationalManage/playerGoldHandle.vue` + `playerGoldHandle/{save,saveRecord}.vue`、`cloudPlatform/src/views/operationalManage/accountAdjust.vue` + `accountAdjust/{adjust,adjustAudit,adjustList}.vue`、`cloudPlatform/src/views/operationalManage/audit.vue` + `audit/{bonusAudit,bonusList,matchList,platformTransfer}.vue` + `bonusAudit/*`、`cloudPlatform/src/views/operationalManage/giftManage.vue` + `giftManage/{auditList,auditLuckyDrawList,deliveryList,deliveryLuckyDrawList}.vue`、`cloudPlatform/src/views/operationalManage/gameRiskControl.vue` + `gameRiskControl/{blackList,whiteList,regConfig}.vue` + `blackList/*` + `whiteList/*`、`cloudPlatform/src/views/operationalManage/downloadCsvManage.vue`、`cloudPlatform/src/views/operationalManage/countrySet.vue`、`cloudPlatform/src/views/operationalManage/activity.vue` + `activity/*`、`cloudPlatform/src/views/operationalManage/leaderboard.vue` + `leaderboard/manage*`、`cloudPlatform/src/views/operationalManage/voucher.vue` + `voucher/manage*`、`cloudPlatform/src/views/operationalManage/rewardMall.vue` + `rewardMall/*`、`cloudPlatform/src/views/operationalManage/helpManage.vue`、`cloudPlatform/src/views/operationalManage/whiteList.vue` + `whiteList/*`、`cloudPlatform/src/views/operationalManage/helpOrder.vue`、`cloudPlatform/src/views/operationalManage/relationQuery.vue`、`cloudPlatform/src/views/operationalManage/playerList.vue` + `playerList/list.vue` + `playerList/{advanced-search,bulkAccountDialog}.vue`、`cloudPlatform/src/views/operationalManage/playerLevel.vue` + `playerLevel/PlayerLevelTab.vue`、`cloudPlatform/src/views/memberManage/memberLog.vue` + `memberLog/{loginRecords,table}.vue`、`cloudPlatform/src/views/memberManage/gameRecord.vue` + `gameDetails/gameRecord.vue`、`cloudPlatform/src/views/memberManage/fundFlowManage.vue`、`cloudPlatform/src/views/memberManage/venueChange.vue`、`cloudPlatform/src/views/memberManage/playerAuthentication.vue` + `playerAuthentication/{authenticationList,authenticationRecord,authenticationSetting,authenticationEmail,authenticationInfoSwtich}.vue`、`cloudPlatform/src/views/memberManage/walletManage.vue` + `wallet-manage/{cards,e-wallet,crypto-address-manage,card-multi-bind}.vue`、`cloudPlatform/src/views/memberManage/verifyCode.vue` + `verifyCode/{mobile-code-query,mobile-area-code,email-code-query,email-outgoing-accounts,otp-manage,create-code}.vue`、`cloudPlatform/src/views/operationalManage/playerDetails.vue` + `common/playerDetail/*`、`cloudPlatform/src/views/gameManage/inclusionDeploy.vue`、`cloudPlatform/src/views/gameManage/addPackage.vue` + `addPackage/{color,pcColor}TemplateDialog.vue`、`cloudPlatform/src/views/gameManage/createChannel.vue` + `channelManage/channelManage.vue` + `channelManage/*`、`cloudPlatform/src/views/gameManage/rechargeManage.vue` + `rechargeManage/aisleManage.vue` + `rechargeManage/{fastPayment,usdtFastPayment,voucherPayment}.vue`、`cloudPlatform/src/views/gameManage/withdrawConfig.vue` + `withdrawConfig/{first,thirdly,thirdRecharge,bankConfig,passageData,withdrawSetting,withdrawAccessData}.vue`、`cloudPlatform/src/views/gameManage/domainManage.vue` + `domainManage/{myDomain,fengDomain,luoDomain,h5Domain,money,moneytg,brandDomain,moneyH5Domain,spreadAppDomain,spreadH5Domain,spreadBackstageDomain}.vue`、`cloudPlatform/src/views/gameManage/packageManage.vue` + `packageManage/shelfBag.vue` + `enterprisePackage.vue` + `enterprisePackage/manage.vue`、`cloudPlatform/src/views/gameManage/siteManage.vue` + `venueManage/{venueManager,subgameMaintain}.vue`、`cloudPlatform/src/views/gameManage/messageManage.vue` + `messageManage/{overview,total,channelConfig,template,regOtpDetail,regRecallNextDay,firstRecallNextDay}.vue` + `messageManage/regOtp/{report,detail}.vue`、`cloudPlatform/src/views/gameManage/backWater.vue` + `backWater/{config,record,hand,audit,addConfig}.vue` + `backWater/components/{statistics,detail,systemApplication}.vue` + `common/gameConfigs.vue`、`cloudPlatform/src/views/gameManage/advertisementManage.vue` + `advertisementManage/{advertisementLoop,homeDialog,paymentAdvertisement,elseAdvertising}.vue`、`cloudPlatform/src/views/gameManage/vipSetting.vue` + `vipSetting/{vipGrade,vipUpgrade,vipRecord,vipIcon}.vue`、`cloudPlatform/src/views/gameManage/systemSetting.vue` + `systemSetting/{gameHelp,commonSetting,sports-tutorial,exchangeRateSetting,systemTemplates}.vue` + `systemTemplates/{agentTemp,venuesTemp}.vue`、`cloudPlatform/src/views/generalizeManage/generalizeManageact.vue`、`cloudPlatform/src/views/generalizeManage/addGeneralize.vue`、`cloudPlatform/src/views/generalizeManage/promoteData.vue` + `generalizeData/{channelDatas,dropChange,invalidUser,dataWrite,channelRecoupCostsData}.vue`、`cloudPlatform/src/views/generalizeData/timeshareData.vue` + `timeshareData/{addNumber,addDevice,allLogin,addPayMoney,addPayNum,addExchangeMoney,addExchangeNum}.vue`、`cloudPlatform/src/views/generalizeManage/teamDaily.vue` + `teamDaily/{tabOne,tabTwo}.vue`、`cloudPlatform/src/views/generalizeManage/teamQuery.vue`、`cloudPlatform/src/views/generalizeManage/closeOrder.vue`、`cloudPlatform/src/views/generalizeManage/closeManage.vue` + `teamWithdrawAccount.vue`、`cloudPlatform/src/views/generalizeManage/brokerageSet.vue`

## 状态约定（请双方统一使用下列枚举值）

| 字段 | 谁维护 | 可选值 | 说明 |
| --- | --- | --- | --- |
| **协作状态** | 双方 | `待反馈` / `处理中` / `待验证` / `已关闭` | 整单流转：提报→后端处理→前端复测→关闭 |
| **后端解决** | 后端 | `未处理` / `处理中` / `已修复` / `无法复现` / `不予修复` / `需补充信息` | 后端对该接口问题的处理结论 |
| **前端验证** | 前端 | `—` / `未验证` / `通过` / `不通过` | 后端标「已修复」后，前端复测结果；未到验证阶段填 `—` |
| **责任方** | 提报时标注 | `后端` / `前端` / `待定` | 便于分流 |

### 建议流转

1. 前端提报：协作状态=`待反馈`，后端解决=`未处理`，前端验证=`—`
2. 后端接手：协作状态=`处理中`，后端解决=`处理中`，并在详情「后端备注」写进展
3. 后端改完：后端解决=`已修复`（或 `无法复现`/`不予修复`），协作状态=`待验证`，写清修复说明/环境/版本
4. 前端复测：前端验证=`通过`/`不通过`；通过则协作状态=`已关闭`；不通过则打回 `处理中` 并写「验证备注」

---

## 清单

| 编号 | 日期 | 协作状态 | 后端解决 | 前端验证 | 责任方 | 优先级 | 页面模块 | 接口 | 问题 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| API-001 | 2026-07-21 | 待反馈 | 未处理 | — | 后端 | P1 | 数据总览·今日数据 KPI+折线 | `GET /backend/dashboard/reportinfo` | `TotalHours` 分时异常；自选 `Date1/2/3` 对应分时也为空 |
| API-002 | 2026-07-21 | 待反馈 | 未处理 | — | 后端 | P1 | 数据总览·亏损游戏 | `GET /backend/dashboard/rankbygameid` | `ItemsWin` 为 `null`，亏损游戏表空；`ItemsLose` 有数据 |
| API-003 | 2026-07-21 | 待反馈 | 未处理 | — | 后端 | P2 | 数据总览·今日运营 | `GET /backend/dashboard/reporttoday` | `Recharged15.*Items`、`OnlineUser.*Items` 为 `null`；充值行缺 `PayTypeName` |
| API-004 | 2026-07-21 | 待反馈 | 未处理 | — | 后端 | P1 | 在线总览·在线人数折线 | `GET /backend/dashboard/reportinfo` | `Items.SumOnlinePlayerNum` 有值，但 `TotalCount` 各日点位 `Count` 全 `0` |
| API-005 | 2026-07-21 | 待反馈 | 未处理 | — | 后端 | P2 | 在线总览·场馆/地区分布 | `GET /backend/dashboard/onlinesummary` | `GameResult.LastGameId=0` 场馆名为 `-`；`MapResult.IpDetailName=""` 地区名为空 |
| FE-001 | 2026-07-21 | 待验证 | — | 未验证 | 前端 | P1 | 数据总览·15 分钟充值 | （同 reporttoday） | 未按旧站合并 `Last15Items`（**已修代码，待有数据复测**） |
| FE-002 | 2026-07-21 | 待验证 | — | 未验证 | 前端 | P2 | 数据总览·在线风控 | （同 reporttoday） | 未合并 `YestDayOnlineUserItems`（**已修代码，待有数据复测**） |
| FE-003 | 2026-07-21 | 已关闭 | — | 通过 | 前端 | P3 | 在线总览 | — | 地区空串展示、场馆名用 `formatGameId`、自选日期参数过滤、隐藏无效图类型切换（**已修**） |
| API-006 | 2026-07-21 | 待反馈 | 未处理 | — | 后端 | P1 | 公司日报·上架包日报 | `iosappstoredata/*` | `list`/`getiosappstoredata.Items`/`todayappstorecsv` 均为 `null`；Banner 登录数疑似串用公司日报汇总 |
| FE-004 | 2026-07-21 | 已关闭 | — | 通过 | 前端 | P1 | 公司日报·设备/VIP 历史 | `devicedayreport` / `vipdayreport` | 历史未传 `Page`/`PageSize`、无翻页，VIP 仅见首页 15/100（**已修**） |
| API-007 | 2026-07-21 | 待反馈 | 未处理 | — | 后端 | P1 | 代理分组日报 | `GET /backend/agentnetcashgroup/dailyreport` | 明细 `Items` 恒 `null`/`MaxCount=0`；`SearchType=total` 的 `BannerItems` 却有投注人数且金额全 0 |
| FE-005 | 2026-07-21 | 已关闭 | — | 通过 | 前端 | P2 | 代理数据·历史区间报 | `channelreport*` | 区间报日期列用行内空 `BeginTime/EndTime`，应对齐旧站展示查询区间（**已修**） |
| API-008 | 2026-07-21 | 待反馈 | 未处理 | — | 后端 | P2 | 排行榜·游戏排行 | `GET /backend/operation/realgamewinrank` | `ItemsWin` 有数据，`ItemsLose` 恒为 `null`（亏损游戏表空） |
| API-009 | 2026-07-21 | 待反馈 | 未处理 | — | 后端 | P2 | 排行榜·无限代理 | `GET /backend/wnlimitedproxydata/ranking` | `Items`/`Pagination` 均为 `null`（多日区间、正式/全部同） |
| FE-006 | 2026-07-21 | 已关闭 | — | 通过 | 前端 | P0 | 充值列表·通道充值 | `GET /backend/playerpayment/list` | Sort 写成 `CreateTime desc` 导致 `Items=null`（应 `-CreateTime`）（**已修**） |
| FE-007 | 2026-07-21 | 已关闭 | — | 通过 | 前端 | P0 | 充值列表·快捷支付 | `playereasyrecharge` | 复议误用 Status===1 且权限 10271/10272 对调（**已修**） |
| FE-008 | 2026-07-21 | 已关闭 | — | 通过 | 前端 | P1 | 充值列表·黑名单·游戏账号 | `rechargeblackplayer/list` | 默认昨日+自动选产品导致漏数据；应对齐旧站空日期/空产品（**已修**） |
| API-010 | 2026-07-21 | 待反馈 | 未处理 | — | 后端 | P1 | 充值列表·自助查单·访问记录 | `visitstatistic/selfreview*` | 明细/统计均 `status=20001` 远程服务器未连接 |
| API-011 | 2026-07-21 | 待反馈 | 未处理 | — | 后端 | P3 | 充值列表·自助查单开关 | `playerpaymentselfreview/checkconfig` | `status=10021` 数据格式化失败 |
| FE-009 | 2026-07-21 | 已关闭 | — | 通过 | 前端 | P0 | 提现列表·兑换列表 | `GET /backend/playerwithdraw/list` | Sort 写成 `CreateTime desc` 导致 `status=10001`（应 `-CreateTime`）（**已修**） |
| FE-010 | 2026-07-21 | 已关闭 | — | 通过 | 前端 | P0 | 提现列表·订单统计 | `GET /backend/playerwithdrawreport/list` | 默认昨日导致空表；旧站为当月（**已修**） |
| FE-011 | 2026-07-21 | 已关闭 | — | 通过 | 前端 | P2 | 提现列表·出款列表 | `withdrawfinancelist/list` | 订单编号误传 `Keyword`；应对齐旧站 `OrderIds`（**已修**） |
| FE-012 | 2026-07-21 | 已关闭 | — | 通过 | 前端 | P2 | 提现列表·出款/三方权限 | — | 出款表权限应对齐 `10382`；三方页签应对齐 `13164`（**已修**） |
| FE-013 | 2026-07-21 | 已关闭 | — | 通过 | 前端 | P2 | 提现列表·派单管理开关 | `withdrawautoswitch/getswitch2` | 未读取返回字段 `Status`，开关态可能错（**已修**） |
| API-012 | 2026-07-21 | 待反馈 | 未处理 | — | 后端 | P3 | 提现列表·三方冲正记录 | `GET /backend/playerwithdraw/revertedlist` | 空数据时 `Items=null` 且 `Pagination` 无 `MaxCount` |
| FE-014 | 2026-07-22 | 已关闭 | — | 通过 | 前端 | P0 | 客服工单 | `customsupport/list` | 模板调用未定义的 `isProcessing`，操作列运行时异常（**已修**） |
| FE-015 | 2026-07-22 | 已关闭 | — | 通过 | 前端 | P1 | 客服工单 | `customsupport/list` | 默认日期昨日；旧站为今天（**已修**） |
| FE-016 | 2026-07-22 | 已关闭 | — | 通过 | 前端 | P0 | 客服工单 | — | 权限 key 未注册导致通过/问题类型筛选失效；缺查看；驳回权限误绑（**已修**） |
| FE-017 | 2026-07-22 | 已关闭 | — | 通过 | 前端 | P1 | 公告管理·游戏公告 | `gamenotice/list` | 未处理行内 `Status==2`「已关闭」；LangText 未按当前语言组（**已修**） |
| FE-018 | 2026-07-22 | 已关闭 | — | 通过 | 前端 | P1 | 公告管理·公告/邮件列表 | `gamenotice/list` `gameemail/list` | 缺 Sort；`CreateTime desc` 会导致 `Items=null`（**已补 `-field`**） |
| FE-019 | 2026-07-22 | 已关闭 | — | 通过 | 前端 | P1 | 公告管理·邮件表单 | `gameemail/` | 新建默认 SendTime=此刻会误判立即发送（**已修为空**） |
| FE-020 | 2026-07-22 | 处理中 | — | — | 前端 | P1 | 公告管理·邮件表单 | `gameemail/` | 相对旧站缺 IsAll/VIP/推送跳转/完整多语言等（列表可用，表单能力缩水） |
| API-013 | 2026-07-22 | 待反馈 | 未处理 | — | 后端 | P1 | 公告/邮件·访问明细与统计 | `visitstatistic/list` `statisticslist` | `Group=Dialog/Mail` 均 `status=20001` 远程服务器未连接 |
| API-014 | 2026-07-22 | 待反馈 | 未处理 | — | 后端 | P3 | 公告管理·游戏公告 | `GET /backend/gamenotice/list` | `Sort=-CreateTime` 等与空 Sort 顺序一致，疑似 Sort 未按字段生效；`CreateTime desc` 则 `Items=null` |
| FE-021 | 2026-07-22 | 已关闭 | — | 通过 | 前端 | P2 | 红利发放·发放记录 | `playergoldhandle/list` | Done/Reason 单选应对齐旧站多选；缺实际金额总计 TotalReal（**已修**） |
| FE-022 | 2026-07-22 | 已关闭 | — | 通过 | 前端 | P1 | 红利发放·发放表单 | `playergoldhandle/` | Reason 默认 3、流水倍数可小数、批量 FailItems Msg 未 JSON 解析（**已修**） |
| API-015 | 2026-07-22 | 待反馈 | 未处理 | — | 后端 | P2 | 红利发放·发放记录 | `GET /backend/playergoldhandle/list` | 无明细 `Items=null`；昨日默认常空，扩窗/无日期才有数据；`Sort=CreateTime desc`→`10001` |
| FE-023 | 2026-07-22 | 已关闭 | — | 通过 | 前端 | P1 | 账户调整·审核/记录 | `playergoldaccountchange/approvelist` | 默认/重置自动选首个产品；旧站 `PackageId=''` 查全部（**已修**） |
| FE-024 | 2026-07-22 | 已关闭 | — | 通过 | 前端 | P2 | 账户调整·审核/记录 | `approvelist` | 游戏账号未转小写；旧站 keyup 强制小写（**已修**） |
| API-016 | 2026-07-22 | 待反馈 | 未处理 | — | 后端 | P2 | 账户调整·审核/记录 | `GET /backend/playergoldaccountchange/approvelist` | 无明细 `Items=null`；`Sort=CreateTime desc`→`10001`；待审 `Approve=1,4` 本环境恒空 |
| FE-025 | 2026-07-22 | 已关闭 | — | 通过 | 前端 | P1 | 红利管理·发放审核/记录/申请 | `approvelist` `redlist` `activitymatchbonus*` | 默认/重置自动选首个产品；旧站空产品查全部（**已修**） |
| FE-026 | 2026-07-22 | 已关闭 | — | 通过 | 前端 | P2 | 红利管理·各列表 | 多接口 | 游戏账号未转小写；旧站 keyup 强制小写（**已修**） |
| FE-027 | 2026-07-22 | 已关闭 | — | 通过 | 前端 | P2 | 红利管理·记录/申请/活动审核 | `redlist` / match / event | 缺 SendType=3、缺 ApplyNote 筛、活动日期未按日边界（**已修**） |
| FE-028 | 2026-07-22 | 处理中 | — | — | 前端 | P2 | 红利管理·审核能力 | approve / excel | 相对旧站缺改金额、一键拒全部、活动/申请 Excel 批量上传等 |
| API-017 | 2026-07-22 | 待反馈 | 未处理 | — | 后端 | P2 | 红利管理·红利审核 | `GET /backend/playergoldhandle/approvelist` | 无明细 `Items=null`；`Sort=CreateTime desc`→`10001`；待审本环境空 |
| API-018 | 2026-07-22 | 待反馈 | 未处理 | — | 后端 | P2 | 红利管理·红利记录 | `GET /backend/playergoldhandle/redlist` | `Sort=CreateTime desc`→`10001`；空结果 `Items=null` |
| API-019 | 2026-07-22 | 待反馈 | 未处理 | — | 后端 | P2 | 红利管理·平台转账 | `GET /backend/playerwallettransferorder/list` | `Sort=CreateTime desc` 与 `Sort=-CreateTime` 均 `10001` |
| FE-029 | 2026-07-22 | 已关闭 | — | 通过 | 前端 | P0 | 礼品管理·主题抽奖发货 | `playergift/refuse` `received` | 可拒已发货、多余收货按钮；应对齐旧站禁用规则（**已修**） |
| FE-030 | 2026-07-22 | 已关闭 | — | 通过 | 前端 | P2 | 礼品管理·主题抽奖 | `playergift/list` `deliverlist` | 活动类型下拉过宽；应对齐旧站 5 类活动（**已修**） |
| FE-031 | 2026-07-22 | 处理中 | — | — | 前端 | P0 | 礼品管理·主题抽奖审核 | 人工录单 | 权限 10183 录单未迁移（旧站完整） |
| API-020 | 2026-07-22 | 待反馈 | 未处理 | — | 后端 | P2 | 礼品管理·审核/发货列表 | `playergift/list` `deliverlist` | 发货空结果 `Items=null`（审核为空数组）；`Sort=CreateTime desc`→`10001`；本环境无礼品单 |
| FE-032 | 2026-07-22 | 已关闭 | — | 通过 | 前端 | P0 | 游戏风控·银行卡黑名单 | `playerbankcard/blacklist` | 缺来源 SourceType 筛、重置、批量导入入口（逻辑已有未挂 UI）（**已修**） |
| FE-033 | 2026-07-22 | 已关闭 | — | 通过 | 前端 | P2 | 游戏风控·IP/设备列表 | `gameipriskcontrol` `gameriskcontrol` | 空结果 `Items=null` 未归一；Total/MaxCount 取值不稳健（**已修**） |
| API-021 | 2026-07-22 | 待反馈 | 未处理 | — | 后端 | P2 | 游戏风控·黑白名单列表 | `gameipriskcontrol/list` `gameriskcontrol/list` `playerbankcard/blacklist` | 空数据 `Items=null`；银行卡 `Sort=CreateTime desc`→`10001`；本环境列表空 |
| API-022 | 2026-07-22 | 待反馈 | 未处理 | — | 后端 | P3 | 游戏风控·App Store 白名单 | `GET /backend/appstorewhitelistrisk/list` | 空配置时 `WhitelistList=null`（应对 `[]`） |
| FE-034 | 2026-07-22 | 已关闭 | — | 通过 | 前端 | P0 | 导出管理 | `downloadexcel/list` | 默认/重置日期误为「今天−1月」；旧站为当月 1 日～今日（**已修**） |
| FE-035 | 2026-07-22 | 已关闭 | — | 通过 | 前端 | P1 | 导出管理 | `downloadexcel/list` | `show-time` 却强制 `startOf/endOf('day')` 丢掉时分秒（**已修**） |
| API-023 | 2026-07-22 | 待反馈 | 未处理 | — | 后端 | P2 | 导出管理 | `GET /backend/downloadexcel/list` | 空结果 `Items=null`（应对 `[]`）；本环境各时间窗 `MaxCount=0` |
| FE-036 | 2026-07-22 | 已关闭 | — | 通过 | 前端 | P0 | 区域屏蔽 | `countriesconfig/list` + `gamecountries/` | 按名称服务端筛选后再保存会覆盖未展示国家；改为全量加载+前端过滤（**已修**） |
| FE-037 | 2026-07-22 | 已关闭 | — | 通过 | 前端 | P1 | 区域屏蔽 | `GET /backend/gamecountries/` | `Option=0` 时旧站开关不渲染；新站归一为模式 1，保存合并状态原字段（**已修**） |
| API-024 | 2026-07-22 | 待反馈 | 未处理 | — | 后端 | P1 | 区域屏蔽 | `GET /backend/countriesconfig/list` | 空结果 `Items=null` 且无 `Pagination`；本环境国家配置库空 |
| FE-038 | 2026-07-22 | 已关闭 | — | 通过 | 前端 | P0 | 活动设置·预约明细 | `reservewithdrawactivity/list` | 取款时间误传 `WithdrawBegin/EndTime`；应对齐 `BeginTime/EndTime`+默认今天（**已修**） |
| FE-039 | 2026-07-22 | 已关闭 | — | 通过 | 前端 | P0 | 活动设置·优惠码兑换 | `giftcoderecord` | 误用 `BeginTime/EndTime`+近7日；应对齐 `ClaimBegin/EndTime`+今天（**已修**） |
| FE-040 | 2026-07-22 | 已关闭 | — | 通过 | 前端 | P1 | 活动设置·邀请明细 | `playerinvitefunc/listdetail` | 缺默认注册日窗；账号未转小写（**已修**） |
| FE-041 | 2026-07-22 | 已关闭 | — | 通过 | 前端 | P1 | 活动设置·VIP | `listprize` `vipfunction/list` | 列字段 Vip/enabled 与接口 VipLevel/UpgradePrizeEnabled 不符；Title 空未回退 Name（**已修**） |
| FE-042 | 2026-07-22 | 处理中 | — | — | 前端 | P2 | 活动设置·多子页 | — | 相对旧站缺：优惠码历史/报表、邀请规则/统计、代理方案/绑定日志、转盘方案编辑、签到当前/历史拆分、存款规则编辑器等 |
| API-025 | 2026-07-22 | 待反馈 | 未处理 | — | 后端 | P2 | 活动设置·多列表 | `operationactivitiesconfig/*` 等 | 多数列表空结果 `Items=null` MaxCount=0（当前/历史/优惠码/签到/邀请/代理/预约等） |
| API-026 | 2026-07-22 | 待反馈 | 未处理 | — | 后端 | P2 | 活动设置·类别/VIP规则 | `activitytype/list` `vipbaserule/list` | `status=200` 但 `respond=null` |
| API-027 | 2026-07-22 | 待反馈 | 未处理 | — | 后端 | P1 | 活动设置·存款优惠方案 | `GET /backend/playerrechargeofferscheme/list` | 有权限仍 `status=10000` 请求失败 |
| FE-043 | 2026-07-22 | 已关闭 | — | 通过 | 前端 | P0 | 排行榜·结算记录 | `leaderboard/record` | 日期误传 unix→`10037`；应对齐旧站 `YYYY-MM-DD`；补排序/账号小写（**已修**） |
| FE-044 | 2026-07-22 | 处理中 | — | — | 前端 | P2 | 排行榜·新增/编辑 | `leaderboard` POST/PUT | 相对旧站奖励档位/多语言/实物奖等表单能力缩水 |
| API-028 | 2026-07-22 | 待反馈 | 未处理 | — | 后端 | P2 | 排行榜·当前/历史列表 | `GET /backend/leaderboard/list` | 空结果 `Items=null`；`StartTime/EndTime` 用 `YYYY-MM-DD`→`10000`（需 unix） |
| API-029 | 2026-07-22 | 待反馈 | 未处理 | — | 后端 | P2 | 排行榜·全局配置 | `GET /backend/leaderboard/getleaderboardmainconfig` | `respond.Items=null`（旧站取 `Data.Items` 配置对象） |
| FE-045 | 2026-07-22 | 已关闭 | — | 通过 | 前端 | P1 | 票券中心·历史详情 | — | 历史票券详情权限误用 `13351`；应对齐旧站 `13352`（**已修**） |
| FE-046 | 2026-07-22 | 已关闭 | — | 通过 | 前端 | P2 | 票券中心·记录/发放记录 | `voucherdetailrecord` `issuevoucherrecord` | 游戏账号未转小写（**已修**） |
| API-030 | 2026-07-22 | 待反馈 | 未处理 | — | 后端 | P2 | 票券中心·列表/记录 | `voucher/list` `voucherdetailrecord` 等 | 空结果 `Items=null`；列表/明细日期串 `YYYY-MM-DD`→`10000`（须 unix） |
| API-031 | 2026-07-22 | 待反馈 | 未处理 | — | 后端 | P2 | 票券中心·全局配置 | `GET /backend/voucher/getvouchermainconfig` | `respond.Items=null` |
| FE-047 | 2026-07-22 | 已关闭 | — | 通过 | 前端 | P0 | 积分商城·兑换/积分记录/调整审核/记录 | 各 list | 默认日期误用昨日；应对齐旧站今天（**已修**） |
| FE-048 | 2026-07-22 | 已关闭 | — | 通过 | 前端 | P2 | 积分商城·各列表/商品详情 | exchange/point/adjust/details | 账号未转小写；商品详情 ChannelId/VipLevel 误传数组（**已修**） |
| API-032 | 2026-07-22 | 待反馈 | 未处理 | — | 后端 | P2 | 积分商城·商品/兑换/积分/调整列表 | `rewardsmall/*` `playerpointhandle/approvelist` | 空结果 `Items=null`；兑换日期串 `YYYY-MM-DD`→`10000`（须 unix） |
| API-033 | 2026-07-22 | 待反馈 | 未处理 | — | 后端 | P2 | 积分商城·全局配置/页签/任务 | `getrewardsmallmainconfig` `producttag/list` `rewardtask/list` | `respond.Items=null`（无 Pagination） |
| FE-049 | 2026-07-22 | 已关闭 | — | 通过 | 前端 | P0 | 协助管理 | `helprecords/list` | 默认日期误用昨天；`show-time` 却强制日边界丢掉时分秒（**已修**） |
| API-034 | 2026-07-22 | 待反馈 | 未处理 | — | 后端 | P2 | 协助管理 | `GET /backend/helprecords/list` | 空结果 `Items=null`；日期串 `YYYY-MM-DD`→`10000`；`Sort=CreateTime desc`→`10001` |
| FE-050 | 2026-07-22 | 已关闭 | — | 通过 | 前端 | P0 | 白名单·IP/使用者 | `whitelist/list` `listuser` | Tab 权限误用 10207；使用者搜索字段 Username→应 Name；缺 Status/开关/必填校验等（**已修**） |
| FE-051 | 2026-07-22 | 处理中 | — | — | 前端 | P3 | 白名单·免白名单 | — | 旧站 `freeWhiteSetting` Tab 已注释；NEW 未迁（若产品恢复需补） |
| API-035 | 2026-07-22 | 待反馈 | 未处理 | — | 后端 | P2 | 白名单 | `whitelist/list` `listuser` `pickuser` | list/pickuser 空 `Items=null`；`listuser`+`Sort=CreateTime desc`→HTTP 500 |
| FE-052 | 2026-07-22 | 已关闭 | — | 通过 | 前端 | P0 | 协助工单 | `helprecords/listhelp` | 默认日期误用昨天；`show-time` 却强制日边界丢掉时分秒（**已修**） |
| API-036 | 2026-07-22 | 待反馈 | 未处理 | — | 后端 | P2 | 协助工单 | `GET /backend/helprecords/listhelp` | 空结果 `Items=null`；日期串 `YYYY-MM-DD`→`10000`；`Sort=CreateTime desc`→`10001` |
| FE-053 | 2026-07-22 | 已关闭 | — | 通过 | 前端 | P0 | 关联号查询 | `relationsearch/list` | 默认日期误用昨天；产品下拉缺「全部」空值（**已修**） |
| API-037 | 2026-07-22 | 待反馈 | 未处理 | — | 后端 | P2 | 关联号查询 | `GET /backend/relationsearch/list` | 空结果 `Items=null`；日期串 `YYYY-MM-DD`→`10000`；`Sort=CreateTime desc`→`10001` |
| FE-054 | 2026-07-22 | 已关闭 | — | 通过 | 前端 | P0 | 公告访问统计·明细/统计 | `visitstatistic/list` `statisticslist` | 默认日期误用昨天；应对齐旧站今天（**已修**；共用面板亦影响 emailTitle） |
| FE-055 | 2026-07-22 | 已关闭 | — | 通过 | 前端 | P0 | 会员列表 | `playerinfo/list` | 绑定手机枚举误用 `0/1`；应对齐旧站 `1` 已绑定 / `2` 未绑定（`0`→`10043`）（**已修**） |
| FE-056 | 2026-07-22 | 已关闭 | — | 通过 | 前端 | P2 | 会员列表 | `playerinfo/list` `playerremark/list` | 账号未小写、产品缺「全部」、备注缺 `IsSelf`、注册时间未受 11399 门控（**已修**） |
| API-038 | 2026-07-22 | 待反馈 | 未处理 | — | 后端 | P2 | 会员列表 | `playerinfo/list` `listcsv` `playertag/list` | 空结果 `Items=null`；`Sort=CreateTime desc`→`10001`；日期串→`10000`；标签空 `Items=null` |
| FE-057 | 2026-07-22 | 已关闭 | — | 通过 | 前端 | P2 | 会员层级 | `listPlayerDetail` / 列表文案 | 所属会员产品缺「全部」、账号未小写；返水/风控默认文案未对齐旧站（**已修**） |
| API-039 | 2026-07-22 | 待反馈 | 未处理 | — | 后端 | P2 | 会员层级 | `playerlevel/list` `listPlayerDetail` | 空结果 `Items=null`；列表日期串 `YYYY-MM-DD`→`10000`；本环境仅 DefaultLevel=0 一条 |
| FE-058 | 2026-07-22 | 已关闭 | — | 通过 | 前端 | P0 | 会员日志·明细/统计 | `playeranalysis/log` | 默认日期误用昨天；应对齐旧站今天（**已修**）；产品「全部」+账号小写 |
| API-040 | 2026-07-22 | 待反馈 | 未处理 | — | 后端 | P2 | 会员日志 | `playeranalysis/log` `listcsv` | 空结果 `Items=null`；日期串→`10000`；无日期可返回全量；导出无校验→`10103` |
| FE-059 | 2026-07-22 | 已关闭 | — | 通过 | 前端 | P0 | 游戏记录 | `gametransactiondetail` | 默认日期误用「前天→昨天」；应对齐旧站「昨天→今天」（**已修**）；产品补「全部」 |
| API-041 | 2026-07-22 | 待反馈 | 未处理 | — | 后端 | P2 | 游戏记录 | `gametransactiondetail` `detaillog` `detailexcel` | 空 Items=null；非 SumAll 时 MoreItems 汇总为 0；日期串→10000；结算流水 respond=null；坏 Sort→Items=null |
| FE-060 | 2026-07-22 | 已关闭 | — | 通过 | 前端 | P0 | 资金流管理 | `operation/gamedetail` | 默认日期误用「近7天→昨天」；应对齐旧站「近7自然日含今天」（**已修**）；产品补「全部」 |
| API-042 | 2026-07-22 | 待反馈 | 未处理 | — | 后端 | P2 | 资金流管理 | `GET /backend/operation/gamedetail` | 空 Items=null；无账号/订单→10000；日期串→10000；坏 Sort→Items=null；部分账号 SumAddGold=0 与明细不符 |
| FE-061 | 2026-07-22 | 已关闭 | — | 通过 | 前端 | P0 | 场馆账变 | `playerwallettransferorder/list` | 默认日期误用昨天；应对齐旧站今天；数据类型多出「测试」（**已修**） |
| API-043 | 2026-07-22 | 待反馈 | 未处理 | — | 后端 | P2 | 场馆账变 | `playerwallettransferorder/list` | 空 Items=null；`State=5/17`→`10001`；日期串→10000；`Sort=CreateTime desc`→10001 |
| FE-062 | 2026-07-22 | 已关闭 | — | 通过 | 前端 | P1 | 身份验证 | `playerauthentication/*` | 邮件 LangText 对象解析失败；Days=0 误显 7；产品缺「全部」；AuthScenario 全部应对齐传 -1（**已修**） |
| API-044 | 2026-07-22 | 待反馈 | 未处理 | — | 后端 | P2 | 身份验证 | `list` `record` `setting` | 空 Items=null；日期串→10000；setting 缺 SubType=1005；本环境审核列表/记录恒空 |
| FE-063 | 2026-07-22 | 已关闭 | — | 通过 | 前端 | P1 | 钱包管理 | `playerbankcard` `playerwithdrawdigitaladdress` | 虚拟币首屏/重置误强制近1月；应对齐旧站空日期；银行卡账号未小写；备注详情条件应为且（**已修**） |
| API-045 | 2026-07-22 | 待反馈 | 未处理 | — | 后端 | P2 | 钱包管理 | `playerbankcard/list` 等 | 空 Items=null；日期串→10000；`Sort=CreateTime desc`/`-CreateTime`→10001；电子钱包/虚拟币/多账号本环境空 |
| FE-064 | 2026-07-22 | 已关闭 | — | 通过 | 前端 | P1 | 验证码查询 | `phoneverifycode` `emailverifycode` | 产品缺「全部」；账号未小写；白名单缺渠道筛选/空 PackageId；邮箱空响应未归一（**已修**） |
| API-046 | 2026-07-22 | 待反馈 | 未处理 | — | 后端 | P2 | 验证码查询 | `phoneverifycode/*` `emailverifycode/*` `phonecountrycode/list` | 无账号/手机→10037；空 Items=null；日期串→10000；邮箱 list/config `respond=null` |
| FE-065 | 2026-07-22 | 已关闭 | — | 通过 | 前端 | P0 | 玩家详情 | 多 Tab 列表 | 近7日误用「→昨天」；调整/日志/场馆转账/流水/积分/返水/访问误用昨天；账号未小写（**已修**）；注单复用游戏记录 |
| API-047 | 2026-07-22 | 待反馈 | 未处理 | — | 后端 | P2 | 玩家详情 | `gametransactiondetail` 等 | 空 Items=null；非 SumAll MoreItems 常 0；日期串→10000；detaillog/getbyorderid 异常；aisearch→10010 |
| FE-066 | 2026-07-22 | 已关闭 | — | 通过 | 前端 | P1 | 产品配置 | `package/list` `listlogogroup` | 列表空结果/Resources 未归一；Logo 空列表丢当前值；返水列仅「已配置」未显方案名/结算模式（**已修**） |
| API-048 | 2026-07-22 | 待反馈 | 未处理 | — | 后端 | P2 | 产品配置 | `package/*` `listlogogroup` `getactivitylist` | listlogogroup/`getactivitylist` 无参→null；Sort 空列表；日期串→10000；未成年 Type=1 空对象；colortheme Items=null |
| FE-067 | 2026-07-22 | 已关闭 | — | 通过 | 前端 | P0 | 创建/编辑产品 | `package/` `resource/list` | SkinColor=0 误当配色提交；资源缺 AgentId 未拦截；配色/资源 Items=null 未归一；PC 皮肤未校验（**已修**） |
| API-049 | 2026-07-22 | 待反馈 | 未处理 | — | 后端 | P2 | 创建/编辑产品 | `resource/list` `packagecolortheme*` | 无 AgentId→10000；配色 list/detail `Items=null`；空参 list→10000 |
| FE-068 | 2026-07-22 | 已关闭 | — | 通过 | 前端 | P0 | 渠道管理 | `resource/list` 包体弹窗 | 批量落地页缺 PictureType=1；包体 Id=0 未当空；邀请码改严拦历史短码；表头误 sortable（**已修**） |
| API-050 | 2026-07-22 | 待反馈 | 未处理 | — | 后端 | P2 | 渠道管理 | `channel/*` `resource/list` 等 | 空 Items=null；坏 Sort→10001；ios/android 上架包 respond=null；国家/企业包空；层级 Son/Parents null |
| FE-069 | 2026-07-22 | 已关闭 | — | 通过 | 前端 | P0 | 充值管理 | `thirdrechargetypeagentconfig/list` `playerlevel/list` | 未按 TypeList Sort 首项选中；层级 BeginTime=1 导致空（**已修**） |
| API-051 | 2026-07-22 | 待反馈 | 未处理 | — | 后端 | P2 | 充值管理 | `thirdrecharge*` `agentrecharge*` `playerlevel` | 缺 MaxCount；空 Items=null；Sort→10001；BeginTime=1 空层级；TypeList 含无通道类型 |
| FE-070 | 2026-07-22 | 已关闭 | — | 通过 | 前端 | P0 | 提现配置 | 访问记录/通道数据/账户类型 | 访问默认误用昨天；通道数据多减 1 天；账户类型写死 2（**已修**） |
| API-052 | 2026-07-22 | 待反馈 | 未处理 | — | 后端 | P2 | 提现配置 | `visitstatistic/*` `withdrawstatistics` 等 | 访问 20001；空 Items/Pagination=null；白名单 WhiteType=0→10037；提示 Items=null |
| FE-071 | 2026-07-22 | 已关闭 | — | 通过 | 前端 | P0 | 域名管理 | `domain/list` | 列表/导出未传 `IsAll=1`，Type1/2/3 空表（**已修**） |
| API-053 | 2026-07-22 | 待反馈 | 未处理 | — | 后端 | P2 | 域名管理 | `domain/list` `domain/edit` 等 | 无 IsAll 时空 Items=null；`Domain` 筛选无效；空结果 Items=null；坏 Id 编辑文案含糊 |
| FE-072 | 2026-07-22 | 已关闭 | — | 通过 | 前端 | P1 | 包体管理 | `package/list` 上传/列表归一 | IPA 上传响应字段不全；列表 Items=null 未归一；空搜残留详情（**已修**） |
| API-054 | 2026-07-22 | 待反馈 | 未处理 | — | 后端 | P2 | 包体管理 | `iosappstoredata/*` `androidappstoreconfig/*` 等 | 上架包/企业包明细/国家配置空 Items=null 且缺 Pagination；坏 Sort 空表 |
| FE-073 | 2026-07-22 | 已关闭 | — | 通过 | 前端 | P0 | 场馆管理 | `apifeeswitch/list` 子游戏默认场馆 | 状态缺字段未回退；BTI 无 GameId；空 GameId；list null 崩溃（**已修**） |
| API-055 | 2026-07-22 | 待反馈 | 未处理 | — | 后端 | P2 | 场馆管理 | `apifeeswitch/list` `subgamemaintain/list` | 缺 LoginStatus/WalletStatus/ApiFeeName；筛空 respond=null；子游戏全场馆 Items=null |
| FE-074 | 2026-07-22 | 已关闭 | — | 通过 | 前端 | P1 | 短信管理 | 通道/模板/OTP/召回 | 通道误传分页；Items=null 未归一；模板 ApplyGamePackageIds 数组；OTP Day 精确比对；召回开关取消回滚（**已修**） |
| API-056 | 2026-07-22 | 待反馈 | 未处理 | — | 后端 | P1 | 短信管理 | `smschannelconfig/list` `depositrecall` 等 | 通道 list→10000；首存召回 list→10000；多处空 Items=null / 缺 Pagination |
| FE-075 | 2026-07-23 | 已关闭 | — | 通过 | 前端 | P0 | 返水设置 | 审核默认日/状态文案/手动查询/空 Items | 审核误用昨天；状态文案简化丢分支；手动空账号直打 10043；列表 Items=null（**已修**） |
| API-057 | 2026-07-23 | 待反馈 | 未处理 | — | 后端 | P2 | 返水设置 | `playerbackwaterrecord/*` `scheme` | 空 list/detaillist/reviewlist Items=null；日期串→10000；空账号 waterlist→10043 |
| FE-076 | 2026-07-23 | 已关闭 | — | 通过 | 前端 | P1 | VIP返水配置页 | `addConfig` 保存同步/GameId | 保存未强制同步游戏比例；GameId 字符串；session 未清理（**已修**） |
| API-058 | 2026-07-23 | 待反馈 | 未处理 | — | 后端 | P2 | VIP返水配置页 | `scheme` `config` `/api/game/info` | 无 Id→10000；坏 Id→10304；本环境 Config Games 全空 |
| FE-077 | 2026-07-23 | 已关闭 | — | 通过 | 前端 | P1 | 广告管理 | 注册天数/列表参/公告标题/恢复参 | RegStartEndDate 未提交；轮播缺开关；空日期 undefined；公告 Title 未解析 LangText；恢复多传 SubType；空列表未归一（**已修**） |
| API-059 | 2026-07-23 | 待反馈 | 未处理 | — | 后端 | P2 | 广告管理 | `gameadconfig/*` `gameadimage` `gameadtemplate` | 空广告/图片 respond=null；Type4+SubType 方案 Items=null；活动列表 null；详情/坏 Id→20002 |
| FE-078 | 2026-07-23 | 已关闭 | — | 通过 | 前端 | P0 | VIP设置 | 等级记录默认日/日期组件 | 默认昨日起始；RangePicker value-format 破坏 unix；Status 空值；保级天数 0（**已修**） |
| API-060 | 2026-07-23 | 待反馈 | 未处理 | — | 后端 | P2 | VIP设置 | `viplevelconfig/*` `vipcoefficientconfig` | 空筛 Items=null；系数 GameInfo=''；坏日期 10000；无 TemplateId 仍返图标 |
| FE-079 | 2026-07-23 | 已关闭 | — | 通过 | 前端 | P1 | 系统设置 | 语言组/空列表归一/场馆匹配 | Default 语言；手机号段无 LangGroup；空 respond；场馆 ClientClassify（**已修**） |
| API-061 | 2026-07-23 | 待反馈 | 未处理 | — | 后端 | P2 | 系统设置 | `sportsconfig` `exchangeratesetting` `netcashconfig` | 空列表 Items=null / respond=null；删坏 Id→10305 |
| FE-080 | 2026-07-23 | 已关闭 | — | 通过 | 前端 | P0 | 推广列表 | Items=null 崩溃/排序字段 | 空列表 null.map；CreateTime 缺 dataIndex；筛空值（**已修**） |
| API-062 | 2026-07-23 | 待反馈 | 未处理 | — | 后端 | P2 | 推广列表 | `promoter/list` `domain/list` | 空列表 Items/ItemsTotal=null；坏 Id→10002；域名 Type=10 空 |
| FE-081 | 2026-07-23 | 已关闭 | — | 通过 | 前端 | P1 | 创建/编辑推广 | 分组失败拖垮详情/确认缺密码 | getadminteams 失败整页失败；分组空可提交；备注 255（**已修**） |
| API-063 | 2026-07-23 | 待反馈 | 未处理 | — | 后端 | P1 | 创建/编辑推广 | `sportsteamerteam/*` `promoter/:id` | list/getadminteams→20001；坏 Id→10002；本环境无推广数据 |
| FE-082 | 2026-07-23 | 已关闭 | — | 通过 | 前端 | P1 | 推广报表 | 默认日窗/利润ROI/回本遮罩/空列表 | 近 8 日；利润ROI 乘汇率；未到期未遮罩；Items=null（**已修**） |
| API-064 | 2026-07-23 | 待反馈 | 未处理 | — | 后端 | P1 | 推广报表 | `channelbreakevenreport` `handrecord` `landingpage` `exchangeratesetting` | 回本→10103；坏详情→10001；listall/汇率 respond=null；空 Items=null |
| FE-083 | 2026-07-23 | 已关闭 | — | 通过 | 前端 | P1 | 时段报表 | AdminType 权限/日期跨度/图例 | 非2误裁 Tab；缺 7 日限制；图例跨 Tab 丢失（**已修**） |
| API-065 | 2026-07-23 | 待反馈 | 未处理 | — | 后端 | P2 | 时段报表 | `promotedata/hourreport` | 未来日仍返全 0 骨架；坏日期→10000；无日期默认多日 |
| FE-084 | 2026-07-23 | 已关闭 | — | 通过 | 前端 | P2 | 代理日报表 | 日期跨度/空 History/失败兜底 | 缺 31 日限制；HistoryItems=null 已加固（**已修**） |
| API-066 | 2026-07-23 | 待反馈 | 未处理 | — | 后端 | P2 | 代理日报表 | `accountteamdaily/list` | HistoryItems=null；今日/汇总全 0；坏日期→10000 |
| FE-085 | 2026-07-23 | 已关闭 | — | 通过 | 前端 | P2 | 分销列表 | 空 Items/日期跨度/失败兜底 | Items=null；缺 30 日限制；catch 重抛（**已修**） |
| API-067 | 2026-07-23 | 待反馈 | 未处理 | — | 后端 | P2 | 分销列表 | `accountteamsearch/list` | Items/Items2=null；无 Pagination；坏日期→10000 |
| FE-086 | 2026-07-23 | 已关闭 | — | 通过 | 前端 | P2 | 分销结算报表 | 状态清空崩溃/空列表/失败兜底 | Status allow-clear join；Items/MoreItems 归一；catch 重抛（**已修**） |
| API-068 | 2026-07-23 | 待反馈 | 未处理 | — | 后端 | P2 | 分销结算报表 | `accountteamwithdrawapply/*` | Items/MoreItems=null；坏日期→10000；坏 Id→10402/10403 无 msg |
| FE-087 | 2026-07-23 | 已关闭 | — | 通过 | 前端 | P2 | 收益结算 | 类型清空崩溃/空列表/元数据失败 | MoneyType allow-clear；Items 归一；loadMeta 隔离（**已修**） |
| API-069 | 2026-07-23 | 待反馈 | 未处理 | — | 后端 | P2 | 收益结算 | `accountteamwithdraw/*` `accountteambank/*` | Items=null；bank list respond=null；坏日期/写接口无 msg |
| FE-088 | 2026-07-23 | 已关闭 | — | 通过 | 前端 | P2 | 代理设定 | 列表失败兜底/参数对齐/操作列 | Items=null；缺 Sort 等字段；查询无 catch（**已修**） |
| API-070 | 2026-07-23 | 待反馈 | 未处理 | — | 后端 | P1 | 代理设定 | `accountteamgamerate/*` | Items=null；坏 GameId 批量/恢复仍 200；写错误无 msg |

### 本轮通过（登录态，参数与旧站一致）

| 模块 | 接口 | 前端参数核对 | 实测结果 |
| --- | --- | --- | --- |
| Banner 四卡 | 复用 `reportinfo.Items` 今日行 | 无独立请求 | 有字段；本环境新增/登录偏少、充值相关多为 0（属数据量，非参数错误） |
| 渠道今日数据 | `GET /backend/dashboard/reportchannelinfo` | 旧站 `Sort:''`，新站 `{}`（等价） | `RealtimeItems` 有 1 条渠道，表格可展示 |
| 盈利/亏损玩家 | `GET /backend/dashboard/goldbyplayergame` | `GameId=''` `StartTime=''` `Sort=''`（与旧站一致） | `ItemsWin=3` / `ItemsLose=10` / `Users=13`，可合并展示 |
| 盈利游戏 | `GET /backend/dashboard/rankbygameid` | 无参（与旧站一致）；`ItemsLose`→盈利、金额 `* -1` | `ItemsLose` 有 6 条，盈利游戏可展示 |
| 在线人数卡片 | 复用 `reportinfo.Items` | 无独立请求 | 今日 `SumOnlinePlayerNum=1`，卡片可展示 |
| 在线设备分布 | `onlinesummary.DeviceResult` | 无参（与旧站一致） | `DevicePlatform=uniapp2-web`，`Count=1`，饼图可展示 |
| 公司日报·日报实时 | `GET /backend/operation/dailyreport` `SearchType=today` | 账号/渠道/产品/日期与旧站一致 | `TodayItems` 有值（投注/充值非空） |
| 公司日报·日报历史 | 同上 `SearchType=old` | 日报/月报日期格式与旧站一致 | `Items` `list(len=20)` |
| 公司日报·日报汇总 | `GET /backend/operation/dailyreporttotal` | 与历史同参 | `BannerItems` 有汇总（`SumLogin=92` 等） |
| 设备日报·实时 | `GET /backend/operation/devicedayreport` `today` | 与旧站一致（实时不传设备多选） | `TodayItems` `list(len=12)` |
| 设备日报·历史 | 同上 `old` | 补齐 `Page`/`PageSize`（FE-004） | `Items=15`，`Pagination.MaxCount=20` |
| VIP日报·实时 | `GET /backend/operation/vipdayreport` `today` | 与旧站一致 | `TodayItems` `list(len=11)` |
| VIP日报·历史 | 同上 `old` | 补齐分页（FE-004） | `Items=15`，`Pagination.MaxCount=100` |
| 代理分组·分组树 | `GET /backend/agentnetcashgroup/grouplist?IsTop=1` | 与旧站一致 | 返回 1 个根节点（`Id=0` 推广中心，`List=null`）；无 `IsTop` 时 `Items=null` |
| 代理数据·今日代理 | `GET /backend/operation/channelreport` `SearchType=today` | Sort/分页一致；今日无日期控件 | `RealTimeItems` 20 条，`MaxCount=37` |
| 代理数据·今日渠道 | `GET /backend/operation/channelreportbychannel` `today` | 一致 | `RealTimeItems` 20 条，`MaxCount=84` |
| 代理数据·历史代理 | `channelreport` `SearchType=old` | Sort=`-ReportDay`；字段取 `Items`+`TotalItems` | 昨日报有数据；月累计 `MaxCount=361` |
| 代理数据·历史渠道 | `channelreportbychannel` `old` | 字段取 `RealTimeItems`+`TotalRealTimeItems` | 昨日报有数据；月累计 `MaxCount=475` |
| 排行榜·输赢 | `GET /backend/operation/gamewinrank` | 昨日 unix 区间、`Limit=100`；合并 Users；SumAddGold 正负过滤 | `ItemsWin=3` / `ItemsLose=98` / `Users=101`，可展示 |
| 排行榜·充值 | `GET /backend/operation/gamepaymentrank` | 同上；金额字段 `SumGold` | `Items=1`，合并 Users 后可展示 |
| 排行榜·兑换 | `GET /backend/operation/gamewithdrawrank` | 同上 | `Items=2`，可展示 |
| 排行榜·游戏盈利 | `GET /backend/operation/realgamewinrank` | 押注/返奖计算对齐旧站 | `ItemsWin=8`（月区间 9）可展示 |
| 充值列表·通道 | `GET /backend/playerpayment/list` | 昨日 unix；Sort 须 `-CreateTime`（FE-006） | 昨日 1 条；近 30 日 `MaxCount=399` |
| 充值列表·失败记录 | `GET /backend/playerpayment/faillist` | 昨日 unix 一致 | `Items` 5 条 |
| 提现列表·兑换 | `GET /backend/playerwithdraw/list` | 昨日 unix；Sort 须 `-CreateTime`（FE-009） | 昨日空；近 30 日 `MaxCount=80`，有 `Total` |
| 提现列表·出款 | `GET /backend/withdrawfinancelist/list` | 昨日 unix；订单号 `OrderIds`（FE-011） | 昨日空；近 30 日 `MaxCount=44` |
| 提现列表·订单统计 | `GET /backend/playerwithdrawreport/list` | 默认当月（FE-010） | 当月 `Items=20`；昨日空 |
| 提现列表·白名单 | `GET /backend/withdrawwhitelist/list` | 无默认日期 | `MaxCount=0`（空配置） |
| 提现列表·派单管理 | `withdrawsendorderusermanage/list` + `getswitch2` | 一致；开关读 Status（FE-013） | 人员空；`Status=0` |
| 提现列表·派单列表 | `withdrawsendorderlist/list` | 昨日+`RiskStatus=-1` | 近 90 日仍空（本环境无派单） |
| 提现列表·提款流水 | `playerwithdrawflow/list` + `flowList` | 昨日 unix | 流水空；`flowList.Switch=2` |
| 客服工单·列表 | `GET /backend/customsupport/list` | 默认今天；Sort `-field`（FE-014~016） | 今/昨/近 730 日均 `MaxCount=0`（空库） |
| 客服工单·问题类型 | `customersupporterworkquestiontype/list` | `Enabled=0` | `Items=12` |
| 客服工单·结束理由 | `customersupporterquestiontype/list` | `Type=0` | `Items=8` |
| 客服工单·提醒 | `customsupport/alert` | 无参 | `Count=0` |
| 公告管理·游戏公告 | `GET /backend/gamenotice/list` | 无默认日期；Sort 须 `-field`（FE-018） | `MaxCount=8`；标题可读 |
| 公告管理·邮件通知 | `GET /backend/gameemail/list` | 无默认日期 | `MaxCount=0`（空库） |
| 公告管理·公告详情 | `GET /backend/gamenotice/{id}` | 路径一致 | Id=21 可读 |
| 红利发放·红标标题 | `GET /backend/playergoldhandle/redtitle` | 无参 | 1 条「注册转盘」 |
| 红利发放·发放记录 | `GET /backend/playergoldhandle/list` | 昨日 unix；`HandleType=1`（FE-021） | 昨日空；近 30 日 5 条；无日期 MaxCount=27 |
| 账户调整·调整记录 | `GET /backend/playergoldaccountchange/approvelist` | 昨日；`IsApprove=1` `Approve=2,3,4`；空产品（FE-023） | 昨日 MaxCount=39；近 30 日 2226；有 Total/TotalReal |
| 账户调整·单人查玩家 | `GET /backend/playerinfo/queryplayer` | `LoginAccount`+`PackageId` | 可查到 `PlayerId`；详情 `playerinfo/{id}` 可读 |
| 红利管理·红利记录 | `GET /backend/playergoldhandle/redlist` | 昨日；空产品（FE-025）；SendType 含 3（FE-027） | 昨日 1 条；近 30 日 MaxCount=174；SumBonus 有值 |
| 红利管理·平台转账 | `GET /backend/playerwallettransferorder/list` | 昨日；`State=-2` | 昨日 MaxCount=231；近 30 日 14959；有 Total |
| 礼品管理·审核晋级 | `GET /backend/playergift/list` `ApplyType=4` | 空日期；`Sort=''`；PackageName | 本环境 `Items=[]` MaxCount=0 |
| 礼品管理·审核主题 | `GET /backend/playergift/list` `ApplyType=5,6,7,8` | `AuditDeliverStatus=-1` | 本环境空 |
| 礼品管理·发货晋级/主题 | `GET /backend/playergift/deliverlist` | 主题默认待发货 Status=1 | 本环境 `Items=null` MaxCount=0（API-020） |
| 游戏风控·注册上限 | `GET /backend/gameipriskcontrol/registerconfigs` | `Type=1` | 返回 IP/设备各 1 条 LimitAmt=50 IsOn=true |
| 游戏风控·IP/设备黑白名单 | `gameipriskcontrol/list` `gameriskcontrol/list` | 近 30 日；Type/RiskType 对齐 | 本环境空 `Items=null` Total=0 |
| 游戏风控·银行卡黑名单 | `playerbankcard/blacklist` | 近 30 日；FilterKey=SourceType | 本环境空 `Items=null` MaxCount=0 |
| 游戏风控·App Store | `appstorewhitelistrisk/list` | Key=`h4_ios` | `WhitelistList=null` TotalItems=0 |
| 充值列表·充值统计 | `GET /backend/golobalgametipconfig/list` | 当月 unix 一致 | `Items` 4 条 |
| 充值列表·黑名单游戏账号（空日期） | `rechargeblackplayer/list` | 空 Begin/End（FE-008 后） | `Items` 2 条；自动配置 `rechargeblackconfig` 可读 |
| 充值列表·自助查单列表 | `playerpaymentselfreview/list` | 昨日/近 30 日 | `MaxCount=0`（空单，非参数错误） |
| 充值列表·自助入口图 | `playerpaymentselfreview/icon` | 一致 | `Items` 空数组 |

---

## 详情

### API-001

- 协作状态：待反馈
- 后端解决：未处理
- 前端验证：—
- 责任方：后端
- 后端备注：（后端填写）
- 验证备注：2026-07-21 `leying_niu` 登录态实打复现

**现象：** 「今日数据」KPI（`Items`）有投注金额，下方折线无有效曲线；自选日期分时仍空。

**前端参数核对（对照旧站 `admin/index.vue`）：** 首屏无参；自选 `Date1/2/3` 规则一致（`<今日` 且 `≠昨日`）。**非前端参数错误。**

**请求：**

| 场景 | 请求 | Query |
| --- | --- | --- |
| 首屏 | `GET /backend/dashboard/reportinfo` | 无 |
| 自选 | 同上 | `Date1`/`Date2`/`Date3`=`YYYY-MM-DD` |

**实测：**

- `Items`：今日投注 `101602389`，昨日 `124554898`
- `TotalHours["2026-07-20"]=null`；今日仅 1 条 `Hours=22` 且金额全 `0`
- `?Date1=2026-07-18` → `TotalHours["2026-07-18"]=null`；`Items` 仍只有今昨 2 条

**请后端确认：** `Items` 与 `TotalHours` 同日口径为何不一致；`Date1/2/3` 是否应返回对应分时；勿返回 `null`（用 `[]` 或完整小时数组）。

**摘录：**

```json
{
  "Items": [
    { "ReportDay": "2026-07-21", "SumTransBetMoney1": 101602389 },
    { "ReportDay": "2026-07-20", "SumTransBetMoney1": 124554898 }
  ],
  "TotalHours": {
    "2026-07-20": null,
    "2026-07-21": [{ "Hours": 22, "SumTransBetMoney1": 0, "SumTransWinMoney1": 0 }]
  }
}
```

---

### API-002

- 协作状态：待反馈
- 后端解决：未处理
- 前端验证：—
- 责任方：后端
- 验证备注：2026-07-21 登录态实打；前端映射与旧站一致

**现象：** 「盈利游戏」有数据，「亏损游戏」为空。

**前端参数核对（对照旧站 `GameProfitloss.vue`）：**

- 请求：无参 `GET /backend/dashboard/rankbygameid`（与旧站一致）
- 映射：盈利 ← `ItemsLose`，亏损 ← `ItemsWin`，展示 `SumAddGold * -1`（与旧站一致）
- **非前端参数 / 映射错误。**

**实测：**

- `ItemsLose`：`list(len=6)`（盈利游戏可出）
- `ItemsWin`：`null`（亏损游戏空）

**请后端确认：** `ItemsWin` 是否应返回亏损游戏列表；无数据请返回 `[]` 而非 `null`。

**摘录：**

```json
{
  "ItemsLose": [{ "GameId": 1030, "SumAddGold": -95284900 }],
  "ItemsWin": null
}
```

---

### API-003

- 协作状态：待反馈
- 后端解决：未处理
- 前端验证：—
- 责任方：后端
- 验证备注：2026-07-21 登录态实打

**现象：** 今日运营中「近 15 分钟充值」「在线风控员」无列表；充值通道行无 `PayTypeName`。

**前端参数核对（对照旧站 `todayInfo.vue`）：** 无参 `GET /backend/dashboard/reporttoday`（与旧站一致）。**非前端参数错误。**

**实测 `respond` 结构：**

| 字段 | 结果 | 页面影响 |
| --- | --- | --- |
| `Recharged.TodayItems` | `len=1`（`PayType=1`，无 `PayTypeName`） | 今日充值成功率表有行；名称依赖前端 `RechargeTypeList` 回退 |
| `Recharged.TodayTotal` | `SumSuccessCount=0` / `SumTotalCount=1` | 成功率 0% |
| `Recharged15.Curr15Items` | `null` | 近 15 分钟表空 |
| `Recharged15.Last15Items` | `null` | 无法做环比 |
| `Recharged15.*Total` | 成功/总数均为 0 | 汇总为 0 |
| `WithDraw` | 今日耗时全 0；昨日有值（如 `YestDayTotalTime=3405`） | 提现耗时卡片今日为 0（可能业务真实） |
| `OnlineUser.OnlineCount` | `0` | 在线人数 0 |
| `OnlineUser.TodayOnlineUserItems` | `null` | 风控员表空 |
| `OnlineUser.YestDayOnlineUserItems` | `null` | 无昨日对比源 |

**请后端确认：**

1. 本环境近 15 分钟 / 在线风控是否本应有数据
2. 无数据统一 `[]`，避免 `null`
3. `TodayItems` 是否应带 `PayTypeName`（旧站亦有前端 filter，但接口带名更稳）

**摘录：**

```json
{
  "Recharged": {
    "TodayItems": [{ "PayType": 1, "SuccessCount": 0, "TotalCount": 1, "Children": [{}] }],
    "TodayTotal": { "SumSuccessCount": 0, "SumTotalCount": 1 }
  },
  "Recharged15": {
    "Curr15Items": null,
    "Last15Items": null,
    "Curr15Total": { "SumSuccessCount": 0, "SumTotalCount": 0 }
  },
  "OnlineUser": {
    "OnlineCount": 0,
    "TodayOnlineUserItems": null,
    "YestDayOnlineUserItems": null
  }
}
```

---

### FE-001

- 协作状态：待验证
- 责任方：前端
- 前端验证：未验证
- 验证备注：2026-07-21 已对齐旧站 `todayInfo.vue` 合并逻辑；本环境 `Last15Items=null`，待有数据后复测环比

**原问题：** 近 15 分钟充值只读行上 `LastSuccessRate`，未合并 `Last15Items`。

**修复：** `today-ops-panel.vue` 的 `enrich15Rows(curr, last)` 按 `PayType` / 子级 `RechargeId` 合并后计算 `CurrSuccessRate` / `LastSuccessRate`；`null` 列表按 `[]` 处理。

---

### FE-002

- 协作状态：待验证
- 责任方：前端
- 前端验证：未验证
- 验证备注：2026-07-21 已对齐旧站合并逻辑；本环境风控列表为 `null`，待有数据后复测

**原问题：** 只取 `TodayOnlineUserItems`，丢弃 `YestDayOnlineUserItems`。

**修复：** `enrichOnlineRows` 按 `RiskAuditorId` 合并昨日 `AvgTime` → `YesterdayAvgTime`；平均耗时列展示环比百分比。

---

### API-004

- 协作状态：待反馈
- 后端解决：未处理
- 前端验证：—
- 责任方：后端
- 验证备注：2026-07-21 `leying_niu` 登录态实打；前端参数与旧站一致

**现象：** 在线总览「在线人数」卡片有值，折线为全 0 平线（或无有效波动）。

**前端参数核对（对照旧站 `onlineSummary/index.vue`）：**

- 接口：`GET /backend/dashboard/reportinfo`（旧站 `getLinchartData`）
- 首屏无参；自选 `Date1/2/3`（`<今日` 且 `≠昨日`）
- 折线数据源：`TotalCount` 按 5 分钟序列取 `Count`（`activeKey=SumOnlinePlayerNum`）
- **非前端参数错误。**

**实测：**

- `Items` 今日：`SumOnlinePlayerNum=1`（卡片正常）
- `TotalCount["2026-07-20"]`：288 点，`Count` 全 `0`
- `TotalCount["2026-07-21"]`：276 点，`Count` 全 `0`
- `?Date1=2026-07-18`：新增 key 仍全 `0`

**请后端确认：** 有日汇总在线人数时，`TotalCount` 为何各 5 分钟点均为 0；自选日期分时是否应有非零点。

**摘录：**

```json
{
  "Items": [{ "ReportDay": "2026-07-21", "SumOnlinePlayerNum": 1 }],
  "TotalCount": {
    "2026-07-21": [{ "Count": 0 }, { "Count": 0 }]
  }
}
```

---

### API-005

- 协作状态：待反馈
- 后端解决：未处理
- 前端验证：—
- 责任方：后端
- 验证备注：2026-07-21 登录态实打；前端字段与旧站一致（`LastGameId` / `IpDetailName`）

**现象：** 在线场馆分布名称为 `-`；地区分布表格地区列空白（地图侧显示 `-`）。设备分布正常。

**前端参数核对：** 无参 `GET /backend/dashboard/onlinesummary`（与旧站 `getOnlineSummary` 一致）。**非前端参数错误。**

**实测：**

| 字段 | 结果 | 页面影响 |
| --- | --- | --- |
| `GameResult[0]` | `Count=1`，`LastGameId=0` | 场馆名 `-`（旧站 `formatGameId(0)` 同为 `-`） |
| `DeviceResult[0]` | `Count=1`，`DevicePlatform=uniapp2-web` | 设备饼图正常 |
| `MapResult[0]` | `Count=1`，`IpDetailName=""` | 地区名为空；前端已兜底展示 `-` |

**请后端确认：**

1. 有在线时 `LastGameId` 是否应返回真实场馆/游戏 ID（或附带 `GameName`）
2. 有 `Count` 时 `IpDetailName` 为何为空（IP 归属是否未解析）

**摘录：**

```json
{
  "GameResult": [{ "Count": 1, "LastGameId": 0 }],
  "DeviceResult": [{ "Count": 1, "DevicePlatform": "uniapp2-web" }],
  "MapResult": [{ "Count": 1, "IpDetailName": "" }]
}
```

---

### FE-003

- 协作状态：已关闭
- 责任方：前端
- 前端验证：通过
- 验证备注：2026-07-21 已改代码；属体验/对齐优化，不依赖后端有数据即可验收

**修复内容：**

1. 地区表空串 `IpDetailName` 展示为 `-`（与地图一致）
2. 场馆名改用 `formatGameId`（对齐旧站；`-1`→`离线`）
3. `loadReport` 仅透传合法 `Date1/2/3`，首屏无参
4. 在线总览隐藏折线/柱状切换（旧站仅折线；原切换无父组件监听）
5. 空态文案按 `SumOnlinePlayerNum` 提示 `TotalCount`

---

### API-006

- 协作状态：待反馈
- 后端解决：未处理
- 前端验证：—
- 责任方：后端
- 验证备注：2026-07-21 `leying_niu` 登录态；区间 `2026-07-01`~`2026-07-21`；参数对齐旧站 `PutawayDaily.vue`

**现象：** 「上架包日报」实时/历史明细为空；上架包筛选项为空；今日 CSV 导出无行。历史区可能仅见「总计」且登录数与公司日报汇总相同。

**前端参数核对（对照旧站 `PutawayDaily.vue`）：**

| 参数 | 新站 | 旧站 | 结论 |
| --- | --- | --- | --- |
| 账号/渠道模糊精准 | `AdminSearch*` / `ChannelSearch*` | 同 | 一致 |
| `PackageId` / `AppUrl` / `DataSearchType` / `ReportType` | 有 | 有 | 一致 |
| `AdminGroupIds` | 固定 `''` | 旧站 UI 已注释，亦为 `''` | 一致 |
| 实时展示 | 仅 `TodayItems` | 仅 `TodayItems`（`YesterdayItems` 入表已注释） | 一致 |

**非前端参数错误。** `Items || []` 已对 `null` 兜底，但接口无明细可展示。

**实测：**

| 接口 | 关键字段 | 结果 |
| --- | --- | --- |
| `GET /backend/iosappstoredata/list` | `Items` | `null`（下拉无选项） |
| `GET /backend/iosappstoredata/getiosappstoredata` | `Items` | `null`（历史明细空） |
| 同上 | `TodayItems` | 有对象，金额/登录多为 `0` |
| 同上 | `BannerItems.SumLogin` | `92`（与同日 `dailyreporttotal.BannerItems.SumLogin` 相同，疑似串口径） |
| `GET /backend/iosappstoredata/todayappstorecsv` | `Items` | `null` |

**请后端确认：**

1. 本环境是否未配置上架包，导致 `list.Items=null`；有配置时应返回 `[]` 而非 `null`
2. `getiosappstoredata.Items` 为空时，`BannerItems` 是否误用了公司日报汇总
3. `todayappstorecsv` 无数据时返回 `[]`

---

### FE-004

- 协作状态：已关闭
- 责任方：前端
- 前端验证：通过
- 验证备注：2026-07-21 已改 `dimension-daily-panel.vue`；对照旧站 `DeviceHistoryData` / `VipHistoryData` 分页

**现象：** 设备/VIP「历史数据」只显示后端默认第一页（`PageSize=15`），无翻页；VIP 区间内 `MaxCount=100`（7 页）时后页不可见。导出亦可能只拿到当前页。

**修复内容：**

1. 历史查询透传 `Page` / `PageSize`（默认 15，与后端默认一致）
2. 表格下方增加 `Pagination`（可改页大小）
3. 导出使用 `IsExp` + `PageSize=10000`（对齐渠道日报导出写法）

---

### API-007

- 协作状态：待反馈
- 后端解决：未处理
- 前端验证：—
- 责任方：后端
- 验证备注：2026-07-21 `leying_niu` 登录态；对照旧站 `groupDaily.vue`

**现象：** 「代理分组日报」表格无明细行；页脚「总计」可能出现投注人数>0 但金额全 0。

**前端参数核对（对照旧站 `groupDaily.vue`）：**

| 参数 | 新站 | 旧站 | 结论 |
| --- | --- | --- | --- |
| 默认日期 | `getBeforeDateStr(2)` 前天单日 | 同 | 一致 |
| `ReportType` / `DataSearchType` / `Level` / `Page` / `PageSize` | 有 | 有 | 一致 |
| `ParentTreeStr` | `,id,` 多选用 `#` 拼接；含路径去重 | `filterchangeStyle` 同算法 | 一致 |
| 汇总 | 并行 `SearchType=total` 取 `BannerItems` | 同 | 一致 |
| 分组树 | `grouplist?IsTop=1` | 同 | 一致 |
| 下钻样式 | `applyGroupDrillStyles` 按 Level 打标 | `fnGetList` 同 | 一致 |

**非前端参数 / 下钻逻辑错误。** `Items || []` 已对 `null` 兜底。

**实测（默认前天 `2026-07-19`，以及 `2026-07-01`~`07-19`、`2026-06`、月报 `2026-04`~`06`、`DataSearchType=2`、`Level=1&ParentTreeStr=,0,`）：**

| 接口 / 场景 | 结果 |
| --- | --- |
| `dailyreport` 明细 | `Items=null`，`Pagination.MaxCount=0`（各场景均如此） |
| `dailyreport&SearchType=total`（前天） | `BannerItems.SumTransBetNum1=93`，`SumTransBetMoney1=0`，充提金额 0 |
| `SearchType=total`（`07-01`~`07-19`） | `SumTransBetNum1=173`，金额仍全 0 |
| `grouplist?IsTop=1` | 仅 1 节点：`Id=0`「推广中心」，`List=null` |
| `grouplist`（无 IsTop） | `Items=null` |

**请后端确认：**

1. 有投注人数时为何无分组明细行（`MaxCount` 为何为 0）
2. `BannerItems` 投注人数与金额口径是否一致（人数有、金额全 0）
3. 根分组仅 `Id=0` 且无子树时，日报是否应仍按代理/分组产出 `Items`（本环境 `agentnetcashgroup/list` 有 39 个代理账号）
4. 无数据时 `Items` 请返回 `[]` 而非 `null`

---

### FE-005

- 协作状态：已关闭
- 责任方：前端
- 前端验证：通过
- 验证备注：2026-07-21 已改 `channel-report-table.vue` / `channel-report-panel.vue`

**现象：** 历史「区间」报（`ReportType=3`）日期列显示为空的 ` ~ `。

**原因：** 新站取行内 `BeginTime`/`EndTime`，实测恒为 `null`，仅有 `ReportDay`；旧站 `HistoryAgentData` 区间列展示的是查询条件 `param.BeginTime~param.EndTime`。

**修复内容：**

1. 区间报日期列改为展示当前查询区间（`queryDateLabel`）
2. 列表/导出按维度优先取字段：今日与渠道维 → `RealTimeItems`（+`TotalRealTimeItems`）；历史代理维 → `Items`（+`TotalItems`），对齐旧站

---

### API-008

- 协作状态：待反馈
- 后端解决：未处理
- 前端验证：—
- 责任方：后端
- 验证备注：2026-07-21 `leying_niu`；对照旧站 `gameRanking.vue`（`ItemsWin`→盈利、`ItemsLose`→亏损，**不**与 dashboard `rankbygameid` 的映射混淆）

**现象：** 「游戏排行」盈利侧有数据，亏损侧为空。

**前端参数核对：** 昨日 unix 起止、`Limit=100`、账号/渠道筛选与旧站一致；盈亏金额/返奖计算对齐 `SumBet-(SumWin-SumProfit)`。`ItemsLose || []` 已兜底。**非前端参数/映射错误。**

**实测：**

| 区间 | `ItemsWin` | `ItemsLose` |
| --- | --- | --- |
| 昨天 `2026-07-20` | `list(len=8)` | `null` |
| 月初～昨天 | `list(len=9)` | `null` |

**请后端确认：** 是否确无亏损游戏；无数据时请返回 `[]` 而非 `null`。

---

### API-009

- 协作状态：待反馈
- 后端解决：未处理
- 前端验证：—
- 责任方：后端
- 验证备注：2026-07-21；对照旧站 `endlessRanking.vue`（旧站未挂 Tab，新站按 `10641`+`10777` 展示）

**现象：** 「无限代理排行」表格为空。

**前端参数核对：** `Limit=100`、昨日 unix 区间、`DataSearchType`（正式/测试/全部）与旧站一致。**非前端参数错误。**

**实测：**

| 场景 | 结果 |
| --- | --- |
| 昨天 + `DataSearchType=0` | `Items=null`，`Pagination=null` |
| `07-19`～`07-21` + 正式/全部 | 同上 |

**请后端确认：** 本环境是否无无限代理排行数据；无数据时 `Items` 返回 `[]`，并补齐 `Pagination`。

---

### FE-006

- 协作状态：已关闭
- 责任方：前端
- 前端验证：通过
- 验证备注：2026-07-21 登录态对比实打；已改 `aisle-recharge.vue`（顺带修 `bank-fast-recharge.vue` 同款 Sort）

**现象：** 通道充值默认按创建时间降序时，分页有 `MaxCount` 但表格无行。

**实测对照：**

| Sort | `Items` | `Pagination.MaxCount` |
| --- | --- | --- |
| `-CreateTime`（旧站） | `list(len=1)` | 1 |
| `CreateTime desc`（修复前新站） | `null` | 1 |

**修复：** Sort 改为升序 `field`、降序 `-field`。

---

### FE-007

- 协作状态：已关闭
- 责任方：前端
- 前端验证：通过
- 验证备注：2026-07-21 对照旧站 `bankCardRecharge.vue`；已改 `bank-fast-recharge.vue`

**现象：** 快捷支付「复议充值」在待处理单（Status=1）上出现；拒绝/复议权限可能错绑。

**对照旧站：**

| 操作 | Status | 权限 |
| --- | --- | --- |
| 游戏充值 | 1 | 10270 |
| 复议充值 | **3** | **10271** |
| 拒绝 | 1 | **10272** |

**修复前新站：** 复议用 Status===1；`canReject=10271`、`canReview=10272`（对调）。

**修复：** 与旧站一致。

---

### FE-008

- 协作状态：已关闭
- 责任方：前端
- 前端验证：通过
- 验证备注：2026-07-21 登录态对比；已改 `blacklist-game-account.vue`

**现象：** 充值黑名单「游戏账号」首屏无数据或偏少。

**实测：**

| 请求日期 | PackageId | `Items` |
| --- | --- | --- |
| 昨日（修复前默认） | （曾自动选首个产品） | `MaxCount=0` |
| 空 Begin/End | 空 | `list(len=2)` |
| 近 30 日 | 空 | `list(len=1)` |

**对照旧站 `gameAccount.vue`：** `BeginTime/EndTime/PackageId` 首屏均为 `''`。

**修复：** 日期默认可空；产品默认「全部」；增加重置。

---

### API-010

- 协作状态：待反馈
- 后端解决：未处理
- 前端验证：—
- 责任方：后端
- 验证备注：2026-07-21；子 Tab「自助查单 → 访问记录 → 明细/统计」

**现象：** 访问明细与访问统计均无法加载。

**前端参数核对（对照 `visitRecordDetails` / `visitRecordTotal`）：**  
`Group=Cash` `SubGroup=Recharge_Appeal_Click`、昨日 unix、`VipLevel=-1` 等与旧站一致。**非前端参数错误。**

**实测：**

| 接口 | 结果 |
| --- | --- |
| `GET /backend/visitstatistic/selfreviewlist` | `status=20001`「远程服务器未连接,操作失败」 |
| `GET /backend/visitstatistic/selfreviewstatisticslist` | 同上（昨日与近 30 日同） |

**请后端确认：** 访问统计依赖的远程服务是否可用；失败时返回明确业务码而非 20001。

---

### API-011

- 协作状态：待反馈
- 后端解决：未处理
- 前端验证：—
- 责任方：后端
- 验证备注：2026-07-21；自助查单开关配置

**现象：** 自助查单游戏端开关配置拉取失败（前端已吞错，列表主流程不受影响）。

**请求：** `GET /backend/playerpaymentselfreview/checkconfig`  
**实测：** `status=10021`「数据格式化失败」

**请后端确认：** 配置结构是否变更；无配置时返回可解析空对象。

---

### FE-009

- 协作状态：已关闭
- 责任方：前端
- 前端验证：通过
- 验证备注：2026-07-21 登录态对比实打；已改 `redeem-list.vue`

**现象：** 兑换列表默认按创建时间降序时，接口失败/表格无数据。

**实测对照：**

| Sort | `status` | `Items` | `MaxCount`（昨日） |
| --- | --- | --- | --- |
| `-CreateTime`（旧站） | 200 | `null`（昨日无单）/ 近 30 日有 80 条 | 0 / 80 |
| `CreateTime desc`（修复前新站） | **10001** | `null` | `Pagination=null` |
| 空 Sort | 200 | 同 `-CreateTime` | 同左 |

**修复：** Sort 改为升序 `field`、降序 `-field`（与 FE-006 同款）。

---

### FE-010

- 协作状态：已关闭
- 责任方：前端
- 前端验证：通过
- 验证备注：2026-07-21；已改 `withdraw-order-stat.vue` 默认用 `getCurrentMonthRangeSeconds()`

**现象：** 订单统计首屏常空。

**实测：**

| 日期窗口 | `Items` | `MaxCount` |
| --- | --- | --- |
| 昨日（修复前默认） | `null` | 0 |
| 当月（旧站默认） | `list(len=20)` | 20 |

**对照旧站 `orderStat.vue`：** `getBeforeOneMonth(0)` 当前月。

---

### FE-011

- 协作状态：已关闭
- 责任方：前端
- 前端验证：通过
- 验证备注：2026-07-21；已改 `withdraw-finance-list.vue` 订单编号 → `OrderIds`

**现象：** 出款列表按订单编号筛选与旧站参数不一致。

**实测（订单 `WI202607201412071007173`）：** `Keyword` 与 `OrderIds` 均可命中 1 条；为对齐旧站 `withdrawListSon` 输入框绑定，改为 `OrderIds`。

---

### FE-012

- 协作状态：已关闭
- 责任方：前端
- 前端验证：通过
- 验证备注：2026-07-21；已改 `withdraw-finance-list.vue` 表权限 `10382`；`index.vue` 三方页签 `13164||13165`

**现象：** 权限门禁与旧站不一致，可能导致有表权限看不到页签/有页签看不到表。

| 范围 | 旧站 | 修复前新站 | 修复后 |
| --- | --- | --- | --- |
| 出款页签 | 10350 | 10350 | 10350 |
| 出款表格 | **10382** | 10350 | **10382** |
| 三方页签/页壳 | **13164**（子页） | 仅 13165 | **13164 \|\| 13165** |
| 三方表格 | 13165 | 13165 | 13165 |

---

### FE-013

- 协作状态：已关闭
- 责任方：前端
- 前端验证：通过
- 验证备注：2026-07-21；已改 `send-order-manage.vue` `loadSwitch`

**现象：** 派单管理总开关回显可能一直为关。

**实测：** `GET /backend/withdrawautoswitch/getswitch2` → `respond.Status=0`（无 `DistributeOrderStatus`）。  
修复前仅读 `Switch`/`DistributeOrderStatus`；现兼容 `Status`。

---

### API-012

- 协作状态：待反馈
- 后端解决：未处理
- 前端验证：—
- 责任方：后端
- 验证备注：2026-07-21；三方冲正记录（昨日/近 30 日/近 90 日/无日期同）

**现象：** 空列表时分页信息不完整，前端只能把总数当 0。

**实测：** `status=200`，`respond.Items=null`，`Pagination={CurrPage, PageSize}` **无 `MaxCount`**。  
（本环境确实无冲正单；建议无数据时 `Items=[]` 且 `MaxCount=0`。）

---

### FE-014

- 协作状态：已关闭
- 责任方：前端
- 前端验证：通过
- 验证备注：2026-07-22；已改 `apply-service-panel.vue` 补 `isProcessing`（`Status===2`）

**现象：** 操作列模板调用 `isProcessing(row)`，脚本中无定义，处理中工单无法操作/控制台报错。

**对照旧站：** 通过/驳回仅当 `Status == 2`。

---

### FE-015

- 协作状态：已关闭
- 责任方：前端
- 前端验证：通过
- 验证备注：2026-07-22；默认日期改为今天

**现象：** 默认查昨日，与旧站首屏不一致。

**对照旧站 `playerOrderPage`：** `getBeforeDateTimestamp(1, false)`～`getBeforeDateTimestamp()` = **今天** 00:00～23:59（非昨日）。

---

### FE-016

- 协作状态：已关闭
- 责任方：前端
- 前端验证：通过
- 验证备注：2026-07-22；`apply-service.ts` 注册权限 key；补查看弹窗/新问题列/Sort

**现象：**

1. `checkPermissionByKey('applyServicePass'|'playerOrderQuestType'|…)` 未 `registerPermissionKeys` → 恒 false（仅 `10079` 超管可见通过/问题类型）
2. 缺「查看」与「新问题」列（旧站 Content 第 3 段）
3. 驳回误绑 `pleyerOrderReject`（客服系统撤销），旧站 applyService 驳回无额外权限门禁
4. 未传 Sort；若误用 `field desc` 会 `status=10001`（同 FE-006/009）

**修复：** 注册 12436/12437/12438/12439/12431 等；操作列门禁对齐；补查看/历史工单；Sort 用 `-field`。

---

### FE-017

- 协作状态：已关闭
- 责任方：前端
- 前端验证：通过
- 验证备注：2026-07-22；已改 `game-notice-list-panel.vue` / `game-email-list-panel.vue`

**现象：** 公告状态与标题展示可能与旧站不一致。

**对照旧站 `notice.vue`：**

| 点 | 旧站 | 修复前新站 |
| --- | --- | --- |
| 行内 `Status==2` | 红色「已关闭」 | 仍按时间/开关算待发布/启用/停用 |
| LangText | `currentLangGroupId` | `Object.values` 取第一项 |

**修复：** `Status==2`→已关闭；LangText 优先 Default 语言组。

---

### FE-018

- 协作状态：已关闭
- 责任方：前端
- 前端验证：通过
- 验证备注：2026-07-22；公告/邮件列表补远程 Sort

**现象：** 列表不可排序；若误传 `CreateTime desc` 则表格空。

**实测 `gamenotice/list`：**

| Sort | `Items` | `MaxCount` |
| --- | --- | --- |
| 空 / `-CreateTime` | 有 8 条 | 8 |
| `CreateTime desc` | `null` | 8 |

**修复：** 列可排序，升序 `field`、降序 `-field`。后端 Sort 是否真正按字段生效见 API-014。

---

### FE-019

- 协作状态：已关闭
- 责任方：前端
- 前端验证：通过
- 验证备注：2026-07-22；`game-email-form-modal.vue`

**现象：** 新建邮件默认 `SendTime=当前时刻`，提交时 `SendNow=true`，易误立即发送。

**对照旧站：** `SendTime: ''`，仅当 `SendTime>0` 才换算并判断 `SendNow`。

**修复：** 默认空；无时间时 `SendTime=0` 且 `SendNow=false`。

---

### FE-020

- 协作状态：处理中
- 责任方：前端
- 前端验证：—
- 验证备注：2026-07-22 对照旧站 `email.vue` 表单；本轮未做全量补齐

**现象：** 新站邮件弹窗仅 Type/Icon/PackageIds/PlayerList/SendTime/Title/Content，相对旧站缺：

- `IsAll`（全部/指定玩家）
- 代理账号 / VIP 范围
- 推送开关与跳转
- 完整多语言 Tab
- Excel 导入玩家等

列表展示列（含推送标题/内容）仍可读已有字段；创建/编辑能力缩水，需后续补齐。

---

### API-013

- 协作状态：待反馈
- 后端解决：未处理
- 前端验证：—
- 责任方：后端
- 验证备注：2026-07-22；子页 `noticeTitle`/`emailTitle` 明细+统计

**现象：** 公告/邮件访问明细与访问统计均无法加载。

**前端参数核对：** `Group=Dialog|Mail`、`Key={Id}`、`PlayerId=-1`、访问时间 unix（旧站今天；NEW 曾误用昨天，见 FE-054），与旧站字段一致。**非前端参数错误（远程服务不可用）。**

**实测：**

| 接口 | Group | 结果 |
| --- | --- | --- |
| `GET /backend/visitstatistic/list` | Dialog / Mail | `status=20001`「远程服务器未连接,操作失败」 |
| `GET /backend/visitstatistic/statisticslist` | Dialog / Mail | 同上 |

（与 API-010 同类依赖远程访问统计服务。）

2026-07-22 复测 `noticeTitle`（Key=21 Dialog）：明细/统计今天、昨天、近 7 天、日期串均 `20001`；导出无校验→`10103`。  
2026-07-22 复测 `emailTitle`（Group=Mail，Key=1；本环境 `gameemail/list` Items=null）：明细/统计今天与近 7 天均 `20001`；导出无校验→`10103`「密码错误」。

**请后端确认：** 访问统计远程服务是否可用。

---

### API-014

- 协作状态：待反馈
- 后端解决：未处理
- 前端验证：—
- 责任方：后端
- 验证备注：2026-07-22

**现象：** 游戏公告列表 Sort 行为异常。

**实测（8 条全量）：**

| Sort | 结果 |
| --- | --- |
| `''` 与 `-CreateTime` | 返回顺序完全相同，且**非**按 CreateTime 降序 |
| `CreateTime` / `-ShowIdx` 等 | 顺序亦未严格按字段 |
| `CreateTime desc` | `Items=null`（MaxCount 仍为 8） |

**请后端确认：** Sort 支持的字段与格式；无数据/无效 Sort 时返回 `Items=[]` 而非 `null`。

---

### FE-021

- 协作状态：已关闭
- 责任方：前端
- 前端验证：通过
- 验证备注：2026-07-22；已改 `gold-record-panel.vue`

**现象：** 发放记录筛选/汇总与旧站不一致。

| 点 | 旧站 `saveRecord` | 修复前新站 |
| --- | --- | --- |
| Done / Reason | 表头多选 → `join(',')` | 单选 |
| 汇总 | `Total` + `TotalReal` | 仅 `Total` |

**修复：** 状态/类型改为多选；展示申请金额总计 + 实际金额总计。

---

### FE-022

- 协作状态：已关闭
- 责任方：前端
- 前端验证：通过
- 验证备注：2026-07-22；已改 `gold-grant-panel.vue`

**现象：** 发放表单若干逻辑偏离旧站，易误操作或失败原因难读。

| 点 | 旧站 | 修复前新站 |
| --- | --- | --- |
| Reason | 默认空、必选 | 默认 `3` |
| 流水倍数 | 整数正则 | `precision=2` 可小数 |
| 账号 | 转小写去空格 | 原样 |
| 批量 FailItems.Msg | `JSON.parse(Msg).Msg` | 原始 JSON 串 |
| PlayersId | `deWeight` 去重 | 未去重 |

**修复：** Reason 必选空默认；倍数整数；账号规范化；FailMsg 解析 + 失败明细表；PlayersId 去重。  
（单人查玩家继续用 `GET queryplayer`，因本环境 `queryplayerexcel` 常 `10053`，见《接口问题汇总》API-044。）

---

### API-015

- 协作状态：待反馈
- 后端解决：未处理
- 前端验证：—
- 责任方：后端
- 验证备注：2026-07-22；对应《接口问题汇总》API-022 实测确认

**现象：** 发放记录首屏（昨日）常空；空列表 `Items=null`；错误 Sort 直接库异常。

**实测 `HandleType=1`：**

| 条件 | `Items` | `MaxCount` | `Total`/`TotalReal` |
| --- | --- | --- | --- |
| 昨日 | `null` | 0 | 0 / 0 |
| 近 30 日 | 5 条 | 5 | 82000 / 82000 |
| 无日期 | 20 条 | 27 | 458900 / 458900 |
| `Sort=CreateTime desc` | — | — | `status=10001`「数据库异常」 |
| `Sort=-CreateTime` | 正常 | 正常 | 正常 |
| `Done=1,2` | 正常 | 正常 | 正常 |

**请后端确认：** 无数据时 `Items=[]`；无效 Sort 返回明确参数错误而非 `10001`；昨日空是否环境无单。

---

### FE-023

- 协作状态：已关闭
- 责任方：前端
- 前端验证：通过

**现象：** 调整审核 / 调整记录在 `onMounted` 与重置时把 `PackageId` 设为产品列表第一项，筛选下拉里还过滤掉「全部产品」，与旧站默认空串查全部不一致，多产品环境下会漏数据。

**修复：** 默认/重置保持 `PackageId=''`；下拉保留「全部产品」并可清空。

---

### FE-024

- 协作状态：已关闭
- 责任方：前端
- 前端验证：通过

**现象：** 审核/记录筛选项对游戏账号仅 `trim`，未像旧站 keyup 那样转小写去空格；单人调整表单侧已规范化。

**修复：** 查询参数与输入 `@change` 对齐小写去空格（本环境大小写不敏感，仍按旧站规范）。

---

### API-016

- 协作状态：待反馈
- 后端解决：未处理
- 前端验证：—
- 责任方：后端
- 验证备注：2026-07-22；`leying_niu` @ ccccc-admin.dk888.link

**现象：** `approvelist` 空结果时 `Items=null`；错误 Sort 触发库异常；待审队列本环境无数据。

**实测（调整记录 `IsApprove=1` `Approve=2,3,4`）：**

| 条件 | `Items` | `MaxCount` | `Total` / `TotalReal` |
| --- | --- | --- | --- |
| 昨日 + 空产品 | 20 条 | 39 | -3864000 / 10414000 |
| 近 30 日 | 20 条 | 2226 | 有 |
| 无日期 | 5 条 | 3388 | 有 |
| `HandleType=1` 昨日 | — | 31 | 3275000 / 3275000 |
| `HandleType=2` 昨日 | — | 8 | -7139000 / 7139000 |
| `Sort=-CreateTime` | 正常（与空 Sort 同序） | 39 | 正常 |
| `Sort=CreateTime desc` | — | — | `status=10001`「数据库异常」 |

**实测（调整审核 `Approve=1,4`）：** 昨日 / 近 30 日 / 无日期 / 指定产品 均为 `Items=null`、`MaxCount=0`（本环境无待审单）。

**请后端确认：** 无数据时 `Items=[]`；无效 Sort 勿返回 `10001`；待审空是否仅环境无单。

---

### FE-025

- 协作状态：已关闭
- 责任方：前端
- 前端验证：通过

**现象：** 红利发放审核 / 红利记录 / 申请记录在挂载与重置时自动选首个产品，并过滤掉「全部产品」，与旧站 `PackageId=''` 查全部不一致。

**修复：** 默认空产品；下拉保留「全部产品」。

---

### FE-026

- 协作状态：已关闭
- 责任方：前端
- 前端验证：通过

**现象：** 红利管理各列表游戏账号筛选未转小写去空格（旧站 keyup 强制）。

**修复：** 查询参数与输入 `@change` 统一规范化。

---

### FE-027

- 协作状态：已关闭
- 责任方：前端
- 前端验证：通过

**现象：**
1. 红利记录发放方式缺「审核后发放」(SendType=3)
2. 申请记录缺旧站「申请信息」`ApplyNote` 筛选项
3. 活动红利审核申请/审核时间未按日 `startOf`/`endOf`，可能截断当天

**修复：** 已补 SendType=3、ApplyNote 筛、日期按日边界。

---

### FE-028

- 协作状态：处理中
- 责任方：前端

**现象：** 相对旧站，审核侧能力缩水：通过时不可改金额（ModifyAmount）、活动红利缺一键拒全部/改审核备注、活动与申请记录缺 Excel 批量上传（权限 12029/12024）。列表主流程可用。

---

### API-017

- 协作状态：待反馈
- 后端解决：未处理
- 前端验证：—
- 责任方：后端
- 验证备注：2026-07-22；`leying_niu` @ ccccc-admin.dk888.link

**现象：** 红利审核 `approvelist`（`Approve=1,4` / 活动 `ApproveStatus=1`）无待审时 `Items=null`；错误 Sort 触发库异常。

**实测：**

| 条件 | `Items` | `MaxCount` |
| --- | --- | --- |
| 发放审核 `Approve=1,4` 昨日/30 日/无日期 | `null` | 0 |
| 活动审核 `ActivityType=10009` `ApproveStatus=1` | `null` | 0 |
| 放宽 `Approve=1,2,3,4` 无日期 | 5 条 | 27（历史含已审） |
| `Sort=CreateTime desc` | — | `status=10001` |

---

### API-018

- 协作状态：待反馈
- 后端解决：未处理
- 前端验证：—
- 责任方：后端
- 验证备注：2026-07-22

**现象：** `redlist` 错误 Sort 格式触发库异常；空列表 `Items=null`。

**实测：**

| 条件 | `Items` | `MaxCount` | `SumBonus` |
| --- | --- | --- | --- |
| 昨日 | 1 | 1 | 800 |
| 近 30 日 | 20 | 174 | 423200 |
| `Sort=-CreateTime` / `Sort=` | 正常 | 174 | 有 |
| `Sort=CreateTime desc` | — | — | `status=10001` |

---

### API-019

- 协作状态：待反馈
- 后端解决：未处理
- 前端验证：—
- 责任方：后端
- 验证备注：2026-07-22

**现象：** 平台转账列表对 `Sort=CreateTime desc` **与** `Sort=-CreateTime` 均返回 `10001`（比其它列表更严）。前端当前不传 Sort，列表可查。

**实测无 Sort：** 昨日 MaxCount=231；近 30 日 14959；无日期 22593；`Total.Total` 有值。

---

### FE-029

- 协作状态：已关闭
- 责任方：前端
- 前端验证：通过

**现象：** 主题抽奖发货相对旧站：
1. 多出「收货」按钮（旧站该子 Tab 无收货）
2. 「拒绝」在 Status=3（已发货）仍可点（旧站禁用 3/4/5）
3. 「发货」禁用规则与旧站不一致

**修复：** 去掉收货；发货禁用 Status∈{3,4}；拒绝禁用 Status∈{3,4,5}。

---

### FE-030

- 协作状态：已关闭
- 责任方：前端
- 前端验证：通过

**现象：** 主题抽奖审核/发货活动类型下拉复用全量 `ACTIVITY_TYPE_OPTIONS`，旧站仅 主题抽奖/积分商城/排行榜/N级代理/票券。

**修复：** 使用 `GIFT_LUCKY_ACTIVITY_TYPE_OPTIONS` 收窄选项。

---

### FE-031

- 协作状态：处理中
- 责任方：前端

**现象：** 主题抽奖人工录单（权限 10183）按钮存在但提示「暂未迁移」，旧站 `manual-form` 完整可用。

---

### API-020

- 协作状态：待反馈
- 后端解决：未处理
- 前端验证：—
- 责任方：后端
- 验证备注：2026-07-22；`leying_niu` @ ccccc-admin.dk888.link

**现象：** 礼品发货列表空数据 `Items=null`（审核列表同条件返回 `Items=[]`）；错误 Sort 触发库异常；本环境无礼品业务单。

**实测：**

| 接口 / 条件 | `Items` | `MaxCount` | 备注 |
| --- | --- | --- | --- |
| `list` ApplyType=4 / 5,6,7,8；空日期或近 365 日 | `[]` | 0 | 参数与旧站一致 |
| `deliverlist` ApplyType=4 / 5,6,7 | `null` | 0 | 前端已兜底 `[]` |
| `Sort=CreateTime desc`（list/deliverlist） | — | — | `status=10001` |
| `Sort=-CreateTime` / `Sort=` | 空结果正常 | 0 | — |

**请后端确认：** 空数据统一 `Items=[]`；无效 Sort 勿 `10001`。

---

### FE-032

- 协作状态：已关闭
- 责任方：前端
- 前端验证：通过

**现象：** 银行卡黑名单已有 `filterSourceType` / `resetFilters` / `importOpen` 状态，但工具栏未挂载「来源」下拉、重置、批量导入按钮，相对旧站无法按 SourceType 筛选、无法导入。

**修复：** 补齐 SourceType Select、重置、批量导入入口。

---

### FE-033

- 协作状态：已关闭
- 责任方：前端
- 前端验证：通过

**现象：** IP/设备列表接口空数据 `Items=null`；分页优先 `Number(MaxCount)` 在缺 Pagination 时可能误判。App Store `WhitelistList=null` 同理。

**修复：** API 层归一 `Items`/`WhitelistList` 为 `[]`；列表 Total 仅在 Pagination 明确带 MaxCount 时用 MaxCount，否则回退 `Total`。

---

### API-021

- 协作状态：待反馈
- 后端解决：未处理
- 前端验证：—
- 责任方：后端
- 验证备注：2026-07-22；`leying_niu` @ ccccc-admin.dk888.link

**现象：** 风控黑白名单空数据返回 `Items=null`；银行卡列表错误 Sort 触发库异常。

**实测：**

| 接口 | 条件 | `Items` | 计数字段 | 备注 |
| --- | --- | --- | --- | --- |
| `gameipriskcontrol/list` Type=1/2 RiskType=1 | 近 30 日/无日期 | `null` | `Total=0`（无 Pagination） | 参数对齐 |
| `gameriskcontrol/list` Type=1/2 RiskType=4 | 同上 | `null` | `Total=0` | 参数对齐 |
| `playerbankcard/blacklist` | 近 30 日；FilterValue 空/1/2/3 | `null` | `MaxCount=0` | 参数对齐 |
| 银行卡 `Sort=CreateTime desc` | — | — | — | `status=10001` |
| IP `Sort=CreateTime desc` | — | `null` | `Total=0` | 本环境空，未触发 10001 |

**请后端确认：** 空数据 `Items=[]`；无效 Sort 勿 `10001`。

---

### API-022

- 协作状态：待反馈
- 后端解决：未处理
- 前端验证：—
- 责任方：后端
- 验证备注：2026-07-22

**现象：** `appstorewhitelistrisk/list` 在 Key=`h4_ios`/`h3_ios` 下 `WhitelistList=null`、`TotalItems=0`。前端已兜底 `[]`。

---

### FE-034

- 协作状态：已关闭
- 后端解决：—
- 前端验证：通过
- 责任方：前端
- 验证备注：2026-07-22；对齐旧站 `getBeforeOneMonth(0)`～`getBeforeDateStr(1,false)`

**现象：** 新站默认/重置为 `dayjs().subtract(1,'month').startOf('day')`～今日，例 7/22 → 6/22；旧站为当月 1 日 00:00:00～今日 23:59:59。已改为 `startOf('month')`～`endOf('day')`。

### FE-035

- 协作状态：已关闭
- 后端解决：—
- 前端验证：通过
- 责任方：前端
- 验证备注：2026-07-22

**现象：** 筛选 `RangePicker` 开了 `show-time`，但 `getQueryParams` 强制 `begin.startOf('day')` / `end.endOf('day')`，用户选的时分秒不生效；旧站 `SearchTypeTwo` 按选择器真实时间转 unix。已改为直接 `begin.unix()` / `end.unix()`。

### API-023

- 协作状态：待反馈
- 后端解决：未处理
- 前端验证：—
- 责任方：后端
- 验证备注：2026-07-22；账号 `leying_niu` @ `ccccc-admin.dk888.link`

**现象：** `GET /backend/downloadexcel/list` 在当月、近 1 月、年初、无时间、Status=0/1/2/-1、`Sort=-CreateTime` / `CreateTime desc`、`Auto=true/false` 等组合下均 `status=200`，但 `Items=null`、`Pagination.MaxCount=0`。前端已 `Items || []`。下载缺验证码返回 `10174`（预期）。

| 探测 | Begin/End | Status | Sort | Items | MaxCount |
| --- | --- | --- | --- | --- | --- |
| 旧站默认窗 | 当月 1 日～今日末 | -1 | — | `null` | 0 |
| 新站原错误窗 | 今日−1 月～今日末 | -1 | — | `null` | 0 |
| 年初～今日 | — | -1 | — | `null` | 0 |
| 无时间 | — | -1 | — | `null` | 0 |
| Status=0/1/2 | 当月 | 分测 | — | `null` | 0 |
| Sort | 年初 | -1 | `-CreateTime` / `CreateTime desc` | `null` | 0（未触发 10001） |

**请后端确认：** 空列表 `Items=[]`；本环境是否确实无导出任务数据。

---

### FE-036

- 协作状态：已关闭
- 后端解决：—
- 前端验证：通过
- 责任方：前端
- 验证备注：2026-07-22

**现象：** 旧站/原新站均以 `Name` 调 `countriesconfig/list` 缩小表格后，保存只序列化当前 `tableList` 的 Status→Countries/CountriesAllow，会把未展示国家从配置中清掉。已改为始终 `Name=''`+`PageSize=9999` 拉全量，名称查询改前端过滤；保存基于全量列表。

### FE-037

- 协作状态：已关闭
- 后端解决：—
- 前端验证：通过
- 责任方：前端
- 验证备注：2026-07-22；本环境 `Option=0`

**现象：** `gamecountries/` 返回 `Option=0` 时，旧站 select 仅有 1/2，开关 `v-if/v-else-if` 均不命中导致状态列空白。新站将 `Option≠2` 归一为模式 1；保存时在状态接口原字段上覆盖 `Countries`/`CountriesAllow`/`Option`（对齐旧站 `Object.assign` 再改）。

### API-024

- 协作状态：待反馈
- 后端解决：未处理
- 前端验证：—
- 责任方：后端
- 验证备注：2026-07-22；账号 `leying_niu` @ `ccccc-admin.dk888.link`

**现象：** `GET /backend/countriesconfig/list`（`Page=1&PageSize=9999`，含/不含 `Name`，以及 Status/Sort/Type 等变体）均 `status=200`，但 `Items=null` 且无 `Pagination`。前端已 `Items || []`，页面表现为空表。同账号 `GET /backend/gamecountries/` 正常返回：`Countries=""`、`CountriesAllow=""`、`Option=0`、`CountriesForBackstage=""`。

| 探测 | 参数 | Items | Pagination |
| --- | --- | --- | --- |
| 全量 | Page=1 PageSize=9999 Name= | `null` | 无 |
| 名称 | Name=中国 | `null` | 无 |
| PageSize=20 | — | `null` | 无 |
| Status/Sort/Type 等 | 分测 | `null` | 无 |

**请后端确认：** 空列表 `Items=[]` 并带 `Pagination`；本环境国家配置库是否未初始化。

---

### FE-038

- 协作状态：已关闭
- 后端解决：—
- 前端验证：通过
- 责任方：前端
- 验证备注：2026-07-22

**现象：** 预约明细取款时间传 `WithdrawBeginTime/WithdrawEndTime`，旧站为 `BeginTime/EndTime`；且缺默认日窗（旧站 `getBeforeDateStr(1)`≈今天）。已改字段名、默认今天、补重置与账号小写。

### FE-039

- 协作状态：已关闭
- 后端解决：—
- 前端验证：通过
- 责任方：前端
- 验证备注：2026-07-22

**现象：** 优惠码兑换记录用 `BeginTime/EndTime` + 默认近 7 日；旧站为 `ClaimBeginTime/ClaimEndTime` + 今天。已改。

### FE-040

- 协作状态：已关闭
- 后端解决：—
- 前端验证：通过
- 责任方：前端
- 验证备注：2026-07-22

**现象：** 邀请明细首屏无注册时间，旧站默认今天注册窗；游戏账号未小写。已补默认日窗/重置/小写。

### FE-041

- 协作状态：已关闭
- 后端解决：—
- 前端验证：通过
- 责任方：前端
- 验证备注：2026-07-22；`listprize` 11 条、`vipfunction` 6 条可展示

**现象：** 晋级好礼列绑 `Vip`/`enabled`，接口为 `VipLevel`/`UpgradePrizeEnabled`；VIP 介绍 `Title` 空未回退 `Name`。已修。

### FE-042

- 协作状态：处理中
- 后端解决：—
- 前端验证：—
- 责任方：前端
- 验证备注：2026-07-22

**现象：** 相对旧站能力缺口（列表查询已对齐的子页除外）：优惠码历史/每日详细/报表；邀请规则/访问统计；会员代理方案与团队绑定日志；新手转盘注册/首存方案编辑；每日签到当前/历史拆分；存款优惠完整规则编辑器；活动模版创建向导等。页面内多为 Alert/禁用按钮占位。

### API-025

- 协作状态：待反馈
- 后端解决：未处理
- 前端验证：—
- 责任方：后端
- 验证备注：2026-07-22

**现象：** 下列列表均 `status=200` 且 `Items=null`、`MaxCount=0`（本环境空库或无业务数据）：`operationactivitiesconfig/list`、`history`、`giftcoderecord`、`activitychangelog/list`、`dailycheckin/list`、`playerrecord`、`playerinvitefunc/listdetail`、`playeragent/listplayeragent*`、`playerpaydiscount/list`、`newplayerdrawconfig/list`、`reservewithdrawactivity/list`、`operationactivitiesdefaultconfig/list?IsTemplate=true`。系统模板 `IsTemplate=false` 有 1 条。前端已 `Items||[]`。

### API-026

- 协作状态：待反馈
- 后端解决：未处理
- 前端验证：—
- 责任方：后端
- 验证备注：2026-07-22

**现象：** `GET /backend/activitytype/list`、`GET /backend/vipbaserule/list` 返回 `status=200` 但 `respond=null`（非空数组/空对象）。前端归一为 `Items=[]`，页面空表。

### API-027

- 协作状态：待反馈
- 后端解决：未处理
- 前端验证：—
- 责任方：后端
- 验证备注：2026-07-22；账号有 10304/10338/10339/12921

**现象：** `GET /backend/playerrechargeofferscheme/list`（含/不含 PackageId、Sort、Status 等）均 `status=10000`「请求操作失败(无权限或输入参数有误)」。旧站存款规则页主要走方案切换/分组接口，非该 list 作主数据源；仍请确认 list 可用性。

### FE-043

- 协作状态：已关闭
- 后端解决：—
- 前端验证：通过
- 责任方：前端
- 验证备注：2026-07-22

**现象：** 结算记录 `leaderboard/record` 日期误用 unix 秒 → `status=10037`；旧站与实测通过格式为 `YYYY-MM-DD`（与 list 接口相反：list 用日期串→`10000`，须 unix）。已改为日期串；并补 Ranking 排序（`Ranking`/`-Ranking`）与账号小写。

### FE-044

- 协作状态：处理中
- 后端解决：—
- 前端验证：—
- 责任方：前端
- 验证备注：2026-07-22

**现象：** 新站 upsert 为精简表单；旧站含多语言 LangText、奖励档位 LeaderBoardRewards、实物/虚拟奖、流水来源等完整向导。列表/下架/克隆/排行弹窗可用，完整创建编辑待补齐。

### API-028

- 协作状态：待反馈
- 后端解决：未处理
- 前端验证：—
- 责任方：后端
- 验证备注：2026-07-22

**现象：** `GET /backend/leaderboard/list`（当前 `IsHistory=false` / 历史 `true`，Page=0/1）均 `Items=null`、`MaxCount=0`。带 `StartTime/EndTime=YYYY-MM-DD` → `status=10000`；unix 秒可 `200`。前端列表已用 unix；请空列表返回 `Items=[]`，并统一日期格式约定。

### API-029

- 协作状态：待反馈
- 后端解决：未处理
- 前端验证：—
- 责任方：后端
- 验证备注：2026-07-22

**现象：** `GET /backend/leaderboard/getleaderboardmainconfig` 返回 `{ Items: null }`（旧站读 `Data.Items` 为配置对象）。前端开关默认关；请无配置时返回默认对象或空对象而非 `Items=null`。

### FE-045

- 协作状态：已关闭
- 后端解决：—
- 前端验证：通过
- 责任方：前端
- 验证备注：2026-07-22

**现象：** 历史票券列表点名称进详情，新站统一校验 `13351`；旧站历史用 `13352`、当前用 `13351`。已按 `isHistory` 分支。

### FE-046

- 协作状态：已关闭
- 后端解决：—
- 前端验证：通过
- 责任方：前端
- 验证备注：2026-07-22

**现象：** 票券记录 / 发放记录查询账号未强制小写；已与单人发放查玩家逻辑对齐。

### API-030

- 协作状态：待反馈
- 后端解决：未处理
- 前端验证：—
- 责任方：后端
- 验证备注：2026-07-22

**现象：**

| 接口 | 无日期 / unix | `YYYY-MM-DD` |
| --- | --- | --- |
| `voucher/list`（当前） | 200，`Items=null` | `10000` |
| `voucher/list` IsHistory | 200，`Items=null` | Claim* 日期串也可 200 |
| `voucherdetailrecord` | 200，`Items=null` | 注册时间串→`10000` |
| `issuevoucherrecord`（需 VoucherId） | unix 200 | 日期串→`10000` |
| `listallwithexpired` / redeem | 200，`Items=null` | — |

前端列表/记录已用 unix。请空列表 `Items=[]`，并统一日期格式。

### API-031

- 协作状态：待反馈
- 后端解决：未处理
- 前端验证：—
- 责任方：后端
- 验证备注：2026-07-22

**现象：** `GET /backend/voucher/getvouchermainconfig` 返回 `{ Items: null }`；前端开关默认关。请返回默认配置对象。

### FE-047

- 协作状态：已关闭
- 后端解决：—
- 前端验证：通过
- 责任方：前端
- 验证备注：2026-07-22

**现象：** 兑换记录 / 积分记录 / 调整审核 / 调整记录默认日期用 `getYesterdayRangeSeconds()`（昨天）；旧站均为 `getBeforeDateTimestamp(1,false)`～今天 23:59。已改为 `getTodayRangeSeconds()`。

### FE-048

- 协作状态：已关闭
- 后端解决：—
- 前端验证：通过
- 责任方：前端
- 验证备注：2026-07-22

**现象：** 兑换/积分记录/调整审核与记录查询账号未强制小写；商品详情兑换子表 `ChannelId`/`VipLevel` 以数组序列化（与兑换记录页逗号串不一致）。已对齐。

### API-032

- 协作状态：待反馈
- 后端解决：未处理
- 前端验证：—
- 责任方：后端
- 验证备注：2026-07-22

**现象：**

| 接口 | unix / 无日期 | `YYYY-MM-DD` |
| --- | --- | --- |
| `rewardsmall/product/list`（IsHistory false/true） | 200，`Items=null`，`MaxCount=0` | — |
| `rewardsmall/exchange/recordlist` | 200，`Items=null`（今天/昨日/近 30 天/无日期同） | `10000` |
| `rewardsmall/point/recordlist` | 200，`Items=null` | — |
| `playerpointhandle/approvelist`（审核/记录） | 200，`Items=null`，`Total=0` | — |

前端列表日期已用 unix；空列表请 `Items=[]`。

### API-033

- 协作状态：待反馈
- 后端解决：未处理
- 前端验证：—
- 责任方：后端
- 验证备注：2026-07-22

**现象：**

| 接口 | 实测 |
| --- | --- |
| `GET /backend/rewardsmall/getrewardsmallmainconfig` | `{ Items: null }` |
| `GET /backend/rewardsmall/producttag/list` | `{ Items: null }`（无 Pagination） |
| `GET /backend/rewardsmall/rewardtask/list` | `{ Items: null }`（无 Pagination） |

前端对 `Items=null` 已按空配置/空列表兜底；请返回默认配置对象或 `Items=[]`。

### FE-049

- 协作状态：已关闭
- 后端解决：—
- 前端验证：通过
- 责任方：前端
- 验证备注：2026-07-22

**现象：** 协助管理默认日期写成「昨天」；旧站 `getBeforeDateStr(1)` / `getBeforeDateTimestamp(1,false)` 因 GLOBAL `days-1` 规则实际为**今天**。另 `RangePicker` 开了 `show-time` 却 `startOf/endOf('day')` 丢掉时分秒（同 FE-035）。已改为今天默认 + 直接 `unix()`。

### API-034

- 协作状态：待反馈
- 后端解决：未处理
- 前端验证：—
- 责任方：后端
- 验证备注：2026-07-22

**现象：**

| 条件 | 结果 |
| --- | --- |
| unix 今天/昨天/近 30 天/无日期 | 200，`Items=null`，`MaxCount=0` |
| `BeginTime/EndTime=YYYY-MM-DD` | `status=10000` |
| `Sort=CreateTime desc` | `status=10001` |
| `Sort=-CreateTime` / 空 Sort | 200，空列表 |

前端已用 unix + `-field` 排序；请空列表 `Items=[]`，并统一日期/Sort 约定。

### FE-050

- 协作状态：已关闭
- 后端解决：—
- 前端验证：通过
- 责任方：前端
- 验证备注：2026-07-22

**现象：**

| 问题 | 旧站 | 修复 |
| --- | --- | --- |
| IP Tab 权限 | 内层 `10214`，列表门禁 `10216` | NEW 误用外层 `10207`→已改 |
| 使用者搜索 | `Name` | NEW 误传 `Username`→已改 |
| IP 筛选 | `WhiteIp`+`WhiteUsername` 分栏 | 已恢复；支持从使用者点击跳转 |
| Status | 列表筛 + 开关（使用者 `edituser`/`10225`） | 已补 |
| 新增 IP | `IpWhiteUserId`/`ExpirationTime` 必填 | 已对齐 |
| 统计 | `ipuserlist` | 已补 IP/使用者计数 |

### FE-051

- 协作状态：处理中
- 后端解决：—
- 前端验证：—
- 责任方：前端
- 验证备注：2026-07-22

**现象：** 旧站 `whiteList.vue` 中「免白名单设置」（`10208`→`freeWhiteSetting.vue` / `whitelistfree/*`）Tab 已注释停用；NEW 未迁。若产品恢复该能力需补页面与接口封装。

### API-035

- 协作状态：待反馈
- 后端解决：未处理
- 前端验证：—
- 责任方：后端
- 验证备注：2026-07-22

**现象：**

| 接口 | 实测 |
| --- | --- |
| `GET /backend/whitelist/list` | 200，`Items=null`，`MaxCount=0`（空 Sort / `-CreateTime` / `CreateTime desc` 均 200） |
| `GET /backend/whitelist/listuser` | 200，`Items=[]`；`Sort=CreateTime desc`→**HTTP 500**（beego HTML） |
| `GET /backend/whitelist/pickuser` | 200，`{ Items: null }` |
| `GET /backend/whitelist/ipuserlist` | 200，`IpList=0` `UserList=0` |
| `GET /backend/whitelistfree/list` | 200，`Items=null`（旧站 Tab 已注释） |

前端列表已 `Items\|\|[]` 且默认空 Sort；请空列表统一 `Items=[]`，并修复 `listuser` 非法 Sort 的 500。

### FE-052

- 协作状态：已关闭
- 后端解决：—
- 前端验证：通过
- 责任方：前端
- 验证备注：2026-07-22

**现象：** 协助工单默认日期写成「昨天」；旧站 `getBeforeDateStr(1)` / `getBeforeDateTimestamp(1,false)` 因 GLOBAL `days-1` 规则实际为**今天**。另 `RangePicker` 开了 `show-time` 却 `startOf/endOf('day')` 丢掉时分秒。已改为今天默认 + 直接 `unix()`；重置时同时清空 `Creator`。

### API-036

- 协作状态：待反馈
- 后端解决：未处理
- 前端验证：—
- 责任方：后端
- 验证备注：2026-07-22

**现象：**

| 条件 | 结果 |
| --- | --- |
| unix 今天/昨天/近 30 天/无日期 | 200，`Items=null`，`MaxCount=0` |
| `BeginTime/EndTime=YYYY-MM-DD` | `status=10000` |
| `Sort=CreateTime desc` | `status=10001` |
| `Sort=-CreateTime` / 空 Sort | 200，空列表 |
| `GET /backend/helprecords/help`（无 Id） | `status=10002` |

前端已用 unix + `-field` 排序；请空列表 `Items=[]`，并统一日期/Sort 约定。

### FE-053

- 协作状态：已关闭
- 后端解决：—
- 前端验证：通过
- 责任方：前端
- 验证备注：2026-07-22

**现象：** 关联号查询默认日期用 `getYesterdayRangeSeconds()`；旧站 `getBeforeDateStr(1)` / `getBeforeDateTimestamp(1,false)` 实际为**今天**。另所属产品下拉未提供「全部」空选项（旧站 `getAllPackageList` 含 `PackageId=''`）。已改为今天默认并补「全部」。

### API-037

- 协作状态：待反馈
- 后端解决：未处理
- 前端验证：—
- 责任方：后端
- 验证备注：2026-07-22

**现象：**

| 条件 | 结果 |
| --- | --- |
| unix 今天 / 昨天 | 200，`Items=null`，`MaxCount=0`，Total 全 0 |
| unix 近 30 天 | 200，`Items` 有数据（本环境 MaxCount≈170） |
| 无日期 | 200，有数据（MaxCount≈553） |
| `BeginTime/EndTime=YYYY-MM-DD` | `status=10000` |
| `Sort=CreateTime desc` | `status=10001` |

前端已用 unix、空 Sort；空列表请 `Items=[]`。

### FE-054

- 协作状态：已关闭
- 后端解决：—
- 前端验证：通过
- 责任方：前端
- 验证备注：2026-07-22

**现象：** `visit-detail-panel` / `visit-statistics-panel`（`noticeTitle` 与 `emailTitle` 共用）默认日期用 `getYesterdayRangeSeconds()`；旧站 `noticeDetail`/`noticeStatistics` 为 `getBeforeDateTimestamp(1,false)`～今天。已改为 `getTodayRangeSeconds()`。接口仍受 API-013（`20001`）阻塞。

### FE-055

- 协作状态：已关闭
- 后端解决：—
- 前端验证：通过
- 责任方：前端
- 验证备注：2026-07-22

**现象：** 会员列表「绑定手机」下拉误用 `0=未绑定 / 1=已绑定`；旧站为 `1=已绑定 / 2=未绑定`（`-1` 全部）。实测 `BindPhone=0` → `status=10043`「请求参数格式错误」；`BindPhone=2` 正常返回未绑定列表。已改为 `1/2`。

### FE-056

- 协作状态：已关闭
- 后端解决：—
- 前端验证：通过
- 责任方：前端
- 验证备注：2026-07-22

**现象与修复：**

1. `LoginAccount` / `InviterLoginAccount` 未强制小写去空格（旧站 keyup）；已在 `normalizePlayerQuery` 与批量账号弹窗对齐
2. 所属产品下拉缺「全部」空值选项；已补
3. 备注抽屉未传 `IsSelf: true`（旧站 `remarkListQuery`）；已补
4. 注册时间 RangePicker 未受权限 `11399` 门控（旧站该筛选项在 `11399` 下，当前源码虽注释但仍应按权限展示）；已门控

### API-038

- 协作状态：待反馈
- 后端解决：未处理
- 前端验证：—
- 责任方：后端
- 验证备注：2026-07-22 `leying_niu`；参数对齐旧站后复测

**现象：**

| 场景 | 结果 |
| --- | --- |
| 默认空日期 `Sort=-CreateTime` BindPhone/PlayerLevelId=`-1` DataSearchType=`2` | 200，`Items` 20 条，`MaxCount=553`，`Total` 有汇总 |
| `Sort=CreateTime desc` | `status=10001` 数据库异常 |
| `BeginTime/EndTime=YYYY-MM-DD` | `status=10000` |
| 空筛选子集（如 BindPhone=1 本环境无人绑定） | 200，`Items=null`，`MaxCount=0` |
| `GET playertag/list` | 200，`Items=null`，`MaxCount=0`（本环境无标签） |
| `listcsv` 无安全校验 | `status=10103` 密码错误（预期） |

前端已用 `-CreateTime`、unix 日边界、空结果归一 `[]`；请空列表统一 `Items=[]`。

### FE-057

- 协作状态：已关闭
- 后端解决：—
- 前端验证：通过
- 责任方：前端
- 验证备注：2026-07-22

**现象与修复：**

1. 所属会员弹窗产品下拉缺「全部」空值（旧站 `allPkg` → `PackageId=''`）；已补
2. 游戏账号未强制小写去空格；已对齐
3. 列表/下拉默认文案「默认返水/风控方案」改为旧站「产品的返水方案」「预设风控方案」
4. 空 `LevelName` 改为不传（对齐旧站 omit）

### API-039

- 协作状态：待反馈
- 后端解决：未处理
- 前端验证：—
- 责任方：后端
- 验证备注：2026-07-22 `leying_niu`；参数对齐旧站后复测

**现象：**

| 场景 | 结果 |
| --- | --- |
| `GET playerlevel/list?Page=1&PageSize=20` | 200，`Items` 1 条（`DefaultLevel=0` 未分层），`MaxCount=1` |
| `BeginTime/EndTime=YYYY-MM-DD` | `status=10000` |
| unix 日窗（今日等，未含 CreateTime≈0） | 200，`Items=null`，`MaxCount=0` |
| `BeginTime=0&EndTime=2000000000` | 200，可命中未分层 |
| `listPlayerDetail?Id=3`（PlayerCount=0） | 200，`Items=null`，`MaxCount=0` |
| `allscheme` / `listSchemeName` | 200，各 1 条方案 |

前端已用 unix、空结果归一 `[]`；请空列表统一 `Items=[]`。本环境无 `DefaultLevel=1` 自定义层级，增删改未实写。

### FE-058

- 协作状态：已关闭
- 后端解决：—
- 前端验证：通过
- 责任方：前端
- 验证备注：2026-07-22

**现象与修复：**

1. 明细/统计默认日期用 `getYesterdayRangeSeconds()`；旧站 `getBeforeDateTimestamp(1,false)`～`getBeforeDateTimestamp()` / `getBeforeDateStr(1)` = **今天**。已改为 `getTodayRangeSeconds()`
2. 明细产品下拉补「全部」`PackageId=''`；游戏账号强制小写去空格

### API-040

- 协作状态：待反馈
- 后端解决：未处理
- 前端验证：—
- 责任方：后端
- 验证备注：2026-07-22 `leying_niu`；参数对齐旧站后复测

**现象：**

| 场景 | 结果 |
| --- | --- |
| 明细 unix 今天 | 200，`Items=null`，`MaxCount=0`（本环境今日暂无登录） |
| 明细 unix 昨天 | 200，`Items` 1 条，`MaxCount=1` |
| 明细无日期 | 200，`MaxCount=553`（全量） |
| 明细 `BeginTime/EndTime=YYYY-MM-DD` | `status=10000` |
| 统计 unix 今天 `Summary=1` DataSearchType=0 | 200，`TotalPlatform/TotalVip=0`，VipList 全 0 |
| 统计 unix 昨天 | 200，`TotalPlatform=1`（Web=1） |
| `listcsv` 无安全校验 | `status=10103` 密码错误（预期，pageId=34） |

前端已用今天默认、unix、空结果归一 `[]`；请空列表统一 `Items=[]`。

### FE-059

- 协作状态：已关闭
- 后端解决：—
- 前端验证：通过
- 责任方：前端
- 验证备注：2026-07-22

**现象与修复：**

1. 全局游戏记录默认日期写成「前天 00:00 → 昨天 23:59」；旧站 `getBeforeDateTimestamp(2,false)`～`getBeforeDateTimestamp(1)` = **昨天 00:00 → 今天 23:59**。已改为 `getYesterdayToTodayRangeSeconds()`（玩家详情嵌套仍为近 7 天→昨天）
2. 产品下拉补「全部」`PackageId=''`

实测：旧窗 `MaxCount=5121`；错误「前天→昨天」窗 `MaxCount=9653`（漏今、多前天，数据明显偏移）。

### API-041

- 协作状态：待反馈
- 后端解决：未处理
- 前端验证：—
- 责任方：后端
- 验证备注：2026-07-22 `leying_niu`；参数对齐旧站后复测

**现象：**

| 场景 | 结果 |
| --- | --- |
| 列表昨天→今天（无 SumAll） | 200，`Items` 20，`MaxCount=5121`；`MoreItems` 汇总金额全 0 |
| 同窗 `SumAll=1` | 200，`Items=null`，`MaxCount=5121`；`MoreItems` 有真实汇总 |
| `BeginTime/EndTime=YYYY-MM-DD` | `status=10000` |
| `Sort=CreateTime desc` | 200，`Items=null`（MaxCount 仍有） |
| `Sort=-WinGold` | 200，有 Items |
| `gametransactiondetaillog?TransactionId=` | 200，`respond=null` |
| `getbyorderid`（无 Detail 兜底） | 可 `10000`「三方注单查询失败」（行内有 Detail 时前端不走此接口） |
| `gametransactiondetailexcel` 无校验 | `status=10103`（预期，pageId=35） |

前端已双请求（列表 + SumAll）、`-field` Sort、空结果归一；请空列表 `Items=[]`，结算流水空时勿 `respond=null`。

### FE-060

- 协作状态：已关闭
- 后端解决：—
- 前端验证：通过
- 责任方：前端
- 验证备注：2026-07-22

**现象与修复：**

1. 默认日期用 `getLast7DaysToYesterdayRangeSeconds()`（今天−7 ～ 昨天）；旧站 `getBeforeDateTimestamp(7)`～`getBeforeDateTimestamp()` = **今天−6 日 00:00 ～ 今天 23:59**（近 7 自然日含今天）。已改为 `getLast7CalendarDaysRangeSeconds()`
2. 产品下拉补「全部」`PackageId=''`

实测账号 `123xy`：旧窗 `MaxCount=171` / `SumAddGold=8761`；错误窗（至昨天）`MaxCount=144` / `SumAddGold=0`（漏今日账变）。

### API-042

- 协作状态：待反馈
- 后端解决：未处理
- 前端验证：—
- 责任方：后端
- 验证备注：2026-07-22 `leying_niu`；须带 `LoginAccount` 或 `LogId`

**现象：**

| 场景 | 结果 |
| --- | --- |
| 无账号且无订单号 | `status=10000`（前端已拦截） |
| `123xy` + 近 7 日含今天 `DataSearchType=0` | 200，`Items` 有数据，`MaxCount=171`，`SumAddGold=8761` |
| `t520520` 同行有负向 `AddGold` | 200，`SumAddGold=0`（与明细不符） |
| `BeginTime/EndTime=YYYY-MM-DD` | `status=10000` |
| `Sort=CreateTime desc` | 200，`Items=null`（MaxCount/Sum 仍有） |

前端已强制账号/订单、unix、空结果归一 `[]`；请空列表 `Items=[]`，并核对 `MoreItems.SumAddGold` 与明细一致性。

### FE-061

- 协作状态：已关闭
- 后端解决：—
- 前端验证：通过
- 责任方：前端
- 验证备注：2026-07-22

**现象与修复：**

1. 默认日期用 `getYesterdayRangeSeconds()`；旧站 `getBeforeDateTimestamp(1,false)`～`getBeforeDateTimestamp(1,true)` / `getBeforeDateStr(1)` = **今天**。已改为 `getTodayRangeSeconds()`
2. 数据类型下拉误用含「测试」的 `memberTypeOptions`；旧站仅「全部/正式」。已改为本地 `dataSearchTypeOptions`

实测：今天 `MaxCount=164`；昨天 `MaxCount=231`（默认用昨天会漏今日转账）。

### API-043

- 协作状态：待反馈
- 后端解决：未处理
- 前端验证：—
- 责任方：后端
- 验证备注：2026-07-22 `leying_niu`

**现象：**

| 场景 | 结果 |
| --- | --- |
| 今天 `State=-2` | 200，`Items` 20，`MaxCount=164` |
| `State=0` 成功 | 200，有数据 |
| `State=-1` 处理中 | 200，`Items=null`，`MaxCount=0` |
| `State=5` 转人工 / `State=17` | `status=10001` 数据库异常 |
| `State=18` 失败 | 200，`MaxCount=2` |
| `BeginTime/EndTime=YYYY-MM-DD` | `status=10000` |
| `Sort=CreateTime desc` | `status=10001` |
| 客户端导出 `IsExp=true` PageSize=99999 | 200，`Items` 164 |

前端已用今天默认、空结果归一 `[]`、客户端导出；请修复 `State=5` 查询异常，空列表 `Items=[]`。

### FE-062

- 协作状态：已关闭
- 后端解决：—
- 前端验证：通过
- 责任方：前端
- 验证备注：2026-07-22

**现象与修复：**

1. 验证结果通知邮件：`LangText` 接口常返回「按语言组 Id 的对象」字符串；前端按数组 `forEach` 解析 → 落入 catch 后模板恒空。已兼容对象/数组两种结构
2. 注册宽限期：`Days=0` 时用 `|| 7` 误显示为 7。已改为保留 0
3. 审核列表/记录产品下拉补「全部」；`AuthScenario` 全部时对齐旧站传 `-1`；验证设置区块顺序对齐旧站（信息开关 → 同证件 → 邮件）

### API-044

- 协作状态：待反馈
- 后端解决：未处理
- 前端验证：—
- 责任方：后端
- 验证备注：2026-07-22 `leying_niu`

**现象：**

| 场景 | 结果 |
| --- | --- |
| `GET playerauthentication/list`（空日期 / AuthScenario=-1/0/1/2） | 200，`Items=null`（无待审） |
| `GET playerauthentication/record` Page=1 | 200，`Items=null`，`MaxCount=0` |
| list/record `BeginTime/EndTime=YYYY-MM-DD` | `status=10000` |
| `GET playerauthentication/setting` | 200，SubType=1–4、1001–1004；**缺 1005**（验证信息开关） |
| `GET .../recordexport` 无导出口令 | `status=10103`（符合口令校验） |

前端已空结果归一 `[]`、unix 日期、邮件 LangText 兼容解析；请空列表 `Items=[]`，并补齐/返回 `SubType=1005` 配置。

### FE-063

- 协作状态：已关闭
- 后端解决：—
- 前端验证：通过
- 责任方：前端
- 验证备注：2026-07-22

**现象与修复：**

1. 虚拟币地址：新站首屏/重置强制「近1月→今天」，旧站 `created/reset` 的 `BeginTime/EndTime` 为空串（全量）。已改为空日期不传
2. 银行卡：游戏账号未按旧站 keyup 转小写去空格。已补 normalize
3. 银行卡备注「详情」：旧站需 `MerchantOrderNo && ThirdPartyUserId`，新站误用 `||`。已改为且

银行卡/电子钱包/多账号默认近1月对齐旧站 `getBeforeOneMonth(0)`；本环境银行卡近1月 `MaxCount=5`、无日期 `MaxCount=11`。

### API-045

- 协作状态：待反馈
- 后端解决：未处理
- 前端验证：—
- 责任方：后端
- 验证备注：2026-07-22 `leying_niu`

**现象：**

| 场景 | 结果 |
| --- | --- |
| `GET playerbankcard/list` 近1月 | 200，`Items` 5，`MaxCount=5` |
| 同上无日期 | 200，`MaxCount=11` |
| `LoginAccount` 大小写 | 均可命中同账号 |
| 空账号查询 | 200，`Items=null`，`MaxCount=0` |
| `BeginTime/EndTime=YYYY-MM-DD`（四接口） | `status=10000` |
| `Sort=CreateTime desc` / `Sort=-CreateTime` | `status=10001`；`-BankCardTime` 正常 |
| `playerwalletaccount/listall` / `playerwithdrawdigitaladdress/list` / `listcardmultiple` | 200，`Items=null`，`MaxCount=0`（本环境无数据） |

前端已空结果归一 `[]`、unix 日期；请空列表 `Items=[]`，并避免非法 Sort 直接数据库异常。

### FE-064

- 协作状态：已关闭
- 后端解决：—
- 前端验证：通过
- 责任方：前端
- 验证备注：2026-07-22

**现象与修复：**

1. 手机/邮箱验证码查询：产品下拉缺「全部」；游戏账号未按旧站转小写去空格。已补
2. 白名单：缺渠道 ID 筛选；空 `PackageId` 仍上传。已对齐旧站补渠道、无产品不传 PackageId
3. 邮箱 list / listemailconfig：`respond=null` 时前端未统一成 `[]`（页面易异常）。API 层已归一

手机验证码无账号/手机号前端拦截（对齐旧站 msg6）；区码管理首屏可查。

### API-046

- 协作状态：待反馈
- 后端解决：未处理
- 前端验证：—
- 责任方：后端
- 验证备注：2026-07-22 `leying_niu`

**现象：**

| 场景 | 结果 |
| --- | --- |
| `GET phoneverifycode/list` 无 LoginAccount/PhoneNum | `status=10037` 参数错误 |
| 同上有账号 | 200，`Items=null`，`MaxCount=0`（本环境无验证码记录） |
| 同上带 `BeginTime=YYYY-MM-DD` | `status=10000` |
| `GET phoneverifycode/whitelist` | 200，`Items=null`，`MaxCount=0` |
| `GET phonecountrycode/list` | 200，`Items` 20，`MaxCount=244`，`FrequentCount=7` |
| `GET emailverifycode/list` / `listemailconfig` | 200，**`respond=null`** |
| `GET gameotpconfig/getconfig` | 200，`[{IpLimit:1,DeviceLimit:10}]` |

前端已拦截无条件查询、空结果归一；请空列表返回 `[]`/`Items=[]`，勿对邮箱配置返回 `null`。


### FE-065

- 协作状态：已关闭
- 后端解决：—
- 前端验证：通过
- 责任方：前端
- 验证备注：2026-07-22

**现象与修复：**

1. 注单详情 / 场馆统计 / 日期统计 / 金币详情 / 金币时段 / 充提 / 易充 / 额度 / 红利 / 游戏信息：默认日期误用「近 7 天→昨天」（`getLast7DaysToYesterdayRangeSeconds`）。已改为 `getLast7CalendarDaysRangeSeconds`（对齐旧站 `getBeforeDateTimestamp(7)`～今天）
2. 调整记录 / 操作日志 / 场馆转账 / 流水信息 / 积分 / 返水 / 问题·访问记录：默认误用昨天整天。已改为 `getTodayRangeSeconds`（对齐旧站今天）
3. 顶部按账号查询：`LoginAccount` 未转小写去空格。已对齐其它页
4. 注单列表与三方详情：已复用 `GameRecordListPanel`（`scope=player`）+ `GameRecordThirdDetailModal`，与游戏记录展示/解析一致；玩家域默认近 7 自然日含今天，并带 `DataSearchType=2` + `PlayerId`

### API-047

- 协作状态：待反馈
- 后端解决：未处理
- 前端验证：—
- 责任方：后端
- 验证备注：2026-07-22 `leying_niu`；样例玩家 `q7950029` / `PlayerId=5247247`

**现象：**

| 场景 | 结果 |
| --- | --- |
| `GET /backend/operation/gametransactiondetail` 近7含今天 + `GameIds=` + `DataSearchType=2` | 200，`Items` 6 条（含 `Detail` JSON）；非 SumAll 时 `MoreItems` 金额多为 0 |
| 同上 `SumAll=1` | 200，`MaxCount=6`，`MoreItems.SumBetGold=61900`，但 **`Items=null`** |
| 误传 `GameIds=-1`（非「场馆类型已选且未选具体游戏」） | `MaxCount=0` / 汇总全 0（FE 默认传空串，勿作默认） |
| `BeginTime/EndTime=YYYY-MM-DD` | `status=10000` |
| `GET /backend/gametransactiondetail`（无 operation 前缀） | `status=10010` |
| `GET .../gametransactiondetaillog?TransactionId=` | 200，**`respond=null`** |
| `GET .../getbyorderid`（无行内 Detail 时的兜底） | `status=10000`（行内 `Detail` 可 structured 展示） |
| `GET /backend/aiassistantsearch/list` | `status=10010` 权限不足 |
| 多列表空结果（充值/红利/积分/转账/返水/访问等） | 200，**`Items=null`**，`MaxCount=0` |

前端已空结果归一、unix 日期、注单复用游戏记录组件；请空列表返回 `Items=[]`，SumAll 与明细 Items 行为一致，并修复 detaillog / getbyorderid。

---


### FE-066

- 协作状态：已关闭
- 后端解决：—
- 前端验证：通过
- 责任方：前端
- 验证备注：2026-07-22

**现象与修复：**

1. `package/list` 空结果时 `Items`/`MoreItems.Resources`/`VIPBadgeGroups` 可能为 `null`，列表图标/VIP 列易异常。API 层已归一为 `[]`
2. `listlogogroup` 本环境 `respond=null` 时 Logo 下拉空白，已配置的 `LogoGroupId` 无法回显。已兜底补「方案 {id}」选项，并对 null 归一 `[]`
3. `getactivitylist` 空响应归一为 `[]`（查询仍传 `IsShowAll=1`，对齐旧站）
4. 列表「返水配置」仅显示已配置/未配置；已对齐旧站展示方案名 + 结算模式（日结/按天数/周结）

列表查询参数（`PackageName`+分页）与旧站一致；更多配置多 Tab 参数与旧站一致。

### API-048

- 协作状态：待反馈
- 后端解决：未处理
- 前端验证：—
- 责任方：后端
- 验证备注：2026-07-22 `leying_niu`；样例产品 `Id=7`「乐赢网」

**现象：**

| 场景 | 结果 |
| --- | --- |
| `GET /backend/package/list` 默认分页 | 200，`Items` 1，`Resources` 1，`VIPBadgeGroups` 1 |
| 同上 `PackageName=乐赢` | 200，命中 1 |
| 同上 `Sort=CreateTime desc` | 200，**`Items=[]` MaxCount=0**（`-CreateTime` 正常） |
| 同上带 `BeginTime/EndTime=YYYY-MM-DD` | `status=10000` |
| `GET /backend/package/{id}` 及注册/支付/登录/统计/SEO/欢迎邮件等配置 | 200，对象可读 |
| `GET .../getunderageconfig?Id=7&Type=1` | 200，**空对象 `{}`**（Type=2 有完整配置） |
| `GET /backend/gameadtemplate/listlogogroup` | 200，**`respond=null`**（产品上已有 LogoGroupId=731） |
| `GET .../getactivitylist` 无参 | 200，**`respond=null`** |
| 同上 `IsShowAll=1` | 200，list(1) 但 Type=10022（本环境无 10014/10018 推广活动可选项） |
| `GET /api/resource/packagecolorthemelist` | 200，`Items=null` |
| `GET /api/resource/list` 无业务参 | `status=10000` |

前端已归一空列表并兜底 Logo 回显；请 `listlogogroup`/活动列表空时返回 `[]`，非法 Sort 勿静默清空，未成年未配置请明确空结构。

---


### FE-067

- 协作状态：已关闭
- 后端解决：—
- 前端验证：通过
- 责任方：前端
- 验证备注：2026-07-22

**现象与修复：**

1. 编辑产品详情 `SkinColor`/`PackageColorStyle*` 为 `0` 时，前端误当作有效配色写入 `PackageColorStyle`（旧站 `if (SkinColor)` 会跳过）。已按「0=未配置」处理，保存/预览均不传空配色
2. `/api/resource/list` 缺 `AgentId` 会 `10000`；加载皮肤前已校验 AgentId 并提示
3. 资源列表 / 配色主题 list+detail：`Items=null` 已在 API 层归一为 `[]`；无配色模板时提示「暂无配色模板」（避免旧站 `ThemeList[0]` 空列表崩溃同类问题）
4. 提交校验补 PC 皮肤（有 PC 资源时必选），对齐向导步骤完整性

七步向导参数（语言→风格系列→APP 皮肤 PictureType=12→PC 皮肤 15→产品信息→场馆→预览）与旧站一致。

### API-049

- 协作状态：待反馈
- 后端解决：未处理
- 前端验证：—
- 责任方：后端
- 验证备注：2026-07-22 `leying_niu`；编辑样例 `package/7`

**现象：**

| 场景 | 结果 |
| --- | --- |
| `GET /api/resource/list` 无参 | `status=10000` |
| 同上无 `AgentId`（有 PictureType/Theme） | `status=10000` |
| `AgentId`+`PictureType=3`（图标） | 200，`Items` 15 |
| `AgentId`+`PictureType=12`+`PictureTheme=1`（APP） | 200，`Items` 1（Style 100） |
| `AgentId`+`PictureType=15`+`PictureTheme=1`（PC） | 200，`Items` 1 |
| `PictureType=8`（旧初始值，现已改 12） | 200，`Items=null` MaxCount=0 |
| `GET packagecolorthemelist?DeviceType=0/1` | 200，**`Items=null`** |
| `GET packagecolorthemedetaillist` DeviceType+ThemeId='' | 200，**`Items=null`** MaxCount=0 |
| `GET /backend/package/7` | 200，详情可读（StyleType=100，SkinColor=0，Games=1300） |

前端已归一空列表并修正 0 配色；请配色接口空结果返回 `[]`，资源缺 AgentId 返回明确错误码/文案。

### FE-068

- 协作状态：已关闭
- 后端解决：—
- 前端验证：通过
- 责任方：前端
- 验证备注：2026-07-22

**现象与修复：**

1. 批量「落地页」拉资源未传 `PictureType: 1`（旧站 `bgQuery.PictureType=1`）；缺省时 `/api/resource/list` 返回 `Items=null`，落地页列表空。已补 `PictureType` + AgentId 校验
2. 创建/编辑表单落地页：缺 `AgentId` 时直接提示，避免静默空列表
3. 包体弹窗：`IosPkgConfigId`/`AndroidPkgConfigId`/`IosPackageId` 为 `0` 时按旧站视为未设置；上架包接口 `respond=null` 时不强制必选上架包，可改签名方式后保存
4. 邀请码：历史短码（如 `100`/`103`）允许原样保留；改码仍对齐旧站「字母+数字、4–20」
5. 列表「创建时间/渠道号」去掉表头 `sortable`，排序统一走筛选区 `Sort`（`-CreateTime` 等），避免与坏格式 `CreateTime desc` 混淆

列表默认 `IsHidden=1`、`PackStatus=''`、`Sort=''`；层级→`PromoterAdminId`；表单步序/落地页 Tab 与旧站一致。

### API-050

- 协作状态：待反馈
- 后端解决：未处理
- 前端验证：—
- 责任方：后端
- 验证备注：2026-07-22 `leying_niu`；样例渠道 `Id=16` `ChannelId=7775754_3` `PackageConfigId=7`

**现象：**

| 场景 | 结果 |
| --- | --- |
| `GET /backend/channel/list` 空结果（如 IsHidden=2+PackStatus=2） | 200，**`Items=null`** MaxCount=0 |
| 同上 `Sort=-CreateTime` / `CreateTime` / 空 | 200，有数据 |
| 同上 `Sort=CreateTime desc` | **`status=10001`** Database Exception |
| 列表带日期字符串 Begin/End | **`status=10000`**（前端列表未传日期） |
| `GET /backend/channel/listallsonpromoter` | 200，**`ItemsSon`/`Parents`=null**；`ItemsAdmin` 为单对象 |
| `GET /backend/channel/iosapppackageconfig?PackageId=7` | 200，**`respond=null`** |
| `GET /backend/channel/androidapppackageconfig` | 200，**`respond=null`** |
| `GET /backend/packagelinkios/listall?PackageId=7` | 200，**`Items=null`** |
| `GET /backend/countriesconfig/list` | 200，**`Items=null`**（无 Pagination） |
| `GET /api/resource/list` 有 AgentId、无 PictureType | 200，**`Items=null`** |
| 同上 `PictureType=1` | 200，Items 1（Id=2884 Style=103） |
| `GET /backend/package/packagesiteconfig?PackageId=1` | **`status=10037`**；PackageId=7→200 list(0) |
| `GET /backend/channel/showshorturl` | 200，`respond=""`（空串） |
| `GET /backend/channel/16` / `channelurl` / `analyticinfo` / `packageregisterloginconfig` / `defaulttagvenues` / `domain/list` / `package/listall` / `availablechannels` | 200，可读 |

前端列表/层级/域名/落地页已对 `Items=null` 做 `[]` 归一；请空列表返回 `[]`，坏 Sort 返回明确参数错误，上架包配置空时返回 `[]` 而非 `null`。

### FE-069

- 协作状态：已关闭
- 后端解决：—
- 前端验证：通过
- 责任方：前端
- 验证备注：2026-07-22

**现象与修复：**

1. 通道管理默认写死 `PayType=1`，未对齐旧站「`active=0` → 取 TypeList 按 Sort 升序首项」。本环境 Sort 最小为 PayType=12，旧站会先进入该类型。已改为首次用 PayType=1 拉元数据后自动选中 Sort 首项并二次拉列表
2. 普通通道编辑 / 私卡 / USDT 拉会员层级时传 `BeginTime=1&EndTime=1`，本环境 **`Items=null`**（无参或 `0`/`''` 有 DefaultLevel）。已改为不传日期，对齐同页兑换码面板的全量拉法
3. 列表分页在接口缺 `MaxCount` 时回退当前页条数（避免 `Number(null)||n` 语义歧义）

说明：旧站其余父 Tab（全部通道/通道数据/通用规则/存款访问/风控/教程/兑换码管理等）均已注释，NEW 仅挂载「通道管理」与旧站一致。

### API-051

- 协作状态：待反馈
- 后端解决：未处理
- 前端验证：—
- 责任方：后端
- 验证备注：2026-07-22 `leying_niu`

**现象：**

| 场景 | 结果 |
| --- | --- |
| `GET thirdrechargetypeagentconfig/list` PayType=1/2 OnShelf=1 | 200，有 Items；**Pagination 无 MaxCount**（仅 CurrPage/PageSize） |
| 同上 PayType=12（TypeList Sort 首项） | 200，**`Items=null`** |
| 同上 `Sort=-CreateTime` / `CreateTime desc` | **`status=10001`** |
| TypeList | 含 PayType 1/2/3/12/13；Total 仅 1/2/3/10/26/100（12/13 无开闭统计） |
| `GET agentrechargecardconfig/list` PayType=10/212 | 200，**`Items=null`** |
| `GET agentrechargecardconfig/total` | 200，Opened=0 Closed=0 |
| `GET agentrechargeusdtconfig/list` | 200，**`Items=null`**（无 MaxCount） |
| `GET agentrechargeusdtconfig/total` | 200，Opened=0 Closed=0 |
| `GET coindealersupporter/list` / `coindealer/listall` | 200，**`Items=null`** |
| `GET rechargetestchannelquicksetting/list` | 200，**`Items=null`** |
| `GET playerlevel/list?BeginTime=1&EndTime=1` | 200，**`Items=null`** MaxCount=0 |
| 同上无日期 / BeginTime=0 | 200，Items 1（DefaultLevel） |

前端已对多数 `Items=null` 归一为 `[]` 并修正默认选中/层级参数；请列表补齐 `Pagination.MaxCount`，空结果返回 `[]`，坏 Sort 返回明确参数错误，`BeginTime=1` 哨兵勿当真实时间过滤。

### FE-070

- 协作状态：已关闭
- 后端解决：—
- 前端验证：通过
- 责任方：前端
- 验证备注：2026-07-22

**现象与修复：**

1. 提现访问记录（明细/统计）默认日期误用「昨天～昨天」；旧站 `getBeforeDateTimestamp(1,false)` + `getBeforeDateTimestamp()` 实为**今日 0 点～今日结束**。已改今日窗
2. 通道数据默认 `subtract(31)`，旧站 `(31,false)` 经 `days-1` 为 **30 天前～今日**。已改为 30 天
3. 提现配置账户类型写死 `PayType=2`，且可能在类型加载前请求列表；已改为按 Sort 优先选 Status=1，并 `autoLoad:false` 待类型就绪后再拉表
4. `bankconfig/list` 返回顶层数组时统一走 `toListResult` 归一

父 Tab（10978–10983、11961）与通用规则子 Tab（参数/提示/钱包/强制/银行卡/虚拟币/到账时间）均已对照登录态核查。

### API-052

- 协作状态：待反馈
- 后端解决：未处理
- 前端验证：—
- 责任方：后端
- 验证备注：2026-07-22 `leying_niu`

**现象：**

| 场景 | 结果 |
| --- | --- |
| `GET visitstatistic/list` Group=Withdraw SubGroup=Enter（今日/昨日/日期串） | **`status=20001`** 远程服务器未连接 |
| `GET visitstatistic/statisticslist` 同上 | **`status=20001`** |
| `GET withdrawstatistics/list` 近 30 天 | 200，Items 2，MaxCount=21 |
| 同上今日空窗 | 200，**`Items=null`** 且 **`Pagination=null`** |
| `GET bankconfig/list` | 200，**respond 为数组**（非 Items 信封） |
| `GET withdrawtip/list` | 200，**`Items=null`** |
| `GET withdrawwalletbindingconfig/list` | 200，**`Items=null`**（有 SwitchWallet） |
| `GET withdrawtipconfig/whitelist/?WhiteType=0` | **`status=10037`** Parameter error |
| 同上 WhiteType=1/2 | 200，**`Items=null`** |
| `GET withdrawautoconfig/list?Id=1` | **`status=10243`** 方案不存在；真实 Id=9→规则 list(36) |
| `GET agentwithdrawaccount/list` AccountType=1/2 | 200，各 1 条 |
| `GET withdrawpaytypeconfig/list` | 200，3 条（PayType 2/1 开启，3 关闭） |
| `GET thirdwithdrawtypeagentconfig/list` | 200，Items 2 |
| `GET withdrawgameconfig` / `withdrawtipconfig` / `getusdtrate` / `getfirstconfig` / `withdrawestimatedtimeconfig` / `playerbindcardconfig` | 200，可读 |

前端已修默认日期/账户选中；请访问统计恢复连通，空列表返回 `[]` 并带 Pagination，白名单非法 WhiteType 返回明确文案。

### FE-071

- 协作状态：已关闭
- 后端解决：—
- 前端验证：通过
- 责任方：前端
- 验证备注：2026-07-22

**现象与修复：**

1. `GET /backend/domain/list` 在 `Type=1/2/3` 且**不传** `IsAll=1` 时 `Items=null`/`MaxCount=0`；同参加 `IsAll=1` 分别返回 3/2/2 条。`Type=5` 无 IsAll 也可返回。渠道创建/代理域名选域已用 `IsAll:1`，本页列表/导出原先漏传，默认「防封域名」空表。已统一 `buildDomainListQuery` 带 `IsAll:1`
2. 导出 `IsExp=true` 同样依赖 `IsAll=1`，否则 Type1 导出空。已一并带上
3. 10 类 Segmented（旧站「我的域名」子组件 Type1–10）均已登录态核对；购买/主站配置旧站已注释，NEW 未挂，与范围一致

旧站 `fengDomain` 等亦未传 IsAll（本环境同样空），NEW 对齐渠道选域语义补参，避免库存页无数据。

### API-053

- 协作状态：待反馈
- 后端解决：未处理
- 前端验证：—
- 责任方：后端
- 验证备注：2026-07-22 `leying_niu`

**现象：**

| 场景 | 结果 |
| --- | --- |
| `GET domain/list?Type=1&Page=1&PageSize=20`（无 IsAll） | 200，**`Items=null`** `MaxCount=0`（有库存） |
| 同上 + `IsAll=1` | 200，Items 3，MaxCount=3（`channel-test.info` 等） |
| Type=2/3 + IsAll=1 | Items 2/2；无 IsAll → `Items=null` |
| Type=5 无/有 IsAll | 均可 Items 1 |
| Type=4/6–10 + IsAll=1 | 本环境 **`Items=null`** MaxCount=0（暂无库存） |
| `Keyword` 部分/完整域名 + IsAll | 可筛到 1 条 |
| `Domain=channel-test.info&Keyword=` + IsAll | 仍返回 **3** 条（`Domain` 筛选疑似未生效） |
| `InUsed=2` + IsAll Type1 | **`Items=null`**（无停用域） |
| `IsExp=true` 无 IsAll Type1 | **`Items=null`**；有 IsAll→3，`MaxCount=null` |
| `Sort=Foo desc` + IsAll | 仍 200 返回 3 条（坏 Sort 未报错） |
| `PUT domain/edit` Id 空/不存在 | msg **Request parameter format error**（非明确「记录不存在」） |
| `PUT domain/inused` Domain 空 | Record not found |
| `PUT domain/inusedbatch` DomainIds=[] | Data update failed |

请：无 IsAll 时 Type 过滤应返回完整「我的域名」库存或文档标明必传；空结果返回 `[]`；修复 `Domain` 精确筛选；坏 Id 编辑返回明确业务错误；导出补齐 Pagination.MaxCount。

### FE-072

- 协作状态：已关闭
- 后端解决：—
- 前端验证：通过
- 责任方：前端
- 验证备注：2026-07-22

**现象与修复：**

1. 企业包 IPA 上传成功回调仅读 `Data.url`，与渠道上传不一致；缺 `Url`/`Path`/`FileName` 时会误报成功且地址为空。已对齐多字段解析并在无地址时报错
2. `package/list`、`listpackageiosdetail`、上架包 `*/list` 等空结果常 `Items=null`；API 层未归一时调用方偶发脆弱。已 `normalizeListResult`（保留 `MoreItems.Resources`）
3. 企业包游戏搜索无结果时未清空流程/企业包详情态。已 `clearSelectedDetail`
4. iOS 上架包屏蔽安装选项为企业包时未清 `IosPackageId`（旧站注释要求与超级签互斥）。已补清

三 Tab（企业包 / iOS 上架包 / Android 上架包）均已对照登录态核查；超级签旧站已注释，NEW 未挂。

### API-054

- 协作状态：待反馈
- 后端解决：未处理
- 前端验证：—
- 责任方：后端
- 验证备注：2026-07-22 `leying_niu`

**现象：**

| 场景 | 结果 |
| --- | --- |
| `GET package/list` PageSize=5 | 200，Items 1，Resources 1（产品「乐赢网」） |
| 同上 `Sort=CreateTime desc` / `Foo desc` | 200，**Items 空**（空串/`-CreateTime` 正常） |
| `GET channel/listall?PackageId=7` | 200，respond 数组 4 |
| `GET packagelinkios/iospackversion?PackageId=7` | 200，Items 对象（BindChannel/DownUrl 空） |
| `GET packagelinkios/listpackageiosdetail?PackageId=7` | 200，**`Items=null`** 无 Pagination |
| `GET packagelinkios/listall?PackageId=7` | 200，**`Items=null`** |
| `GET iosappstoredata/list`（带/不带日期） | 200，**`Items=null`** 无 Pagination |
| `GET androidappstoreconfig/list` | 200，**`Items=null`** 无 Pagination |
| `GET */analyticinfo?AppPackageConfigId=1` | **`status=10037`** Parameter error |
| `GET countriesconfig/list` PageSize=9999 | 200，**`Items=null`** 无 Pagination（屏蔽国家选项空） |

前端已归一空 Items；请空结果返回 `[]` 并带 Pagination，坏 Sort 返回明确参数错误，国家配置在本环境补齐或说明权限/数据条件。

### FE-073

- 协作状态：已关闭
- 后端解决：—
- 前端验证：通过
- 责任方：前端
- 验证备注：2026-07-22

**现象与修复：**

1. `apifeeswitch/list` 本环境不返回 `LoginStatus`/`WalletStatus`/`ApiFeeName`，页面状态显示 `-` 且开启场馆仍可点维护。已按 `Switch`/`WalletLock` 回退展示与禁用
2. `ApiFee=BTI` 时 `games` 字典无同名项，`findGameIdByApiFee` 失败导致无法开关。已回退 `GroupPlatformGameType`（BTI→1300）
3. 游戏管理默认 `GameId=''`（「全部场馆」），旧站强制首个场馆；空 GameId 列表恒空。已去掉全部并默认/重置为首个 ParentId=0 场馆，补 `Sort:''`
4. 场馆名筛选无匹配时 `respond=null`，`toListResult` 读 `Items` 崩溃。已对 null 归一为空列表

热门排序/特色/大奖等旧站 Tab 已注释，NEW 未挂，范围与旧站一致。

### API-055

- 协作状态：待反馈
- 后端解决：未处理
- 前端验证：—
- 责任方：后端
- 验证备注：2026-07-22 `leying_niu`

**现象：**

| 场景 | 结果 |
| --- | --- |
| `GET apifeeswitch/list`（无参/分页/PageSize=9999） | 200，**respond 为数组** list(1)：`ApiFee=BTI` `Switch=1` `WalletLock=0`，**无** LoginStatus/WalletStatus/ApiFeeName/GameMerchant |
| `GET apifeeswitch/list?ApiFeeName=不存在xxx` | 200，**`respond=null`**（非空 Items/[]） |
| 同上 ApiFeeName=BTI | 200，list(1) |
| `LangText` | 对象串 `{"20":{...}}`（非数组） |
| `GET subgamemaintain/list` GameId 空/`1001`~`1038`/`1300` 等 | 200，**`Items=null`** MaxCount=0（全场馆无库存） |
| `GET /api/game/info` | 200，含 games/platformGameType/GroupPlatformGameType（BTI gameid=1300） |

请 list 补齐 LoginStatus/WalletStatus/ApiFeeName（或文档标明用 Switch/WalletLock），空筛选返回 `[]` 非 null；子游戏在有配置场馆时返回数据或说明本环境无子游戏库存。

### FE-074

- 协作状态：已关闭
- 后端解决：—
- 前端验证：通过
- 责任方：前端
- 验证备注：2026-07-22

**现象与修复：**

1. 通道配置旧站 `fetchSmsChannelList()` **无 Page/PageSize**，一次拉全量后过滤 `AgentId!==0`；NEW 曾传分页导致与旧站不一致。已改为无参拉全量 + 客户端切片分页；下拉仍用全量；接口 10000 时兜底空表
2. 日/月/OTP/召回/模板等列表 `Items=null` 时表格易空指针。API 层统一 `withItems` 归一为 `[]`；概览 `info` null→`{}`
3. 模板编辑 `ApplyGamePackageIds` 旧站存逗号串、打开时 split；NEW 曾直接传多选数组。已 `join(',')` 提交
4. OTP 日报 `Day` 若带时间前缀，精确 `===` 会漏「今日」行。已改为 `startsWith(今天)`
5. 召回开关改为受控乐观更新 + 取消/失败回滚，与旧站 v-model + 取消重拉一致

### API-056

- 协作状态：待反馈
- 后端解决：未处理
- 前端验证：—
- 责任方：后端
- 验证备注：2026-07-22 `leying_niu`

**现象：**

| 场景 | 结果 |
| --- | --- |
| `GET shortmessageservice/info` | 200，ShortMessage=100，IsAutoBuy=1 |
| `GET shortmessageservice/list` 近 30 日→今 | 200，**Items=null** MaxCount=0，Total 全 0 |
| `GET shortmessageservice/monthlist`（空月/有月范围） | 200，**Items=null**，**Pagination=null**，Total 全 0 |
| `GET smschannelconfig/list`（无参 / Page+PageSize） | **status=10000** respond=null（Msg 含嵌套 Code 10000） |
| `GET smsannouncementconfig/list` | 200，Items=[]（无 Pagination） |
| `GET phoneregisterotpday` 近 7 日 | 200，**Items=null**（无 Pagination） |
| `GET phoneregisterotpdetail` 近 7 日 | 200，**Items=null** MaxCount=0，Total 全 0 |
| `GET registerrecall` 近 30 日→昨 YYYY-MM-DD | 200，**Items=null** MaxCount=0，Switch 可读 |
| `GET depositrecall` 前天单日（ymd / unix / 30 日区间） | **status=10000** respond=null（与 register 同参风格却失败） |
| `GET registerrecalldetail` / `depositrecalldetail` | 200，**Items=null** MaxCount=0 |

请修复通道 list、首存召回 list 的 10000（权限或参数文档）；空列表返回 `[]` 并带 Pagination，避免 `Items=null` / `Pagination=null`。

### FE-075

- 协作状态：已关闭
- 后端解决：—
- 前端验证：通过
- 责任方：前端
- 验证备注：2026-07-23

**现象与修复：**

1. 返水审核默认日期误用「昨天」。旧站 `getBeforeDateStr(1)` / `getBeforeDateStr(1, false)` 因 `days=days-1` 实际是**今天** 00:00～23:59。已改回今天
2. 统计「返水状态/发放类型」文案简化，丢 `ReviewAdminName`、Reject 分支。已对齐旧站 `statistics.vue` 判定顺序
3. 手动返水空账号/空产品仍打 `waterlist`→10043。已前端必填校验并 catch 清空
4. `list`/`detaillist`/`reviewlist` 空结果 `Items=null`、订单 detail null、`allscheme` 非数组时列表易崩。API 层统一归一为 `[]`

### API-057

- 协作状态：待反馈
- 后端解决：未处理
- 前端验证：—
- 责任方：后端
- 验证备注：2026-07-23 `leying_niu`

**现象：**

| 场景 | 结果 |
| --- | --- |
| `GET playerbackwaterscheme/allscheme` | 200，list(1)「自定义返水方案」Id=8 |
| `GET playerbackwaterscheme/scheme?Id=8` | 200，Config/Rule 为数组，LangText JSON 对象串 |
| `GET playerbackwaterscheme/scheme` 无 Id | **status=10000** |
| `GET playerbackwaterrecord/list` 今/昨 unix（统计默认） | 200，**Items=null**，Total/Sum/UnSum=0（无 Pagination） |
| `GET playerbackwaterrecord/detaillist` 今日 | 200，**Items=null** Total=0 |
| `GET list` + `Reject=1&Sum=1`（系统申请） | 200，**Items=null** |
| `GET reviewlist` 今日（手动申请） | 200，**Items=null** Count=0 |
| `GET list` AwardTime 日期串 | **status=10000**（须 unix 秒） |
| `GET waterlist` 空账号或空产品 | **status=10043** 参数格式错误 |
| `GET waterlist` LoginAccount=test | **status=10002** Record not found（本环境无该玩家） |
| `GET listcsv` GoogleCode=000000 | **status=10103** Wrong password（验证码校验正常） |

请空列表返回 `[]`；日期串给出明确参数错误；`waterlist` 对缺账号返回可读业务错误而非笼统 10043。

### FE-076

- 协作状态：已关闭
- 后端解决：—
- 前端验证：通过
- 责任方：前端
- 验证备注：2026-07-23

**现象与修复：**

1. 独立页 `addConfig` 保存时若用户未改动游戏比例，可能未带回最新 `Games/WaterAvg|Min|Max`。已 `defineExpose(buildConfig)`，保存前强制同步；初始化后主动 emit
2. 游戏 `Id` 旧站 `parseInt`，NEW 曾提交字符串 key。已改为可解析数字则传 number
3. 离开/保存后未清理 `sessionStorage.backWaterAddConfigContext`，下次直进可能串方案。已清理
4. 保存时显式保留原行 `VipLevel`；按钮顺序对齐旧站（先保存后返回）；标题展示方案名与 VIP

单位换算与旧站一致：金额/比例 `*100` 入库，`MinTurnoverMultiple` 不乘 100；接口 `PUT .../config` `{ Id, Config: JSON.stringify(rows) }`。

### API-058

- 协作状态：待反馈
- 后端解决：未处理
- 前端验证：—
- 责任方：后端
- 验证备注：2026-07-23 `leying_niu`

**现象：**

| 场景 | 结果 |
| --- | --- |
| `GET scheme?Id=8` | 200，Config list(11) VIP0～10；字段齐全 |
| 同上 Config[].Games | 本环境 **全空 `[]`**，金额/比例字段均为 0（页面可编辑但无存量比例） |
| `GET scheme` 无 Id | **status=10000** |
| `GET scheme?Id=999999` | **status=10304** An error occurred while querying... |
| `GET /api/game/info` | 200，`GameSetting` 含 Type=`games`（约 38 款）、platformGameType、GameTypeLangGroup 等 |
| `PUT playerbackwaterscheme/config` | 参数对齐旧站；本轮未做写测（避免改生产方案） |

请坏 Id 返回明确「方案不存在」；空 Games 若属未配置可保持 `[]`（建议文档说明）。

### FE-077

- 协作状态：已关闭
- 后端解决：—
- 前端验证：通过
- 责任方：前端
- 验证备注：2026-07-23 `leying_niu`；对照旧站 `advertisementLoop` / `homeDialog` / `paymentAdvertisement` / `elseAdvertising`

**现象与修复：**

1. 轮播/弹窗保存未正确携带 `RegStartEndDate`（轮播 1/2，弹窗 1/0）；关闭限制时未清空 `RegStartDate/RegEndDate`。已补开关 UI 与保存归一
2. 轮播列表空日期曾传 `undefined`，旧站为空串 `''`。已改为 `?? ''`；弹窗列表仅 `TemplateId+Type`（对齐旧站）
3. `getnoticelist` 顶层 `Title=null`，旧站从 `LangText[langId].Title` 解析。已补解析，避免跳转公告下拉只显示 Id
4. 其他广告恢复预设旧站仅 `{ Id: programmList[0].Id }`，NEW 多传 `SubType`。已去掉
5. 方案/广告列表 `Items=null` / `respond=null` 已在面板 `toItems` 归一为 `[]`；活动/公告加载用 `Array.isArray`
6. 其他广告明细表按旧站权限 `11238` 展示

### API-059

- 协作状态：待反馈
- 后端解决：未处理
- 前端验证：—
- 责任方：后端
- 验证备注：2026-07-23 `leying_niu` @ ccccc-admin.dk888.link

**现象：**

| 场景 | 结果 |
| --- | --- |
| `GET gameadtemplate/list?Type=1/3/6/4` | 200，各 `Items=list(1)`（Id 724/726/729/727） |
| 同上 `Type=4&SubType=1` | 200，**Items=null**（勿带 SubType 拉方案） |
| `GET gameadconfig/list` Type=1 TemplateId=724 | 200，**respond=array(2)**，含 `RegStartEndDate=2` |
| Type=3/6 及 Type=4 SubType1～7 | 200，**respond=null**（空环境） |
| `GET gamevenuerebate/list?TemplateId=727` | 200，respond=array(0) |
| `GET gameadimage/list` 多 Type/SubType/ImageType | 200，**respond=null** |
| `GET gameadconfig/getnoticelist` | 200，array(2)；顶层 Title=null（靠 LangText） |
| `GET gameadconfig/getactivitylist` | 200，**respond=null** |
| `GET gameadtemplate/724` / `999999999` | **status=20002** 远程客户端未连接 |

请空列表统一 `[]`（或 Items=[]）；活动列表空时勿 null；方案详情/坏 Id 返回明确业务码而非 20002。

### FE-078

- 协作状态：已关闭
- 后端解决：—
- 前端验证：通过
- 责任方：前端
- 验证备注：2026-07-23 `leying_niu`；对照旧站 `vipGrade` / `vipUpgrade` / `vipRecord` / `vipIcon`

**现象与修复：**

1. VIP 等级记录默认区间误用「昨天 0 点～今天结束」；旧站为 `getBeforeDateTimestamp(1,false)`～`getBeforeDateTimestamp()`=**今天整天**。已改 `getTodayRangeSeconds`
2. `RangePicker` 设置 `value-format` 后 v-model 变为字符串，`.unix()` 报错导致改期查询失败。已去掉 value-format，统一 `dayjs` 转 unix
3. 状态筛空值改为 `''`（对齐旧站）；补 `PlayerId:''`；`Items=null` 归一为 `[]`
4. 保级天数接口返回 `0` 时旧站不覆盖默认 90；NEW 曾用 `|| 90` 语义接近，已改为仅 `>0` 覆盖
5. 升级系数 `GameInfo` 空串 `''` 解析加固；图标方案列表按 `TemplateId` 去重

### API-060

- 协作状态：待反馈
- 后端解决：未处理
- 前端验证：—
- 责任方：后端
- 验证备注：2026-07-23 `leying_niu` @ ccccc-admin.dk888.link

**现象：**

| 场景 | 结果 |
| --- | --- |
| `GET listvirtualprize` | 200，arr(11) VIP0～10 |
| `GET getviplevelmode` / `getrelegationday` | 200；本环境 `VipLevelMode=1`，`RelegationDay=0`（两接口返回体字段高度重合） |
| `GET vipcoefficientconfig/detail` | 200，`DefaultCoefficient=10000`，**GameInfo=''**（空串非 `{}`/`null`） |
| `GET viplevelconfig/list` 今天 | 200，Items=1，Total 有汇总 |
| 同上 `Status=0` 无数据 | 200，**Items=null**，MaxCount=0 |
| 坏日期 BeginTime=abc | **status=10000** |
| `GET listiconstemplates/` | 200，arr(1)（含 TemplateId=1） |
| `POST listicons/` `{TemplateId:1}` | 200，arr(14) VIP0～13（等级档位数与 listvirtualprize 的 11 档不一致） |
| `POST listicons/` `{}` | 200，仍返回 arr(14)（缺 TemplateId 未报错） |

请空列表统一 `Items=[]`；`GameInfo` 空配置返回 `{}` 或 `null`；`listicons` 缺 TemplateId 应 4xx/业务码；图标档位与等级配置档位数建议对齐说明。

### FE-079

- 协作状态：已关闭
- 后端解决：—
- 前端验证：通过
- 责任方：前端
- 验证备注：2026-07-23 `leying_niu`；对照旧站 `gameHelp` / `commonSetting` / `sports-tutorial` / `exchangeRateSetting` / `systemTemplates/*`（父页 Tab2～5 源码仍在，虽旧站入口曾注释）

**现象与修复：**

1. 游戏帮助 `currentLangGroupId` 未优先 Default 语言组，多语言内容可能展示为空。已对齐旧站优先 Default，并在缺省时拉 `config/info`
2. 通用规则未确保项目语言组：LangGroup 空时可能保存空 `LangText=[]` 覆盖提示。已拉配置；`Value/LangText` 空串/`null` 解析加固；无语言组禁止保存
3. 体育教程 / 汇率 / 系统模板对 `respond=null`、`Items=null` 归一为 `[]`；汇率列表补 `Keyword:''`
4. 场馆模板 `ClientClassify` 匹配对齐旧站 Number/String；代理成员加载失败时提示而非静默空 Transfer

### API-061

- 协作状态：待反馈
- 后端解决：未处理
- 前端验证：—
- 责任方：后端
- 验证备注：2026-07-23 `leying_niu` @ ccccc-admin.dk888.link

**现象：**

| 场景 | 结果 |
| --- | --- |
| `GET gamehelpcenter/list` | 200，arr(5) |
| `GET gamehelpcenter/listall` | 200，arr(18)；LangText 为语言组对象 map |
| `GET phonecountrycode/blocklist` | 200，Value.code 22 段；LangText 有提示 |
| `GET sportsconfig/list?ConfigType=1` | 200，**Items=null**，MaxCount=0 |
| `GET operation/exchangeratesetting?Page&PageSize` | 200，**respond=null**（消息「操作成功」） |
| `GET netcashconfig/list?Type=1/2` | 200，**respond=null** |
| `GET config/getchildadmininfo` | 200，ChildAdminInfo=38 |
| `DELETE sportsconfig/999999999` | **status=10305** |

请空列表统一 `Items=[]` 或 `respond=[]`；坏 Id 删除返回明确「不存在」文案。

### FE-080

- 协作状态：已关闭
- 后端解决：—
- 前端验证：通过
- 责任方：前端
- 验证备注：2026-07-23 `leying_niu`；对照旧站 `generalizeManageact.vue`（本页无子 Tab；树表懒加载下级）

**现象与修复：**

1. `promoter/list` 空环境返回 `Items=null` / `ItemsTotal=null`，NEW `normalizeRows(result.Items)` 对 null 调用 `.map` 会白屏。已在 API 层与页面双端归一为 `[]`
2. 创建时间列仅有 `key` 无 `dataIndex`，排序 `field` 可能为空。已补 `dataIndex: CreateTime`，`columnKey` 兜底
3. 状态/结算方式清空后对齐旧站传 `''`；查询补 `Keyword:''`；域名列表 `Items=null` 归一

### API-062

- 协作状态：待反馈
- 后端解决：未处理
- 前端验证：—
- 责任方：后端
- 验证备注：2026-07-23 `leying_niu` @ ccccc-admin.dk888.link

**现象：**

| 场景 | 结果 |
| --- | --- |
| `GET promoter/list` 默认分页（含空筛） | 200，**Items=null**，**ItemsTotal=null**，MaxCount=0；Config.Domain='' |
| `GET promoter/list?Status=1` | 同上空 |
| `GET domain/list?Type=10` | 200，**Items=null**，MaxCount=0 |
| `GET promoter/999999999` | **status=10002** 无法找到该记录 |

请空列表统一 `Items=[]` / `ItemsTotal=[]`（保留 Pagination/Config）。

### FE-081

- 协作状态：已关闭
- 后端解决：—
- 前端验证：通过
- 责任方：前端
- 验证备注：2026-07-23 `leying_niu`；对照旧站 `addGeneralize.vue`（三步：账号信息 → 协作配置 → 信息确认；无子 Tab）

**现象与修复：**

1. 编辑态 `promoter/:id` 成功后若 `getadminteams` 返回 20001，整段 `loadDetail` 失败，账号信息也丢失。已将直属分组请求与详情解耦，失败时提示并回落默认分组
2. `sportsteamerteam/list` / `getadminteams` 空 `Items`/`Teams` 未归一；API 层统一 `[]`；无分组时禁用提交
3. 确认步未展示密码（旧站有）；备注长度 255 与旧站 `validRemark`（≤400）不一致。已对齐

**参数核对（与旧站一致，非本轮前端错误）：** 创建需 `Hash`；`RoleDataField` JSON `{HaveFunction}`；比例类 SettleType 4～7 写入 `SettlePrice`；`TeamIds`/`QingLiu`；编辑带 `Id`/`Status`。

### API-063

- 协作状态：待反馈
- 后端解决：未处理
- 前端验证：—
- 责任方：后端
- 验证备注：2026-07-23 `leying_niu` @ ccccc-admin.dk888.link

**现象：**

| 场景 | 结果 |
| --- | --- |
| `GET sportsteamerteam/list`（无参 / Page+PageSize） | **status=20001**「远程服务器未连接,操作失败」，respond=null |
| `GET sportsteamerteam/getadminteams`（无 AdminId / 空 / AdminId=1） | 同上 **20001** |
| `GET promoter/list`（创建页入口同源） | 200，**Items=null**（本环境无推广员可测详情回填） |
| `GET promoter/999999999` | **status=10002** 无法找到该记录 |

请恢复直播分组服务连通性；空列表统一 `Items=[]`/`Teams=[]`；坏 Id 保持明确文案即可。

### FE-082

- 协作状态：已关闭
- 后端解决：—
- 前端验证：通过
- 责任方：前端
- 验证备注：2026-07-23 `leying_niu`；对照旧站 `promoteData.vue` + `generalizeData/*`（五 Tab：账户数据 / 落地页转化 / 无效用户 / 数据填写 / 渠道回本）

**现象与修复：**

1. 默认日期 `subtract(7)` 成 8 日窗，旧站 `getBeforeDateStr(7)` 实为近 7 日。已改 `subtract(6)`；账户/数据填写补 `maxRangeDays` 6/30
2. 利润 ROI 误对成本乘汇率，已对齐旧站（成本不乘汇率）
3. 渠道回本缺未到期周期遮罩，已移植旧站 `formatValue`；模糊查询恢复账号/渠道下拉
4. `Items`/`Item`/`ItemsCost`/汇率/落地页空值归一；各 Tab 查询失败兜底空表

### API-064

- 协作状态：待反馈
- 后端解决：未处理
- 前端验证：—
- 责任方：后端
- 验证备注：2026-07-23 `leying_niu` @ ccccc-admin.dk888.link

**现象：**

| 场景 | 结果 |
| --- | --- |
| `GET promotedata/channelreport` 近 7 日 | 200，Items 有数据；**ItemsCost=null**；空未来日 Items=null 但 ItemsTotal 仍有行 |
| `GET promotedata/getsumrecord` | 200，Item 有分页数据 |
| `GET promotedata/invaliduser` | 200，Items 有计数字段（本环境设备相关多为 0） |
| `GET handrecord/list` | 200，**Items=null**，MaxCount=0 |
| `GET handrecord/999999999` | **status=10001**「数据库异常，请联系管理员」 |
| `GET landingpage/listall` | 200，**respond=null** |
| `GET operation/exchangeratesetting`（无参/带分页） | 200，**respond=null** |
| `GET operation/channelbreakevenreport`（周报/日报，多组参数） | **status=10103**「密码错误」 |

请：回本报表勿误报 10103；空列表 `Items=[]`；坏 Id 用明确「不存在」；落地页/汇率空返回 `[]`。

### FE-083

- 协作状态：已关闭
- 后端解决：—
- 前端验证：通过
- 责任方：前端
- 验证备注：2026-07-23 `leying_niu`；对照旧站 `timeshareData.vue` + 七个子指标组件（柱/折/表三种视图）

**现象与修复：**

1. 权限：旧站仅 `AdminType===2` 裁剪 Tab；NEW 对非 1 一律校验权限，可能误藏 Tab。已对齐「仅 2 裁剪」
2. 日期：补齐旧站 `limit-number=7` 的跨度限制；查询失败兜底空数据
3. 图例选中态跨指标 Tab 不共享（旧站 vuex），已提到模块级 `timeshareLegendSelected`

**参数核对：** `GET /backend/promotedata/hourreport` 使用 `AdminId`/`ChannelId`（逗号拼接）+ `BeginTime`/`EndTime`（近 7 日），与旧站一致。金额类 `/100` 与旧站一致。

### API-065

- 协作状态：待反馈
- 后端解决：未处理
- 前端验证：—
- 责任方：后端
- 验证备注：2026-07-23 `leying_niu` @ ccccc-admin.dk888.link

**现象：**

| 场景 | 结果 |
| --- | --- |
| `GET hourreport` 近 7 日（AdminId/ChannelId 空） | 200，Items=7 组×24 小时；有 SumReg/付费/兑换等非零 |
| 同上 8 日窗 | 200，Items=8 组 |
| 未来日期区间 | 200，仍返回 7 组**全 0 骨架**（非空），易误导为「有数据」 |
| 无 Begin/End | 200，默认返回多日骨架 |
| `BeginTime=abc&EndTime=def` | **status=10000** 请求操作失败 |

请未来/无有效日期返回 `Items=[]`（或明确 Message）；非法日期保持明确错误即可。

### FE-084

- 协作状态：已关闭
- 后端解决：—
- 前端验证：通过
- 责任方：前端
- 验证备注：2026-07-23 `leying_niu`；对照旧站 `teamDaily.vue` + `tabOne`（提现 TeamType=1）/`tabTwo`（利润 TeamType=2）；TeamType=0 时双 Tab

**现象与修复：**

1. 补齐旧站 `limit-number=31` 的日期跨度限制；查询失败兜底空表/空汇总
2. `HistoryItems=null` API 层与页面双端归一；`AdminId` 空选传 `''`；利润模式账号下拉仅 `TeamType===2`（对齐 `team-zong`）
3. `PageSize=200` 覆盖近 31 日明细（旧站用全局 pageSize，易截断）

**参数核对：** `GET /backend/accountteamdaily/list`：`Page/PageSize/Sort/AdminId/TeamType/BeginTime/EndTime`（unix 秒，近 31 日），与旧站一致。金额 `/100` 展示。

### API-066

- 协作状态：待反馈
- 后端解决：未处理
- 前端验证：—
- 责任方：后端
- 验证备注：2026-07-23 `leying_niu` @ ccccc-admin.dk888.link（本账号 `AccountTeamInfo.TeamType=0`，`ChildAccountTeam=null`）

**现象：**

| 场景 | 结果 |
| --- | --- |
| `TeamType=1/2` + 近 31 日 unix | 200，TodayItems/BannerItems 为**全 0 对象**；**HistoryItems=null**；Pagination=null |
| 未来日期 | 同上空/全 0 |
| 无 Begin/End | 同上 |
| `BeginTime=abc` | **status=10000** |
| `config/info` ChildAccountTeam | **null**（下拉无子团队账号） |

请空历史返回 `HistoryItems=[]`；无业务数据时 Today/Banner 可保持结构但建议前端可识别的空态；非法日期保持明确错误。

### FE-085

- 协作状态：已关闭
- 后端解决：—
- 前端验证：通过
- 责任方：前端
- 验证备注：2026-07-23 `leying_niu`；对照旧站 `teamQuery.vue`（本页无子 Tab；支持层级下钻面包屑）

**现象与修复：**

1. `Items=null` 归一为 `[]`；缺 `Pagination` 时 MaxCount 兜底 0
2. 补齐旧站 `limit-number=30` 日期跨度限制；`AdminId` 空传 `''`；查询失败不再重抛（避免表格二次报错）
3. 参数与列计算（自营+下级、分成 `/10`、金额 `/100`）对齐旧站

**参数核对：** `GET /backend/accountteamsearch/list`：`Page/PageSize/Sort/AdminId/AdminUsername/BeginTime/EndTime`（unix 秒，近 31 日），与旧站一致。

### API-067

- 协作状态：待反馈
- 后端解决：未处理
- 前端验证：—
- 责任方：后端
- 验证备注：2026-07-23 `leying_niu` @ ccccc-admin.dk888.link

**现象：**

| 场景 | 结果 |
| --- | --- |
| 默认近 31 日 + 空 AdminId/AdminUsername | 200，**Items=null**，**Items2=null**，**无 Pagination** |
| AdminUsername=test / AdminId=1 | 同上空 |
| 无 Begin/End | 同上空 |
| `BeginTime=abc` | **status=10000** |

请空列表统一 `Items=[]` 并返回 `Pagination`（MaxCount=0）；非法日期保持明确错误。

### FE-086

- 协作状态：已关闭
- 后端解决：—
- 前端验证：通过
- 责任方：前端
- 验证备注：2026-07-23 `leying_niu`；对照旧站 `closeOrder.vue`（本页无子 Tab；结束订单弹窗为子页面）

**现象与修复：**

1. 状态多选 `allow-clear` 后 `filterStatus` 可能为 `undefined`，`join` 导致查询崩溃 → 已兜底 `(|| []).join(',')`
2. `Items`/`MoreItems` 空值归一为 `[]`；汇总按 Status 1/2/3/4 取 `SumNum`/`SumNumRate`；失败不再重抛
3. 「显示更多列」偏好改为与旧站一致的 `boolean[]`（`teamCloseOrder`）；开始处理后按 `OrderId+Status=2` 刷新再开结束弹窗，避免 `IsYourSure` 误判
4. 结束弹窗 `IsYourSure` 对齐旧站 `UpdateAdminId != Admin.Id`；写接口失败由 requestClient 提示

**参数核对：** `GET /backend/accountteamwithdrawapply/list`：`Page/PageSize/Sort/AdminUserName/OrderId/BeginTime/EndTime/Status`（Status 为逗号拼接，日期 unix 秒，旧站 SearchTypeTwo 默认无跨度限制）  
写操作：`POST .../applydo` `{Id}`；`POST .../applyfinish` `{Id,IsAccept,IsYourSure,Desc}`，与旧站一致。

### API-068

- 协作状态：待反馈
- 后端解决：未处理
- 前端验证：—
- 责任方：后端
- 验证备注：2026-07-23 `leying_niu` @ ccccc-admin.dk888.link

**现象：**

| 场景 | 结果 |
| --- | --- |
| 默认空筛 / Status=1\|2\|3,4 / 近 90 日 / 坏 OrderId / Page=999 | 200，**Items=null**，**MoreItems=null**，Pagination MaxCount=0（有分页壳） |
| 仅 `Page/PageSize` | 同上 |
| `BeginTime=abc` | **status=10000**，msg=null，respond=null |
| `POST applydo` Id=999999999 | **status=10402**，msg=null |
| `POST applyfinish` 坏 Id | **status=10403**，msg=null |

请空列表统一 `Items=[]`、`MoreItems=[]`；业务错误码补充可读 `msg`；非法日期保持明确错误。

### FE-087

- 协作状态：已关闭
- 后端解决：—
- 前端验证：通过
- 责任方：前端
- 验证备注：2026-07-23 `leying_niu`；对照旧站 `closeManage.vue` + `teamWithdrawAccount.vue`（子页：提现弹窗 / 提现账号管理 / 安全设置）

**现象与修复：**

1. 资金类型多选 `allow-clear` 后 `MoneyType.join` 可能崩溃 → 已兜底
2. `Items=null` / `MoreItems.PayRate` 归一；列表查询失败不再重抛；`loadMeta` 用 `allSettled` 隔离账户列表与登录信息
3. 提现金额区间校验对齐旧站（仅当 min/max 均非 0）；编辑账号 `AccountType` 用 `??` 避免误落到支付宝
4. 提现/账号增删改失败由 requestClient 提示，不再误报成功

**参数核对：**
- `GET /backend/accountteamwithdraw/list`：`Page/PageSize/Sort/MoneyType/BeginTime/EndTime`（unix 秒；MoneyType 逗号拼接；旧站 SearchTypeTwo 默认无跨度限制）
- `GET /backend/accountlogin/info`：无参
- `GET /backend/accountteambank/list`：无参（respond 可为数组）
- `POST /backend/accountteamwithdraw/`：`{GetMoney,AccountNo,Account,PrivatePassword,Hash}`
- 账号 CRUD：`POST/PUT /backend/accountteambank/`、`GET/DELETE /backend/accountteambank/:id`；验证码 `GET /api/phonevalidcode/`
- 安全设置：`POST .../privatepassword`、`.../phone`、`.../deletephone`

### API-069

- 协作状态：待反馈
- 后端解决：未处理
- 前端验证：—
- 责任方：后端
- 验证备注：2026-07-23 `leying_niu` @ ccccc-admin.dk888.link

**现象：**

| 场景 | 结果 |
| --- | --- |
| list 默认/按类型/近 90 日 | 200，**Items=null**；MoreItems 有 Money/FreezeMoney/PayRate；Pagination MaxCount=0 |
| `BeginTime=abc` | **status=10000**，msg=null |
| `accountteambank/list` | 200，**respond=null**（应 `[]`） |
| `accountlogin/info` | 200，Phone 空，IsSetPrivatePassword=2 |
| `POST accountteamwithdraw/` 坏参 | **status=10002**，msg=null |
| `POST accountteambank/` 坏验证码 | **status=10173**，msg=null |
| `GET/DELETE accountteambank/坏Id` | **10001** / **10002**，msg=null |

请空资金明细 `Items=[]`；空银行卡列表 `respond=[]`；业务错误补充可读 `msg`。

### FE-088

- 协作状态：已关闭
- 后端解决：—
- 前端验证：通过
- 责任方：前端
- 验证备注：2026-07-23 `leying_niu`；对照旧站 `brokerageSet.vue`（本页无子 Tab；编辑/批量设置为弹窗）

**现象与修复：**

1. 列表查询补齐旧站字段 `Sort/PlayerId/PlayerName/GameId`；`PageSize=500` 拉取全量覆盖项（页面无分页，树由游戏配置合并）
2. `Items=null` 归一；查询/游戏配置失败兜底空树；保存/批量/恢复失败不再误报成功
3. 操作列始终挂载，按钮仍受 `10858` + `resType!=9` 控制（避免 setup 时权限未就绪漏列）
4. 业务保持：Rate÷10 展示、编辑 create/update、`resType==8` 联动子游戏 batchupdate、恢复需 Hash

**参数核对：**
- `GET /backend/accountteamgamerate/list`：`Page/PageSize/Sort/PlayerId/PlayerName/GameId`
- `POST/PUT /backend/accountteamgamerate/`：`{GameId,Rate,Desc[,Id][,Hash]}`（Rate 为系数×10）
- `POST .../batchupdate`：`{GameId[],Rate,Desc}`
- `POST .../recover`：`{Hash}`

### API-070

- 协作状态：待反馈
- 后端解决：未处理
- 前端验证：—
- 责任方：后端
- 验证备注：2026-07-23 `leying_niu` @ ccccc-admin.dk888.link

**现象：**

| 场景 | 结果 |
| --- | --- |
| list 默认/旧站全参/PageSize=1 | 200，**Items=null**，**TeamGameDefaultRate=25**，无 Pagination |
| `POST /` 坏 GameId | **status=10000**，msg=null |
| `PUT /` 坏 Id | **status=10002**，msg=null |
| `POST batchupdate` GameId=[999999] | **status=200**（无效游戏仍成功，风险） |
| `POST recover` 空 body `{}` | **status=200**（无 Hash 仍成功，风险） |

请空覆盖列表返回 `Items=[]`；非法 GameId/缺 Hash 的写操作应拒绝并返回可读 `msg`；`batchupdate`/`recover` 勿对无效请求直接 200。

---

## 模块核查总表（dashboard/index）

| 模块 | 组件 | 接口 | 参数 vs 旧站 | 登录态结论 | 编号 |
| --- | --- | --- | --- | --- | --- |
| 今日数据 KPI+折线 | `panel-metrics` + `trend-chart` | `reportinfo` | 一致 | `Items` 正常；`TotalHours` 异常 | API-001 |
| Banner 四卡 | `banner-stats` | 复用 `Items` 今日行 | 一致 | 有字段；充值相关多为 0 | 通过（数据量） |
| 今日运营 | `today-ops-panel` | `reporttoday` | 无参一致 | 充值日表有；15 分钟/风控空；缺名 | API-003 + FE-001/002 |
| 盈利/亏损玩家 | `profit-panels` | `goldbyplayergame` | 三空串一致 | 有数据可展示 | 通过 |
| 盈利/亏损游戏 | `profit-panels` | `rankbygameid` | 无参+映射一致 | 盈利有、亏损 `ItemsWin=null` | API-002 |
| 渠道今日 | `channel-today-table` | `reportchannelinfo` | `{}`≈`Sort:''` | `RealtimeItems` 有 1 条 | 通过 |

## 模块核查总表（dashboard/onlineSummary）

本页无子 Tab；区块：在线人数卡 + 折线、设备饼图、场馆分布、地区分布（表+地图）。权限：`10634` 图表区 / `13197` 分布区。

| 模块 | 组件 | 接口 | 参数 vs 旧站 | 登录态结论 | 编号 |
| --- | --- | --- | --- | --- | --- |
| 在线人数卡 | `online-count-card` | 复用 `reportinfo.Items` | 一致 | 今日 `SumOnlinePlayerNum=1` | 通过 |
| 在线人数折线 | `trend-chart` | `reportinfo.TotalCount` | 无参/自选日期一致 | 点位全 `Count=0` | API-004 |
| 在线设备分布 | `online-pie-chart` | `onlinesummary.DeviceResult` | 无参一致 | 有 `DevicePlatform` | 通过 |
| 在线场馆分布 | `online-distribution-panels` | `onlinesummary.GameResult` | 无参一致 | `LastGameId=0`→`-` | API-005 |
| 在线用户地区 | 表 + `online-world-map` | `onlinesummary.MapResult` | 无参一致 | `IpDetailName=""` | API-005 + FE-003 |

## 模块核查总表（operationalData/everydayData）

权限：日报 `10688`+role1 / 上架包 `10689`+role2 / 设备 `12160` / VIP `12450`。日期实测区间默认约 `2026-07-01`~`2026-07-21`。

| 模块 | 组件 | 接口 | 参数 vs 旧站 | 登录态结论 | 编号 |
| --- | --- | --- | --- | --- | --- |
| 日报·实时 | `daily-report-panel` | `dailyreport?SearchType=today` | 一致 | `TodayItems` 有投注/充值 | 通过 |
| 日报·历史 | 同上 | `dailyreport?SearchType=old` | 一致 | `Items` 20 行 | 通过 |
| 日报·汇总/图 | 同上 | `dailyreporttotal` | 一致 | `BannerItems` 有值 | 通过 |
| 上架包·选项 | `package-daily-panel` | `iosappstoredata/list` | 一致 | `Items=null` | API-006 |
| 上架包·报表 | 同上 | `getiosappstoredata` | 一致 | 明细 `null`；Banner 疑串公司汇总 | API-006 |
| 上架包·今日导出 | 同上 | `todayappstorecsv` | 一致 | `Items=null` | API-006 |
| 设备日报 | `device-daily-panel` → dimension | `devicedayreport` | 已对齐并补分页 | 实时/历史有数据 | 通过 + FE-004 |
| VIP日报 | `vip-daily-panel` → dimension | `vipdayreport` | 已对齐并补分页 | 实时/历史有数据；历史需翻页 | 通过 + FE-004 |

## 模块核查总表（operationalData/groupDaily）

本页无子 Tab；权限：`10686` 查看 / `10687` 导出。对照旧站 `groupDaily.vue`。默认日期：前天单日。

| 模块 | 组件 | 接口 | 参数 vs 旧站 | 登录态结论 | 编号 |
| --- | --- | --- | --- | --- | --- |
| 分组筛选 | `group-daily-panel` Cascader | `grouplist?IsTop=1` | 一致 | 仅根 `Id=0`，无子节点 | 通过（树稀疏） |
| 分组明细 | 同上 + `group-daily-table` | `dailyreport` | Level/ParentTreeStr/分页一致 | `Items=null`，`MaxCount=0` | API-007 |
| 总计行 | 同上 | `dailyreport?SearchType=total` | 一致 | 有 `BannerItems`；人数>0 金额 0 | API-007 |
| 下钻 | 点击一~四级 | 重查 + Level | 样式标记与旧站一致 | 本环境无明细可点 | 前端逻辑通过 |
| 导出 | 导出 Excel | `dailyreport&IsExp` | 一致（新站 PageSize=10000） | 无明细可导出 | 随 API-007 |

## 模块核查总表（operationalData/channelData）

权限：今日 `10662` / 历史 `10663`；内层代理今日 `10665`、历史 `10684`；渠道 `12170`；导出今日 `10666`、历史 `10683`。对照旧站仅「今日/历史」两 Tab（设备/VIP/上架包在 everydayData）。

| 模块 | 组件 | 接口 | 参数 vs 旧站 | 登录态结论 | 编号 |
| --- | --- | --- | --- | --- | --- |
| 今日·代理 | `channel-report-panel` | `channelreport?SearchType=today` | Sort=`-sum_bet_game_money`；无日期控件 | `RealTimeItems` 有数据，`MaxCount=37` | 通过 |
| 今日·渠道 | 同上 | `channelreportbychannel?today` | 同上 | `RealTimeItems` 有数据，`MaxCount=84` | 通过 |
| 历史·代理 | 同上 | `channelreport?old` | Sort=`-ReportDay`；日报/月报/区间 | `Items`+`TotalItems` 有数据 | 通过 |
| 历史·渠道 | 同上 | `channelreportbychannel?old` | 同历史；合计取 `TotalRealTimeItems` | `RealTimeItems` 有数据 | 通过 |
| 历史·区间报日期 | `channel-report-table` | （同上） | 应对齐查询区间 | 行内 Begin/End 为空 | FE-005 |
| 月报列 | 同上 | `ReportType=2` | 显示场馆费/净输赢 | `RealCleanMoney` 有值 | 通过 |

## 模块核查总表（operationalData/ranking）

权限：输赢 `10638`（赢 `10642`/输 `10643`）、充兑 `10639`（充 `10644`/兑 `10645`）、游戏 `10640`（盈 `10647`/亏 `10648`）、无限代理 `10641`+`10777`。默认日期：昨天 00:00～23:59（unix）。`Limit=100`。

| 模块 | 组件 | 接口 | 参数 vs 旧站 | 登录态结论 | 编号 |
| --- | --- | --- | --- | --- | --- |
| 输赢排行 | `win-loss-rank-panel` | `gamewinrank` | 一致；Users 合并 + SumAddGold 正负过滤 | 赢 3 / 输 98，可展示 | 通过 |
| 充兑排行 | `pay-rank-panel` | `gamepaymentrank` + `gamewithdrawrank` | 一致；金额 `SumGold` | 充 1 / 兑 2，可展示 | 通过 |
| 游戏排行·盈利 | `game-rank-panel` | `realgamewinrank` | 计算列对齐旧站 | `ItemsWin` 有 8 条 | 通过 |
| 游戏排行·亏损 | 同上 | 同上 `ItemsLose` | 映射与旧站一致（非 dashboard 反转） | `ItemsLose=null` | API-008 |
| 无限代理 | `endless-rank-panel` | `wnlimitedproxydata/ranking` | 一致（含 DataSearchType） | `Items=null` | API-009 |

## 模块核查总表（operationalManage/rechargeList）

**约定：** 含内层 Radio 的子 Tab 须逐项实打。9 个顶层 Tab 权限与旧站一致。

### 顶层 Tab

| 模块 | 组件 | 接口 | 参数 vs 旧站 | 登录态结论 | 编号 |
| --- | --- | --- | --- | --- | --- |
| 通道充值 | `aisle-recharge` | `playerpayment/list` | Sort 已对齐 `-field` | 有数据 | 通过 + FE-006 |
| 快捷充值 | `fast-recharge` | （见子 Tab） | — | — | 见下表 |
| 充值黑名单 | `recharge-blacklist-tabs` | （见子 Tab） | — | — | 见下表 |
| 充值次数设置 | `recharge-limit-settings` | `rechargelimitconfig/list` | 一致 | `Items=null` | 通过（空配置） |
| 充值统计 | `recharge-stats` | `golobalgametipconfig/list` | 当月一致 | 4 条 | 通过 |
| 代存记录 | `credit-record-list` | `agentcreditlimittransaction/list` | 新站默认昨日（旧站可空） | 空 | 通过（空数据） |
| 自助查单 | `self-check-tabs` | （见子 Tab） | — | — | 见下表 |
| 失败记录 | `error-record-list` | `playerpayment/faillist` | 一致 | 昨日 5 条 | 通过 |
| CP补单 | `cp-reissue-list` | `cpspayment/list` | 一致 | `10010` 权限不足 | 权限/环境 |

### 子 Tab（快捷充值）

| 子 Tab | 权限 | 接口 | 登录态结论 | 编号 |
| --- | --- | --- | --- | --- |
| 快捷支付 | 12333 | `playereasyrecharge/list?PayType=10` | 近 30 日无单；复议 Status/权限已修 | 通过 + FE-007 |
| USDT快捷 | 12337 | `playerusdteasyrecharge/list` | 无单；复议 Status===3 本正确 | 通过（空数据） |
| 兑换码充值 | （表 12333） | `playereasyrecharge?PayType=212` | 无单 | 通过（空数据） |

### 子 Tab（充值黑名单）

| 子 Tab | 权限 | 接口 | 登录态结论 | 编号 |
| --- | --- | --- | --- | --- |
| 游戏账号 | 10283 / 表 10285 | `rechargeblackplayer/list` + `rechargeblackconfig` | 空日期有 2 条；配置可读 | 通过 + FE-008 |
| 设备号 | 10284 / 表 10293 | `rechargeblackdevice/list` | `MaxCount=0` | 通过（空数据） |

### 子 Tab（自助查单）

| 子 Tab | 权限 | 接口 | 登录态结论 | 编号 |
| --- | --- | --- | --- | --- |
| 自助查单 | 12260 | `playerpaymentselfreview/list` | `MaxCount=0` | 通过（空单） |
| └ 开关/图标 | — | `checkconfig` / `icon` | checkconfig=`10021`；icon 空数组 | API-011 |
| 访问记录 | 12265 | （再分明细/统计） | — | 见下 |
| └ 明细 | 12266 | `visitstatistic/selfreviewlist` | `20001` 远程未连接 | API-010 |
| └ 统计 | 12267 / 表 12270 | `visitstatistic/selfreviewstatisticslist` | `20001` 远程未连接 | API-010 |

## 模块核查总表（operationalManage/withdrawList）

**约定：** 本页无内层 Radio 子 Tab；8 个顶层 Tab 均已登录态实打。对照旧站 `withdrawList.vue` + `withdrawList/*`。

| 模块 | 权限 | 组件 | 接口 | 参数 vs 旧站 | 登录态结论 | 编号 |
| --- | --- | --- | --- | --- | --- | --- |
| 兑换列表 | 10346 / 表 10352 | `redeem-list` | `playerwithdraw/list` | Sort 已对齐 `-field`；默认昨日 | 昨日空；30 日 80 条 | 通过 + FE-009 |
| 提现白名单 | 10347 / 表 10366 | `withdraw-white-list` | `withdrawwhitelist/list` | 无默认日期 | `MaxCount=0` | 通过（空配置） |
| 派单管理 | 10348 / 表 10371 | `send-order-manage` | `usermanage/list` + `getswitch2` | 开关读 Status | 人员空；开关 `Status=0` | 通过 + FE-013 |
| 派单列表 | 10349 / 表 10377 | `send-order-list` | `withdrawsendorderlist/list` | 昨日+`RiskStatus=-1` | 近 90 日仍空 | 通过（空数据） |
| 出款列表 | 10350 / 表 10382 | `withdraw-finance-list` | `withdrawfinancelist/list` | `OrderIds`；表权限 10382 | 昨日空；30 日 44 条 | 通过 + FE-011/012 |
| 订单统计 | 10387 | `withdraw-order-stat` | `playerwithdrawreport/list` | 默认当月 | 当月 20 条 | 通过 + FE-010 |
| 三方冲正记录 | 13164\|\|13165 | `withdraw-third-party-record` | `playerwithdraw/revertedlist` | 昨日 unix | 空；Pagination 无 MaxCount | API-012 + FE-012 |
| 提款流水 | 13230 | `withdraw-water-list` | `playerwithdrawflow/list` + `flowList` | 昨日 unix | 流水空；配置 Switch=2 | 通过（空数据） |

## 模块核查总表（operationalManage/applyService）

**约定：** 本页无子 Tab（旧站 `applyService.vue` 仅包一层 `playerOrderPage` 且 `inServiceManage=false`）。权限页签 `10925||10079`。

| 模块 | 权限 | 组件 | 接口 | 参数 vs 旧站 | 登录态结论 | 编号 |
| --- | --- | --- | --- | --- | --- | --- |
| 客服工单列表 | 10925 / 10079 | `apply-service-panel` | `customsupport/list` | 默认今天；Sort `-field`；Status 多选 | 近 730 日 `MaxCount=0` | 通过（空数据）+ FE-014/015/016 |
| 问题类型下拉 | 12436 | 同上 | `customersupporterworkquestiontype/list` | `Enabled=0` | 12 条 | 通过 |
| 结束理由下拉 | — | 同上 | `customersupporterquestiontype/list` | `Type=0` | 8 条 | 通过 |
| 通过/驳回 | 12437 / 无门禁 | 同上 | `handle` / `reject` | `HandleType=3` Status=1 | 无处理中单可操作 | 通过（逻辑已对齐） |
| 查看 | 12438\|\|12439 | 同上 | list by LoginAccount | 历史工单 | 无单可点 | 通过 + FE-016 |
| 导出 | 12546\|\|12547 | 同上 | list `IsExp=true` | 一致 | 空 | 通过（空数据） |

## 模块核查总表（operationalManage/gameNotice）

**约定：** 顶层 Tab「游戏公告 / 邮件通知」；点击标题进入子页 `noticeTitle` / `emailTitle`（内层明细/统计须实打）。全服公告/跑马灯旧站已下线，新站无。

### 顶层 Tab

| 模块 | 权限 | 组件 | 接口 | 参数 vs 旧站 | 登录态结论 | 编号 |
| --- | --- | --- | --- | --- | --- | --- |
| 游戏公告 | 10071 / 表 10073 | `game-notice-list-panel` | `gamenotice/list` | 无默认日期；Sort `-field` | 8 条可展示 | 通过 + FE-017/018 |
| 邮件通知 | 10072 / 表 10077 | `game-email-list-panel` | `gameemail/list` | 无默认日期 | `MaxCount=0` | 通过（空数据）+ FE-018/019 |
| 邮件表单能力 | 10078 | `game-email-form-modal` | `gameemail/` | 字段少于旧站 | 列表空；表单缩水 | FE-020 |

### 子页（公告访问 noticeTitle）

| 子 Tab | 权限 | 接口 | 登录态结论 | 编号 |
| --- | --- | --- | --- | --- |
| 明细 | 11942 / 载 11946 | `visitstatistic/list?Group=Dialog` | 默认今天已对齐；`20001` 远程未连接 | FE-054 + API-013 |
| 访问统计 | 11945 / 载 11949 | `visitstatistic/statisticslist?Group=Dialog` | 默认今天已对齐；`20001` | FE-054 + API-013 |

### 子页（邮件访问 emailTitle）

| 子 Tab | 权限 | 接口 | 登录态结论 | 编号 |
| --- | --- | --- | --- | --- |
| 明细 | 11950 / 载 11952 | `visitstatistic/list?Group=Mail` | 默认今天已对齐；游客列隐藏；`20001` | FE-054 + API-013 |
| 访问统计 | 11951 / 载 11955 | `statisticslist?Group=Mail` | 默认今天已对齐；游客列隐藏；`20001` | FE-054 + API-013 |

## 模块核查总表（operationalManage/playerGoldHandle）

**约定：** 顶层 Tab「红利发放 / 发放记录」；发放内层单人/批量须分测。旧站 `take`/`takeRecord` 未挂载（Unused），新站亦无。权限：发放 `10081`、记录 `10086`；单人 `10088`、批量 `10089`；记录表 `10090`、导出 `10091`。

### 顶层 Tab

| 模块 | 权限 | 组件 | 接口 | 参数 vs 旧站 | 登录态结论 | 编号 |
| --- | --- | --- | --- | --- | --- | --- |
| 红利发放 | 10081 | `gold-grant-panel` | （见子 Tab） | — | — | 见下表 |
| 发放记录 | 10086 / 表 10090 | `gold-record-panel` | `playergoldhandle/list` | 昨日；Done/Reason 多选；Total+TotalReal | 昨日空；30 日 5 条 | API-015 + FE-021 |

### 子 Tab（红利发放）

| 子 Tab | 权限 | 接口 | 登录态结论 | 编号 |
| --- | --- | --- | --- | --- |
| 单人发放 | 10088 | `queryplayer` + `playergoldhandle/` + `redtitle` | redtitle 1 条；查玩家用 queryplayer | 通过 + FE-022 |
| 批量发放 | 10089 | `queryplayerexcel` + `playergoldhandle/batch` | 未实发（写接口）；FailMsg 解析已修 | 通过 + FE-022 |

## 模块核查总表（operationalManage/accountAdjust）

**约定：** 顶层 Tab「账户调整 / 调整审核 / 调整记录」须分测；账户调整内层单人/批量须分测。权限：调整 `10094`、审核 `10095`、记录 `10096`；单人 `10108`、批量 `10109`；审核表 `10099`、通过/拒绝 `10101/10102`、批通/批拒 `10103/10104`、导出 `10105`；记录表 `10097`、导出 `10098`。审核安全页 `49`（PassPopup/`ValidCode`）。

### 顶层 Tab

| 模块 | 权限 | 组件 | 接口 | 参数 vs 旧站 | 登录态结论 | 编号 |
| --- | --- | --- | --- | --- | --- | --- |
| 账户调整 | 10094 | `adjust-form` | （见子 Tab） | — | — | 见下表 |
| 调整审核 | 10095 / 表 10099 | `adjust-audit-list` | `approvelist` `Approve=1,4` | 昨日 unix；空产品（FE-023）；账号小写（FE-024） | 本环境待审恒空 `Items=null` | API-016 + FE-023/024 |
| 调整记录 | 10096 / 表 10097 | `adjust-record-list` | `approvelist` `IsApprove=1` | 昨日；Done/Water 多选；空产品 | 昨日 39；30 日 2226 | API-016 + FE-023/024 |

### 子 Tab（账户调整）

| 子 Tab | 权限 | 接口 | 登录态结论 | 编号 |
| --- | --- | --- | --- | --- |
| 单人调整 | 10108 | `queryplayer` + `playerinfo/{id}` + `playergoldaccountchange/` | 查玩家成功；未实写调整单 | 通过 |
| 批量调整 | 10109 | `queryplayerexcel` + `playergoldaccountchange/batch` | 未实发（写接口）；FailItems Msg 已 JSON 解析 | 通过 |

## 模块核查总表（operationalManage/audit）

**约定：** 顶层 Tab「红利审核 / 红利记录 / 申请记录 / 平台转账」须分测；红利审核内层「红利发放 / 活动红利」须分测。权限：审核 Tab `10112\|\|11968`、记录 `10114`、申请 `11658`、转账 `10116`；发放审核表 `10117`、活动审核表 `11969`；记录表 `10127`；申请表 `11659`；转账表 `10131`。

### 顶层 Tab

| 模块 | 权限 | 组件 | 接口 | 参数 vs 旧站 | 登录态结论 | 编号 |
| --- | --- | --- | --- | --- | --- | --- |
| 红利审核 | 10112\|\|11968 | `bonus-audit-panel` | （见子 Tab） | — | — | 见下表 |
| 红利记录 | 10114 / 表 10127 | `bonus-record-list` | `redlist` | 昨日；空产品；SendType+3 | 昨日 1；30 日 174 | API-018 + FE-025~027 |
| 申请记录 | 11658 / 表 11659 | `match-record-list` | `activitymatchbonusapplyrecord/list` | 昨日；空产品；ApplyNote | 本环境恒空 | FE-025~027 |
| 平台转账 | 10116 / 表 10131 | `platform-transfer-list` | `playerwallettransferorder/list` | 昨日；State=-2 | 昨日 231；30 日 14959 | API-019 + FE-026 |

### 子 Tab（红利审核）

| 子 Tab | 权限 | 接口 | 登录态结论 | 编号 |
| --- | --- | --- | --- | --- |
| 红利发放 | 10112 / 表 10117 | `approvelist` `Approve=1,4` | 待审空；放宽 Approve 可见历史 | API-017 + FE-025/026 |
| 活动红利 | 11968 / 表 11969 | `approvelist` `ActivityType=10009` | 待审空 | API-017 + FE-026/027/028 |

## 模块核查总表（operationalManage/giftManage）

**约定：** 顶层 Tab「审核列表 / 发货列表」须分测；各自内层「晋级豪礼 / 主题抽奖」须分测。权限：审核 `10170`、发货 `10171`；晋级审核表 `10172`、主题审核表同挂审核；发货表 `10188`。主题人工录单 `10183`（FE-031 未迁）。

### 顶层 Tab

| 模块 | 权限 | 组件 | 接口 | 参数 vs 旧站 | 登录态结论 | 编号 |
| --- | --- | --- | --- | --- | --- | --- |
| 审核列表 | 10170 | `gift-audit-panel` | （见子 Tab） | — | — | 见下表 |
| 发货列表 | 10171 | `gift-deliver-panel` | （见子 Tab） | — | — | 见下表 |

### 子 Tab

| 子 Tab | 权限 | 接口 | 登录态结论 | 编号 |
| --- | --- | --- | --- | --- |
| 审核·晋级豪礼 | 10172 | `playergift/list` `ApplyType=4` | 空库 `Items=[]`；参数对齐 | API-020 |
| 审核·主题抽奖 | — | `list` `ApplyType=5,6,7,8` | 空库；活动类型已收窄 | API-020 + FE-030/031 |
| 发货·晋级豪礼 | 10188 | `deliverlist` `ApplyType=4` | 空库 `Items=null` | API-020 |
| 发货·主题抽奖 | 10188 | `deliverlist` `ApplyType=5,6,7` 默认待发货 | 空库；操作态已对齐 | API-020 + FE-029/030 |

## 模块核查总表（operationalManage/gameRiskControl）

**约定：** 顶层 Tab「黑名单 / 白名单 / 注册上限配置」须分测；黑/白名单内层子 Tab 须分测。权限：黑名单 `10027`、白名单 `10028`、注册上限 `12973`；黑名单子：IP `11412`、银行卡 `10031`、设备 `10032`；白名单子：IP `11448`、设备 `10036`、App Store `11342`。

### 顶层 Tab

| 模块 | 权限 | 组件 | 接口 | 参数 vs 旧站 | 登录态结论 | 编号 |
| --- | --- | --- | --- | --- | --- | --- |
| 黑名单 | 10027 | （见子 Tab） | — | — | — | 见下表 |
| 白名单 | 10028 | （见子 Tab） | — | — | — | 见下表 |
| 注册上限配置 | 12973 | `reg-config-panel` | `registerconfigs` | `Type=1` | IP/设备 LimitAmt=50 | 通过 |

### 子 Tab

| 子 Tab | 权限 | 接口 | 登录态结论 | 编号 |
| --- | --- | --- | --- | --- |
| 黑名单·IP | 11412 | `gameipriskcontrol/list` Type=1 RiskType=1 | 空 `Items=null` Total=0 | API-021 + FE-033 |
| 黑名单·银行卡 | 10031 / 表 10047 | `playerbankcard/blacklist` | 空；SourceType/导入已补 | API-021 + FE-032 |
| 黑名单·设备 | 10032 | `gameriskcontrol/list` Type=1 RiskType=4 | 空 `Items=null` | API-021 + FE-033 |
| 白名单·IP | 11448 | `gameipriskcontrol/list` Type=2 | 空 | API-021 + FE-033 |
| 白名单·设备 | 10036 | `gameriskcontrol/list` Type=2 RiskType=4 | 空 | API-021 + FE-033 |
| 白名单·App Store | 11342 / 表 11343 | `appstorewhitelistrisk/list` | `WhitelistList=null` | API-022 |

## 模块核查总表（operationalManage/downloadCsvManage）

本页**无子 Tab**；单列表。权限：查看 `12041`、删除 `12042`、自动刷新 `12043`、下载 `12044`；安全页 `17`（谷歌码）。对照旧站 `downloadCsvManage.vue`。

| 模块 | 权限 | 接口 | 参数 vs 旧站 | 登录态结论 | 编号 |
| --- | --- | --- | --- | --- | --- |
| 导出任务列表 | 12041 | `GET /backend/downloadexcel/list` | 已对齐当月 1 日～今日；保留时分秒；Status/Id/Path/Auto | 各窗空 `Items=null` MaxCount=0 | API-023 + FE-034/035 |
| 删除任务 | 12042 | `DELETE /backend/downloadexcel/{id}` | 导出中且创建24h 禁用同旧站 | 未实删（空库） | 前端逻辑通过 |
| 下载校验 | 12044 / 安全页 17 | `GET /backend/downloadexcel/download` → `/api/download` | FileName+ValidCode；Path 已 encode | 缺码 `10174`；空库无成功行可下 | 通过（校验链路） |
| 自动刷新 | 12043 | 复用 list `Auto` | 15s；新站 Auto=true 正确（旧站布尔与 `==1` 导致恒 false） | 逻辑通过 | 通过 |

## 模块核查总表（operationalManage/countrySet）

本页**无子 Tab**；单页「推广地区屏蔽」。权限：查看 `10024`、编辑 `10025`（菜单入口另有 `11571`/`104`）。对照旧站 `countrySet.vue`。

| 模块 | 权限 | 接口 | 参数 vs 旧站 | 登录态结论 | 编号 |
| --- | --- | --- | --- | --- | --- |
| 国家配置列表 | 10024 | `GET /backend/countriesconfig/list` | 全量 PageSize=9999；名称改前端过滤 | `Items=null` 无 Pagination | API-024 + FE-036 |
| 屏蔽状态 | 10024 | `GET /backend/gamecountries/` | 无参 | Countries/Allow 空串；`Option=0` | FE-037 |
| 保存提交 | 10025 | `PUT /backend/gamecountries/` | Countries + CountriesAllow + Option；合并状态原字段 | 未实写（空配置库） | FE-036/037 |
| 模式切换 / 一键开闭 / 恢复默认 | 10025 | （本地态） | 模式1→Status1/Allow；模式2→Status2/Countries | 逻辑对齐旧站 | 通过 |

## 模块核查总表（operationalManage/activity）

**约定：** 13 个顶层 Tab 及各自内层子 Tab 均须分测。权限与旧站一致。

### 顶层 Tab

| 模块 | 权限 | 组件 | 登录态结论 | 编号 |
| --- | --- | --- | --- | --- |
| 当前活动 | 10298 | `activity-ongoing-panel` | `list` Items=null | API-025 |
| 活动模版 | 10299 | `activity-template-panel` | 见子 Tab | 见下表 |
| 活动记录 | 10300 | `activity-records-panel` | 见子 Tab | 见下表 |
| VIP特权 | 10302 | `activity-vip-panel` | 见子 Tab | FE-041 + API-026 |
| 优惠码 | 13208 | `activity-promo-code-panel` | 见子 Tab；历史/报表未迁 | FE-039/042 + API-025 |
| 每日签到 | 13245 | `activity-daily-checkin-panel` | 见子 Tab；当前/历史未拆 | FE-042 + API-025 |
| 邀请好友 | 10303 | `activity-invite-panel` | 见子 Tab；规则/统计未迁 | FE-040/042 + API-025 |
| 会员代理 | 12624 | `activity-player-agent-panel` | 见子 Tab；方案未迁 | FE-042 + API-025 |
| 存款优惠 | 10304 | `activity-deposit-promo-panel` | 见子 Tab | API-027/025 + FE-042 |
| 新手转盘 | 13169 | `activity-beginner-turntable-panel` | 明细空；方案编辑未迁 | API-025 + FE-042 |
| 预约取款 | 11910 | `activity-appointment-panel` | 规则有配置 Id=3；明细空 | FE-038 + API-025 |
| 确认到账 | 12395 | `activity-bonus-withdraw-panel` | 配置可读 Id=3 | 通过 |
| 滚动大奖 | 12657 | `jackpot-display-panel` | `Items` 为配置对象（26 字段）可加载 | 通过 |

### 子 Tab（抽样实打）

| 子 Tab | 权限 | 接口 | 登录态结论 | 编号 |
| --- | --- | --- | --- | --- |
| 模版·系统 | 10308 | `operationactivitiesdefaultconfig/list` IsTemplate=false | Items=1 | 通过 |
| 模版·我的 | 10308 | 同上 IsTemplate=true | Items=null | API-025 |
| 模版·类别 | 10308 | `activitytype/list` | respond=null | API-026 |
| 记录·历史 | 10310 | `operationactivitiesconfig/history` | Items=null | API-025 |
| 记录·编辑日志 | 12102 | `activitychangelog/list` | Items=null | API-025 |
| VIP·晋级好礼 | 10314 | `viplevelconfig/listprize` | 数组 11 条；字段已对齐 | FE-041 |
| VIP·活动介绍 | 10315 | `vipfunction/list` | 数组 6 条；Name 回退 | FE-041 |
| VIP·基础规则 | 11805 | `vipbaserule/list` | respond=null | API-026 |
| 优惠码·列表 | 13208 | `operationactivitiesconfig/list` Type=10017 | Items=null | API-025 |
| 优惠码·兑换 | — | `giftcoderecord` Claim*+今天 | Items=null；参数已对齐 | FE-039 + API-025 |
| 签到·设置/记录 | 13246/13248 | `dailycheckin/list` `playerrecord` | Items=null | API-025 |
| 邀请·明细 | 10329 | `listdetail` 默认注册今天 | Items=null | FE-040 + API-025 |
| 代理·佣金/领奖/团队 | 12626/12627 | `listplayeragent*` | Items=null | API-025 |
| 存款·规则 | 10338\|\|12921 | `playerrechargeofferscheme/list` | status=10000 | API-027 + FE-042 |
| 存款·明细 | 10339 | `playerpaydiscount/list` 昨日 | Items=null | API-025 |
| 转盘·明细 | 13171 | `newplayerdrawconfig/list` | Items=null | API-025 |
| 预约·规则 | 11911 | `GET reservewithdrawactivity` | 有配置 | 通过 |
| 预约·明细 | 11912 | `list` Begin/End+今天 | Items=null | FE-038 + API-025 |

## 模块核查总表（operationalManage/leaderboard）

本页内层 Radio 三子 Tab：**当前排行榜 / 历史排行榜 / 排行结算记录**。权限：新增 `13435`、导出 `13436`、全局开关/设置 `13437`、下架 `13438`；导出安全页 `87`。对照旧站 `leaderboard.vue` + `manage/*`。

| 子 Tab | 权限 | 接口 | 参数 vs 旧站 | 登录态结论 | 编号 |
| --- | --- | --- | --- | --- | --- |
| 当前排行榜 | — / 13435·13437·13438 | `leaderboard/list` IsHistory=false；主配置/开关/下架 | 时间用 unix（日期串 10000） | Items=null；配置 Items=null | API-028/029 |
| 历史排行榜 | — | `list` IsHistory=true；克隆；点 ID→结算 | 同列表 | Items=null | API-028 |
| 排行结算记录 | 13436 导出 | `leaderboard/record`；`recordexport` | 日期须 YYYY-MM-DD（unix→10037） | Items=null；参数已对齐 | FE-043 + API-028 |
| 排行查看弹窗 | — | `record` ActivityId+Sort=Ranking | 对齐 | 空库 | 通过 |
| 新增/编辑/克隆 | 13435 | POST/PUT `leaderboard` | 精简表单 | 未实写 | FE-044 |

## 模块核查总表（operationalManage/voucher）

页面门禁 `13292`。内层 Radio：**票券列表 / 历史票券 / 票券记录**；列表点名称打开详情（记录 + 手动发放）。对照旧站 `voucher.vue` + `manage/*`。

### 子 Tab

| 子 Tab | 权限 | 接口 | 参数 vs 旧站 | 登录态结论 | 编号 |
| --- | --- | --- | --- | --- | --- |
| 票券列表 | 13293 / 表 13354 | `voucher/list`；主配置/开关 | 时间 unix（日期串 10000） | Items=null；配置 Items=null | API-030/031 |
| 历史票券 | 13350 / 表 13356 | `list` IsHistory=true | Claim* unix | Items=null；详情权限已对齐 13352 | FE-045 + API-030 |
| 票券记录 | （同旧站无独立权限） | `voucherdetailrecord`；`listallwithexpired`；导出 page 86 | 时间 unix；账号小写 | Items=null | FE-046 + API-030 |

### 详情内层

| 子页 | 权限 | 接口 | 登录态结论 | 编号 |
| --- | --- | --- | --- | --- |
| 详情·票券记录 | 13351 / 历史 13352 | 同 `voucherdetailrecord`+Id | 空库 | FE-045 |
| 手动发放·单人/批量 | 13444 / 13445 / 13446 | `issuevoucher` `batchissuevoucher` | 未实写 | 通过（参数对齐） |
| 手动发放·发放记录 | 13448 导出 | `issuevoucherrecord`（必传 VoucherId） | 无 Id→10037；有 Id 空列表 | API-030 |

## 模块核查总表（operationalManage/rewardMall）

页面门禁按 Tab：`13378` 商品设置 / `13331` 兑换记录 / `13332` 积分设置 / `13333` 积分记录 / `13334` 积分调整。对照旧站 `rewardMall.vue` + `rewardMall/*`。日期字段均为 **unix 秒**（兑换日期串→`10000`）。

### 顶层 / 子 Tab

| 子 Tab | 权限 | 接口 | 参数 vs 旧站 | 登录态结论 | 编号 |
| --- | --- | --- | --- | --- | --- |
| 商品设置·商品列表 | 13379 | `rewardsmall/product/list` IsHistory=false；主配置/开关；页签/任务 | 日期空默认；无 Sort | Items=null；配置/页签/任务 Items=null | API-032/033 + FE 兜底 |
| 商品设置·商品记录 | 13380 | `product/list` IsHistory=true | 同列表 | Items=null | API-032 |
| 兑换记录 | 13331 / 导出 page | `exchange/recordlist`；`recordlistcsv` | 默认今天；账号小写；Channel/Vip 逗号串 | Items=null；日期串 10000 | FE-047/048 + API-032 |
| 积分设置 | 13332 | `water/payment` `water/bet` + switch | 配置对象直出 | 有 Config/Switch（值多为 0） | 通过 |
| 积分记录 | 13333 / 导出 | `point/recordlist`；`recordlistcsv` | 默认今天；账号小写；PlayerStatus/BonusCategory/SendType=-1 | Items=null | FE-047/048 + API-032 |
| 积分调整·账户调整 | 13335 | POST `playerpointhandle` / `batch` | 账号小写（表单已对齐） | 未实写 | 通过（参数对齐） |
| 积分调整·调整审核 | 13336 | `playerpointhandle/approvelist` IsApprove=true | 默认今天；ChannelIds；账号小写 | Items=null；Total=0 | FE-047/048 + API-032 |
| 积分调整·调整记录 | 13337 | `approvelist` IsApprove=false Approve=2,3,4 Done=0~4 | 默认今天（申请+审批双窗）；账号小写 | Items=null；Total=0 | FE-047/048 + API-032 |

### 商品列表内弹层

| 子页 | 接口 | 登录态结论 | 编号 |
| --- | --- | --- | --- |
| 全局配置 | `getrewardsmallmainconfig` / update / switch | Items=null→前端空表兜底 | API-033 |
| 页签管理 | `producttag/list` add/edit/del/switchsort | Items=null | API-033 |
| 积分任务 | `rewardtask/list` add/edit/del/switchsort | Items=null | API-033 |
| 商品详情·兑换 | 同 `exchange/recordlist`+ProductId | 日期可空；Channel/Vip 已改逗号串 | FE-048 + API-032 |

## 模块核查总表（operationalManage/helpManage）

本页无子 Tab；页面门禁 `10230`，操作：同意 `10231` / 拒绝 `10232` / 终止 `10233`。对照旧站 `helpManage.vue`（协助对象为代理）。日期 **unix 秒**；状态为客户端过滤（对齐旧站表头 filters）。

| 模块 | 接口 | 参数 vs 旧站 | 登录态结论 | 编号 |
| --- | --- | --- | --- | --- |
| 协助列表 | `GET /backend/helprecords/list` | 默认今天；`HelperAccount`/`Sort`/`BeginTime`/`EndTime`；保留时分秒 | Items=null；日期串 10000；坏 Sort→10001 | FE-049 + API-034 |
| 同意 | `PUT /backend/helprecords/confirm` `{Id}` | 权限/按钮条件对齐 | 未实写（空库） | 通过（参数对齐） |
| 拒绝 | `PUT /backend/helprecords/denied` `{Id}` | 同旧站 | 未实写 | 通过（参数对齐） |
| 终止 | `PUT /backend/helprecords/cancel` `{Id}` | 同旧站 Status==2 | 未实写 | 通过（参数对齐） |

说明：旧站另有「协助订单」页 `helpOrder.vue`（权限 `10234`，对象为协助者）；已单独核查，见模块表 `operationalManage/helpOrder`。

## 模块核查总表（operationalManage/whiteList）

外层旧站壳权限 `10207`（免白名单 `10208` 已注释）。NEW 直接展示内层 Tab。对照 `whiteList.vue` + `whiteSetting.vue` / `white.vue` / `users.vue`。

### 子 Tab

| 子 Tab | 权限 | 接口 | 参数 vs 旧站 | 登录态结论 | 编号 |
| --- | --- | --- | --- | --- | --- |
| IP白名单 | Tab 10214 / 列表 10216 | `whitelist/list`；开关 PUT `/`；备注 `editipremark`；删；`pickuser`；统计 `ipuserlist` | WhiteIp+WhiteUsername+Status；空 Sort；新增必填使用者/过期 | Items=null；统计 0/0；pickuser Items=null | FE-050 + API-035 |
| 使用者列表 | Tab 10215 / 列表 10221 | `listuser`；开关 `edituser`；备注 `edituserremark`；删；点名称→IP 筛 | **Name**（非 Username）+Status | Items=[]；坏 Sort→500 | FE-050 + API-035 |
| 免名单（旧站已注释） | 10208 | `whitelistfree/*` | — | Items=null | FE-051 + API-035 |

## 模块核查总表（operationalManage/helpOrder）

本页无子 Tab；页面门禁 `10234`，操作「协助」`10235`。对照旧站 `helpOrder.vue`（对象为协助者；与 `helpManage` 代理视角不同）。日期 **unix 秒**；状态为客户端过滤（对齐旧站表头 filters）。

| 模块 | 接口 | 参数 vs 旧站 | 登录态结论 | 编号 |
| --- | --- | --- | --- | --- |
| 协助工单列表 | `GET /backend/helprecords/listhelp` | 默认今天；`Keyword`/`Creator`；保留时分秒；Sort=`-field` | Items=null；日期串 10000；坏 Sort→10001 | FE-052 + API-036 |
| 协助 | `GET /backend/helprecords/help?Id=` → Link；再 `POST /public/user/login`（带 HelpLink） | 按钮条件 Status==2 且未过期 | 空库未实写；无 Id→10002 | 通过（参数对齐） |

## 模块核查总表（operationalManage/relationQuery）

本页无子 Tab；页面门禁 `10023`，导出 pageId `91`。对照旧站 `relationQuery.vue`。日期 **unix 秒**（日边界）。

| 模块 | 接口 | 参数 vs 旧站 | 登录态结论 | 编号 |
| --- | --- | --- | --- | --- |
| 关联号列表+统计 | `GET /backend/relationsearch/list` | 默认今天；PackageId=''；ChannelIds/ChannelSearch+Type；账号小写校验 | 今天/昨天 Items=null；近 30 天有数据+Total | FE-053 + API-037 |
| 导出 CSV | `GET /backend/relationsearch/recordexport` | 同列表筛选；需安全校验 | 无校验→10103（预期） | 通过（参数对齐） |

## 模块核查总表（operationalManage/noticeTitle）

从游戏公告列表点击标题进入（query `Id`/`Title`）；门禁 Tab `11942` 明细 / `11945` 访问统计。对照 `noticeTitle.vue` + `gameNotice/noticeDetail.vue` / `noticeStatistics.vue`。共用组件 `visit-detail-panel` / `visit-statistics-panel`（Group=`Dialog`）。

| 子 Tab | 权限 | 接口 | 参数 vs 旧站 | 登录态结论 | 编号 |
| --- | --- | --- | --- | --- | --- |
| 明细 | Tab 11942 / 载 11946 / 导出 11948 page=33 | `visitstatistic/list`；`visitstatisticexcel` | Group=Dialog；Key；PlayerId 空→`-1`；默认今天；时长 HH:mm:ss | Key=21 任意日期窗均 `20001` | FE-054 + API-013 |
| 访问统计 | Tab 11945 / 载 11949 | `visitstatistic/statisticslist` | Group=Dialog；Key；SubGroup；默认今天；含游客列 | 同上 `20001` | FE-054 + API-013 |

## 模块核查总表（operationalManage/emailTitle）

从邮件通知列表点击标题进入（query `Id`/`Title`）；门禁 Tab `11950` 明细 / `11951` 访问统计。对照 `emailTitle.vue` + `gameNotice/emailDetail.vue` / `emailStatistics.vue`。共用 `visit-detail-panel` / `visit-statistics-panel`（Group=`Mail`，`MailDropDownList`）。

| 子 Tab | 权限 | 接口 | 参数 vs 旧站 | 登录态结论 | 编号 |
| --- | --- | --- | --- | --- | --- |
| 明细 | Tab 11950 / 载 11952 / 导出 11954 page=33 | `visitstatistic/list`；`visitstatisticexcel` | Group=Mail；Key；PlayerId 空→`-1`；默认今天 | Key=1 均 `20001`；本环境无邮件单 | FE-054 + API-013 |
| 访问统计 | Tab 11951 / 载 11955 | `visitstatistic/statisticslist` | Group=Mail；默认今天；**游客列隐藏**（旧站已注释） | 同上 `20001` | FE-054 + API-013 |

说明：本页前端参数/权限/游客列与旧站一致；默认日期问题已在 FE-054（共用面板）修复，本轮无新增前端缺陷。

## 模块核查总表（operationalManage/playerList）

旧站顶层仅「所有玩家」Tab（`10012`）启用；玩家分组/TG监控/姓名手机访问等已注释，新站亦为单页。对照 `playerList.vue` + `playerList/list.vue`。导出 pageId `32`。

| 模块 | 权限 | 接口 | 参数 vs 旧站 | 登录态结论 | 编号 |
| --- | --- | --- | --- | --- | --- |
| 会员列表 | 页 10012 / 表 10014 / 操作 10015 / 导出 10016 / 批量 11460 | `GET playerinfo/list`；`listcsv` | 空日期；Sort=`-CreateTime`；BindPhone/PlayerLevelId=`-1`；DataSearchType=`2`；账号小写；产品「全部」 | 默认 MaxCount=553；坏 Sort→10001；日期串→10000；空子集 Items=null | FE-055/056 + API-038 |
| 标签下拉 | 操作内 | `GET playertag/list` | PageSize=999 | Items=null MaxCount=0 | API-038 |
| 会员层级筛 | 12296 | `GET playerlevel/list` | Page=1 Size=200 | Items=1（未分层） | 通过 |
| 行内改状态/踢人 | 10015 | `PUT playerext/` | Status 0/1/2/3/4/6/7/8；取消踢=8（旧站同） | 本轮未写操作 | 通过（参数对齐） |
| 批量编辑 | 11460 | `POST playerinfo/batchupdate` | ActType 1–5 + PlayerIds | 本轮未写操作 | 通过（参数对齐） |
| 改标签/层级 | 10015 | `edittag` / `editplayerlevel` | PlayerId + Tag/Level | 本轮未写操作 | 通过（参数对齐） |
| 备注抽屉 | 看 10015 / 增 10062 | `playerremark/list`(+IsSelf) CRUD | IsSelf=true | 样本账号备注空 | FE-056 |
| 高级搜索 | 无独立权限 | 写入 `Filters` JSON → list | 日期→unix 秒字符串 | 依赖 list | 通过（参数对齐） |

## 模块核查总表（operationalManage/playerLevel）

旧站仅单 Tab「会员层级」（`12281`）；新站同为单页。对照 `playerLevel.vue` + `playerLevel/PlayerLevelTab.vue`。

| 模块 | 权限 | 接口 | 参数 vs 旧站 | 登录态结论 | 编号 |
| --- | --- | --- | --- | --- | --- |
| 层级列表 | 页 12281 / 搜 12282 / 增 12283 / 改 12285 / 删 12286 | `GET playerlevel/list` | Page/PageSize；LevelName；Begin/End unix；无 Sort；DefaultLevel=0 未分层不可编删 | Items=1（仅未分层）；日期串→10000；空窗 Items=null | FE-057 + API-039 |
| 返水方案下拉 | 表单内 | `GET playerbackwaterscheme/allscheme` | 前置 Id=0「产品的返水方案」 | list len=1 | 通过 |
| 风控方案下拉 | 表单内 | `GET withdrawautoconfig/listSchemeName` | SchemeType=0 为默认；编辑 Id=0 时 remap | list len=1（预设风控） | 通过 |
| 所属会员 | 筛 12282 | `GET listPlayerDetail`；`POST deletePlayerDetail` | Id；LoginAccount 小写；PlayerId；ChannelId；PackageId='' 全部 | 未分层不可进；空 Id 探 Items=null | FE-057 + API-039 |
| 新增/编辑/删除 | 12283/12285/12286 | `add` / `edit` / `DELETE :id` | LevelName+SchemeId+Withdraw…；仅 DefaultLevel===1 出按钮 | 本环境无自定义层，未实写 | 通过（参数对齐） |

## 模块核查总表（memberManage/memberLog）

旧站顶层仅「登录记录」Tab（`12220`）；内层 Radio：明细 `12221` / 统计 `12222`；导出 `12246` pageId=`34`。对照 `memberLog.vue` + `memberLog/loginRecords.vue` + `table.vue`。

| 子 Tab | 权限 | 接口 | 参数 vs 旧站 | 登录态结论 | 编号 |
| --- | --- | --- | --- | --- | --- |
| 明细 | 12221 / 导出 12246 | `GET playeranalysis/log`；`listcsv` | 默认今天；账号/产品/玩家ID/渠道/设备/IP；不传 DataSearchType | 今天空；昨天 1 条；无日期 MaxCount=553；日期串→10000 | FE-058 + API-040 |
| 统计 | 12222 | 同接口 `Summary=1` | 默认今天；DataSearchType 默认 0；端口+VIP 分布 | 今天全 0；昨天 TotalPlatform=1 | FE-058 + API-040 |

## 模块核查总表（memberManage/gameRecord）

旧站顶层仅「游戏记录」Tab（`12205`）启用；EVO Sidebet / 游戏记录2 已注释。新站同为单页。对照 `gameRecord.vue` + `gameDetails/gameRecord.vue`。导出 pageId `35`（权限 `12206`）；点账号进详情 `12207`。

| 模块 | 权限 | 接口 | 参数 vs 旧站 | 登录态结论 | 编号 |
| --- | --- | --- | --- | --- | --- |
| 注单列表 | 12205 | `GET gametransactiondetail` | 默认昨天→今天；SelectTimeType=1；IsBetTrade/SettleCount=0；账号小写；GameIds 场馆类型空→`-1`；双请求 SumAll | 旧窗 MaxCount=5121；前天→昨天偏移；日期串→10000 | FE-059 + API-041 |
| 汇总 | 同 | 同上 `SumAll=1` | MoreItems 取汇总 | 非 SumAll MoreItems 全 0；SumAll 有金额 | API-041 |
| 结算流水 | 已结算点状态 | `GET gametransactiondetaillog` | TransactionId | respond=null | API-041 |
| 三方详情 | 行内按钮 | 优先行内 Detail；否则 `getbyorderid` | GameType/OrderId/BeginTime/RoundCode/SubGameId | 有 Detail 走结构化；兜底可 10000 | 通过（参数对齐） |
| 导出 | 12206 page=35 | `GET gametransactiondetailexcel` | 同列表去分页 | 无校验→10103 | 通过（参数对齐） |

## 模块核查总表（memberManage/fundFlowManage）

本页无子 Tab；页面门禁 `12208`，点账号进详情 `12209`。对照旧站 `fundFlowManage.vue`。无导出。须订单号或游戏账号之一；默认近 7 自然日（含今天）；日期 unix 秒。

| 模块 | 权限 | 接口 | 参数 vs 旧站 | 登录态结论 | 编号 |
| --- | --- | --- | --- | --- | --- |
| 资金流水列表 | 12208 | `GET /backend/operation/gamedetail` | 近7日含今天；DataSearchType=0/2；Reason 逗号；账号小写 4–20；PackageId=''；不自动查询 | 123xy 有数据；空条件→10000；日期串→10000；坏 Sort→Items=null | FE-060 + API-042 |
| 汇总 | 同响应 MoreItems | — | SumAddGold 分 | 部分账号 SumAddGold=0 与明细不符 | API-042 |

## 模块核查总表（memberManage/venueChange）

本页无子 Tab；页面门禁 `12210`，导出 `12211`（客户端 Excel，`IsExp`），转人工 `12212`，变更状态 `12213`。对照旧站 `venueChange.vue`。默认今天；`State=-2` 全部。

| 模块 | 权限 | 接口 | 参数 vs 旧站 | 登录态结论 | 编号 |
| --- | --- | --- | --- | --- | --- |
| 转账列表 | 12210 | `GET playerwallettransferorder/list` | 默认今天；Type/Out/In；State=-2/-1/0/5/18；DataSearchType 0/2；账号校验 | 今天 164；State=5→10001；日期串→10000；坏 Sort→10001 | FE-061 + API-043 |
| 转人工 | 12212（State==-1） | `POST .../manual` | `{ Id }` | 本环境今日无处理中，未实写 | 通过（参数对齐） |
| 变更状态 | 12213（State==5\|\|17） | `POST .../editstate` | `{ Id, State:1\|2 }` | 筛 State=5 接口异常，未实写 | 通过（参数对齐）+ API-043 |
| 导出 | 12211 | 同 list `IsExp` + 前端 CSV | PageSize=99999 | 今天 164 条可导 | 通过 |

## 模块核查总表（memberManage/playerAuthentication）

三 Tab：审核列表 / 审核记录 / 验证设置（内含 OTP、宽限期、验证信息开关、同证件、结果邮件）。对照旧站 `playerAuthentication.vue` + 子组件。默认空日期（与旧站一致）。

| 模块 | 权限/备注 | 接口 | 参数 vs 旧站 | 登录态结论 | 编号 |
| --- | --- | --- | --- | --- | --- |
| 审核列表 | 待审全量无分页 | `GET playerauthentication/list` | AuthScenario=-1；空日期；Package 全部；批量 approve | 本环境 Items=null；日期串→10000 | FE-062 + API-044 |
| 审核通过/拒绝 | 单条/批量 | `PUT playerauthentication/approve` | `{ IsApprove, OrderId, Remark }` | 无待审未实写；参数对齐 | 通过（参数对齐） |
| 审核记录 | 分页；导出 pageId=90 | `GET .../record`；`recordexport` | Status 空/2/3；上传+审核时间；AuthScenario=-1 | Items=null MaxCount=0；无口令导出→10103 | FE-062 + API-044 |
| 验证设置表 | SubType 1–4 | `GET/PUT setting` `edit` `switch` | 示意图 ExInfo[0..3]；活动 SubType=4 | 4 行配置可读，开关均关 | 通过 + API-044 |
| OTP / 宽限期 / 同证件 | SubType 1001/1002/1003 | `PUT .../switch` | IsOpen；1002 带 Day | Days=0 可读（FE 已修误显） | FE-062 |
| 验证信息开关 | SubType 1005 | `setting` + `switch` | VerificationInfo 1–7 | **setting 无 1005，表空** | API-044 |
| 结果通知邮件 | SubType 1004 | `switch` + `edit` LangText | 按语言组标题/正文 | 有 1004；LangText 为对象结构 | FE-062 + 通过 |

## 模块核查总表（memberManage/walletManage）

四 Tab：银行卡 / 电子钱包 / 虚拟币地址 / 多账号绑定。对照旧站 `walletManage.vue`（后三 Tab 源码仍在 `wallet-manage/*`，入口曾注释）。GoogleCode：银行卡 pageId=8、电子钱包=9、虚拟币=18。

| 模块 | 权限 | 接口 | 参数 vs 旧站 | 登录态结论 | 编号 |
| --- | --- | --- | --- | --- | --- |
| 银行卡 | 页 12949/11469；增删改 11470–11472 | `GET/POST/PUT/DELETE playerbankcard` | 默认近1月；账号小写；BankCardNum | 近1月 5 条；无日期 11；日期串→10000；坏 Sort→10001 | FE-063 + API-045 |
| 电子钱包 | 12945–12948 | `listall` / `add` / `edit` / `del` | 默认近1月；PayType；AccountNum | 本环境 Items=null；日期串→10000 | API-045 |
| 虚拟币地址 | 11474–11477 | `playerwithdrawdigitaladdress` | 首屏/重置空日期；账号小写 | 空日期/近1月均空；日期串→10000 | FE-063 + API-045 |
| 多账号绑定 | 12702 | `listcardmultiple` / `add` / `del` | 默认近1月；BankAccount | 本环境空；日期串→10000 | API-045 |

## 模块核查总表（memberManage/verifyCode）

五 Tab：手机验证码 / 区码管理 / 邮箱验证码 / 邮箱通道 / OTP。对照旧站 `verifyCode.vue` + 子页。权限 10026 / 11921 / 13011 / 13089 / 13204；白名单 12565。

| 模块 | 权限 | 接口 | 参数 vs 旧站 | 登录态结论 | 编号 |
| --- | --- | --- | --- | --- | --- |
| 手机验证码 | 10026 | `GET phoneverifycode/list` | 必填账号或手机；Package 全部；账号小写；不自动查询 | 无条件→10037；有账号 Items=null | FE-064 + API-046 |
| 后台生成 | — | `generatecode` + 查玩家 | PlayerId | 参数对齐，未实写 | 通过（参数对齐） |
| 白名单 | 12565 | `whitelist` `add` `del` | Account/Package/Channel；空 Package 不传 | 本环境空 | FE-064 + API-046 |
| 区码管理 | 11921 等 | `phonecountrycode/list` `switch` `frequentlyused` `resetdefault` `switchsort` | CountryName/DialingCode；分页 | MaxCount=244；中国/86 可筛 | 通过 + API-046 |
| 邮箱验证码 | 13011 | `GET emailverifycode/list` | 必填账号或邮箱；Package 全部 | respond=null | FE-064 + API-046 |
| 邮箱通道 | 13089 | `listemailconfig` CRUD + select | Params JSON | respond=null | API-046 |
| OTP 管理 | 13204 | `gameotpconfig/getconfig` `editconfig` | IpLimit/DeviceLimit | 可读 IpLimit=1 DeviceLimit=10 | 通过 |


## 模块核查总表（operationalManage/playerDetails）

多 Tab（权限见 `PLAYER_DETAIL_TABS`）+ 注单子 Radio（11639/10431/11638）+ 问题纪录子 Tab（搜索/访问）。对照旧站 `playerDetails.vue` + `common/playerDetail/*`。注单列表/三方详情对齐游戏记录。

| 模块 | 权限 | 接口 | 参数 vs 旧站 | 登录态结论 | 编号 |
| --- | --- | --- | --- | --- | --- |
| 搜索/概况 | 10407 | `playerinfo/{id}` `queryplayer` | 账号小写；产品必选 | info OK；空 PackageId→10000 | FE-065 |
| 钱包 | 10408 | `playerwallet/list` | PlayerId | Items 1（中心钱包） | 通过 |
| 金币详情 | 10409 | `playerinfo/gamedetail` | 近7含今天 | MaxCount=10；SumAddGold=159 | FE-065 |
| 注单·详情记录 | 10411/11639 | `operation/gametransactiondetail` | 近7含今天；GameIds=''；DataSearchType=2；复用游戏记录面板 | Items 6 + Detail；SumAll 汇总 61900 但 Items=null | FE-065 + API-047 |
| 注单·场馆/日期统计 | 10431/11638 | `gametransactiongametype` `gameday` | 同窗 | Count=6 SumBetGold=61900 | FE-065 |
| 三方详情 | 同注单 | 行内 Detail / `getbyorderid` | 与游戏记录同组件 | Detail JSON 可解析；兜底 getbyorderid→10000；detaillog null | API-047 |
| 金币统计 | 10412 | `goldtotal` `playergoldchange` `playersettle` | 近7含今天 / 无日期变更 | goldtotal MaxCount=22；change 6；settle 1 | FE-065 |
| 登录信息 | 10413 | `playerloginip` `playerlogininfo` | 当月 / LType | 当月登录空；无日期 1；统计 1 | 通过（数据量） |
| 充提记录 | 10414 | `playerpayment` `playerwithdraw` 等 | 近7含今天 | 充值空；提现 1 | FE-065 + API-047 |
| 场馆转账 | 12092 | `playerwallettransferorder/list` | 今天 | Items=null | FE-065 + API-047 |
| 调整记录 | 12097 | `playergoldhandle/list` | 今天 | Items=null | FE-065 + API-047 |
| 红利信息 | 10415 | `activitylogstatistics/listbyplayer` | 近7含今天 | Items=null | FE-065 + API-047 |
| 流水信息 | 12936 | `playerdrawwaterrecord/list` | 今天 | Items=null | FE-065 + API-047 |
| 积分信息 | 13412 | `rewardsmall/point/recordlist` | 今天 | Items=null | FE-065 + API-047 |
| 关联号 | 10416 | `playerinfo/devicesiptotal` | Type+分页 | Items=null | API-047 |
| 返水信息 | 11609 | `playerbackwaterrecord/list` | 今天 | Items=null | FE-065 + API-047 |
| 问题纪录 | 11865 | `aiassistantsearch` `aiassistantv2/contentviewlist` | 搜索无日期；访问今天 | aisearch→10010；访问空 | API-047 |
| 风控分析 | 12377 | `riskanalysis` `riskanalysisgame` | PlayerId | 分析 5 条；游戏空 | 通过 + API-047 |
| 操作日志 | 13313 | `playerinfo/playerinfolog` | 今天 | Items=null | FE-065 + API-047 |


## 模块核查总表（gameManage/inclusionDeploy）

产品列表 + 备注/未成年弹窗 +「更多配置」多 Tab（基础/外观/推送/推广返水/登录/支付/客服/官方邮件/SEO 站点/VIP 交易/统计/场馆/游戏客服）。对照旧站 `inclusionDeploy.vue`。

| 模块 | 权限 | 接口 | 参数 vs 旧站 | 登录态结论 | 编号 |
| --- | --- | --- | --- | --- | --- |
| 产品列表 | 10775 | `GET package/list` | PackageName+分页；Resources/VIPBadge | MaxCount=1；坏 Sort 空表 | FE-066 + API-048 |
| 创建/编辑跳转 | 10776/10778 | → `addPackage` | 同旧站 | 路由对齐 | 通过 |
| 删除 | 10779 | `DELETE package/{id}` | Id | 参数对齐，未实删 | 通过（参数对齐） |
| 预览 | 11103 | 本地拼 GameTestPreviewUrl | 对齐旧站 res/hall/login | 依赖环境预览域名 | 通过 |
| 备注 | 11853 | `package/{id}` `editdescription` | Id/Description | detail 可读 | 通过 |
| 未成年登录/注册 | 12740 | `getunderageconfig` `editunderageconfig` | Id=PackageId；Type 1/2 | Type1 空对象；Type2 有配置 | API-048 |
| 更多·外观 Logo | 11105 | `listlogogroup` | 无参 | respond=null；已兜底当前 Id | FE-066 + API-048 |
| 更多·广告方案 | 11109 | `gameadtemplate/list?Type=` | Type 1/2… | Items 有默认方案 | 通过 |
| 更多·推广活动 | 11107 | `getactivitylist?IsShowAll=1` | IsShowAll=1 | 有数据但无 10014/10018 | API-048 |
| 更多·返水方案 | 11108 | `playerbackwaterscheme/allscheme` | 无参 | list(1)「自定义返水方案」 | 通过 + FE-066 |
| 更多·登录注册 | 11110 | `packageregisterloginconfig` | PackageId | 可读 | 通过 |
| 更多·三方登录 | 12935 | `package/loginconfig` | LoginPlatformType 1/2 | LoginConfig 有 | 通过 |
| 更多·充提绑定 | 13010 | `packagepaymentwithdrawbindconfig/{id}` | — | BindingType/OTP 可读 | 通过 |
| 更多·SEO/站点 | 13178/10775 | `seoconfig` `packagesiteconfig` | PackageId | SEO 空；站点 list(0) | 通过（数据量） |
| 更多·欢迎邮件 | 12909 | `gameemail/newcomerlist` | PackageId | Id=0 IsOpen=false | 通过 |
| 更多·数据统计 | 13179 | `package/analyticinfo` | PackageId | 扁平字段可读；保存仍 JSON 分组 | 通过 |
| 更多·场馆/游戏客服 | 11669/11408 | detail 内 Venue/Cs 字段 | 解析 detail | detail 含 CsLineConfig 等 | 通过 |
| 购买额度 | AccountLevel=3 | `buypackagenum` | 无参 | 本账号未见入口（Level≠3） | 通过（门控） |


## 模块核查总表（gameManage/addPackage）

七步向导：语言 / 产品风格 / APP·H5 皮肤 / PC 皮肤 / 产品信息 / 场馆游戏 / 预览确认。创建权限 10776，编辑 10778；快速配色 12287（StyleType=100）。对照旧站 `addPackage.vue` + 配色弹窗。

| 模块 | 权限 | 接口 | 参数 vs 旧站 | 登录态结论 | 编号 |
| --- | --- | --- | --- | --- | --- |
| 编辑详情 | 10778 | `GET package/{id}` | Id | package/7 可读；SkinColor=0 | FE-067 |
| 图标资源 | 同 | `GET /api/resource/list` | AgentId；PictureType=3；PageSize=1000 | Items 15 | 通过 + API-049 |
| APP 皮肤 | 同 | 同上 | PictureType=12；PictureTheme=StyleSetting | Theme1→1 条 Style100 | 通过 |
| PC 皮肤 | 同 | 同上 | PictureType=15；PictureTheme | Theme1→1 条 | FE-067（补校验） |
| 快速配色 | 12287 | `packagecolorthemelist` `…detaillist` | DeviceType 0/1；ThemeId=''；PageSize=999 | Items=null | FE-067 + API-049 |
| 创建提交 | 10776 | `POST /backend/package/` | Hash；Games 逗号；CsLineConfig JSON；0 配色不传 | 参数对齐，未实写 | FE-067 |
| 编辑提交 | 10778 | `PUT /backend/package/` | Id；LangText 序列化；配色条件同旧站 | 参数对齐，未实写 | FE-067 |
| 新手引导 | 创建后 | `PUT /backend/noviceguidance` | NoviceGuidanceState+PackageId | 条件触发，未实写 | 通过（参数对齐） |

## 模块核查总表（gameManage/createChannel）

渠道列表 + 层级导航 + 创建/编辑多步表单（归属/渠道设置/落地页/确认）+ 批量配置多 Tab（首页标签/版本平台/落地页/推广模式/域名/重打包）+ 邀请码/包体/地址预览弹窗。对照旧站 `createChannel.vue` → `channelManage/channelManage.vue`。

| 模块 | 权限 | 接口 | 参数 vs 旧站 | 登录态结论 | 编号 |
| --- | --- | --- | --- | --- | --- |
| 渠道列表 | 12303 | `GET channel/list` | IsHidden=1；PackStatus；Sort `-CreateTime`；PromoterAdminId | MaxCount=2；空→Items=null；坏 Sort→10001 | FE-068 + API-050 |
| 层级导航 | 12302/12308 | `listallsonpromoter` | AdminId+渠道/推广筛 | ItemsSon/Parents=null；ItemsAdmin 单对象 | API-050 |
| 创建/编辑向导 | 12305/12306 | `channel/` POST/PUT + detail | 三步+落地页；IosType 默认 5 | detail16 OK；Id=0 已当空 | FE-068 |
| 落地页资源 | 同 | `GET /api/resource/list` | AgentId；PictureType=1；Style Tab；PageSize=4 | Style 全量 1 条；无 Type→空 | FE-068 + API-050 |
| 包体/域名依赖 | 同 | `package/listall` `domain/list` ios/android/企业包 | OnlyUnused；好友域 Type 3/9 | 域名 8；上架包/企业包空 | API-050 |
| 屏蔽国家 | 同 | `countriesconfig/list` | PageSize=9999 | Items=null | API-050 |
| 默认专属场馆 | 同 | `channel/defaulttagvenues` | 无参（JSON 串） | 可读字符串数组 | 通过 |
| 批量配置 | 12322 | `PUT channel/editbatch` | ActionType 1–6；落地页须 PictureType=1 | 参数对齐；落地页缺 Type 已修 | FE-068 |
| 邀请码 | 12319 | `PUT editinvitationcode` | 全量 detail+ReqPathType=2 | 样例码 100/103；改码规则对齐 | FE-068 |
| iOS/Android 包体 | 12309 | `ios/androidapppackageconfig` | 全量 detail；0→未设置 | respond=null | FE-068 + API-050 |
| 重打包 | 列表 | `POST channel/repack?ChannelId=` | query ChannelId | 参数对齐，未实写 | 通过 |
| 地址预览 | 12313 | 前端拼 Domain/H5Domain1+cid | 同旧站 | 可预览/复制 | 通过 |
| 启停用 | 12306/12315 | detail + `PUT channel/` IsHidden | 1 启用 / 2 停用 | 参数对齐 | 通过 |

## 模块核查总表（gameManage/rechargeManage）

旧站父级多 Tab 已注释，当前仅「通道管理」(10817)。左侧支付类型 + 普通通道表；专用类型 10 私卡 / 26 USDT / 100 VIP 客服 / 212 兑换码。对照 `rechargeManage.vue` → `aisleManage.vue` + `fastPayment`/`usdtFastPayment`/`voucherPayment`/`customeList`。

| 模块 | 权限 | 接口 | 参数 vs 旧站 | 登录态结论 | 编号 |
| --- | --- | --- | --- | --- | --- |
| 通道类型+普通列表 | 10817 | `GET thirdrechargetypeagentconfig/list` | 首次 PayType=1；OnShelf=1；再按选中类型 | TypeList 5；PayType1/2 有数据；12 空；缺 MaxCount | FE-069 + API-051 |
| 类型开关/热门/排序 | 11141/11142/11143 | `paytypeconfig/switch` `PUT` `sort` | Id；IsHot；Ids | 参数对齐，未实写 | 通过 |
| 刷新通道 | 12633 | `…/refresh` | PayType；禁 0/100/200 | 参数对齐 | 通过 |
| 通道启停/下架/重置/排序 | 同 | `used` `shelf` `reset` `exchange` | Id；InUsed；Ids | 参数对齐 | 通过 |
| 普通通道编辑 | 同 | `PUT thirdrechargetypeagentconfig/` | CustomRate×10000；Conditions JSON | 费率换算对齐；层级已修 | FE-069 |
| 私卡 PayType=10 | 10821 | `agentrechargecardconfig/list` `total` | PayType=10 | list 空；total 0/0 | API-051 |
| USDT PayType=26 | 10821 | `agentrechargeusdtconfig/list` `total` | PayType=26；PageSize=999 | list 空；total 0/0 | API-051 |
| VIP 客服 PayType=100 | 10821 | `coindealersupporter/list` `coindealer/listall` `customersupporterglobalconfig` | 分页+OrderSwitch | list/listall 空；order 可读 | API-051 |
| 兑换码通道 212 | 10821 | `agentrechargecardconfig/list` | PayType=212 | Items=null | API-051 |
| 会员层级/快捷模板 | 编辑依赖 | `playerlevel/list` `rechargetestchannelquicksetting/list` | 勿 BeginTime=1 | BeginTime=1 空；模板 null | FE-069 + API-051 |
| 父 Tab（已注释） | 10818+ | allAisle/aisleData/showConfig 等 | 旧站注释；NEW 未挂 | 与旧站一致不测 | 通过（范围） |

## 模块核查总表（gameManage/withdrawConfig）

父 Tab：提现配置 / 兑换风控 / 三方代付 / 出款银行 / 通道数据 / 通用规则 / 提现访问。通用规则内子 Tab：参数、提示(+自定义文案)、钱包绑定、强制提现、银行卡绑定、虚拟币、预计到账。对照 `withdrawConfig.vue`。

| 模块 | 权限 | 接口 | 参数 vs 旧站 | 登录态结论 | 编号 |
| --- | --- | --- | --- | --- | --- |
| 提现配置·类型+账户 | 10978/11014+ | `withdrawpaytypeconfig/list` `agentwithdrawaccount/list` | 优先 Status=1；AccountType | 类型 3；账户各 1 | FE-070 |
| 兑换风控规则 | 10979/10991+ | `listSchemeName` `withdrawautoconfig/list` | Scheme Id | 方案 1；规则 36 | 通过 |
| 三方代付 | 10980/10988+ | `thirdwithdrawtypeagentconfig/list` | Page；ShowName；OnShelf | Items 2 | 通过 |
| 出款银行 | 10981/10985+ | `bankconfig/list` switch/batch | 无参 | respond 数组 list(1) | FE-070 + API-052 |
| 通道数据 | 10982/10984 | `withdrawstatistics/list` | Begin 近30天～今日；DataSearchType=0 | MaxCount=21；空窗 Items=null | FE-070 + API-052 |
| 通用·参数/提示 | 10994/10995 | `withdrawgameconfig` `withdrawtipconfig` `withdrawtip/list` | 无参/分页 | 配置可读；tips Items=null | API-052 |
| 通用·钱包/强制 | 12920/12951 | `withdrawwalletbindingconfig/list` | 分页 | Items=null | API-052 |
| 通用·银行卡绑定 | 10996 | `playerbindcardconfig` ignoreplayers | 配置对象 | 可读 | 通过 |
| 通用·虚拟币 | 11647 | `getusdtrate` `getfirstconfig` whitelist/quota | WhiteType 1/2 | 配置可读；白名单空；Type0→10037 | API-052 |
| 通用·预计到账 | 12386/12398 | `withdrawestimatedtimeconfig` | 无参 Items 对象 | 分钟 120/120 | 通过 |
| 访问·明细/统计 | 11961/11962+ | `visitstatistic/list` `statisticslist` | Group=Withdraw；今日窗；PlayerId=-1 | **20001** | FE-070 + API-052 |

## 模块核查总表（gameManage/domainManage）

父页仅「我的域名」；内层 Segmented Type **1–10**（防封/落地/H5/代理 APP/代理后台/主站/代理 H5/推广 APP/H5/后台）。旧站购买/主站配置 Tab 已注释，NEW 未挂。权限：页 `11013`、列表 `12051`、批量编辑 `13235`（限 Type 1/2/3/4/7/8/9）、批量启停 `12053/12054`、导出 `12057`、单条编辑 `12608`、单条启停 `12055/12056`。对照 `domainManage.vue` + `myDomain` + 各 Type 子组件。

| 模块 | 权限 | 接口 | 参数 vs 旧站 | 登录态结论 | 编号 |
| --- | --- | --- | --- | --- | --- |
| Type1 防封 | 12051 | `domain/list` | **补 IsAll=1**（旧站未传） | 无 IsAll 空；有则 3 | FE-071 + API-053 |
| Type2 落地 | 12051 | 同上 | 同上 | 无空；有则 2 | FE-071 + API-053 |
| Type3 H5 | 12051 | 同上 | 同上 | 无空；有则 2 | FE-071 + API-053 |
| Type4 代理 APP | 12051 | 同上 | IsAll=1 | Items=null（无库存） | API-053 |
| Type5 代理后台 | 12051 | 同上 | IsAll 可选 | Items 1 | 通过 |
| Type6 主站 | 12051 | 同上 | IsAll=1 | Items=null | API-053 |
| Type7–10 代理H5/推广* | 12051 | 同上 | IsAll=1 | Items=null | API-053 |
| 域名 Keyword/状态筛 | 12051 | 同上 | Keyword；InUsed | Keyword 有效；Domain 参无效 | API-053 |
| 导出 | 12057 | `domain/list` IsExp | 补 IsAll | 无 IsAll 空；有则可导 | FE-071 + API-053 |
| 编辑/启停 | 12608/12055+ | `domain/edit` `inused` `inusedbatch` | 与旧站一致 | 坏参文案含糊；未做破坏性写测 | API-053 |

## 模块核查总表（gameManage/packageManage）

父 Tab：企业包 / iOS 上架包 / Android 上架包。旧站超级签 Tab 已注释，NEW 未挂。权限：企业包页 `12355`、流程 `10973`、企业包列表 `12387`、更新 `12388`、删除 `12393`；iOS 上架 `13189`/`13193`/`13198`/`13195`；Android 上架 `13190`/`13194`/`13201`/`13196`。对照 `packageManage.vue` + `enterprisePackage/manage.vue` + `packageManage/shelfBag.vue`。

| 模块 | 权限 | 接口 | 参数 vs 旧站 | 登录态结论 | 编号 |
| --- | --- | --- | --- | --- | --- |
| 企业包·游戏列表 | 12355 | `package/list` | PackageName/Page/PageSize=5 | Items 1 + Resources | FE-072 + API-054 |
| 企业包·流程 | 10973 | `iospackversion` `channel/listall` | PackageId | Items 对象；渠道 4 | 通过 |
| 企业包·我的包 | 12387 | `listpackageiosdetail` | PackageId | Items=null（无已传企业包） | API-054 |
| iOS 上架包列表 | 13189 | `iosappstoredata/list` | AppName/AppUrl/日期/Sort='' | Items=null | API-054 |
| Android 上架包列表 | 13190 | `androidappstoreconfig/list` | 同上 | Items=null | API-054 |
| 上架包选项 | 表单依赖 | `package/listall` `countriesconfig/list` `packagelinkios/listall` | 一致 | listall 1；国家/企业包选项空 | API-054 |
| 数据统计弹窗 | 行内 | `*/analyticinfo` | AppPackageConfigId | 无库存未实开；坏 Id→10037 | API-054 |
| IPA 上传解析 | 12388 | `/api/uploadfile` | 对齐渠道上传字段 | 响应字段兜底（**已修**） | FE-072 |

## 模块核查总表（gameManage/siteManage）

父 Tab：场馆管理 / 游戏管理。旧站热门排序/特色/大奖/提款流水 Tab 已注释，NEW 未挂。权限：场馆 `10948`、维护编辑 `10951`；游戏 `12407`。对照 `siteManage.vue` + `venueManage/venueManager.vue` + `subgameMaintain.vue`。

| 模块 | 权限 | 接口 | 参数 vs 旧站 | 登录态结论 | 编号 |
| --- | --- | --- | --- | --- | --- |
| 场馆管理列表 | 10948 | `apifeeswitch/list` | ApiFeeName；NEW 一次拉 PageSize=9999 | list(1) BTI；缺状态字段 | FE-073 + API-055 |
| 场馆开关/维护 | 10951 | `apifeeswitch/switch` | IsEdit 0/1；GameId 由 ApiFee 解析 | GameId 依赖 GroupPlatform 回退 | FE-073 |
| 游戏管理列表 | 12407 | `subgamemaintain/list` | 默认首个场馆；Lang；Sort='' | 各 GameId Items=null | FE-073 + API-055 |
| 游戏编辑/排序/批量 | 12407 | `updatelist` `updatesortid` `batchedit` | 与旧站一致 | 无库存未做写测 | API-055 |

## 模块核查总表（gameManage/messageManage）

七 Tab：短信总览 / 月度统计 / 通道配置 / 短信模板 / 注册 OTP 明细（内层日报·明细） / 注册次日召回 / 首存次日召回。权限：10930 / 10931 / 12906（内容另需 12908） / 13239 / 13372 / 13408 / 13414。对照 `messageManage.vue` + `messageManage/{overview,total,channelConfig,template,regOtpDetail,regRecallNextDay,firstRecallNextDay}.vue` + `regOtp/{report,detail}.vue`。

| 模块 | 权限 | 接口 | 参数 vs 旧站 | 登录态结论 | 编号 |
| --- | --- | --- | --- | --- | --- |
| 短信总览·余额 | 10936/10937 | `shortmessageservice/info` | 无参 | ShortMessage=100 | — |
| 短信总览·日列表 | 10934/10932 | `shortmessageservice/list` | 近 30 日→今 unix；Page | Items=null | API-056 |
| 月度统计 | 10931 | `monthlist` | Begin/End 月 unix；Sort='' | Items=null Pagination=null | API-056 |
| 通道配置 | 12908 | `smschannelconfig/list` 等 | **无分页**拉全量；筛 AgentId≠0 | list→10000 | FE-074 + API-056 |
| 短信模板 | 13239 | `smsannouncementconfig/list` `editpackage` `switch` | IsOpen/日期/分页；包 Id 逗号串 | Items=[] | FE-074 + API-056 |
| OTP 日报 | 13372 | `phoneregisterotpday` | 近 7 日 unix；PageSize=9999 | Items=null | FE-074 + API-056 |
| OTP 明细 | 13372 | `phoneregisterotpdetail` | 同上 + 手机/来源/是否注册 | Items=null | API-056 |
| 注册次日召回 | 13408 | `registerrecall` `*detail` `*send` `*switch` | YYYY-MM-DD 近 30→昨 | list 空；Switch OK | API-056 |
| 首存次日召回 | 13414 | `depositrecall` 等 | YYYY-MM-DD 前天单日（≤7 天） | **list→10000**；detail 空 | FE-074 + API-056 |

## 模块核查总表（gameManage/backWater）

四 Tab：返水配置 / 返水记录（内层统计·明细） / 手动返水 / 返水审核（内层系统·手动）。权限：11073 / 11074 / 11075 / 11076。对照 `backWater.vue` + `backWater/{config,record,hand,audit}.vue` + `components/{statistics,detail,systemApplication}.vue`。

| 模块 | 权限 | 接口 | 参数 vs 旧站 | 登录态结论 | 编号 |
| --- | --- | --- | --- | --- | --- |
| 返水配置·方案列表 | 11096 | `allscheme` | 无参，respond 数组 | list(1) Id=8 | — |
| 返水配置·方案详情 | 11091 | `scheme?Id=` | Id 必填；Config/Rule JSON | Id=8 OK；无 Id→10000 | API-057 |
| 返水记录·统计 | 11077/11079 | `list` | 发放日=今天 unix；Reject=-1 | Items=null | FE-075 + API-057 |
| 返水记录·明细 | 11078/11082 | `detaillist` | 游戏日=今天 unix | Items=null | API-057 |
| 手动返水查询 | 11075 | `waterlist` | 账号+产品名+Time unix | 缺参 10043；无玩家 10002 | FE-075 + API-057 |
| 手动发放 | 11090 | `backwater` | Water 分；Time unix | 无库存未写测 | — |
| 审核·系统申请 | 12662/12663 | `list` Reject=1 Sum=1 | **今天** unix（非昨天） | Items=null | FE-075 + API-057 |
| 审核·手动申请 | 12668/12669 | `reviewlist` | 今天；Apply 分；PackageId | Items=null Count=0 | API-057 |
| 审核操作 | 通过/拒绝 | `review` | 系统 10/11；手动 1/2 | 未写测 | — |

## 模块核查总表（gameManage/backWater/addConfig）

独立子页（路由 `/gameManage/backWater/addConfig`，权限 11100）。旧站由配置 Tab「设置」带 `schemeData+index` 跳转；NEW 用 `schemeId/index/vipLevel` + 重新拉 `scheme`。对照 `addConfig.vue` + `common/gameConfigs.vue`。

| 模块 | 权限 | 接口 | 参数 vs 旧站 | 登录态结论 | 编号 |
| --- | --- | --- | --- | --- | --- |
| 进入页拉方案 | 11100 | `scheme?Id=` | Id 来自 query/session | Id=8 Config 11 档 | FE-076 + API-058 |
| 游戏字典 | 编辑器依赖 | `/api/game/info` | GameSetting.Type=games | games≈38 | — |
| 保存 VIP 配置 | 11100 | `PUT .../config` | Id + Config JSON；金额/比例×100 | 参数对齐；未写测 | FE-076 + API-058 |

## 模块核查总表（gameManage/advertisementManage）

四 Tab：首页轮播 Type=1 权限 11027 / 首页弹窗 Type=3 11029 / 支付广告 Type=6 12058 / 其他广告 Type=4 11030（内层 SubType 1～7 Radio）。对照旧站 `advertisementManage.vue` + `advertisementLoop` / `homeDialog` / `paymentAdvertisement` / `elseAdvertising`（旧站已注释的直播轮播/启动页/浮窗等未迁）。

| 模块 | 权限 | 接口 | 参数 vs 旧站 | 登录态结论 | 编号 |
| --- | --- | --- | --- | --- | --- |
| 轮播·方案列表 | 11032 | `gameadtemplate/list?Type=1` | Type only | Items=list(1) Id=724 | — |
| 轮播·广告列表 | 11038 | `gameadconfig/list` | TemplateId+Type+Title+Status+Begin/End=`''` | respond=array(2) | FE-077 |
| 弹窗·方案/列表 | 11058/11064 | template Type=3；config 仅 TemplateId+Type | 已对齐 | 方案有；广告 respond=null | API-059 |
| 支付·方案/列表 | 12062/12068 | Type=6；Title+Status | 对齐 | 方案有；广告 null | API-059 |
| 其他·方案 | 11231 | Type=4 **不带** SubType | 带 SubType→Items=null | Items=list(1) | API-059 |
| 其他·SubType1～6 | 11238 | config Type=4+SubType | 对齐 | respond=null | API-059 |
| 其他·场馆返水 | 11238 | `gamevenuerebate/list` | TemplateId | array(0) | — |
| 系统图库 | — | `gameadimage/list` | Type/SubType/ImageType | respond=null | API-059 |
| 跳转·公告 | — | `gameadconfig/getnoticelist` | 无参 | array(2)；Title 需解析 LangText | FE-077 |
| 跳转·活动 | — | `gameadconfig/getactivitylist` | 无参 | respond=null | API-059 |
| 恢复预设 | 11235 | `gameadtemplate/recover` | 仅 `{Id}`（首方案） | 参数已对齐；未写测 | FE-077 |

## 模块核查总表（gameManage/vipSetting）

四 Tab：VIP等级 10963 / 升级系数 10964 / 等级记录 10965 / 图标配置 13156。对照旧站 `vipSetting.vue` + `vipGrade` / `vipUpgrade` / `vipRecord` / `vipIcon`。

| 模块 | 权限 | 接口 | 参数 vs 旧站 | 登录态结论 | 编号 |
| --- | --- | --- | --- | --- | --- |
| 等级列表 | 10999 | `listvirtualprize` | 无业务参（Token 头） | arr(11) | — |
| 升级模式 | 12145 | `get/setviplevelmode` | `{VipLevelMode}` | Mode=1 | — |
| 保级天数 | 11704 | `get/setrelegationday` | `{RelegationDay}` | Day=0→前端默认 90 | FE-078 |
| 编辑/新增等级 | 11000/13181 | `virtualprize` / `addvirtualprize` | 金额×100 | 参数对齐；未写测 | — |
| 升级系数详情 | 11001/11002 | `vipcoefficientconfig/detail` | 无参 | Default=10000；GameInfo='' | API-060 + FE-078 |
| 保存系数 | 11002 | `PUT vipcoefficientconfig/` | Default×100 + GameInfo JSON + Hash | 参数对齐；未写测 | — |
| 等级记录 | 11003 | `viplevelconfig/list` | **今天** unix；Status=''；分页 | 今天 Items=1；空筛 Items=null | FE-078 + API-060 |
| 记录导出 | 12040 | 同上 `IsExp=true` | 对齐 | 客户端 CSV | — |
| 图标方案 | 13156/13158… | `listiconstemplates` / `listicons` POST | TemplateId | 方案 1；图标 14 档 | API-060 |

## 模块核查总表（gameManage/systemSetting）

五 Tab：游戏帮助 12240 / 通用规则 12241 / 体育教程 13104 / 汇率 13362 / 系统模板 13453。旧站父页目前仅挂载帮助中心（其余 Tab 源码仍在）。NEW 五 Tab 均已登录态实打。

| 模块 | 权限 | 接口 | 参数 vs 旧站 | 登录态结论 | 编号 |
| --- | --- | --- | --- | --- | --- |
| 帮助·页签 | 11004/11005… | `gamehelpcenter/list` | 无参 | arr(5) | — |
| 帮助·内容 | 11004 | `listall` | 无参 | arr(18) | FE-079 |
| 通用规则 | 12241 | `phonecountrycode/blocklist` `block` | Value.code + LangText | 有号段+提示 | FE-079 |
| 体育教程 | 13104 | `sportsconfig/list` ConfigType=1 | Page/PageSize | Items=null | API-061 + FE-079 |
| 汇率列表 | 13363 | `exchangeratesetting` | Page/PageSize/Keyword='' | respond=null | API-061 + FE-079 |
| 系统模板 | 10005（列表）/13453（Tab） | `netcashconfig/list` Type=1/2 | 分页+Type | respond=null | API-061 + FE-079 |
| 代理成员 | 模板依赖 | `config/getchildadmininfo` | 无参 | ChildAdminInfo=38 | — |

## 模块核查总表（generalizeManage/generalizeManageact）

本页**无子 Tab**；树表懒加载下级（`ParentId`）。权限：列表 10908、编辑 10909、启停 10910、成本 10911、下级代理 10912、渠道推广 10913、域名 11917。对照旧站 `generalizeManageact.vue`（`SearchTypeTwo` 未开日期控件；首屏无 Begin/End）。

| 模块 | 权限 | 接口 | 参数 vs 旧站 | 登录态结论 | 编号 |
| --- | --- | --- | --- | --- | --- |
| 推广列表 | 10908 | `promoter/list` | Page/PageSize/Username/Name/Status/SettleType/Sort/ParentId='' | Items=null | FE-080 + API-062 |
| 懒加载下级 | 10908 | 同上 ParentId + PageSize=9999 | 对齐 | 本环境无数据 | FE-080 |
| 详情/启停/编辑 | 10909/10910 | `promoter/:id` PUT | Status/密码等 | 坏 Id→10002 | API-062 |
| 团队比例 | 10909 | `PUT accountteam/` | 比例×10 | 参数对齐；未写测 | — |
| 分摊成本 | 10911 | `agent/setcostodd/` | 比例×100 | 参数对齐；未写测 | — |
| 推广域名 | 11917 | `domain/list?Type=10` + `promoter/domain` | Type=10 | 域名 Items=null | API-062 |

## 模块核查总表（generalizeManage/addGeneralize）

本页**无子 Tab**；三步向导：账号信息 → 协作配置 → 信息确认。创建权限 `10913`+根团队(Id/AgentId=0)；编辑 `10909`（`?id=`）；直播分组改选 `12577`。对照旧站 `addGeneralize.vue`。

| 模块 | 权限 | 接口 | 参数 vs 旧站 | 登录态结论 | 编号 |
| --- | --- | --- | --- | --- | --- |
| 直播分组列表 | 12577（改选） | `sportsteamerteam/list` | 无参（旧站同） | **20001** 远程未连接 | API-063 + FE-081 |
| 直属分组回填 | 编辑 | `sportsteamerteam/getadminteams?AdminId=` | AdminId | **20001** | API-063 + FE-081 |
| 推广详情 | 10909 | `GET promoter/:id` | 路径 Id | 本环境无数据；坏 Id→10002 | API-062/063 |
| 创建推广 | 10913 | `POST promoter/` | Hash + RoleDataField JSON + TeamIds/QingLiu + SettlePrice | 参数对齐旧站；未写测 | FE-081 |
| 编辑推广 | 10909 | `PUT promoter/` | Id/Status + 同上 | 参数对齐；比例类回填 SettleRate | FE-081 |

## 模块核查总表（generalizeManage/promoteData）

五 Tab：账户数据 `10886`(+表 `10901`) / 落地页转化 `10887` / 无效用户 `10888` / 数据填写 `10889`(+表/增/改/删 `10904`～`10907`) / 渠道回本 `13187`(+导出 `10016`)。对照旧站 `promoteData.vue` + `generalizeData/*`。

| 模块 | 权限 | 接口 | 参数 vs 旧站 | 登录态结论 | 编号 |
| --- | --- | --- | --- | --- | --- |
| 账户数据 | 10886/10901 | `promotedata/channelreport` + `exchangeratesetting` | AdminIds/ChannelIds/近7日 | Items 有；ItemsCost=null；汇率 null | FE-082 + API-064 |
| 落地页转化 | 10887 | `promotedata/getsumrecord` | 同上+Page/PageSize | Item=20，MaxCount=99 | FE-082 |
| 无效用户 | 10888 | `promotedata/invaliduser` | 同上 | Items 有计数字段 | FE-082 |
| 数据填写 | 10889/10904+ | `handrecord/list` `landingpage/listall` | 近7日+Sort='' | Items=null；listall null；坏 Id→10001 | FE-082 + API-064 |
| 渠道回本 | 13187 | `channelbreakevenreport` | 周报默认本周；日报/月报切换 | **10103** 密码错误 | API-064 + FE-082 |

## 模块核查总表（generalizeData/timeshareData）

七指标 Tab（同一接口）：新增用户 `10878` / 新增设备 `10880` / 总登录 `10881` / 付费金额+人数 `10882`∧`10883` / 兑换金额+人数 `10884`∧`10885`。视图：柱状/折线/表格。对照旧站 `timeshareData.vue` + 子组件；**仅 AdminType=2 做权限裁剪**。

| 模块 | 权限 | 接口 | 参数 vs 旧站 | 登录态结论 | 编号 |
| --- | --- | --- | --- | --- | --- |
| 时段报表（共用） | 见上 | `promotedata/hourreport` | AdminId/ChannelId/近7日 | 7×24 有数据；未来日全0骨架；坏日期10000 | FE-083 + API-065 |
| 柱/折/表切换 | — | 同上（前端切视图） | 不重复请求（优于旧站切图重拉） | 正常 | FE-083 |

## 模块核查总表（generalizeManage/teamDaily）

入口按 `AccountTeamInfo.TeamType`：`1` 仅提现面板 / `2` 仅利润面板 / 其它双 Tab（`10868` 提现 / `10869` 利润）。子权限：提现今日 `10872` 历史 `10873`；利润今日 `10874` 历史 `10875`。对照旧站 `teamDaily.vue` + `tabOne`/`tabTwo`。

| 模块 | 权限 | 接口 | 参数 vs 旧站 | 登录态结论 | 编号 |
| --- | --- | --- | --- | --- | --- |
| 提现模式 | 10868/10872/10873 | `accountteamdaily/list` TeamType=1 | unix 近31日+AdminId='' | Today/Banner 全0；History=null | FE-084 + API-066 |
| 利润模式 | 10869/10874/10875 | 同上 TeamType=2 | 同上；账号筛 TeamType=2 | 同上 | FE-084 + API-066 |

## 模块核查总表（generalizeManage/teamQuery）

本页**无子 Tab**；层级下钻面包屑（根账号可点下级推广账号）。权限：`10867`。对照旧站 `teamQuery.vue`。

| 模块 | 权限 | 接口 | 参数 vs 旧站 | 登录态结论 | 编号 |
| --- | --- | --- | --- | --- | --- |
| 分销列表 | 10867 | `accountteamsearch/list` | Page/PageSize/Sort/AdminId/AdminUsername/近31日unix | Items/Items2=null；无 Pagination | FE-085 + API-067 |
| 层级下钻 | 10867（且 AccountInfo.AdminId==AgentAccount.Id） | 同上 AdminId=下级 | 对齐 | 本环境无数据可点 | FE-085 |

## 模块核查总表（generalizeManage/closeOrder）

本页**无子 Tab**；结束订单为弹窗子页（打款/拒绝）。权限：查看 `10863`、开始处理 `10865`、结束订单 `10866`。对照旧站 `closeOrder.vue`。

| 模块 | 权限 | 接口 | 参数 vs 旧站 | 登录态结论 | 编号 |
| --- | --- | --- | --- | --- | --- |
| 结算工单列表+汇总 | 10863 | `accountteamwithdrawapply/list` | Page/PageSize/Sort/账号/单号/时间/Status 逗号 | Items/MoreItems=null；有 Pagination | FE-086 + API-068 |
| 开始处理 | 10865 | `accountteamwithdrawapply/applydo` | `{Id}` | 坏 Id→10402 无 msg | API-068 |
| 结束订单（完成/拒绝） | 10866 | `accountteamwithdrawapply/applyfinish` | `{Id,IsAccept,IsYourSure,Desc}` | 坏 Id→10403 无 msg | FE-086 + API-068 |

## 模块核查总表（generalizeManage/closeManage）

本页**无列表子 Tab**；子页面：提现弹窗、提现账号管理、安全设置（手机/取款密码）。权限：资金明细 `10924`、提现 `10927`、账号管理 `10929`。对照旧站 `closeManage.vue` + `teamWithdrawAccount.vue`。

| 模块 | 权限 | 接口 | 参数 vs 旧站 | 登录态结论 | 编号 |
| --- | --- | --- | --- | --- | --- |
| 资金明细+余额汇总 | 10924 | `accountteamwithdraw/list` | Page/PageSize/Sort/MoneyType/时间 | Items=null；MoreItems 有费率 | FE-087 + API-069 |
| 登录安全信息 | — | `accountlogin/info` | 无参 | Phone 空；IsSetPrivatePassword=2 | API-069 |
| 提现账号列表 | 10929 | `accountteambank/list` | 无参 | respond=null | FE-087 + API-069 |
| 发起提现 | 10927 | `accountteamwithdraw/` POST | GetMoney/AccountNo/Account/PrivatePassword/Hash | 坏参→10002 | FE-087 + API-069 |
| 账号增删改+验证码 | 10929 | `accountteambank/*` + `phonevalidcode` | 对齐旧站 | 坏验证码 10173；坏 Id 10001/10002 | API-069 |
| 安全设置 | — | `privatepassword`/`phone`/`deletephone` | 对齐旧站个人中心 | 本环境未绑定手机 | FE-087 |

## 模块核查总表（generalizeManage/brokerageSet）

本页**无子 Tab**；编辑/批量设置为弹窗。权限：列表 `10856`、编辑 `10858`、批量 `10859`、恢复 `10860`。对照旧站 `brokerageSet.vue`。

| 模块 | 权限 | 接口 | 参数 vs 旧站 | 登录态结论 | 编号 |
| --- | --- | --- | --- | --- | --- |
| 佣金树表（游戏配置合并） | 10856 | `accountteamgamerate/list` | Page/PageSize/Sort/PlayerId/PlayerName/GameId | Items=null；默认费率 25 | FE-088 + API-070 |
| 新增/编辑系数 | 10858 | `accountteamgamerate/` POST/PUT | GameId/Rate/Desc[/Id/Hash]；resType8 联动 batch | 坏参 10000/10002 | FE-088 + API-070 |
| 批量设置 | 10859 | `accountteamgamerate/batchupdate` | GameId[]+子游戏/Rate/Desc | 坏 GameId 仍 200 | API-070 |
| 恢复默认 | 10860 | `accountteamgamerate/recover` | `{Hash}` | 空 body 仍 200 | API-070 |
