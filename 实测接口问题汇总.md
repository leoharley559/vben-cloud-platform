# 实测接口问题汇总

按天汇总实测结果，供前端与后端互相核对修复进度。  
（与《接口问题汇总》结构一致，本文件仅收录实测确认的问题。）

## 状态约定（请双方统一使用下列枚举值）

| 字段 | 谁维护 | 可选值 | 说明 |
| --- | --- | --- | --- |
| **协作状态** | 双方 | `待反馈` / `处理中` / `待验证` / `已关闭` | 整单流转：提报→后端处理→前端复测→关闭 |
| **后端解决** | 后端 | `未处理` / `处理中` / `已修复` / `无法复现` / `不予修复` / `需补充信息` | 后端对该接口问题的处理结论 |
| **前端验证** | 前端 | `—` / `未验证` / `通过` / `不通过` | 后端标「已修复」后，前端复测结果；未到验证阶段填 `—` |

### 建议流转

1. 前端提报：协作状态=`待反馈`，后端解决=`未处理`，前端验证=`—`
2. 后端接手：协作状态=`处理中`，后端解决=`处理中`，并在详情「后端备注」写进展
3. 后端改完：后端解决=`已修复`（或 `无法复现`/`不予修复`），协作状态=`待验证`，写清修复说明/环境/版本
4. 前端复测：前端验证=`通过`/`不通过`；通过则协作状态=`已关闭`；不通过则打回 `处理中` 并写「验证备注」

---

## 清单

| 编号 | 日期 | 协作状态 | 后端解决 | 前端验证 | 优先级 | 页面 | 接口 | 问题 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| API-001 | 2026-07-21 | 待反馈 | 未处理 | — | P1 | 数据总览 `/dashboard/index` | `GET /backend/dashboard/reportinfo` | `TotalHours` 分时异常导致「今日数据」折线图不显示；自选日期 `Date1/2/3` 对应分时也为空 |

---

## 详情

### API-001

- 协作状态：待反馈
- 后端解决：未处理
- 前端验证：—
- 后端备注：（后端填写：修复说明 / 影响范围 / 环境或版本）
- 验证备注：（前端填写：复测账号、时间、结论）

**现象：** 「今日数据」模块上方 KPI（`Items`）有投注金额等数据，下方折线/柱状图不显示；页面空态提示「暂无分时数据：接口 TotalHours 为空或为 null」。自选日期后对应分时仍为空。

**前端链路（已核实，非前端渲染问题）：**

- 页面：`apps/web-antd/src/views/dashboard/index/index.vue`
- 图表：`apps/web-antd/src/views/dashboard/index/components/trend-chart.vue`
- 请求：`GET /backend/dashboard/reportinfo`
- 逻辑：`Items` → KPI 卡片；`TotalHours` → 业务折线（投注/返奖/盈利/充值/兑换/充兑差）；`TotalCount` → 在线人数折线
- `hasChartData` 为 false 时不调用 ECharts 渲染，并展示空态文案（见 `trend-chart.vue`）

**请求参数（自选日期）：**

- 前端传 `Date1` / `Date2` / `Date3`（`YYYY-MM-DD`，须小于今日且不等于昨日）
- 例：`GET /backend/dashboard/reportinfo?Date1=2026-07-15`

**字段用途：**

- `Items` → 按天，KPI 卡片
- `TotalHours` → 按小时，业务折线（含今日/昨日/自选日期）
- `TotalCount` → 按 5 分钟，仅在线人数折线

**实测（2026-07-17 初测 / 2026-07-21 复核前端链路）：**

- `Items`：今日/昨日投注正常（例：`55015729` / `621889885`）→ 卡片可显示
- `TotalHours["2026-07-16"]`：`null` → 昨日折线无点
- `TotalHours["2026-07-17"]`：仅 1 条（`Hours: 14`），金额全 `0` → 折线几乎无有效数据
- 自选日期（如 `Date1=2026-07-15`）：`TotalHours["2026-07-15"]` 也为空 / `null` / 无有效小时数据
- `TotalCount`：结构有，`Count` 全 `0`

**请后端确认：**

1. 为何 `Items` 有金额，`TotalHours` 同日为 0 / `null`
2. 传 `Date1/2/3` 后，`TotalHours` 是否应返回对应日期的按小时数据
3. 昨日 / 自选日期不要返回 `null`（应 `[]` 或完整小时数组）
4. 今日应按小时返回（约 0–当前小时），金额单位与 `Items` 一致（分）

**摘录：**

```json
{
  "Items": [
    { "ReportDay": "2026-07-17", "SumTransBetMoney1": 55015729 },
    { "ReportDay": "2026-07-16", "SumTransBetMoney1": 621889885 },
    { "ReportDay": "2026-07-15", "SumTransBetMoney1": 12345678 }
  ],
  "TotalHours": {
    "2026-07-16": null,
    "2026-07-17": [{ "Hours": 14, "SumTransBetMoney1": 0 }],
    "2026-07-15": null
  }
}
```
