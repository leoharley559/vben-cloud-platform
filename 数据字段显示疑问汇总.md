# 数据字段显示疑问汇总

按天汇总新旧站对照时发现的**数据字段显示疑问**，供产品 / 后端核对口径与展示规则。  
（结构对齐《实测接口问题汇总》；本文件聚焦**字段含义、单位、空值展示、新旧站差异**等显示侧疑问，不替代接口协议类 `API-*` 单据。）

**核查范围约定：** 页面若存在多层 Tab / 子 Tab（含 Radio 内层切换），**每一层都须分项对照旧站字段显示**，不得只看父 Tab 首屏。

**实测账号 / 环境：** `leying_niu` @ `https://ccccc-admin.dk888.link`（2026-07-24 代码对照 + 口径提报）  
**对照旧站：** `cloudPlatform/src/views/dashboard/admin/index.vue`、`cloudPlatform/src/views/dashboard/admin/components/PanelGroup.vue`  
**新站页面：** `apps/web-antd/src/views/dashboard/index/index.vue`、`.../components/panel-metrics.vue`、`apps/web-antd/src/utils/dashboard.ts`

**单据约定：** 每条 `FIELD-*` 详情须含「**对照字段**」表（页面位置 + 字段名/接口字段 + 旧站显示 + 新站显示 + 疑问点），便于双方按相同场景复现。

**收录范围：** 字段错位、单位/精度、空值与 `0`/`-`/`空` 展示不一致、文案/枚举映射、新旧站同参不同展示、疑似口径变更等。接口报错、远程不可用等仍归《实测接口问题汇总》。

---

## 状态约定（请双方统一使用下列枚举值）

| 字段 | 谁维护 | 可选值 | 说明 |
| --- | --- | --- | --- |
| **协作状态** | 双方 | `待反馈` / `处理中` / `待验证` / `已关闭` | 整单流转：提报→确认口径→前端/后端调整→复测→关闭 |
| **后端解决** | 后端 | `未处理` / `处理中` / `已确认口径` / `已修复` / `无法复现` / `不予修复` / `需补充信息` | 后端对该字段疑问的处理结论 |
| **前端验证** | 前端 | `—` / `未验证` / `通过` / `不通过` | 口径确认或修复后，前端复测结果；未到验证阶段填 `—` |
| **责任方** | 提报时标注 | `后端` / `前端` / `待确认` | 便于分流（显示层 vs 数据层） |

### 建议流转

1. 前端提报：协作状态=`待反馈`，后端解决=`未处理`，前端验证=`—`
2. 后端/产品接手：协作状态=`处理中`，并在详情「后端备注」写口径说明
3. 口径确认或改完：后端解决=`已确认口径`/`已修复`（或 `无法复现`/`不予修复`），协作状态=`待验证`
4. 前端复测：前端验证=`通过`/`不通过`；通过则协作状态=`已关闭`；不通过则打回 `处理中` 并写「验证备注」

---

## 清单

共 **1** 条。

| 编号 | 日期 | 协作状态 | 后端解决 | 前端验证 | 责任方 | 优先级 | 页面模块 | 相关接口/字段 | 疑问摘要 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| FIELD-001 | 2026-07-24 | 待反馈 | 未处理 | — | 待确认 | P2 | 数据总览·今日数据·盈利 | `GET /backend/dashboard/reportinfo` · `Items.SumTransBetMoney1` / `SumTransWinMoney1` | 「盈利」无独立返回字段，前端按投注−返奖计算；请确认口径 |

---

## 详情

### FIELD-001

- 协作状态：待反馈
- 后端解决：未处理
- 前端验证：—
- 责任方：待确认
- 后端备注：（后端/产品填写）
- 验证备注：2026-07-24 对照新旧站前端代码梳理字段与计算公式

#### 接口与请求

| 项 | 说明 |
| --- | --- |
| 接口 | `GET /backend/dashboard/reportinfo` |
| 前端封装 | `fetchDashboardReportApi` → `apps/web-antd/src/api/dashboard/index.ts` |
| 首屏参数 | **无 Query**（今日/昨日由 `Items[].ReportDay` 匹配） |
| 自选日期（折线联动，可选） | `Date1` / `Date2` / `Date3`=`YYYY-MM-DD`（须 `<今日` 且 `≠昨日`） |
| KPI 数据源 | 响应 `Items[]`：按 `ReportDay` 取出**今日行**、**昨日行** |
| 金额单位 | 接口为**分**；页面展示 `/100` 转元（`centsToYuan`，四舍五入取整） |

**实测请求参数：**

| 场景 | Method | 路径 | 提交参数 | 备注 |
| --- | --- | --- | --- | --- |
| 今日数据首屏 | GET | `/backend/dashboard/reportinfo` | 无 | 取 `Items` 中今日/昨日两行做 KPI |
| 自选日期（可选） | GET | `/backend/dashboard/reportinfo` | `Date1` / `Date2` / `Date3` | 主要影响 `TotalHours` 折线；KPI 仍取今日行 |

#### 「今日数据」字段对照（含盈利）

| 页面位置 | 展示文案 | 接口字段 / 计算 | 展示规则 | 副文案 |
| --- | --- | --- | --- | --- |
| 数据总览 · 今日数据 | 投注金额 | `Items[].SumTransBetMoney1`（今日行） | `centsToYuan(SumTransBetMoney1)` | 较昨日：与昨日同行环比 |
| 数据总览 · 今日数据 | 返奖金额 | `Items[].SumTransWinMoney1`（今日行） | `centsToYuan(SumTransWinMoney1)` | 较昨日：与昨日同行环比 |
| 数据总览 · 今日数据 | **盈利** | **无独立字段**；前端 key=`WinSubBet` | 见下方计算公式 | 盈率，见下方 |
| 数据总览 · 今日数据 | 充值金额 | `Items[].SumPayMergerMoney`（今日行） | `centsToYuan(SumPayMergerMoney)` | 较昨日；悬停提示官方/币商拆分 |
| 数据总览 · 今日数据 | 兑换金额 | `Items[].SumWithdrawMoney`（今日行） | `centsToYuan(SumWithdrawMoney)` | 较昨日 |
| 数据总览 · 今日数据 | 充兑差 | **无独立字段**；前端 key=`profits` | `SumPayMergerMoney - SumWithdrawMoney` 再转元 | 兑充比 |

#### 盈利：计算方法（新旧站一致）

新站：`panel-metrics.vue`  
旧站：`admin/index.vue` → `calc: cur => cur.SumTransBetMoney1 - cur.SumTransWinMoney1`；`PanelGroup.vue` 同口径。

```text
bet  = Number(今日行.SumTransBetMoney1)   // 投注金额（分）
win  = Number(今日行.SumTransWinMoney1)   // 返奖金额（分）

盈利（分） = bet - win
盈利（展示元） = Math.round((bet - win) / 100)

盈率（副文案） =
  bet === 0 ? "0%"
            : (((bet - win) / bet) * 100).toFixed(2) + "%"
```

说明：

1. 响应里**没有**名为 `WinSubBet` / `盈利` 的金额字段；`WinSubBet` 仅为前端图表/卡片选中 key。
2. 盈利 = **投注 − 返奖**（不是返奖 − 投注）。
3. 先相减再 `/100` 转元，与投注/返奖各自转元后再减在整数上可能差 1 元（四舍五入）；当前实现为**先减后转**。

#### 现象 / 疑问

- 「盈利」完全依赖前端用 `SumTransBetMoney1`、`SumTransWinMoney1` 相减，后端是否另有标准利润字段未使用？
- 口径是否确认为 **投注 − 返奖**（平台视角）？是否存在需改成 **返奖 − 投注**（玩家视角）的产品定义？
- `Items` 是否可能已返回预计算利润字段（如有请给出字段名），前端应直取还是继续自算？
- 盈率公式 `(投注−返奖)/投注` 是否为正式口径？分母为 0 时展示 `0%` 是否可接受？

#### 前端核对

- 已对照旧站：`cloudPlatform/src/views/dashboard/admin/index.vue`（`type: 'WinSubBet'`）、`PanelGroup.vue`
- 新站实现：`apps/web-antd/src/views/dashboard/index/components/panel-metrics.vue`、`utils/dashboard.ts`（`PANEL_METRICS` / `WinSubBet`）
- **非前端参数传错**；首屏无参与旧站一致。本单为**展示口径确认**，非接口报错。

#### 请确认

1. 盈利是否继续用 `SumTransBetMoney1 - SumTransWinMoney1`？
2. 若后端有权威字段，请给出字段名与单位（分/元）。
3. 盈率副文案公式是否保留。

#### 摘录（结构示意）

```json
{
  "Items": [
    {
      "ReportDay": "2026-07-24",
      "SumTransBetMoney1": 101602389,
      "SumTransWinMoney1": 98000000,
      "SumPayMergerMoney": 0,
      "SumWithdrawMoney": 0
    },
    {
      "ReportDay": "2026-07-23",
      "SumTransBetMoney1": 124554898,
      "SumTransWinMoney1": 120000000
    }
  ]
}
```

按上式：今日盈利（分）=`101602389 - 98000000`，展示元=`Math.round(3602389 / 100)`。

---
